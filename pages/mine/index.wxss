/* pages/mine/index.wxss —— 「我的」页（继承 app.wxss 共享组件） */
/* 修 6：注销等危险操作按钮（视觉与原来内联样式一致：#e74c3c 底白字） */
.btn.danger { background: #e74c3c; color: #fff; }

/* G5：头像昵称（AD-15） */
.profile { display: flex; align-items: center; gap: 24rpx; }
.profile-avatar { flex: 0 0 auto; }
.avatar-btn { width: 120rpx; height: 120rpx; border-radius: 50%; padding: 0; margin: 0;
  background: #f0f0f0; display: flex; align-items: center; justify-content: center; overflow: hidden; }
.avatar-img { width: 120rpx; height: 120rpx; border-radius: 50%; }
.avatar-ph { color: #999; font-size: 28rpx; }
.profile-name { flex: 1; display: flex; flex-direction: column; gap: 8rpx; }
.nickname-inp { font-size: 30rpx; min-height: 88rpx; line-height: 88rpx; }
