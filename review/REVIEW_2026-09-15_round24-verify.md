# REVIEW_2026-09-15 round24 · R52/R55/R57 复核（含我 OCR 独立验 R52 证据）+ 四件请示答复

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 24 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round23-verify.md`；本轮对象 = `a58378c`（R55+R57）、`6b00110`（里程碑）。

---

## §0 结论

**你们的自查结论我独立复核通过**：门禁 0、**52/52 套件**、R55 落位正确、R57 六个自测就位、**R52 我用 OCR 独立确认（`admin_user` 空集合）**。
**新增 1 个小发现（R58）**；另有**两条能力边界变化**必须先说清（§1）——它们影响我后续能给什么等级的结论。

| 独立复核项 | 我的结果 |
|---|---|
| HEAD / 工作树 | ✅ `6b00110`；`git status --porcelain` = **0 项**（干净） |
| 提交 | ✅ `a58378c`（R55+R57）、`6b00110`（里程碑补链） |
| 门禁 | ✅ **GATE=0** |
| 套件 | ✅ **SUITES=52 / PASS=52**（我逐个跑，不读它的自测表） |
| R52 证据 | ✅ 我 OCR 了 `r52_admin_user_dev_空集合_20260917.png`：云开发控制台 v2.03 / 环境 `cloud1` / 数据库·集合管理 / 集合列表含 `admin_user`、`order_refund`、`probe_tmp` / 搜索区 **「没有找到记录」** ⇒ **admin_user 确为空集合**，R48 的 fail-closed 在 dev 不会锁死任何管理员 ✅ |
| R55 | ✅ `utils/selftest_batch7.js` **已不存在**、`tools/selftest_batch7.js` **存在**；`review/README.md:21` 立了「测试脚本落位约定（R55）」+ `:23` 记了实例 |
| R57 | ✅ 6 个 `selftest.js` 均就位（`saveAsset`/`saveShopSetting`/`getLedger`/`getMonthList`/`getShopContext`/`getCardVersions`）；**52 = 46 + 6** 自洽 |

---

## §1 两条能力边界变化（影响结论等级，请知悉）

**① 沙箱：`git.exe` / `node.exe` / `powershell.exe` 子进程全部被拒**
本轮中途起连 **`git.exe`、`node.exe`、`powershell.exe -File`** 都报 `Access is denied`（`[sandbox: file access denied under workspace-write mode]`）。我按纪律**申请了一次放行**，把「git 状态 + 门禁 + 52 套件 + OCR」压进**一条命令**才拿到上面的结果。
⇒ 后续每轮我都要**一次放行**；否则我只能做**文件系统层核验**（`Test-Path`/`Get-ChildItem`/`Select-String`/`Get-Content`），**不能**跑门禁/套件/OCR —— 那种情况下我会把结论标为「仅静态核验」。

**② `origin/dev` 现在解析不出来** ⇒ **"是否已推"我核不了**
```
git rev-parse --short origin/dev → fatal: Needed a single revision（无输出）
```
此前每轮都能解析（round23 还是 `fabac9c`）⇒ **远端跟踪引用没有创建/被清理**（可能 remote 名变了，或该分支未建立 upstream）。**请自查并回执**：`git remote -v` + `git branch -vv`。
> ⚠️ 这直接影响你的"本地=远端=6b00110"这句 —— 我**无法独立确证**；建议以后回执里用 `git log --oneline -1 @{u}`（或 `git ls-remote origin dev`）作证据。

---

## §2 🔵 R58（新）：`verify_all.js` 头部注释仍写「46 个套件」，实际 **52**

- 证据：`verify_all.js:3` = `// 串联：46 个套件 = …`，而 `SUITES` 实际 **52** 条（我数了）。
- 这**正是 R21 那条纪律**（"新增/删除套件时必须同步改上面这句数量，否则 `SUITES.length` 与注释不符"）—— 你们**重启键两处已同步为 52 ✅**，但**漏了代码里这一处**。
- **修法**：`:3` 的 46 → **52**（并顺手把枚举里的 "batch0~7 各批次代码自测" 与 R57 补齐说明对齐）。
- **验收**：`Select-String verify_all.js -Pattern "个套件"` 的数字 = `SUITES` 条目数（52）。

---

## §3 对它「顺带发现」的裁决：`saveAsset/validate.js:16` 文案确实该改

- 事实（我读了源码）：`:15` 判据是 `a.value_fen <= 0`（**拒 0**），而 `:16` 文案写「必须是非负**正**整数分」——**"非负" 与 "拒 0" 自相矛盾**。它的判断正确 ✅。
- **我的裁决**：**改文案，不改行为**。理由：资产原值为 0 没有业务意义（0 元资产的摊销恒为 0，属脏数据），**拒 0 是对的**；所以把文案改成「必须是**正整数**分」。
- 它用断言把**实际行为**钉住（而不是先改代码）—— 这个处置**正确**（先固定现状、再改文案，避免"改文案顺手改行为"）。
- **🔵 低风险，可与下一次触碰 `saveAsset` 一起做**；不必单独开一轮。

---

## §4 对你四件请示的答复

**① 开发者工具里的 `wechatide` client 授权** → **属李老师动作，我做不了**（无开发者工具、无浏览器登录态）。打通后建议顺序：
```
a) 先补 39 条索引（A7 已定案"代码建不了索引"⇒ 这是唯一路径）
   ⚠️ 必须逐条核对，不能盲建：索引名/字段/升序/**是否勾唯一**都要对（39 条里 10 条 unique）；
   ⚠️ 建完留证据（截图 + 与 collections.js 的逐条对照），我按对照复核。
b) 再跑真云三验：多版本写入（同 card_code 连存两次）/ 两月 calcAmortize / smokeTest（验 R29 的 commonShape）。
```
**② `adminInit` 的 `ADMIN_SETUP_TOKEN`** → 我可以给规矩，值必须你们设：
- **只放云函数环境变量**（`initDb`/`adminInit` 同款配置方式），**绝不进仓库、不进任何 .md/.txt**；
- **一次性**：首超管建好后**立即轮换/删除**（它是"用后即焚"的）；
- **dev / prod 用不同值**；至少 32 位随机（避免被猜）；
- 你已经建了 `tools/gen_admin_bootstrap.js` ✅（我核到它存在），**请确认它读 token 的方式与环境变量一致**，并在回执里写清"token 从哪来、用过是否已轮换"。

**③ 上线三项（隐私政策 URL / 审核测试账号 / 营业执照商户号）** → **全部属用户侧**（我无账号、无执照）。
补充：**隐私政策正文 + 平台配置 + URL 是审核硬阻塞**（`core/11 微信审核自查清单` 有清单），建议在"上线前收口"里单列一节，别混在代码项里。

**④ 「InsCode 协作值守」有效期到今天 12:00，是否续？** → **我建议续期**，但**必须保留现约束**：
- 理由：接下来是**上线前收口**（补 39 条索引 + 真云三验 + 隐私/注销真机走查 + 提审准备），**长周期且需要巡检**，值守能持续推进。
- **约束照旧（不可放松）**：🚫 **不上传版本、不提交审核、不动 prod 环境、不给真实用户发权益、不改用户数据**；不可逆/对外动作一律**等李老师回来**。
- 另外建议：值守 prompt 里**加一条**"每轮改动必须留回执 + 复跑门禁"，与 `review/README.md §7` 的清单对齐。

---

## §5 队列现状

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🔵 R58 改 `verify_all.js:3` 的 46→52 ② 🔵 `saveAsset` 文案改「正整数分」 ③ 回执里补 `origin/dev` 自查（`git remote -v`/`branch -vv`） |
| **待人工** | ① wechatide client 授权（→ 补 39 条索引 → 真云三验）② `ADMIN_SETUP_TOKEN` 设定与用后轮换 ③ 上线三项 ④ **R45**：套餐选择 UI 前必须实现 iOS 过滤 ⑤ 上传留提审前 |
| **下一步** | 上线前收口；**R54/R56 的处置口径你们已立**（重启键 `:62-63` 收口轮清单），我 round24 已复核通过 |

## §6 能力边界

- **沙箱**：git/node/powershell 子进程全被拒（§1①）⇒ 本轮结论是**申请放行后**跑出来的；未申请放行时我只能静态核验。
- **`origin/dev` 不可解析**（§1②）⇒ **推送状态未独立确认**。
- 云端数据实况（除他们给的截图，我已 OCR 复核其中 R52 那张）、开发者工具、`verify_all` 端到端 一如既往核不了。
- **R48/R49/R50/R51/R53**（你们的收官加固项）**我仍未逐条复核** —— 若要我核，点名；我建议把 R48（fail-closed）与 R53（列表截断）优先给我，因为它们是"静默错误"类。
