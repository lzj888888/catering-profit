# NOTE · 2026-09-19 round51 自驱动巡检（我方执行分析件）

> 执行方：WorkBuddy（巴迪）｜仓：`C:\Users\lzj\WorkBuddy\Claw\catering-profit`
> 上轮：round50（HEAD `306777d`）｜本轮编号 **round51**
> 证据目录：`review/evidence/selfdrive_20260919_r51/`

---

## 1. 探针与并发判定

| 项 | 判据 | 结论 |
|---|---|---|
| InsCode | SQLite `inflight_turn`=0、`approval_audit` 末行非 pending、`message_count`=**1465 九连持平**、`idle_sec`=107926（≈29.98h） | **idle 30h**，无待批、无在跑 |
| dsh | 窗口 `DeepSeek Harness`（Edge，hwnd 855322）`rect=-32000` 最小化 → `ShowWindow(9)` 恢复 | **第 7 轮不可用**，见 §2 |
| 并发写入方 | ① 进程级（`wmic`）：除常驻噪音（sheetagent / weixinpay / `dsh bin.js web`）外**无仓内脚本** ② 证据目录 mtime：最新活动 = 我方 round50 `selfdrive_r50` 74min；`r86_timeout_20260919` 静止 **309min** | **已停手** ⇒ 本轮可正常写 |
| HEAD 比对 | `git rev-parse HEAD` = `306777d` == 上轮我方提交；`git log --oneline -10` **无未经复核的新 commit** | 无新代码需复核（§6「队列清空≠无活」检查已做） |

---

## 2. dsh：第 7 轮不可用，且**症状加重为整页白屏**

按技能 §3.2 的「后端不可用」判据 + round50 新增的根因侧证四步，本轮全部只读取证：

| 步骤 | 命令 / 对象 | 本轮结果 | 判读 |
|---|---|---|---|
| ① 进程 uptime | `wmic process where name='node.exe' get CreationDate` | `bin.js web`，`CreationDate=20260917030702` ⇒ **连续运行 ≈66.0h**（round50 为 62.8h） | 长跑未重启，继续恶化 |
| ② 多端点探活 | `/` `/v1/models` `/api/sessions` `/api/health` `/api/config` | `/`、`/v1/models` = **200 len=12076**（SPA 兜底，**非健康信号**）；**`/api/*` 三个全 404** | HTTP 层在应答，API 路由未就绪 |
| ③ 排除外网/额度 | `https://api.deepseek.com` | **401 Authorization Required** | 401 = 上游可达、仅缺鉴权 ⇒ **排除断网与额度耗尽**两条常见误判 |
| ④ 会话层 | `C:\Users\lzj\.dsh\storages\session_projcache.json` mtime | `2026-09-19 09:21:08`，age **11.81h**（round50 记 10.5h，仍在单向增长=从未再写入） | 会话层早停 |
| 🆕 ⑤ 画面 | `screenshot_window` 912×819 | **主色 `rgb(255,255,255)` 占 638615/746928 = 85.5%** ⇒ 整页**白屏** | 🔴 **新症状**：round50 尚能渲染出输入框外壳（「给智能体发消息」可见），本轮连外壳都没了 |

**结论**：dsh 仍为「静态层活 / 业务后端挂」，且**白屏是加重而非好转**（round42–46 沙箱受阻 → round49–50 历史加载失败 → round51 整页白屏，三级递进）。
⇒ **本轮不投递**（投了它跑不动，纯烧额度）。
⇒ 上报选项沿用 round50 的**四选一**：A 放开写+node 权限 / B 改只读判据 / C 退场我方自审（不推荐）/ **D 允许我重启 dsh web**。
🆕 本轮证据把 D 的必要性再推一级：**进程已 66h 未重启 + 页面从「有外壳」退化到「全白」**，属典型的长跑进程内部状态腐坏；
⚠️ 无人值守轮次**仍未动手**（`taskkill` 会杀掉李老师正在用的窗口）。

---

## 3. 🟢 本轮实质产出：隐私政策占位符数量 —— 三处口径不一，独立实扫核准为 **6 处**

### 3.1 发现的缺陷（同族：一个事实三份口径，且**全都不准**）

| 来源 | 声称 | 是否准 |
|---|---|---|
| commit `253e632` message + `上线材料_提审材料包_v1.md:13` + `:126` + round48/round49 NOTE | **4 处** | ❌ 漏计 2 项 |
| round50 工作区记忆 `MEMORY.md` | **7 处** | ❌ 把格式说明行里的 `【】` 元描述误计为占位符 |
| **round51 独立实扫**（正则 `【[^】\n]{0,40}】` 逐行扫 `上线材料_隐私政策_v1.md`，2608 字） | **6 处** | ✅ 本轮判据 |

**实扫命中明细**（9 条原始命中 → 剔除 2 条元描述、1 条纯文字后得 6 条真占位符）：

| 行 | 占位符 | 归属 |
|---|---|---|
| L18 | `【营业执照上的主体全称】` | 第一节 主体名称 |
| L20 | `【常用邮箱】` | 第一节 联系邮箱 |
| L21 | `【微信号 / 客服电话，需与小程序内「意见反馈」一致】` | 第一节 客服方式 |
| L79 | `【客服方式，与第一节一致】` | 第九节 客服方式 |
| L80 | `【15】` | 第六节 响应时限 |
| L89 | `【可公网访问的静态页 / 公众号图文】` | 第七节 公网 URL |
| ~~L7~~ | ~~`【】`~~ | ❌ 元描述（说明"`【】` 内为需填占位符"这一格式本身），非待填项 |
| ~~L7/L86~~ | ~~`占位符`~~ | ❌ 正文文字 |

**为什么这是真缺陷不是文字游戏**：上线硬阻塞只剩这一项，李老师按「4 处」去填会**漏掉第九节客服方式与公网 URL 两项**，
iOS/审核侧隐私指引因此缺项 ⇒ 直接影响提审。且文档第七节的"3 步清单"自身只列了 4 项，与实际 6 处**自相矛盾**。

### 3.2 已修（4 处，含单源 + 复制件）

| 文件 | 改动 |
|---|---|
| `specs/dev-specs/上线材料_隐私政策_v1.md` L86 | 3 步清单第 1 步改为 **6 处**并逐项列出 ①②③④⑤⑥（含"round51 实扫核准，此前 4 处为漏计"） |
| `specs/dev-specs/上线材料_提审材料包_v1.md` L13 | 材料表「4 处」→「**6 处**」+ 列出 6 项 |
| `specs/dev-specs/上线材料_提审材料包_v1.md` L126 | 提交前 Checklist「4 处」→「**6 处**」+ 指回 `隐私政策_v1.md` 第七节 |
| `review/NOTE_2026-09-19_round49-selfdrive-status.md` L120 | 追加 round51 更正行（**我方文件**，说明 4 处与 7 处均不准、以 6 处为准） |

**未改（交接纪律）**：`review/NOTE_2026-09-19_round48-env-guard-and-submit.md:91` 仍写「4 处」——
该文件是**并发方（另一会话）的执行方分析件**，按纪律我不改他人 NOTE 正文，**在本轮记录待其自行更正**（见 §6 待办）。

### 3.3 变异回灌（证明修的是口径、不是把扫描器改窄）

- 反向证据：扫描正则**未改动**，改前改后同一正则扫同一文件 ⇒ 改前的 6 条命中在改后**仍全部存在**（一条没被删），
  说明我方是**修正了外部陈述**，而非"为了让数字对上而删占位符"。
- 复扫全仓 `grep -rnE "[0-9…]+ *处(占位符|待填|需填)"`：`specs/` 与 `review/` 下「4 处」陈述**归零**，仅剩 round48 NOTE（他人件，刻意保留）。

---

## 4. 门禁（改 `specs/` 前后各跑一次，RC 一律重定向取，不走管道）

| 门禁 | 改前 | 改后 |
|---|---|---|
| `node verify_all.js` | **66/66** · RC=0 | **66/66** · RC=0（`_verify_all_after.txt`：`===== 总览：66/66 套件通过 =====`、`[env-ready] ✅ PASS 11 条`） |
| `node specs/dev-specs/prototype/check_error_codes.js`（A–L） | **RC=0** | **RC=0**（`_errcodes_after.txt`：G/H/I/J/K/L 六段均通过） |
| `node tools/selftest_ad_gates.js`（G1–G8） | **24/24** · RC=0 | — |
| `node tools/selftest_ui_fix.js`（R91 F1/F2） | **27/27** · RC=0 | — |
| `[env-ready]`（第 66 套件） | **11/11** · 正样本 `cloud1-d4gphpoxy337f2a25`、负样本回落带 `console.error` 告警 1 次 | — |

⚠️ 取 RC 的方法（round50 踩过的坑）：`node x.js | tail; echo $?` 拿到的是 **`tail` 的退出码**（恒 0），
门禁红了也会被读成绿 ⇒ 一律 `node x.js > out.txt 2>&1; echo $?`。本轮全程遵守。

---

## 5. 队列（§6）状态

| # | 项 | 状态 |
|---|---|---|
| ① | A6b 代码层兜底 | ✅ 已闭环 |
| ② | R91 F1/F2 UI 缺陷 | ✅ 已闭环（27/27） |
| ③ | AD 适配 G1–G8 复验 | ✅ 本轮复跑 24/24 |
| ④ | R86 超时值 | ✅ 已闭环（round45/46/50 三方一致；round50 明写**后续不必再回读**）⇒ 本轮**未重复回读**，把 cli 通道留给新活 |
| ⑤ | 14 页真数据走查 | ✅ 已闭环 |

⇒ 队列五项**全清**。按纪律**不自创新批次**（8 批投喂收官、无「批次 8」；中优先级项明写"上线后 1 月内补"，擅自开工=越权）。
本轮做的 §3 属**接住上轮 round50 未收干净的事实更正**，不是新造活。

---

## 6. 回执与待办

- [2026-09-19 21:20] R51 **已落** · 隐私政策占位符口径核准为 **6 处**，修 3 份文件 3 处 + 我方 NOTE 1 处 · 证据：`python re 扫描 上线材料_隐私政策_v1.md` → 9 条原始命中→6 条真占位符；`grep -rnE "[0-9…]+处(占位符)" specs/ review/` → 改后 `specs/` 零残留 · commit 07d00f2
- [2026-09-19 21:20] R51 **已落** · dsh 第 7 轮不可用取证（进程 66h / `/api/*` 404 / deepseek 401 / storages 11.8h / **白屏 85.5%**）· 证据：`evidence/selfdrive_20260919_r51/dsh_r51_*.png` + 探活输出 · commit 07d00f2
- [2026-09-19 21:20] R51 **未落** · round48 NOTE:91 的「4 处」未改（他人执行方分析件，按纪律不代改）· 理由：交接纪律「`NOTE_*`=执行方分析件，谁写的谁改」，越权改他人件会掩盖归属 · 已在本 NOTE §3.2 点名待办
- [2026-09-19 21:20] R51 **存疑** · dsh 是否已有李老师在手动恢复/重启的意图未知 · 理由：无人值守，窗口状态会随李老师操作变化；我方只读取证，未点 GUI 未重启

**下一轮该做什么**（若仍无人值守）：
1. dsh 若恢复（`/api/sessions` 不再 404 + OCR 见输入框）⇒ 立刻投 round39/round48/round49/round51 四份待审 NOTE 的复审请求。
2. dsh 仍不可用 ⇒ 不再重复取证（已 7 轮一致），改为**只记一行状态**，把时间投到「并发方新落 commit 的独立复核」上。
3. 提醒李老师：**隐私政策按 6 处填**（这是上线唯一硬阻塞，且刚发现旧清单会漏 2 项）。
