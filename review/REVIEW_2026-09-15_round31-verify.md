# REVIEW_2026-09-15 round31 · R74 建库单源同步复核（含我 3 组注入）+ R70/R71 复核 + R75/R76 两处未守漂移

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 31 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round30-verify.md`（含其 §3 回执）；本轮对象 = `3397deb`（R70/R71/R72）、`925effe`（R73 索引）、`266a678`（R74）、`36c7514`/`6504a5b`（回执与 §7 第 12 条）；HEAD `6504a5b`。
> 本轮**申请放行**（git/node + 3 组自还原注入）⇒ 结论为 **①档**（另有两处我明确标为②档/延后）。

---

## §0 结论

**R74 复核通过，而且这轮我把它当"整类判据缺失"来验**：我做了 3 组自己的注入 —— **M1 复现**（S2 精确点名到「集合|索引名|字段键序|唯一」）、**无害注入**（镜像里插含 `]` `}` 的注释 ⇒ 守卫仍 55/0，**自写扫描器经得起**）、**未守副本探针**（见 R75）。
**R70 已落**（`verify_all.js:171-173` 正是我提的 `isDelimRow` + `tail1`），**R71 结构复核通过**；**R72/R73 我这轮明确延后**（§2.4 给了下轮范围与理由，不假装审过）。
**新提两条：🟡 R75（`verify_docx.py` 的期望值**没人守**，我实测证明）+ 🟡 R76（**重启键两处当前态计数仍是 39**，且 `:88` 同一行自相矛盾）** —— 两条都是 R74 自己那条教训（"自检脚本里的写死期望值也是漂移面""改数字必须 grep 全树"）**没有走完**的剩余面。

| 独立复核项 | 档 | 我的结果 |
|---|---|---|
| 提交链 / 两方一致 | ① | ✅ `2a4d0f9` → `266a678`（R74）→ `36c7514` → `6504a5b`；`ls-remote` = HEAD = `6504a5b49129…`；`status` = 0 行 |
| 门禁 / 全闸 | ① | ✅ `GATE=0`；**55/55**、`VERIFYALL=0`；`[suite-count] ≡ SUITES.length = 55` |
| 形状守卫 | ① | ✅ `42/42 合规` + `Δ(SUITES) 双向为零` + **1 条 WARN**（`batch0_selfcheck`，即 R71 的 `R71-ok` 豁免）—— 与你报的一致 |
| 建库守卫基线 | ① | ✅ `55 通过 / 0 失败`；S1 25/25；S2 `audit_log 4 条一致`、总数 40/40；**S4 十四条断言全绿**（手册 2 处 + 工序清单 5 类模式，每类都校验"至少命中一次"+"值==实测"）；S5 定位到对照表（42 行）/ 表 40 行 == 单源 40 条 / 逐行全等 |
| **我的注入 A（= 你的 M1）** | ① | ✅ 镜像摘掉 `idx_audit_idem` → `❌ S2 audit_log (镜像缺：audit_log\|idx_audit_idem\|idempotency_key:1\|-)`、`❌ S2 索引总数一致 (单源 40 / 镜像 39)` ⇒ **53/2**、exit 1；全闸 **54/55**（**只有建库守卫红**）。还原哈希一致 |
| **我的注入 B（无害，测扫描器）** | ① | ✅ 在镜像 `INDEXES` 内插一条**含 `]` `}` 的注释** → 守卫**仍 55/0** ⇒ 「不该红时不红」+ 自写「括号+引号+注释」扫描器**抗注释中的配对符号** |
| **我的注入 C（未守副本探针）** | ① | ⚠️ 把 `tools/verify_docx.py` 的 `EXPECT` 从 `"40 条索引"` 改回 `"39 条索引"` → **建库守卫仍 55/0** ⇒ 该副本**不在任何守卫的扫描面内**（= R75） |
| 3 组还原 | ① | ✅ 均 `还原哈希一致 = True`；终态 `status` 0 行、守卫 55/0、形状 42/42、全闸 55/55 |

---

## §1 R74 逐条核销

**1.1 判据面 ✅ 与我提的 R67/R69 是同一条路数，但更硬**：S5 不是"数行数"，而是**逐行**比 `集合|索引名|字段↑↓|唯一` 四元组（表 → 单源），且**行号必须连续 1..N**、**表内索引名不得重复** ⇒ "行数对但内容错"也逃不掉。S2 用**字段键序**做多重集合（`k:1`/`k:-1` 参与比较）—— 这对复合索引是**正确**的（键序决定索引语义），你 M4 用"keys 改序"击穿它也印证了这一点。
**1.2 `S4 至少声明一次` 是本守卫最聪明的一处**：它把"文档改了措辞导致模式失配"从**静默漏检**变成**响亮转红**（`命中 0 处` ⇒ 红）。这是我在 round30 提 R69 时没设计到的防御。
**1.3 字面量解析 ✅ 经得起我的注入 B**：`extractLiteral` 手写扫描器 + `vm` 干净上下文求值 + 三条 fail-closed（找不到 decl / 括号不配平 / 求值失败）—— 我刚用"注释里塞 `]` `}`"实测了它最脆的一环，**保持全绿**。
**1.4 你"改一处必须 grep 查全"这条教训的方向 ✅ 对，但没走完**：本轮 grep 出的是 6 处活副本，**漏了 `verify_docx.py` 的期望值**（R75）与**重启键自己的两处**（R76）—— 见 §3。

---

## §2 R70 / R71 复核，与 R72/R73 的**明确延后**

**2.1 R70 ✅ 已落且实现与我提案一致**：`verify_all.js:171-173` = `isDelimRow` + `tail1`（剔纯分隔线后取最后 1 行）+ `|| NO_SUMMARY_TAIL.has(rel)` —— **零新增豁免**（`verify_seed_data` 因剔分隔线自然通过），与我的实测预期完全一致。
**2.2 R70 的"增量价值"由你证成，我按代码推演复核 ✅**：你在 `adminExport/selftest.js:162`（R49 中途汇总行之后）注入"段标题 + 1 条 ✅ + `exit(0)`"，新旧判据双对比 = 旧放行 / 新判红。**我未重复该注入**（本轮已用满放行预算），但我核了代码路径：该注入的末行为 `✅ R70 探针断言`（非纯分隔线）⇒ `tail1` 不含 `N 通过` ⇒ 判红 ✅ —— **逻辑闭合**，故记为"②档（你实测）+ 我的代码级确认"。
**2.3 R71 ✅ 结构复核通过（静态①档）**：新规则扫 `check(name, true/1, ...)`，**未豁免 ⇒ ERROR、带 `// R71-ok: 理由` ⇒ WARN**（理由必须写明、不许裸标）；基线的 **1 条 WARN = `batch0_selfcheck`** 正是那条豁免 ✅。你把"实参从未被累积 ⇒ 41 个文件全判零恒真 = **守卫自己假绿**"也写进注释了 —— 这是你们第三次自查出守卫自身缺陷（前两次是 `'r'/'n'` 与 `\)`）。
**2.4 R72 / R73 —— 我这轮**没有**审内容，理由与下轮范围如下（不假装审过）**：
- 我只做了**结构级①档核验**：`tools/check_idempotency.js` 的 **I1–I7** 与**三张登记表**（`EXEMPT_WRITE`/`EXEMPT_KEY`/`NOT_IMPLEMENTED`）都在，且 **I6 的"豁免不腐"对三张表都有 `仍然成立` 断言**（`EXEMPT_WRITE[...]`/`EXEMPT_KEY[...]` 各一条循环 + `NOT_IMPLEMENTED[...]` 一条）—— 这正是你点名要我核的那一点，**机制在** ✅；I1 的 fail-closed（`≥5 个`）也在。
- **未审**：①每条豁免理由**语义上**是否站得住（13 条 `EXEMPT_WRITE` + 若干 `EXEMPT_KEY`）；②I3 的"注入形态改判 `service.js`"这条特例是否留了绕过路径；③R72 与 R73 **分母不同**（23 vs 22）这件事在文档里的表述是否已足够防误读。
- ⇒ **下轮（round32）我建议专审 R72/R73**，范围就是上面三条；你我在点名时对齐即可。

---

## §2.5 新发现（R75 / R76）

### 🟡 R75（新）：`tools/verify_docx.py` 的 `EXPECT` 是**第三份写死计数**，且**不在任何守卫的扫描面内**

- **证据（我实测，①档）**：把 `verify_docx.py` 的 `"40 条索引"` 改回 `"39 条索引"` → **`check_schema_sync.js` 仍 55/0** ⇒ 该副本无人守。守卫的扫描面是 `SINGLE` / `MIRROR` / `MANUAL` / `STEPS` 四个路径，**不含 `tools/verify_docx.py`**（也不含重启键）。
- **为什么这正是你们自己的教训**：重启键 §1.3 ⑨ 写着"**自检脚本里写死的期望值同样是漂移面** —— `verify_docx.py` 的 `EXPECT` 要求 docx 必须含「39 条索引」，计数一改**它自己先转红**"，但**处置只做到"改注释提醒"**（`:37-40` 那两行注释说"当前值以 S4 为准""计数漂移别只改这里"）⇒ **提醒 ≠ 机械**。
- **修法（二选一，约 3 行）**：
  1. 把 `tools/verify_docx.py` 纳入 S4 扫描面（用同一条正则扫 `"N 条索引"`，只认「活文档」那两行、`SMOKETEST_RUNBOOK` 那行继续豁免，理由与现注释一致）；**或**
  2. 让 `EXPECT` **从单源派生**：`check_schema_sync.js --list` 已能打印 `集合 25 张 / 索引 40 条（unique 10）`，`verify_docx.py` 直接解析它即可（**期望值不再有第二份**，这是更彻底的方向）。
- **验收 + 变异**：采用方案①后，把 `verify_docx.py` 的 40 改 39 → 建库守卫**必须转红**；采用方案②后做同样的改动 ⇒ 期望值根本没得改（`EXPECT` 里已无数字）。**基线两者都必须仍 55/55 + `verify_docx` 0。**

### 🟡 R76（新）：**重启键两处当前态计数仍是 39**，且 `:88` **同一行内自相矛盾**

- **证据（①档，逐字）**：
  - `specs/dev-specs/★知识存储点_2026-09-10.md:88`（**当前态**陈述，现在时）："✅ **A7**（已定案）= 不支持，索引改**控制台手工建**（`collections.js` 共 **39** 条索引，其中 **10 条 `unique:true`**；清单见 `新手上云操作手册.md` §5.2b）" —— 而**同一行后半句**又写 B2："⇒ `initDb/collections.js` 已补 `idx_audit_idem`（**39 → 40**）" ⇒ **一行之内自相矛盾**（前说"共 39"、后说"39 → 40"）。
  - `:135`（**人工项**，OPEN 项）："上线前另有一项：补齐 **39** 条索引" ⇒ 应为 40（线上实际 1/40）。
- **为什么这比文档漏改更值得单列**：重启键是**项目侧唯一状态入口**（`review/README.md §5`："活过一轮的项写这里，换会话只读它就能接上"）⇒ 它自相矛盾时，**新任接线人照它建索引会漏一条**，而这正是 R74 要防的场景本身。
- **明确**不列的两处（我核过，属**历史快照**，保留正确）：`:85`"39 条索引**全部**建不上"（2026-09-14 云端实测记录）、`SMOKETEST_RUNBOOK.md` 的"39 条索引"（同一次实测）—— 与你守卫文件头写的理由一致 ✅。
- **修法**：`:88` 改为"共 **40** 条索引"；`:135` 改为"补齐 **40** 条索引（现 1/40）"；并把**重启键纳入 S4 扫描面**（它的当前态句子用的是同一种措辞 `共 **N** 条索引`，一条正则即可覆盖；历史快照那类要**显式豁免**而不是靠"扫不到"）。

### 🔵 一条观察

`S4 至少声明一次` 对每个模式都要求"命中 ≥1"——**这很好**，但也意味着：**若某文档把同一事实写成两种措辞（一种在模式内、一种不在），新的那种不会被发现**。建议在守卫文件头写一句"**新增声明请沿用既有措辞，或在 S4 的模式表里补一行**"，把这条约定前移到编写时。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`；存疑项写「存疑 + 理由」。

（暂无 —— 待本份被取件后追加）

---

## §4 队列

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🟡 **R75** `verify_docx.py` 的期望值纳入扫描面（或改为从单源派生）② 🟡 **R76** 重启键 `:88` / `:135` 两处计数改 40，并把重启键纳入 S4（历史快照显式豁免）③ 🔵 S4 文件头补一句"新增声明沿用既有措辞"的约定 ④ **R72/R73 内容复审**待我 round32 专审（范围见 §2.4） |
| **待人工（复审方核不了）** | ① wechatide client 授权 → **先补 40 条索引**（线上 1/40；⚠️ 其中一条是 `audit_log.idx_audit_idem`，缺它会让幂等预检全表扫描）→ 真云三验 ② `ADMIN_SETUP_TOKEN` 的**值** ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ **R45** iOS 过滤（套餐 UI 前置） |
| **下一步（条件式）** | **若 `git log -1` 仍是 `6504a5b`**（且 `ls-remote` 与之相等），我 round32 **专审 R72/R73**（口径见 §2.4）+ 复跑 R75/R76 的验收；**若 HEAD 已前进**，以新 HEAD 为准，重跑全闸 + 三个守卫 + 上轮我做过的那几组注入。 |
