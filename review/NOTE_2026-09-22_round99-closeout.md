# round99 · 批次收口回执（T6 证据推进 + R122 守卫 + 两处修复）

- **日期**：2026-09-22（收口时刻 20:50）
- **范围**：round99 全批次 —— T6 外卖账单列名核对 → R122 语法守卫 → P0 / apply_indexes 修复 → 门禁 → 推送
- **上游**：`review/NOTE_2026-09-22_round98-dine-discount-accounting.md`（T5 堂食折后口径）
- **T6 专项证据件**：`review/NOTE_2026-09-22_round99-waimai-bill-column-crosscheck.md`（**本件不复述其内容**，只登记落位）
- **门禁证据**：`review/evidence/gate_95_r99.txt`（3123 行）
- **提交**：`9092f64`（代码面） + `469cabb`（文档面 / 证据件）→ **已推 dev**（判据 `ls-remote origin refs/heads/dev` == `rev-parse HEAD` == `469cabba`）

## 1 本批次逐项（事项 → 证据 → 状态）

| # | 事项 | 证据 | 状态 |
|---|---|---|---|
| 1 | T6 · V2「商品总价是否含打包费」 | 李老师 12 张商户后台截图逐列核对 ⇒ 6/6 样本指向「**已含**」，零反例 | **推进至「倾向已含」**（账单面未闭合，见专项件 §5–§7） |
| 2 | R122 新增 `tools/check_js_syntax.js` | 套件 94→95；编译 675 个 `.js`；锚点 6/6；下界 400 满足 | 落 |
| 3 | **P0** 修复 `pages/month/input.js` | `node --check` rc=0；**出码 `√ preview` RC=0**（真机判据，见 §3） | 落 |
| 4 | 修复 `tools/apply_indexes.js` 块注释提前闭合 | R122 首发即抓到；该工具自 2026-09-17 起**从未能跑** | 落 |
| 5 | 活文档同步：A.11.7 V2 行 / `core/13_…总览.md` §A.11 / `★知识存储点` 两处套件数 | 见 `469cabb` | 落 |

## 2 门禁与变异回灌

- **门禁**：`node verify_all.js` → **总览 95/95 套件通过**、**RC=0**、PASS 95 行 / FAIL 0 行。
  关键项：`suite-count` 95、`suite-count-claims` 11/7、`suite-coverage` 10/9、`suite-assert-counts` 20/7、
  `batch8b-features` 123/15、`batch8c-amort-batches` 59/7、`r85-takeaway` 104/22、`js-syntax` 2 条。
- **R122 变异回灌 4/4**（脚本 `_gui/_mut_r99.py`；每步 md5 逐字节还原断言）：

| 组 | 变异 | 预期 | 实测 |
|---|---|---|---|
| BASE | 无 | 绿（且含 R69 收尾行） | RC=0 ✅ |
| M1 | 生产文件语法改坏（`utils/takeaway.js` 末加 `}`） | 红 + 点名文件 | RC=1，命中 `takeaway.js` ✅ |
| M2 | 锚点改成不存在的路径 | 红 + 报未命中 | RC=1，命中「未命中锚点」✅ |
| M3 | 下界调到 9999 | 红 + 报下界 | RC=1，命中「下界」✅（证下界非恒真） |
| N1 | 在排除面 `review/` 放语法坏文件 | **仍绿** | RC=0 ✅（证排除面确实生效） |
| 收尾 | 还原后复跑 | 绿 | RC=0 ✅ |

## 3 出码（唯一真机判据）

- **出码时刻 2026-09-22 20:48**（25 分钟有效 ⇒ 约 **21:13 失效**），`√ preview` **RC=0**，`TOTAL 186.8 KB / 191325 B`
- **版本**：`469cabb`
- **判定**：**P0 修复后微信开发者工具能编译通过并出码** —— 修复前该步骤报 `code 10`（编译失败、小程序起不来）
- 码的落位：`_gui/qr_now.jpg`（**唯一真相源**，短名可自证），旧码已移入 `_gui/_qr_archive/`

## 4 未闭合 / 待办（**不做补全式断言**）

1. **T6 真正闭合仍需一份脱敏账单 Excel** —— 订单详情页证明不了「账单列名」（见专项件 §7 定案清单，一格可定案）
2. **摊销讨论**（李老师本轮指定转向）：待定顺序 = 「摊销在利润里是**减项还是成本**」→ 三副本收口 → 尾差残值
3. 仓根 + `review/evidence/` 下约 40 个历轮**未跟踪**临时脚本 —— **待李老师点头再清理**（不擅自删）

## 5 本轮沉淀（已写入 `review/README.md §7`）

- **第 24 条**：注释里写「通配符 + 斜杠」会**提前闭合块注释** ⇒ 注释写完必须**编译一次**，`grep` 看不见
- **第 25 条**：同一文件的多条 `Edit` **必须串行**发（并行只有最后一条生效；round74 已有一例，本轮复发）
- **第 26 条**：别把「门禁全绿」当「没有缺陷」—— 要问「**这一层谁在管**」；出码失败是**最便宜的真机探针**
