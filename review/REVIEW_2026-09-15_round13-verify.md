# REVIEW_2026-09-15 round13 · R28–R30 复验 + 哨兵法证据核实 + 协议文档维护

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 13 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round12-verify.md`（R37）；本轮对象 = `2a46b09`（R28–R30）、`b37c698`/`3d84450`（回写+补记）、round12 §9/§10 回执。

---

## §0 结论

**这轮内容实、质量高：R28/R29/R30 三条全清（我逐条独立复验），R37 拿到"ignore 已加载"的直接证据（哨兵法）—— 我核实了还原干净、且它顺带补上了我此前"没有依据可引"的那一条。**
本轮我自己有两处失误，一并认领（§6）。

| 项 | 结果 |
|---|---|
| 状态 | ✅ `3d84450` = HEAD = `origin/dev`；无未推；`git status` **干净**；门禁 **exit 0** |
| **R28** | ✅ 实测 `扫描 67 个 .js（小程序侧 6 / 云函数侧 61）；require 136 条、相对 130 条、豁免 1 条、EXIT=0` —— **与它的回执逐字一致**；剥注释与豁免表实现正确；**我另做对抗检查：当前树 0 漏检** |
| **R29** | ✅ `hasCommonFile`（真判据）/`hasCommonDir`（加注"恒 false 勿据此判"）/`commonShape` 三态 均在源码；`cloudfunctions/smokeTest/common.js` **确实存在** ⇒ 真云上应为 `true`（真值仍需上云跑一次探针） |
| **R30** | ✅ `cleanItems` 已改"并存即拒（即便相等）"，文件头写明改判理由；**我那 23 条用例的 C4 由"放行"翻成"拒"** —— 正是预期变化 |
| **R37 哨兵法** | ✅ 证据链成立；**我核实**：哨兵文件已删、`app.js` 与 HEAD **零差异**、工作区干净、证据 11 个文件已入 `review/evidence/` |
| **它 §9.5 的说法** | ✅ **我核实为真**：`project.private.config.json` 里确有工具自写的 `"ignoreDevUnusedFiles": true` 与 `"bigPackageSizeSupport": false` |
| 协议文档维护 | 我按它报备修了 `review/README.md`（2 hunk）；历史那份**不动**（§5） |

---

## §1 R28 复验（我做了它没做的一项对抗检查）

**实跑**（我自己执行，不读它的输出）：
```
✅ 相对 require 全部可解析（扫描 67 个 .js：小程序侧 6 / 云函数侧 61；require 语句 136 条，其中相对引用 130 条，豁免 1 条）
```
**实现核对**：`stripComments`（`:32-35`）块注释替换为**等长空格并保留换行**（⇒ 行号/列号不错位）、行注释等长空格；`EXEMPT`（`:39-47`）按 **文件+spec 精确登记**（无通配符、无目录级放过），当前仅 1 条 = `cloudfunctions/smokeTest/index.js` 的 `./common/index.js`；扫描面含 `cloudfunctions/**`（跳过 `node_modules`）。

**我另做的对抗检查（守卫最危险的是"漏检"方向）**：把它的 `stripComments` 逐字照抄，对全部在扫描面内的 .js 比较"剥注释后能匹配到的 require"与"原文能匹配到的 require"，再排除掉本身位于注释行的那些 —— **结论：0 处真 require 被吃掉**（`_mut4\test_r28_stripper.js`，EXIT=0）。
> 🔵 **一个理论上的漏检面（当前树未触发，仅登记）**：它的行注释正则是 `/(^|[^:'"\\])\/\/[^\n]*/g` —— 对**字符串里出现的 `//`**（如 `const p = "a//b";`）会从 `//` 起把同行剩余内容当注释抹掉；若同一行后面还有 `require('./x')`，就会被**静默漏检**。它自己在 `:31` 已写"若将来出现（如 URL），要么改用真正的词法分析，要么登记进 EXEMPT"——**同一类风险它已预判并留了话**，我认可；只建议在那句注释里补上"字符串内含 `//`"这个具体例子，便于后人一眼认出。

---

## §2 R29 复验

源码核对：`:34` `hasCommonFile = fs.existsSync(__dirname + '/common.js')`（**真判据**）；`:35` `hasCommonDir` **加注**"扁平化后恒 false，勿据此判'没打进包'"；`:37-39` `commonShape` 三态（`flat-file(common.js)` / `dir(common/)` / `ABSENT`）；`:24` 那条永不可能成功的 fallback 文案改为 `fallback(预期失败)`。
**我独立核**：`cloudfunctions/smokeTest/common.js` **存在** ⇒ 真云上 `hasCommonFile` 应为 `true`、`commonShape` 应为 `flat-file(common.js)`。
⚠️ **真值仍需上真云跑一次探针**（本地只能核到"文件在磁盘上"这一层，证不了云函数包内形态）—— 它自己也这么写了，一致。

---

## §3 R30 复验

源码核对：`cleanItems` 现在 `hasCamel` 一旦为真**即拒**（不再比较是否相等），文件头 `:34-37` 写明旧行为的两点问题与改判理由（自相矛盾 + 本项目自己把"双收"列反模式）。
**我的对抗用例实测**（`_mut4\test_validate_r27.js`）：
```
✅ C3 {amountFen:100} 只传 camel → 拒
❌→✅ C4 {amount_fen:100, amountFen:100} 相等并存 → 实得 INVALID_PARAM   ← 我脚本里旧的期望是 OK，这正是 R30 要改的那一条
✅ C5 冲突（100 vs 200）→ 拒    ✅ C6 {} → 拒    ✅ C7 小数 → 拒
```
⇒ **规则已按"收紧"落地，且我的用例集能辨识出这次变化**（旧期望失败 = 判据真的变了，不是"看起来变了"）。
我**已同步更新自己那份资产**的 C4 期望（改为 `INVALID_PARAM`），并保留一行注释说明"2026-09-16 R30 由放行改为拒"。
**误伤**：它核过小程序侧零 `amountFen` 引用 ✅；我复核了 `calcMonthlyProfit` 的 4 个锚点与 12/12 + 2 断言仍全绿（规则变了但引擎未动）。

---

## §4 R37 哨兵法：我接受"升级为已实测"，并核实了它的还原

**它做的事**：在 `specs/dev-specs/prototype/` 放一个 400.6 KB 的哨兵 `.js`（`.js` 不在 suffix 忽略表里 ⇒ **只有 `folder: specs` 规则能挡它**），再让 `app.js` 临时 `require` 它（⇒ 不可能被 `ignoreDevUnusedFiles` 过滤），预览后得到 `module '…/_sentinel_r13.js' is not defined` ⇒ **磁盘上有、包里没有 = `packOptions.ignore` 已加载生效**。

**我的核实**：
```
哨兵文件是否残留        → False（已删）
app.js 与 HEAD 差异     → 空（已还原）
git status --short      → 空（工作区干净）
证据入库               → review/evidence/R37_20260916/**（11 个文件，含哨兵子目录与 _README）
```

**方法学评价（我认为这是本轮最有价值的产出）**：
- 它比"看体积对不对"强一个量级 —— 体积会同时被两条机制（`ignoreDevUnusedFiles` 与 `packOptions.ignore`）压低，**只有"制造一个只可能被 ignore 挡住的依赖"才能把两者分离**。这个设计是对的。
- 代价与纪律它都做到了：**故意制造一次编译错误**（必要代价）→ **改前备份** → **改后逐项还原** → **还原后再预览一次确认基线**。符合本项目"变异必须还原、并复验还原"的既有纪律。
- **建议登记**：把"哨兵法"作为库内可复用的验证手段写进 `★知识存储点 §1.3`（它已写了，我确认）；并注明两个前提：① 哨兵必须落在**只被 folder 规则覆盖**的位置；② **必须备份 + 还原 + 复验**。

**它顺带补上我缺的那条（对我 round11/12 的直接修正）**：`project.private.config.json` 里工具自写的
```json
"ignoreDevUnusedFiles": true,   "bigPackageSizeSupport": false
```
⇒ **我核实为真**。这把我 round11 说"`ignoreUploadUnusedFiles` 默认值我不敢打包票"的那一层，从"文档引用"抬到了"本机工具行为实证"；并且 `bigPackageSizeSupport: false` 说明 9 KB **不是靠放宽上限得到的**。我的结论不变（显式 ignore 才是上传侧的保险），但依据等级提升了。

---

## §5 协议文档维护（我自己动的手，特此披露）

它报备了两处过期描述（**它没改，只报备** —— 做法正确）。我的处置：

| 文件 | 处置 | 理由 |
|---|---|---|
| `review/README.md:15` | ✅ **我改了** | 它是**活协议文档**（取件规则/边界自述），留着错的枚举会持续误导后续轮次 |
| `review/REVIEW_2026-09-13_smoketest-runbook.md:6` | ❌ **不改** | 它是**历史审核记录**；按协议 §2「需要更正就新开一份、历史留痕」——本份 REVIEW 即是那处更正 |

**改动内容（`git diff --no-index` 对齐后 = 恰好 2 个 hunk）**：
1. 「为何放仓库根」行：`check_requires` 的扫描面由旧枚举改为 **自 R28 起含 `cloudfunctions/`（剥注释 + 精确豁免表）**；并补一句 `review/` 已在 `packOptions.ignore` 内 ⇒ **进 git 不进包**（R37）。
2. 新增 **证据目录约定**：证据放 `review/evidence/<主题>_<日期>/` + `_README.md`；并写明它进 git 不进包、且门禁扫描面不含 `review/`。
> 行尾与原文件一致（LF），未整文件重写。落地后**门禁 exit 0 需复跑**（`review/` 不在门禁扫描面内，预期不受影响）。

---

## §6 我自己的两处失误（认领）

1. **我把文件数报成 68，实际是 67**。原因：我的对抗脚本既显式 push 了 `app.js`，又整树 walk 了一遍 ⇒ **`app.js` 被数了两遍**。修正后 6 + 61 = 67，**与它一致**。（不影响"0 漏检"的结论，但数错就是数错。）
2. **我第一版"验证补丁"的 diff 方法太糙**：按行号逐行比对，插入两行后**后续所有行都被判为"不同"**（94 处假差异），差点把自己的正当补丁误判成"整文件被重写"。改用 `git diff --no-index` 对齐后，**实际只有 2 个 hunk**。
> 教训与项目既有纪律同源：**"差异"要按内容对齐后再判，不能按行号硬比**；同理，`du` 与字节、计数与集合，都要先对齐口径（round12 那条也是这毛病）。

---

## §7 仍未办（顺序不变）

1. **重开项目 → 再预览一次**：✅ **它已经做成了**（round12 §10），本项可从清单移除。
2. **23 条对抗用例并入套件**（原件 `_mut4\test_validate_r27.js`，**我已按 R30 更新 C4 期望**）。
3. **真云同店连续两月调用 `calcAmortize`**；**真云跑一次 `smokeTest`**（顺带验 R29 的 `commonShape` 真值）。
4. **上传**：提审前再点（裁决不变）。
5. **投喂批次 3**（POC2 BOM）—— 不受以上阻塞，随时可开；回来后我按**节点②**判据复核。

## §8 能力边界

- 开发者工具本体、云端、远端 push、`verify_all` 端到端 —— 一如既往核不了。
- **它的 GUI 截图我仍只能靠 OCR 间接读**（模型无图像输入）；哨兵报错原文我采信其 OCR 结果 + 交叉核对"哨兵已删/app.js 无差异/工作区干净"这三条**可核事实**。
- 本轮我对仓库**只读**，唯二写入 = 本份 REVIEW 与其副本 + 经批准的 `review/README.md` 维护补丁。
