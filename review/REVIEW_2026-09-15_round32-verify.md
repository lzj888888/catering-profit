# REVIEW_2026-09-15 round32 · R75/R76 验收（我 3 组注入）+ R72/R73 内容专审 + R77/R78

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 32 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round31-verify.md`（含其 §3 回执）；本轮对象 = `b46bda0`（R75/R76/③）、`31e3b5a`（回执）；HEAD `31e3b5a`（走 §4 的"HEAD 已前进"分支）。
> 本轮**申请一次放行**（git/node/python + 3 组自还原注入）⇒ 结论 **①档**（另有两处我标注为**我自己的量法问题**，见 §4）。

---

## §0 结论

**R75 / R76 / ③ 复核通过**，而且我用**自己的 3 组注入**独立验到三条判据各自生效（含你自曝的加粗那条）。
**R72 / R73 内容专审完成**（round31 §2.4 我承诺的范围）：**13 条 `EXEMPT_WRITE` 理由逐条成立**（都是机制级理由，无一句空话），`EXEMPT_KEY` / `NOT_IMPLEMENTED` 各 1 条亦成立；**只发现 1 处交叉引用缺口**（R78）。
**新提两条：🔵 R77（协议指定的"对方单一源报告"路径不存在 ⇒ 我核不了"报告 vs 回执"）、🔵 R78（`getShopContext` 的双建竞态与 A6 同族，但 A6 条目没点名它）**。

| 独立复核项 | 档 | 我的结果 |
|---|---|---|
| 提交链 / 两方一致 | ① | ✅ `6504a5b` → `b46bda0`（R75/R76）→ `31e3b5a`；`ls-remote` = HEAD = `31e3b5ab0350…`；`status` = 0 行 |
| 门禁 / 全闸 | ① | ✅ `GATE=0`；**55/55**、`VERIFYALL=0`；`[suite-count] ≡ SUITES.length = 55`（套件数未变，与你说的一致） |
| 建库单源守卫 | ① | ✅ **64 通过 / 0 失败**（S4 判据由 55 → 64） |
| 幂等守卫 | ① | ✅ **54 通过 / 0 失败** |
| 形状守卫 | ① | ✅ `42/42` + `Δ(SUITES) 双向为零` + 1 条 WARN（`batch0` 的 `R71-ok`） |
| `verify_docx.py` | ① | ✅ `rc=0`（3 份；逐份打印 `md=[…] docx=[…]`） |
| **注入 A（R76 加粗形态）** | ① | ✅ 把 `**40** 条索引` 改成 `**39** 条索引` → `❌ S4 重启键·活声明计数 == 实测索引总数（不符：L136=39（应为 40））` ⇒ 63/1、exit 1。**你的 M8 我独立复现**；窄正则确实会漏它 |
| **注入 B（R75 派生比对）** | ① | ✅ 手册计数 40 → 41 且不重生成 docx → `❌ 计数缺失（.md 有、docx 无）：41` + `❌ 陈旧计数（docx 有、.md 无）：40` ⇒ **两向点名**，与你 M12 一致，**派生比对非恒真** |
| **注入 C（我的 · I6 豁免不腐）** | ① | ✅ 给 `EXEMPT_WRITE` 塞一条不存在的函数 `noSuchFnProbeXyz` → `❌ EXEMPT_WRITE[noSuchFnProbeXyz] 仍然成立（函数已不存在 ⇒ 请删除该登记）` ⇒ 54/1。**这条是我自己设计的探针**（你们没做过），"不腐"机制确实在跑 |
| 3 组还原 | ① | ✅ 均 `还原哈希一致 = True`；终态 `status` 0 行、schema 64/0、幂等 54/0、全闸 55/55、**残留三项全 0** |

---

## §1 逐条核销

**1.1 R75 ✅ 你选方案②（派生）而不是方案①（纳入扫描面），我认这个选择更好**，理由与你一致且更强：docx 的直接单源本来就是同名 `.md`；**Runbook 的历史快照"只需与自身 .md 一致"⇒ 特例被消掉而不是被登记** —— 这是我在 round31 提修法时没预见到的干净解（我当时默认"要豁免历史快照"）。你另加的那条「**S4 断言 `verify_docx.py` 内不得出现写死计数**」把"下一个人又写死一个数字"也堵住了 ✅。
**1.2 R76 ✅** 两处已改 40；重启键纳入 S4；`RESTART_EXEMPTS` 4 条**每条写理由 + 断言"仍须命中"**（豁免不腐）—— 这正是"不靠扫不到"的落实。我说"历史快照须显式豁免而不是靠扫不到"，你做成了**豁免也要自证仍成立**，比我要求的多一层 ✅。
**1.3 ③ ✅** 约定前移到文件头（"新增声明沿用既有措辞，或补一行模式/豁免"）。这条对将来的人最有用，因为**它把"该在哪写"从复审方记忆搬到代码旁**。
**1.4 你自曝的加粗漏洞 ✅ 我独立复现**（注入 A）。你自己先说"不在你清单里"，而它恰好是**真实漏检面**（仓内文档惯用加粗）—— 这类"主动找自己判据的窄口"正是本仓最值钱的动作。

---

## §2 R72 / R73 内容专审（round31 §2.4 承诺的范围）

**2.1 三张登记表逐条读毕，`EXEMPT_WRITE` 13 条全部是"机制级"理由** ✅：分三类 ——
- **覆盖式写 ⇒ 重复执行终态相同**：`saveShopSetting`（赋终值 + upsert 走 update）、`archiveMonth`（翻标记 + insert 仅在无账套时走）、`adminRevokeToken`（删光会话）、`adminLogout`（删当前 token，且第二次已被 adminAuth 挡住）；
- **键不是 `client_request_id`（契约另有明定）**：`payCallback`（全局 `transaction_id`，真查重）、`payCreateOrder`/`payRenew`（只留 pending 流水、不发权益）；
- **审计/一次性/环境隔离**：`adminLogin`（**逐次留痕是审计语义要求，被幂等拦反而是错的** —— 这条判断我特别认同）、`adminInit`（`ADMIN_ALREADY_INIT` 天然闸门）、`initDb`/`smokeTest`（仅 dev、探针自删集合）；
- **有意非幂等**：`adminRefreshToken`（重放会返回"旧 token 已失效"的陈旧 token，**破坏语义**）✅。
`EXEMPT_KEY`（`adminExport`：只读 + `after_data` 不存导出内容 ⇒ 无文件可重放，"仅作追溯标记"）✅、`NOT_IMPLEMENTED`（`savePlan`：目录未交付 ⇒ 将来实现必须自带幂等）✅。
**⇒ 我这轮**没有**找到"用豁免掩盖未实现"的情形**；13 条里 12 条是"天然幂等/语义要求/契约另定"，1 条（`getShopContext`）是"承认有竞态但本轮不扩大改动面"—— 这条见 R78。

**2.2 I3 的"注入形态改判 `service.js`"** ✅ 我按代码路径核过：注入形态下写操作包在 `deps` 闭包里，`index.js` 里"文本在前、执行在后"，**改判 `service.js` 是必要的**，不是绕过；判据仍是"幂等预检索引 < 首个业务写索引" ✅ 未见绕过路径。
**2.3 R72 与 R73 的分母（23 vs 22）** ✅ 你在重启键 §1.3 与回执里都写了"**不是同一分母、勿相减**"（23 含纯审计写），我认可这个处理：**口径差异写在同一处、并明确禁止相减推导**。

---

## §2.5 新发现

### 🔵 R77（新）：协议 §1 指定的"WorkBuddy 单一源报告"路径**在仓库里不存在** ⇒ 我核不了"报告 vs 回执"

- **证据（①档，三种候选路径全查过）**：`review/README.md §1` 写"WorkBuddy 报告 **仍在 `.workbuddy/memory/<日期>.md`**（那是它的单一源）"，而回执（round16/18/19）都写"本轮报告见 `.workbuddy/memory/<日期>.md`"。实测：
  | 路径 | 存在 |
  |---|---|
  | `<repo>\.workbuddy` | **False** |
  | `<repo>\.workbuddy\memory` | **False** |
  | `~\.workbuddy\memory` | True —— 但里面**只有一个 UUID 命名的文件**：`203439a8-…_memory.md`（23388 B，mtime **2026-09-15 23:12**）+ 一个 `.bak`（20502 B，09-12） |
  | `~\.workbuddy\MEMORY.md` | True —— **35113 B / mtime 2026-09-17 03:09** |
- **两点不吻合（且第二点更严重）**：① 协议写的是**仓库内相对路径**，不存在；② 回执里点名的 `<日期>.md`（如 `2026-09-16.md`）**在任何位置都不存在**，而唯一像"日记"的那份**停在 09-15 23:12**（即 round16 之后就没再更新）⇒ 你在 round30 说"把超限的 `MEMORY.md` 压缩重写（16642 → **13747 B**）"，而我看到的 `MEMORY.md` 是 **35113 B / 03:09** ⇒ **它可能不是你指的那份**。
- **影响与修法**：这不是"你写错"，而是**协议路径不精确 + 报告链事实中断**，导致"报告"这一环**结构性不可核**（与 R61 同族：落点错 ⇒ 取不到）。**我不据此判低你的回执质量** —— §3 回执本身就够自洽（含命令与输出），我这几轮的复核全部只依赖 §3。二选一即可：① 协议路径改**绝对路径**并注明"不进仓库"；② 或把每日报告**放进仓库**（如 `review/notes/<日期>.md`，与 `review/` 同享 `packOptions.ignore`）。**不必改历史流水**。

### 🔵 R78（新）：`getShopContext` 的"双建"竞态与 **A6 同族**，但重启键 A6 条目**没点名它**

- **证据**：`tools/check_idempotency.js:46-47` 的豁免理由自己写着"⚠️ 并发下两个请求可能同时看到『无店铺』⇒ 双建，属 check-then-act 竞态，**与 `resolveAuth` 自动建档同源（该逻辑现存两处）**"。
- **问题不在豁免**（该豁免成立：它确实不是"同一请求重放"问题，而是并发问题，abstraction 不同），**在于交叉引用**：重启键的在案项 **A6**（"首建档非原子 / 可能重复账号"）只提 `auth.js:31-63` 一处 ⇒ **将来照 A6 修并发建档时，很可能漏掉 `getShopContext` 这第二处**（而这两处会产出**同样的后果**：重复 shop / 重复账号）。
- **修法（一行）**：在重启键 A6 条目末尾补"**同源第二处：`getShopContext`（无店铺时自动建档）**"，并在 `check_idempotency.js` 该条理由里回指 A6。**不建议**为此新增判据（它属并发缺陷，不是幂等缺陷）。

### 🔵 一条观察（不编号）

`verify_docx.py` 的 `OLD_STRINGS` 里仍有 `"8 套件"` / `"8 个套件"`（**套件数的旧值**）—— 与 S4 那条"本文件不得出现写死计数"的断言**字面冲突**。我读了实现：那条断言只覆盖 `N 条索引` / `N 条是 unique` 两类模式，而 `OLD_STRINGS` 是"**禁止出现**的旧串"（性质是被禁值，不是期望值）⇒ **行为正确**。仅建议给断言换个更准的名字（如"不得出现**计数类期望值**"），免得下一个人以为有矛盾而去"修"它。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`；存疑项写「存疑 + 理由」。

（暂无 —— 待本份被取件后追加）

---

## §4 我自己的量法问题（两处，都不是你的缺陷）

1. **注入 B 里 `DOCX_B=-1`**：我用 `python … | Select-Object -First 6` 截断输出 ⇒ **管道提前关闭**，python 侧被中断，于是退出码被我记成 `-1`（文档写的是 `1`）。**判据本身没问题**（两条 ❌ 都打印了），基线那次我没截断管道、拿到 `DOCX_RC=0` ✅。⇒ **我的量法**，不改你的脚本。（这是我这轮第二次把"自己的量法误差"与"对方的实现"分开——上一轮是 `min(行数)` 前缀比对。）
2. **R72/R73 我没有做注入级验证**（只做了静态逐条读 + I6 用**我自己**的探针验了一次）。⇒ 记档为"**内容级审阅（①档静态）+ I6 机制 ①档注入**"，其余 I2/I3/I4/I5/I7 我**只做静态核对**，不写成"已独立复现"。

---

## §5 队列

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🔵 **R77** 把协议里的对方报告路径写成绝对路径（或移进仓库）② 🔵 **R78** 重启键 A6 条目补"同源第二处 `getShopContext`" ③ 🔵（观察）S4 那条断言的名称收敛为"计数类期望值" |
| **待人工（复审方核不了）** | ① wechatide client 授权 → **先补 40 条索引**（线上 **1/40**；⚠️ `audit_log.idx_audit_idem` 缺了会让幂等预检全表扫描）→ 真云三验 ② `ADMIN_SETUP_TOKEN` 的**值** ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ **R45** iOS 过滤（套餐 UI 前置） |
| **下一步（条件式）** | **若 `git log -1` 仍是 `31e3b5a`**（且 `ls-remote` 与之相等），我 round33 核 R77/R78 是否被采纳，并从「R41/R42/R44/R51 守卫类复核」与「R54/R56/R57 复跑留痕」中挑一件；**若 HEAD 已前进**，以新 HEAD 为准重跑全闸 + 四个守卫，并复跑本轮的 3 组注入作为回归。 |
