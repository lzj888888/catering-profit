# NOTE · 2026-09-21 round70 自驱动巡检

> 本件由 WorkBuddy（我·巴迪）在自驱动巡检第 70 轮产出。仓库 `C:\Users\lzj\WorkBuddy\Claw\catering-profit`。
> 证据目录：`review/evidence/selfdrive_20260921_r70/`（7 份文件，已 `ls` 回读）。

## 0. 一句话结论

**本轮没有"新造活"**，而是接住了并发方（第三个写入方，非 InsCode）**写到一半就停手的产出**：
它起草了后台鉴权安全参数口径守卫（R106）却**没挂 SUITES、也没入库** ⇒ 等于写了个死判据。
我方独立复核后**代为集成**（SUITES 78→79 + 三处套件数同步），并顺带实证了
**坑⑱ 第二次**（元守卫 `check_suite_coverage.js` 只扫 `git ls-files` = 只扫 index，未 `add` 的判据不在扫描面）。

## 1. 探针状态（硬判据，非印象）

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | `inflight=0`、`message_count=1465` **十七连持平**、`idle_sec≈65h`、末条审批 `approved_once` | **idle**，无待批 ⇒ 不点、不投喂 |
| dsh（:3080） | 窗口 `rect=[-32000,...]` | **minimized**；未投递（理由见 §6） |

## 2. 🔴 检出第三个写入方（非 InsCode）→ 降级只读 → 四连判停手

- **08:17 首采**：`git status` 只有 1 个他人 `M`（`core/16_后台鉴权规范.md`）+ 一批 `r70/r71` 命名未跟踪脚本。
- **08:28 复采**：突然多出 `M pages/month/input.js`、`M tools/selftest_batch8b.js`，
  **mtime = 08:25:54 / 08:26:10（就在 2 分钟前）**，且 `git` 报 `LF will be replaced by CRLF`
  ⇒ 纯 LF 外部工具写入（round56 判据③，排除 InsCode：探针 `inflight=0` 且 msg 十七连持平）。
- **处置**：立即按技能 §0.7 降级 **只读旁观** —— 不点 GUI、不跑 `cli`、不 `git add -A`、不改任何文件。
  仅做只读复核（读 diff、读判据源码、跑门禁）。
- **对方在做什么**：堂食分项行 **round-trip 快照防丢值**（`dineDetailRows`：切到快速前快照分项行、切回分项时原样恢复，
  只有"从未填过分项"才走"总额铺首行"旧逻辑）。来源＝李老师 2026-09-21 真机反馈。
- **判停手四连**（§0.7⑤，避免 round55 那次误判）：
  ① 跨分钟 mtime 无变化（08:28 / 08:32 / 08:39 / 08:45 四次采样同值）✅
  ② 无仓内进程（`.py` 采样脚本写文件再跑 + 按 PID 排除自身，三次 0 命中）✅
  ③ **门禁跑完（1m27s 观察窗口）后回读 mtime 仍静止** ✅
  ④ `git log` 无新 commit（HEAD 全程 = `48bf7c9`）✅
  ⇒ **判定停手，恢复写入**。

## 3. 独立复核已提交但未审的 commit `48bf7c9`（并发方自称 round70）

改动：口径句折叠 + 渠道行改两行 + × 视觉收小（李老师真机反馈）。**不采信其自述**，读源码 + 跑判据：

- `selftest_batch8b` 新增 G7（5 条）/ G8（6 条）：折叠块存在、`scopeOpen` 初值 false、
  引导语走 i18n、**"未退回常展开"反向断言**；渠道行拆 `dine-line1`/`dine-line2`、**"一行三列已废"反向断言**、
  金额框不在行①、`×` 的 `min-height: 88rpx` 不动（G2 硬约束）。
- `selftest_batch8c` 口径句判据**升级非放宽**：仍要求 `{{g.scope}}` 被渲染，另加"折叠机制存在 + 老形态消失"。
- **我方独立双向变异**（B 组）：B1 口径句退回常展开 ⇒ 红 / B2 `×` min-height 88→60rpx ⇒ 红 /
  B3 渠道行退回一行三列 ⇒ 红。**B4 折叠样式改名 ⇒ 绿（MISMATCH，漏）** —— 见 §5。

## 4. 🟢 本轮核心：接住并发方悬空的 R106（同族病第 14 例的收口）

**事实链（全部实跑/实扫，非采信自述）**：

1. 并发方在 `core/16_后台鉴权规范.md` 立了后台鉴权参数的口径声明（锁阈值 5 次 / 锁时长 30 分钟 / token 有效期 7 天），
   并点名"由 `tools/check_admin_auth_params.js`（R106）守"。
2. **实查**：文件存在（04:06 建，12615 字节）但状态是 `??`，且 `grep admin-auth-params verify_all.js` **零命中**
   ⇒ 判据写了，**没挂 SUITES、没入库** ⇒ 门禁根本不跑它（与 round53 `verify_docx.py` 同族病第 4 例第三次复发）。
3. **实跑** `node tools/check_admin_auth_params.js` ⇒ **9 通过 / 0 失败**（零违规；补的是"零守卫"不是"已违规"）。
4. **我方 `git add` 保全后**，门禁**当场转红**：
   `❌ S5 面 A 79 个判据零漏网 · 未挂 SUITES：tools/check_admin_auth_params.js`（77/78）
   ⇒ **round60 立的元守卫第二次自证价值**，同时也实证了 ——
   **坑⑱ 第二次**：元守卫扫描面 = `git ls-files`（只扫 index）⇒ 未 `add` 的新判据**不在扫描面**，
   在它被 `add` 之前，这条"防复发"守卫是**暂时失明**的。
5. **处置**：SUITES 追加 `['admin-auth-params', 'tools/check_admin_auth_params.js']`（78→**79**），
   头注 78→79，重启键两处「套件数」同步（R97 `check_suite_count_claims` 已自动校验通过）。
   ⚠️ **代集成说明**：R106 判据本体**一字未改**（只做挂 SUITES 这一集成动作），
   理由＝证据保全（悬空产出有被 `checkout` 误抹风险）+ 让判据真正生效，与 round52 入库他人产出同型。

## 5. 判据强度评审（§0.3⑤⑥：不只测"能抓错"，还要评"守不守得住意图"）

### 5.1 R106 —— 通过，但有一处覆盖缺口（记待办，不代修，属并发方产出）

结构完整：P1 实算单源（正则解析不抄字面量，fail-closed）/ P2 唯一声明处 ≡ 实算 /
P3 全仓当前态引用锚点就近 ≤40 字符 + 三条前提守卫（八进制转义、面非空 ≥30、关键文件必在面内）/
P4 排除面前提 / P5 单源不扩散 / 弱面只 ⚠️ 明示不判红。已自带坑⑪⑫⑭⑮⑯⑱的处理。

🟡 **缺口**：`NUM_RE = /(\d+)\s*\*{0,2}\s*(次|分钟|分|天|小时)/g` 认"小时"，但 `CAT` 里**没有"小时/秒"的类别映射**
⇒ 把锁时长写成「锁定 **1800 秒**」或 token 写成「**168 小时**」时 `keys` 为空 ⇒ 直接 `continue` ⇒ **判绿（漏）**。
（我方未代修：该文件归并发方，且其判据本体本轮未动。）

### 5.2 `48bf7c9` 的 batch8c —— **抓到一处真弱点并已加固**

`tools/selftest_batch8c.js:110` 原判据 `/\.scope-head/` 是**裸子串**：
`.scope-head-x {` 也能命中 ⇒ 样式类被改成带后缀的兄弟类时**判绿（漏）**。
（同族：round55 `c24e0a2` 绑字面名、round59 设置页开关 —— "变异转红"不等于"守得住"。）

**加固**：加边界 `/\.scope-head[\s,{]/`（类名后须紧跟空白/逗号/左花括号＝真选择器声明）。
**双向变异复验**：B4b 改名 `.scope-head-x` ⇒ **转红 ✅**；B5 正确实现换写法（`.scope-head{display:flex;`，去空格）⇒ **仍绿（不错杀）✅**。

### 5.3 🆕 坑⑮ 第四次实证（我自己踩的，值得一记）

写本 NOTE 时我在 §4 里**原文引用了口径声明**（含完整标记串），而 `review/**/*.md` 正在 R106 的 P5 扫描面内
⇒ P5-① 判「另有 1 处自称本口径单源」，门禁 **79 → 78/79 转红**。
**定式（round63 已立，本轮再次印证）**：口径标记词**只许出现在唯一声明处与显式引用行**，
NOTE / 演进链 / 叙述段一律改用普通措辞 —— **修陈述、不动判据**。改后重跑 ⇒ **79/79 RC=0**。
（护栏自检：这也反证 P5「单源不扩散」不是恒真，它真能抓到第二处声明。）

## 6. 变异回灌矩阵（9 组，A 组 R106 / B 组 48bf7c9）

证据：`review/evidence/selfdrive_20260921_r70/mutation_matrix.txt`

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| A1 | 单源 `LOCK_AFTER_FAILS` 5→10 | 红 | 红 ✅ |
| A2 | 单源 `TOKEN_TTL_MS` 7天→30天 | 红 | 红 ✅ |
| A3 | 声明换措辞「连续 5 次密码错误即锁定 30 分钟…」 | **绿（不错杀）** | 绿 ✅ |
| A4 | 删「（唯一声明处）」标记 | 红（fail-closed） | 红 ✅ |
| A5 | 文档另写错值「管理端密码错误 8 次锁定」 | 红 | 红 ✅ |
| B1 | 口径句退回常展开 `wx:if="{{g.scope}}"` | 红 | 红 ✅ |
| B2 | `×` `min-height` 88→60rpx | 红 | 红 ✅ |
| B3 | 渠道行退回一行三列 `dine-line1`→`dine-row` | 红 | 红 ✅ |
| B4 | 折叠样式改名 `.scope-head`→`.scope-head-x` | 红 | **绿（MISMATCH）** ⇒ 已加固，见 §5.2 |

还原：全部 `copy2` 字节级还原（避坑⑳ autocrlf 反向有损），每组还原后复跑 rc=0；
变异前已 `git add` 本轮全部改动（坑⑩），`git diff --stat` 仅剩并发方两个文件。

## 7. 门禁（本轮三次实跑，全留证）

| 时点 | 命令 | 结果 |
|---|---|---|
| 08:20（基线） | `node verify_all.js` | **78/78** RC=0 |
| 08:33（add R106 后） | 同上 | **77/78** RC=1（`suite-coverage` 抓出孤儿判据）|
| 08:39（挂 SUITES 后） | 同上 | **79/79** RC=0 |
| 08:45（加固 batch8c 后·终值） | 同上 | **79/79** RC=0 |
| 08:45 | `check_error_codes.js`（A–L） | RC=0 |
| 08:24 | `selftest_ad_gates.js` / `selftest_ui_fix.js` | 24/24 · 31/31 RC=0 |

⚠️ **R86 超时值本轮未实测**：按 round50 三方一致结论（round45/46/50 均 42/42）不再回读，记**存疑**（人工面）。

## 8. dsh 未投递的两条独立理由

1. 窗口 **minimized**（`rect[0]=-32000`），恢复需 `ShowWindow` 抢焦点 —— 无人值守轮次抢焦点会把并发方固定坐标点击打到错窗口（§0.7）。
2. 即便健康，**当前会话上下文与仓库复审不匹配**（历史 18 轮同因）⇒ 投递＝上下文污染。

## 9. 待办（下轮 / 李老师）

1. 🟡 **R106 单位覆盖缺口**（§5.1）：`CAT` 补「小时/秒」类别映射 —— **归属并发方（round71），不代修**。
2. 🟡 **并发方悬空产出仍未提交**：`M pages/month/input.js`（round-trip 快照，+35 行）、`M tools/selftest_batch8b.js`（+10 行）
   —— **本轮未代提交、未代复核其判据**（它停手时改动尚未收口，且 `selftest_batch8b` 新增 10 行断言本轮**未经我方变异**）。
3. 🟡 **坑⑱ 溢出仍在**：`git ls-files` 只扫 index 的前轮守卫（round68 记为 4 份）—— 本轮**只修了触发点，未代修那 4 份**，待裁决。
4. 🔴 **dsh 方案 D（允许重启 dsh web）仍未定** ⇒ round39 等 5 份 NOTE 复审仍悬（第 19 轮未投递）。
5. 🟡 并发方 1000+ 过程 PNG 待李老师定（不代提交不代删）。
6. 🟡 `init_db.js` 系统字典仍旧 5 项 vs 前端 7 项（对方自述④，未代修）。
7. 🟡 建 prod 时提醒：重走 R86 定值与回读 + 替换 `ENV_MAP.prod` 占位符。
8. 🟡 隐私政策按 **6 处**填（旧清单会漏 2 项，直接卡提审）。

---

## 10. 回执（SOUL.md 硬要求：提交前自检第 2 项）

- [2026-09-21 08:19] R70 **已落** · 探针 `probe_agents.py` → `inscode.inflight=0` / `msg=1465` 十七连持平 / `idle≈65h`；`dsh.rect[0]=-32000`（minimized）· commit `<回填>`
- [2026-09-21 08:28] R70 **已落** · 检出第三写入方：`stat` 得 `input.js` mtime 08:25:54、`selftest_batch8b.js` 08:26:10 + `git` 报 LF→CRLF ⇒ 纯 LF 外部工具写入；探针排除 InsCode · commit `<回填>`
- [2026-09-21 08:45] R70 **已落** · 判停手四连：mtime 四次采样同值 + 进程三采 0 命中（`.py` 排除自身 PID）+ 门禁 1m27s 后回读静止 + `git log` HEAD 全程 `48bf7c9` · commit `<回填>`
- [2026-09-21 08:39] R70 **已落** · 独立复核 `48bf7c9`：读源码见 G7/G8 共 11 条断言**增强非弱化**；B1/B2/B3 变异转红 · commit `<回填>`
- [2026-09-21 08:33] R70 **已落** · R106 悬空实证：`grep admin-auth-params verify_all.js` 零命中 + `git status` 为 `??`；`git add` 后门禁当场报 `❌ S5 未挂 SUITES`（77/78）· commit `<回填>`
- [2026-09-21 08:39] R70 **已落** · 代集成 R106：SUITES 追加 `admin-auth-params` 78→**79** + 头注 78→79 + 重启键两处同步（R97 `suite-count-claims` 9 条 PASS 自动校验）· commit `<回填>`
- [2026-09-21 08:42] R70 **已落** · 加固 `selftest_batch8c.js:110`：`/\.scope-head/` → `/\.scope-head[\s,{]/`（裸子串漏判）；B4b 转红 + B5 不错杀 · commit `<回填>`
- [2026-09-21 08:56] R70 **已落** · 门禁终值 **79/79 RC=0** + A–L RC=0 + AD 24/24 + UI 31/31 · 证据 `review/evidence/selfdrive_20260921_r70/`（9 份，已 `ls` 回读）· commit `<回填>`
- [2026-09-21 08:56] R70 **未落** · **R86 超时值本轮未实测**（按 round50 三方一致结论不再回读）⇒ 记**存疑**，人工面 · commit `<回填>`
- [2026-09-21 08:56] R70 **未落** · **dsh 未投递**：窗口 minimized（抢焦点会打乱并发方固定坐标点击）+ 会话上下文与仓库复审不匹配（第 19 轮同因）· commit `<回填>`
- [2026-09-21 08:56] R70 **未落** · **并发方悬空产出不代提交**：`M pages/month/input.js`（+35，round-trip 快照）、`M tools/selftest_batch8b.js`（+10 断言，**本轮未经我方变异**）⇒ 归属待其自行收口 · commit `<回填>`
- [2026-09-21 08:56] R70 **未落** · **R106 单位覆盖缺口不代修**（`CAT` 无「小时/秒」映射 ⇒ 换单位写法可绕过），归属并发方 round71 · commit `<回填>`
- [2026-09-21 08:56] R70 **未落** · **坑⑱ 溢出 4 份前轮守卫**仅扫 index，本轮只修触发点未代修，待裁决 · commit `<回填>`
