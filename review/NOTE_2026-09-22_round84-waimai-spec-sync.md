# NOTE · 2026-09-22 round84 —— 外卖段规范落定（ModuleA §A.11）+ 新守卫 R114

## 1. 一句话结论

李老师 2026-09-22「**就这样**」拍板 ⇒ 把上一轮（round83）交付的外卖页设计稿（6 屏）**落成写码依据**：
`specs/dev-specs/core/开发规范v1.0_ModuleA_收入费用核算.md` 新增 **§A.11「外卖段取数与录入交互」**，
开篇即声明**不改口径**（口径本体仍归 A.1 / A.2 / A.6，本章只定「从哪取数、填几个、填错怎么发现」）。
同轮新增守卫 `tools/check_waimai_spec_sync.js`（**R114**，10 断言，SUITES **86→87**），
门禁 **87/87 RC=0**，双向变异 **8 组 0 异常**。**未改任何小程序代码、未投喂 InsCode**。

## 2. 本轮性质（不是自驱动巡检，是执行拍板）

round83 交付设计稿 → 李老师看完说「就这样」⇒ 本轮 = **入册 + 配守卫收口**。

## 3. 并发判据（写入前，硬判非印象）

`git status --porcelain` 前缀统计 = **`??` 1079 条 / 已跟踪修改 0 条**
⇒ 并发方（round82 那条线）已收工，工作树**无在途修改** ⇒ 可安全写入。

## 4. 交付物

| 文件 | 动作 | 说明 |
|---|---|---|
| `core/开发规范v1.0_ModuleA_收入费用核算.md` | 改 | +**§A.11**（A.11.1~A.11.7 共 7 小节）；§A.7 末尾加**指路针**指向 A.11，并划清「粘贴 ≠ A.7 的 Excel 导入」边界 |
| `core/13_上线前查缺补漏_决策与待办总览.md` | 改 | +**2 节**（待裁决 / 待验证），见 §6、§7 |
| `tools/check_waimai_spec_sync.js` | **新增** | 守卫 R114，见 §8 |
| `verify_all.js` | 改 | SUITES **86→87** + 头注（R59 自校验同源） |
| `★知识存储点_2026-09-10.md` | 改 | §1.1 一键校验入口行 + §1.3「套件数会漂」行 + 演进链，**三处 86→87** |
| `review/evidence/selfdrive_20260922_r84/` | 新增 | 基线 / 改动后 / 终态三份门禁输出 + 变异输出 + 变异脚本 |

规范文件 **11240 → 20708 字节**（纯 LF，CRLF=0；落笔前后各验一次，避坑⑳ 行尾污染）。

## 5. 落笔时核对出的 3 处偏差（设计稿 vs 代码单源）⇒ **全部以单源为准**

写规范时逐字核对了代码侧的**真实字段名与顺序**，发现上一轮设计稿有 3 处与单源不一致：

| # | 项 | 设计稿 | 代码单源 | 处置 |
|---|---|---|---|---|
| 1 | 营销项**顺序** | 「外卖活动补贴」排第 4 位 | `SEED_EXPENSE_ITEMS` 的 `sort_order=30`（第 3 位） | 规范按 **30** 写 |
| 2 | 「餐盒费 · 打包费」 | 合并写法 | `item_name` = 「**打包费**」 | 规范按单源名写 |
| 3 | 收入侧分项字段 | 随设计稿措辞 | 「商品总价 / 打包费 / 商家活动补贴」（3 项） | 规范按此对齐 |

> 这 3 处**不是缺陷**，是设计稿（探讨件）与代码单源的口径差。落成规范时必须以**代码**为准 ——
> 否则规范一出生就是第 18 例「陈述面 ≠ 实然」。

## 6. 一处**依据悬空**（如实上报，只登记不擅改）

`ModuleA` 有 **3 处**引用 `外卖核算_专项核对.md` / `团购核算_专项核对.md`：
§A.0 锁定声明 1 处（「经 … 逐条验证」）+ §A.10 来源索引 2 处。
**2026-09-22 全仓 `os.walk` 实扫：文件名含「专项核对」者 = 0 个。** ⇒ 依据悬空。

- ✅ **不影响口径正确性**：A.1 / A.2 / A.6 的口径同时被 **42 个云函数**（含 `initDb` 种子）与前端单源独立实现。
- ⚠️ **影响可查证性**：按 §A.10 去查证会扑空。
- 处置：**只登记**（`core/13` 新增「待裁决」节）+ §A.11 开头**明文声明不引用**该文件 ⇒ **不擅自改** §A.0 / §A.10 措辞。
- 归属：**李老师二选一**（补文件 / 改措辞）。

## 7. 配平等式的不确定性与**安全边界**（fail-closed）

§A.11.4 的配平校验式，其账单列名来自 **WebSearch（2026-09-21）的美团账单文档**，**无真实账单核对**。
⇒ §A.11.7 显式登记 **V1–V5** 待验证项，并定死纪律：
**V1–V3 落定前，配平校验只做软提示 —— 不阻断保存、不参与利润计算、不写入任何金额。**
⇒ 即便等式与真实账单不符，最坏结果是「多一句提示」，**不会污染利润数字**（这是本轮最重要的安全设计）。

同时保留一条**专项检测**：若「商品总价」已含打包费而客户又单独填了打包费，差额会**恰好等于打包费金额**
⇒ 系统据此**专项提示**，而不是笼统报「填错」（这是"把不确定性做成看得见"的落地）。

## 8. 守卫 R114 + 双向变异回灌

**为什么必须建**：§A.11 两处**明文声称"严格取自代码单源"**（A.11.2 平台名与顺序 ← `terms.js`；
A.11.3 营销项 key / 名 / 顺序 ← `collections.js`）⇒ 新增两处「**文档抄代码**」派生面。
现成守卫盖不住：**R110 只守三份代码副本之间**（且把外卖列为其**弱面**，只打印不判红），
其余 `check_*` **无一条读 ModuleA 正文** ⇒ 改单源会让规范静默停在旧值且门禁全绿。

**判据 10 条 / 5 段**：
- W3 前提与 fail-closed（5 条）：A.11.2 / A.11.3 两段定位 + `terms.js` / `collections.js` 两单源解析 + 契约数量（平台 4 / 营销 6）
- W4 自失效护栏（2 条）：两侧非空且数量相等（防正则写歪后"两边都空"而假绿）
- W1 平台名与顺序逐项有序（1 条）
- W2 `item_key` + 显示名逐项有序（2 条）

**双向变异 8 组 = 8/8 符合期望**（证据 `mutation_r84.txt`）：

| 组 | 变异 | 期望 | 实得 |
|---|---|---|---|
| M1 | 平台改名（京东外卖 → 京东到家） | 红 | 红 |
| M2 | 平台**换序**（名称集合不变、只有顺序错） | 红 | 红 |
| M3 | `item_key` 改名 | 红 | 红 |
| M4 | 显示名改名 | 红 | 红 |
| M5 | 章节标题改名（段定位失败） | 红 | 红 |
| M6 | A.11.2 **无关列**换措辞 | **绿** | 绿 |
| M7 | A.11.3 **末列**换措辞 | **绿** | 绿 |
| M8 | 追加无关正文 | **绿** | 绿 |

每次变异后**逐条还原并核字节相等**（8/8 还原成功）。
⚠️ 还原**不用** `git checkout --` / `git show :path`：本轮 §A.11 尚未提交 ⇒ index 里是旧版，
上述命令会把新章节一起抹掉，且 autocrlf 仓还会把 LF 还原成 CRLF（坑⑳）⇒ 改用**内存原字节写回**。

## 8.5 本轮踩到的一条**新坑**（R92 拦下）

首次跑门禁 ⇒ **RC=1、仅 9 秒退出**，报：

```
❌ [suite-tracked] 以下 1 个已登记套件**未纳入 git 索引**（R92：登记了没提交 ⇒ 远端复现不出这些成绩，且 R59 不会红）：
   tools/check_waimai_spec_sync.js
```

⇒ **新守卫必须先进 git 索引（`git add`）才能跑门禁** —— 这是 R92 的**设计意图**，不是故障。
⇒ **定式（写进 PITFALLS）**：新增 `tools/check_*.js` 后，流程是「写入 → **`git add`** → 跑门禁」，
而不是「写入 → 跑门禁」。同批 `✅ [suite-count] 头部注释 ≡ SUITES.length = 87` 同时证明三处人工面同步正确。

## 9. 回执

- [2026-09-22 01:1x] R84 **已落** · ① 规范 §A.11 落定 —— 证据：`Read` 章节清单 → ModuleA 由 **11240 → 20708 字节**（A.11.1~A.11.7 齐备，CRLF=0）· commit `<sha1>`
- [2026-09-22 01:1x] R84 **已落** · ② 新守卫 R114 —— 证据：`node tools/check_waimai_spec_sync.js` → **10 通过 / 0 失败 RC=0** · commit `<sha1>`
- [2026-09-22 01:2x] R84 **已落** · ③ 门禁 —— 证据：`node verify_all.js` → **总览：87/87 套件通过 RC=0**（基线 86/86 → 终态 87/87，改动后中间态亦 86/86 无回归）· 证据文件 `gate_86_baseline.txt` / `gate_86_after_spec.txt` / `gate_87_final.txt` · commit `<sha1>`
- [2026-09-22 01:2x] R84 **已落** · ④ 双向变异回灌 —— 证据：`_mut_r84.py` → **8 / 8 符合期望**（5 红 + 3 绿，逐条还原字节相等）· 证据文件 `mutation_r84.txt` · commit `<sha1>`
- [2026-09-22 01:2x] R84 **已落** · ⑤ 三处人工面 + 演进链同步 —— 证据：`node tools/check_suite_count_claims.js` → **9 通过 / 0 失败**（C2-①②③ 全绿）· commit `<sha1>`
- [2026-09-22 01:2x] R84 **未落** · ⑥ **投喂 InsCode** —— 8 批投喂（0~7）已收官，新批次属李老师决策；照 round78 §8 纪律**不擅自造**（越权）
- [2026-09-22 01:2x] R84 **未落** · ⑦ §A.0 / §A.10 依据悬空的**修法** —— 补文件 vs 改措辞属口径留痕决策，**只登记不代选**
- [2026-09-22 01:2x] R84 **存疑** · ⑧ 配平等式 V1–V3 —— 无真实账单 ⇒ **未验**，已定死「软提示、不进利润」边界；待李老师提供账单

## 10. 给下轮的待办

1. **投喂 InsCode** —— 若李老师要把它派给快马，需**显式发话**（新批次）。
2. **V1–V3 待真实账单** —— 拿到账单后回写 §A.11.4 并解除软提示限制。
3. **§A.0 / §A.10 依据悬空** —— 待李老师二选一。
4. **堂食字典 5 vs 7 拉齐** —— round78 遗留，仍待裁定。
