# NOTE · round119 —— M3 v1.1 规范的对抗性复核与批次角色拆分

- 日期：2026-09-24（shell 时钟）
- 仓库：`C:\Users\lzj\WorkBuddy\Claw\catering-profit`，分支 `dev`
- 性质：**只读复核 + 规范勘误 + 投喂包重切**（未改任何代码、未投喂）
- 上游：round118（M3 v1.1 增量规范定案，commit `49481f5` / `ebf3bef`）

---

## 一、一句话结论

**我把刚写好的 v1.1 规范送了一次独立复核，抓出 9 条缺陷并全部修掉 —— 其中 1 条是 P0：投喂包自相矛盾且角色越界（第 9 行禁止改 `specs/`，第 96~123 行却要求改 8 个 `specs/` 文件与 `tools/` 守卫）。同时还定案把批次 A 拆成 A1（写码方）/ A2（门禁方），并发现三处既存偏差（登记在规范附二）。**

---

## 二、复核方法（为什么不采信自述）

| 项 | 做法 |
|---|---|
| 复核方 | **同模型、新上下文**的对抗性复核（无我的锚定；**不是**第三方独立模型，如实标注） |
| 为何不用 dsh | 记忆载「dsh 09-21 停本仓」；实测 dsh workspace 的 `REVIEW_*` 停在 round38（2026-09-18），:3080 虽在跑但**已不承担本仓复审** ⇒ 不强行唤醒（避免无谓的键鼠驱动） |
| 复核范围 | v1.1 规范全文 + 投喂包全文；实读 `calcBom` / `checkQuota` / `saveCostCard` / `getShopList` / `collections.js` / `tools/check_*` / `core/09,10,13` / `pages/card` |
| 反假绿 | 要求复核方**实际 `require` 生产引擎复算锚点**，并**明确列出它验证通过的点**（证明真读了，不是空评） |
| 我不盲信 | 9 条**逐条自行回验**（见下表的"我方自验"列） |

---

## 三、9 条发现与处置（含我方自验）

| # | 严重度 | 缺陷 | 我方自验结果 | 处置 |
|---|---|---|---|---|
| 1 | **P0** | 投喂包**自相矛盾 + 角色越界**：头注「只改 cloudfunctions//tools//pages/，不改 `specs/`」，正文却要求改 8 个 `specs/**` + `tools/` 两个守卫；还漏授权 `miniprogram/`（却要求改其 `terms.js`） | ✅ 与我独立发现一致 | **拆 A1/A2**，投喂包重写 |
| 2 | P1 | 「引擎 5 副本**一行不用改**」与 M3.22#5「`saveCostCard/service.js` 新增 `judgeCardQuota`」直接冲突 | ✅ 属实（L62-64 vs 表内 #5） | 改为「**引擎段**不改，允许**新增**非引擎纯函数」 |
| 3 | P1 | `is_latest` 在**生产代码里不存在**（`saveCostCard/index.js:11` 明说"不翻转"、`getCostCard/index.js:5` 取 `version` 最大者），但规范三处把它当版本机制用 ⇒ **按它过滤在真云会静默取空**（与 round116 静默失败同族） | ✅ grep 全仓：只有 v1.0 规范 + `seed_demo.js:154`（验收种子）有，生产零写入 | 3 处改为「`version` 最大者」+ 登记为**既存偏差**（附二） |
| 4 | P1 | 新增错误码的**同步点不足**：投喂包只说「09 一行 + `ERR.QUOTA_CONFIG`」，实际需 **4 处**（`check_error_codes.js` 双向校验） | ✅ 实读 `prototype/check_error_codes.js:70-108` 确认 B1/B2/B3/B4 + A1/A2/A3 | **批次 A 不新增任何错误码**，缺配置用既有 `SYSTEM_ERROR`（先例 `adminQueryUser/service.js:5`） |
| 5 | P1 | 批次 A 守卫写「R136 反向断言」，但**全仓无 R136**（M3.27 只定义 R131-135/R138） | ✅ 属实 | 并入 **R102 反向断言**，删 R136 |
| 6 | P1 | **计数维度分叉**：写侧要求 `user_id`（跨店），但既有 `checkQuota` 按 `shop_id` 计数；且 `shop_cost_card` **无 `user_id` 字段**（只有 `created_by`），也无该索引 | ✅ 属实，且**比复核更深一层**：`checkQuota/index.js:5` 头注写「计数维度 = user_id（跨店共享）」，**函数体第 41 行却按 `shop_id` 查** ⇒ 同文件自相矛盾 | 统一为 **`shop_id`**（与既有口径一致；免费档仅 1 账套 ⇒ 按店 ≡ 按用户）+ 要求修正该头注；跨店维度登记为待办 |
| 7 | P1 | R117 改造后**退化为恒真**：A2/A4/A5 原靠解析字面量互相穿透，三处同源改配置后恒真，失去防漂移力 | ✅ 实读 `check_free_shop_limit.js:27-42` 判据表确认 | 改为**正反断言**（引用同一配置键 + 不得含字面量；注入字面量必须转红） |
| 8 | P1 | B-b 锚点自相矛盾：规范写"到手率 69.63%"，且公式漏减「配送费(顾客承担)」 | ✅ **我按引擎口径重算 = 70.66%**（`P*=2963` 商品价 → 顾客支付 3163、佣金 178、实收 2235 ⇒ 2235/3163=70.66%；且 70.66% **贴线不触发**软提示，与规范"<70% 触发"相反） | 修正数字 + 补公式口径注 |
| 9 | P2 | 「`feature_permissions` **零变更**」不成立：现种子形状只有 `plan_id/feature_key/enabled`，加 `limits` 属新增字段 | ✅ `collections.js:132` 确认 | 改为「不新建集合，**新增 1 个字段**」 |

**复核方验证通过的关键点（防误报）**：锚点 `975/876/567/1275/1225/1208/1444` 用 `require` 引擎实算**逐项一致**；行号类指控（`checkQuota/service.js:17`、`selftest.js:14`、`pages/card/index.js:62`、`getShopList/index.js:38`、`R102 SRC_REL`、`check_free_shop_limit.js:8`）**全部属实**；**「零新建集合」成立**（25 集合 / 40 索引实数）。

---

## 四、本轮勘误清单（15 处，原子改写 + 逐处断言）

| 文件 | 改动 |
|---|---|
| `specs/dev-specs/core/开发规范v1.1_ModuleM3增量_*.md` | ① D3：`is_latest` → `version` 最大者 ② 引擎副本口径加「引擎段 vs 新增纯函数」边界 ③ M3.16 引用条件同上 ④ B-b 公式补口径注（P* = 商品挂牌总价；配送费须同侧） ⑤ **B-b 锚点 69.63% → 70.66%** ⑥ M3.22#2：`QUOTA_CONFIG_MISSING` → `SYSTEM_ERROR` ⑦ M3.22#3：补 `checkQuota` 头注纠错 ⑧ M3.22#6：明确 **`shop_id` 维度 + 版本不计 + 去重口径** ⑨ M3.22#9：R117 改判据（防恒真） ⑩ M3.24：`feature_permissions` 零变更 → 新增字段 ⑪ M3.25：补「新码 = 4 处同步」+ 批 A 不新增码 ⑫ M3.25 表：QUOTA 行标注**延后** ⑬ M3.28：删不存在的 R136 ⑭ M3.28：**批次按角色拆 A1/A2** ⑮ M3.29 红线#1：`is_latest` → `version`；另追加**附二**（3 处既存偏差登记） |
| `specs/dev-specs/delivery/批次M3v1.1_A_提示词_可直接复制.txt` | **整篇重写为 A1**：只授权 `cloudfunctions/`+`pages/`+`miniprogram/`；显式禁止 `tools/`、`specs/`；删去 #8~#11；不新增错误码；写侧拦截口径精确到"新逻辑卡才占额度 + `shop_id` 去重计 `card_code`"；补 `sync_common.js` 触发条件 |

行数 582 → 608；门禁证据 `review/evidence/gate_r119.txt`。

---

## 五、批次 A 的角色拆分（定案）

| | 范围 | 归属 |
|---|---|---|
| **A1** | M3.22 改造清单 **#1~#7、#12**（`cloudfunctions/` + `pages/` + `miniprogram/`） | **写码方（快马 InsCode）** |
| **A2** | **#8/#9**（`tools/` R102 反向断言 + R117 正反判据）、**#10/#11**（`specs/` 文档面 8 处 + `core/13` 裁决登记 + 双副本 `terms.js` 一致性） | **门禁方（我）** |

🔴 **预期中的暂时红灯**：A1 落地后 R102/R117 仍按旧常量解析 ⇒ 会红，由 A2 收口。已在投喂包中明写"**不得**为让门禁变绿而回退改动、不得碰 `tools/`"。

### A2 执行清单（我，待 A1 落地后逐条打勾）

- [ ] #8 `tools/check_quota_limits.js`：`SRC_REL` 由"解析常量"改为"解析 `feature_permissions` 种子"；**加反向断言**（`checkQuota/service.js` 再现额度字面量 ⇒ 判红）；保留下界/扫描面/fail-closed
- [ ] #9 `tools/check_free_shop_limit.js`：A2/A4/A5 改**正反断言**；修头注第 7-9 行事实错误（"真正执行拦截的那一个"→ 实测无写侧调用）
- [ ] #10 文档面 8 处（`商业化方案_v1.4` 唯一声明处、`core/01`、`core/03`、`core/04`、`delivery/inscode喂投包_8批*`、`★知识存储点`、`core/09` 触发场景文字、`core/10` 契约行）—— **只改额度数字与"读哪张配置表"的措辞**，历史/演进陈述保留
- [ ] #11 `core/13`：该待裁决项标记「**已裁决（路线② 配置化）**」+ 日期 + 依据
- [ ] 双副本 `terms.js`：`cp` + `diff -q` 校验一致（本批次**不新增键**，故只需确认未被动过）
- [ ] 门禁 103/103 + **变异回灌**（在 `service.js` 插回额度字面量 ⇒ R102 必红 ⇒ 还原后逐字节 md5 全等）
- [ ] 真云探针：连续建第 6 张卡 ⇒ 必须被拦（本地 selftest 不算闭环）

---

## 六、三处既存偏差（本轮查证，登记在规范附二）

1. **`checkQuota/index.js` 头注与函数体不符**（头注"跨店共享" vs 函数体按 `shop_id`）—— 修复已并入 A1（#3）。
2. **`is_latest` 规范有、实现无** —— v1.0 锁定版与实现的偏差，**需单独一轮裁决**（改规范 or 补实现），本轮不动锁定版。⚠️ 任何"按 `is_latest` 过滤"的查询在真云会**静默取空**，属 round116 同族病。
3. **多账套未实现**（仓内无 create-shop 云函数）⇒「成本卡额度跨店共享」当前**不可观测**，故批次 A 用 `shop_id` 维度；将来放开第 2 家账套需重新裁决并补 `created_by` 索引。

---

## 七、待办

1. **投喂 A1 给快马 InsCode**（本 NOTE 落定后执行；投喂前须确认 `~/.config/inscode/inscode.db` 的 `inflight_turn` 为空 —— 本轮实测 **0 行，空闲**）。
2. A1 落地后执行 §五 的 A2 清单（含变异回灌与真云探针）。
3. B 批（M3.14/15/20）与后续批次按 M3.28 表推进。
4. `round111` 遗留 4 条 + `PLAN §5` 五条口径（改用户可见数字，按纪律不代选）**未动**。
5. 仓内根目录历史临时脚本（`_fix_r77.py` / `_mut_r*.py` 等）与 `.inscode/aigc/**` 元数据属既有卫生债，未处理（不在本轮范围）。

---

## 八、执行回执

- [2026-09-24 22:4x] round119 **复核+勘误已落** · 证据：
  - 复核：新上下文 agent 报告（9 条，含"验证通过点"自证）—— 全文见本轮对话；结论已归纳进本 NOTE §三
  - 勘误：`_fix_r119_spec.js` 输出 `edits_applied=15`，`lines 582 → 608`；残留检查 `69.63=0`、`R136=0`
  - 格式修复：emoji 字面量 `U0001F534` ×3 → 🔴；代码围栏内误插入 2 行已移出
  - 门禁：`review/evidence/gate_r119.txt` → **103/103 · RC=0**
  - commit：`d12c32a`（推送后 `ls-remote` 核验远端 dev ≡ 本地 HEAD，工作区 clean）
