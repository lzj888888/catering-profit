# NOTE · 2026-09-22 round86 · R85 外卖段产出：独立复核 + 三处「算错钱」级修复

> 一句话：**InsCode 交付的 R85 外卖段（前端取数与录入）主体合格，但独立复核抓到 3 处「静默算错钱」级缺陷 + 2 处单源违规，已全部修复并由 7 组双向变异证伪式验证；门禁 88/88 RC=0。**

本 NOTE 属**写入方**（本轮唯一活跃写入方，见 §5 并发处置），与同日的 `NOTE_2026-09-22_round85-concurrent-writer-detected.md`（**旁观方**视角）互补——**两份都要读**：那份是"看到有人在写"，这份是"写的人自己交底"。

## 1 交付物

InsCode 按投喂件 `specs/dev-specs/delivery/R85_外卖段录入_提示词_可直接复制.txt`（commit `6b0a453`）产出 9 文件 + 新套件：

| 文件 | 性质 | 说明 |
|---|---|---|
| `utils/takeaway.js` | 新增 | 纯函数：模式推断/快照/小计/粘贴提取/补贴合计/配平（阈值 50 元，四态 packaging·delivery·pass·miss） |
| `tools/selftest_r85.js` | 新增 | 自测（InsCode 版 43 条 → 本轮修复后 **60 条**） |
| `pages/month/input.js` `.wxml` `.wxss` | 修改 | 外卖「快速 4×1 / 分项 4×3」互斥模式、粘贴面板、配平软提示、营销项取数路径标注 |
| `miniprogram/i18n/terms.js` + `specs/dev-specs/i18n/terms.js` | 修改 | 双副本（K11）新增 `ledger.takeawayMode` 全文案 + `ledger.expenseItemNotes` |
| `verify_all.js` | 修改 | SUITES 追加 `r85-takeaway`（第 88）+ 头注 87→88 |
| `specs/dev-specs/★知识存储点_2026-09-10.md` | 修改 | 套件数两处 87→88；断言数声明加 `selftest_r85` |
| **`tools/check_suite_assert_counts.js`** | 修改 | **本轮扩面**：`selftest_r85` 纳入受守集合（6→7） |

后端**零改动**（`git status cloudfunctions/` 为空）；平台名与营销项名均来自代码单源，页面零硬编码。

## 2 复核方法与独立验证（不采信自述）

对 InsCode 的「43 条断言全绿 / 88/88」自述**一律不采信**，改为：

1. 读在途源码（`utils/takeaway.js` 逐行 + `input.js` 关键函数段）
2. **直接 `require` 纯函数跑边界用例**（不经它的自测）
3. 交叉核对规范 §A.11 与代码单源（`terms.js` / `collections.js`）
4. 修完后按铁律做**双向变异回灌**（§4）

## 3 抓到的缺陷与修复

### 🔴 P1 粘贴提取把「账期 / 日期 / 时间」当金额（算错钱）
实测：`extractPaste('2026-09')` → **sum=202**；`'2026-09-21\t1234.56'` → **202**；`'13:45'` → **13**。
风险面真实：`PITFALLS §7` 已记美团账单含「**归属账期**」列 ⇒ 商家粘「账期 + 金额」两列即静默算错，
而 UI 仍提示「已提取并填入」⇒ **用户不会察觉**。
**修**：新增 `stripNonMoney()`（抹掉 `YYYY-MM` / `YYYY-MM-DD` / `YYYY年M月D日` / `HH:MM` / ≥11 位长单号），`firstNumber` 前先过它。

### 🔴 P1b 首数字正则会**截断 ≥4 位整数**（更隐蔽，同样算错钱）
原式 `/-?\d{1,3}(?:,\d{3})*(?:\.\d+)?|-?\d+(?:\.\d+)?/` —— 分支一先命中 `\d{1,3}` 就返回，
⇒ `4380.00` 得 **438**、`1234.56` 得 **123**。而平台账单最常见的写法恰恰是**无千分位**的 `4380.00`。
**它的自测用例（`1,234.56` / `2,000`）全部带千分位 ⇒ 天然测不出**（这是"自测覆盖面被作者习惯带偏"的典型）。
**修**：改 `-?(?:\d{1,3}(?:,\d{3})+|\d+)(?:\.\d+)?`（千分位形态与纯数字二选一，先吃满整数再谈小数）。

### 🔴 P5 自动带出被「误锁」⇒ 费用侧不跟随（配平与利润都错）
```
if (g.rows[ri].twCarryLock) return;
if (cur && cur !== target) { ...twCarryLock = true; return; }   // ← 误判
```
`cur` 是**回读的旧值**（上次保存的补贴合计），`target` 是**本次实时合计**。用户改了**收入侧**补贴
⇒ `cur≠target` ⇒ 被误判为"用户手改了费用侧"⇒ **加锁、锁死旧值** ⇒ 费用侧不更新、配平恒偏。
**典型触发**：跨月二次录入（上月存 100、本月改到 200 ⇒ 费用侧仍停 100）。
**修**：只认 `twCarryLock`（真·本会话手改费用侧）；`cur === target` 时幂等返回。

### 🟠 P2 配平读费用走中文硬编码项名（单源违规）
`mrow('外卖平台佣金')` 等三处写死中文 ⇒ 违反本仓「页面零硬编码、项名走单源」；单源改名即**静默读到 0**、配平恒偏。
**修**：terms 立 `takeawayMode.reconcileRoles`（角色→显示名），页面按角色取用；自测加断言「roles 的值 ∈ marketing items」。

### 🟠 P4 promo 高亮由**文案内容**驱动（脆弱耦合）
`promo: /推广中心/.test(note)` ⇒ 改文案即失效。
**修**：terms 立 `takeawayMode.promoItem`，按项名判。

### 🟡 P3 注释与实现不符（陈述面 ≠ 实然，新一例）
terms 注释写「按 **item_key** 挂」，实际按**中文显示名**挂 ⇒ 已改正注释。

### 🟡 P7 已知限制（登记不改）
「快速 → 分项」切换把**总额**铺进分项「商品总价」列 ⇒ 该列语义偏移（值守恒、小计不虚增）。
有 hint 文案提示，按已知限制登记。

## 4 验证证据（全部实测）

| 项 | 结果 |
|---|---|
| 门禁基线（改动前，InsCode 版） | 88/88 RC=0 |
| 门禁终态（修复后） | **88/88 RC=0**，`[r85-takeaway] ✅ PASS (60 条 / 段 15)` |
| 独立边界用例（14 例，含账期/日期/时间/长单号/千分位/4 位无千分位/负数/合计行） | **14/14 PASS** |
| `reconcile` 五态独立用例 | null / pass / packaging / delivery / miss 全对 |
| **双向变异回灌 7 组** | **0 异常**：5 组正向全 RED（且报出精确对应断言名）+ 2 组反向 GREEN（不错杀）；逐组还原**字节相等**，复跑回绿 60/0 |
| 双副本一致（K11） | `diff -q` 0 |
| 相关守卫（terms_forbidden / stale_claims / waimai_spec_sync / suite_assert_counts / income_channel_seed） | 全 GREEN |

变异文件：`review/evidence/r85_feed/mutation_r85.txt`；门禁输出：`gate_88_afterfix.txt`；投喂留证：`p1_focus.png`~`p5_running.png`。

## 5 并发处置（本轮特殊）

1. **投喂**：02:1x 送达 InsCode（`inflight=1`、会话 user 计数 +1、气泡在屏三重确认）；02:50 收工（`inflight=0`、assistant 510→575）。
2. **同仓另一自驱动会话**（round85）01:53 起在同一批工作上：它跑了基线门禁、独立复核了我上一轮的 R114 守卫（变异 10 组 0 异常），
   并正确识别出「有人在写」⇒ 按铁律**只读旁观**，两项计划内工作主动挂起。**它做的对**——单写入方原则被严格遵守。
   ⇒ 本轮我作为**唯一活跃写入方**完成收口；它挂起的「4 份仅扫 index 守卫补工作树面」等项**我未代做**（避免双写撞车，留它下轮）。
3. **InsCode 的产出未被覆盖**：修复前后记录 8 文件 sha256 指纹，终态比对全部 `SAME`。
4. **暂存区**：InsCode 曾自行 `git add` 3 件（旧版本）⇒ 本轮提交**重新 add 全部路径 + 带 pathspec**，不代提交任何非本轮路径。

## 6 本轮自伤（如实上报）

1. 🔴 **补丁脚本无 `__main__` 保护 ⇒ 用 importlib 调试时"import 即执行"，把补丁真实应用了一半**
   （P1 因锚点未命中跳过 ⇒ `utils/takeaway.js` 一度**导出了不存在的 `stripNonMoney`**，require 会抛 ReferenceError）。
   所幸发现及时（<2 分钟）并立即补齐。**教训：任何"会写文件"的脚本必须加 `if __name__ == '__main__':` 守卫，
   且调试读取只许用文本方式读，不得 import。**
2. 🔴 **同文件多处 `Edit` 会静默丢改动**（本轮三次：P1_OLD 缩进、`fix_eol` 定义）。
   症状：工具返回 success，但读回内容未变 ⇒ 之后一律**同文件改多处直接用 Write 整体重写**，或改完立即读回验证。
3. ⚠️ 我按字节统计 CRLF 一度误判「行尾污染」，实际 `core.autocrlf=true` 下 git 会规范化；
   **权威判据是 `git diff --numstat`**（只记真实增删），不是字节计数。

## 7 未落 / 待下轮

1. 真实平台账单核对（§A.11.7 的 V1–V3）：`reconcile` 的列名来自推导、**无真实账单验证** ⇒ 继续维持
   「只做软提示：不阻断保存、不参与利润、不写入金额」的纪律，**待李老师给一份真账单**即可转硬。
2. P7（快速→分项语义偏移）是否需要在 UI 上更明确提示 —— 待李老师看真机后定。
3. 同仓另一会话挂起的「4 份仅扫 index 守卫补工作树面」—— 留它下轮（本轮单写入方原则）。
4. `开发规范v1.0_ModuleA_收入费用核算.md` 引用的 `外卖核算_专项核对.md` 全仓 0 命中（round84 已登记待裁决）。
