# REVIEW_2026-09-15 round2 · 回执复核（R1–R11 逐条独立复测）

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 2 轮**（复核回执）。
> 本份是**新开文件**（协议 §2：不改自己过去的文件，需要更正就新开一份）。
> 上一份：`REVIEW_2026-09-15_restartkey-writeback.md`（R1–R11 + §3 回执）。
> 用户口令：「已落盘」。

---

## §0 基线与我做了什么

**方法**：不复述回执。**每条 R 都用自己的方法重测**（回执里的 grep 我全部重跑，并另加两项回执没做的检查）。

| 复测项 | 我的独立结果 |
|---|---|
| 门禁 A–L | **exit 0**（我在复核时会话内又跑一次） |
| 三对 `.md` ≡ `.txt` | ✅ 哈希相同（`SMOKETEST_RUNBOOK` 4FA0A42CAD6B、`新手上云操作手册` 40B26B44B83E、`下一步工序清单` E72B4235167A） |
| 陈旧串全树清扫 | `30 条索引` / `9 个 unique` / `A7 成立` = **0 命中** ✅ |
| 提交 | HEAD `9feb733`；工作区干净；提交内含**恰好预期路径**（8 份 + 3 份 `.txt` + `review/` 新文件） |
| **回执有没有动我的正文** | ✅ **只追加**：`仓库版.StartsWith(我的正文) == True`（逐字符前缀比对，17268 → 20215 字符） |
| **重启键结构完整性** | ✅ 无重复/丢失章节（`## 0`~`## 12` 全在，§1.2 已插在 §1.1 之后）—— 针对回执自述的「首轮同文件并发写覆盖」风险专门查了 |
| **索引对照表 vs 代码**（回执没做，我补做） | ✅ **逐条一致**：39 行 vs 代码 39 条，`【唯一】` 10 个对齐，**集合/索引名/字段方向(↑↓)/顺序 全对**（我写了机器 diff 脚本跑，非肉眼） |

**R1–R11 逐条判定：全部 ✅ 落地**（R4 并入 R3-4、R5 以横幅实现、R7 的 `app.json` 子项按声明留待实测）。**R1 我特别复验了**：手册 §5.2b 已含漏掉的 `shop_inventory.(shop_id,month)`，且「9 条都在 → A7 成立」的假判读已消失。**R8 那张表质量很高** —— 它是要照着手工建索引的东西，转录零误差。

### ⚠️ 我自己的错误，明确认领（R9）

我在上一份 R9 里写「实测 `SUITES` 8 项 ……（**7 个** specs 套件）」—— **这个数字是我数错的**：specs 套件 = 门禁 A–L + `verify_seed_data` + `test_poc1~4` = **6 个**，加批次0 自测 + 静态路径检查才是 8。WorkBuddy 在回执里按实测写成 6，并在备注里点出了我的错。**认领，并记一笔**：这正是「回执不是抄写员」该有的样子 —— 我给的指令错了它没有照抄。往后我报数字一律先跑计数命令再写。

---

## §1 待办（R12 起顺延上一份编号）

### R12 🔴 仓库内的 `.docx` / `.pdf` 是**陈旧派生副本**，且门禁**一组都守不到**；其中一份还装着**已被否定的结论**

**证据（可复现）**
- mtime 对比（`.md` 今天改过，`.docx`/`.pdf` 没跟着重生成）：
  ```
  09-15 11:35  SMOKETEST_RUNBOOK.md      ← 已改
  09-14 15:05  SMOKETEST_RUNBOOK.docx    ← 陈旧
  09-13 19:46  SMOKETEST_RUNBOOK.pdf     ← 陈旧
  09-15 11:33  新手上云操作手册.md        ← 已改
  09-14 15:05  新手上云操作手册.docx      ← 陈旧
  09-15 11:34  下一步工序清单.md          ← 已改
  09-14 16:29  下一步工序清单.docx        ← 陈旧
  ```
- **内容证据**（把 `.docx` 当 ZIP 解开、剥掉 XML 标签后在 `word/document.xml` 里搜；此法可判「有」，零命中因 run 切分**不构成"没有"的证明**，但**命中的串是确凿存在的**）：
  - `新手上云操作手册.docx`：命中 **「9 个 unique」×1、「A7 成立」×1、「__probe」×2、「touristappid」×2**
    ⇒ 它就是**修复前**的手册：仍教「核对 9 条 unique → 若全在则 **A7 成立**」，并仍写着违规集合名 `__probe`。**这正是 R1 在 `.md` 里刚修掉的那个假结论。**
  - `SMOKETEST_RUNBOOK.docx`：命中「9 个 unique」×2、「__probe」×1、「touristappid」×2、「判据④唯一未完项」×2 ⇒ 陈旧。
  - `下一步工序清单.docx`：**整节缺「工序 5.5 · 索引」**（连旧计数都没有，说明它比该节更早），只剩一行排错「索引建不上 → 云端 `createIndex` 可能不可用」。
- **为什么门禁守不到**：F/D/I/J 组扫描面 = `.md` + `.txt`（`.js` 另计），**K12/K13 只比 `.md` ↔ `.txt`** ⇒ `.docx`/`.pdf` 完全在覆盖外。这与 D4/D6 的「桌面派生件落后」是**同一类病**，只是这次发生在**仓库内**、且是**会打印/会递到人手上的那一份**。

**修法（择一，我建议 A；B 是 D6 先例）**
- **A（推荐）**：`.md` 改完就重生成 —— `python tools/md2docx_portrait.py <in.md> <out.docx> <标题>`；`.pdf` 明确一个出法（若靠 Word 另存，就在文档首行写明"以 `.md` 为准"并接受 pdf 定期重出）。**并在三份文档首行各加一行**：`> ⚠️ 本文档为派生副本，冲突以同名 .md 为准`。
- **B**：把 `.docx`/`.pdf` 移出仓库（或移入 `_archive`），仓库只留 `.md`+`.txt` 单源+派生对（与 D6 处理桌面派生件一致）。

**验收**：`Test-Path`/mtime 显示三份 `.docx` **晚于**同名 `.md`；剥标签后 `新手上云操作手册.docx` 里 **`A7 成立` = 0、`9 个 unique` = 0、`__probe` = 0**、`39 条索引`/`10 条` ≥1；门禁 exit 0。

---

### R13 🟡 未推送：`9feb733` 还在本地（协议 §1 要求 `git push dev`）

**证据**
```
git rev-parse --short origin/dev      → 655ca26
git log --oneline origin/dev..HEAD    → 9feb733 docs: 执行复审 REVIEW_2026-09-15_restartkey-writeback…
```
⇒ 本地 `origin/dev` **没有前进**。前一轮 `655ca26` 是在 `origin/dev` 上的（说明这个仓库的 push 通道本来是通的），所以这是**本轮漏做的一步**，而非通道故障。
> 我的边界：`git ls-remote` 在本环境**卡死超时**（网络不通），所以我只能依据**本地远端引用**判断，不能替你说"确实没推上去"。但「本地远端引用未前进」本身已足够要求补一次 push。

**修法**：`git push dev`（若报权限/网络错，把回显原样贴进回执，不要静默跳过）。
**验收**：`git rev-parse --short origin/dev` = `9feb733`（或更新的提交号），或回执附 push 回显。

---

### R14 🟡 `SMOKETEST_RUNBOOK.md` 步骤 6：行号错 + **与同文件 §一 自相矛盾**（+`.txt`）

**证据**
- `:129`「打开 `miniprogram/config/env.js`，把**第 14–15 行**的占位符换成……真实环境 ID」→ 实测 `env.js` 的 `dev` 值在**第 16 行**、`prod` 在**第 17 行**，`14–15` 行现在是 `module.exports = {` / `ENV_MAP: {`。
- `:131`「**这是判据④唯一未完项**」→ 与本文件**今天刚改好的** `:38`「判据④ dev 侧已闭环」、`:130`「dev 那行**已填**」**直接矛盾**。同一事实在同一文件里出现三种说法，正是本项目反复吃亏的「同一事实写两遍」。

**修法**
- `:129` → 「打开 `miniprogram/config/env.js`，把 **prod 槽（第 17 行）**的占位符换成步骤 1 记下的真实环境 ID（dev 槽已在第 16 行填好）。」
- `:131` → 「判据④ 现**仅剩 prod 这一项**（dev 侧已闭环）；prod 环境建好并替换后，batch0 自测的「点4 未完成」会变 ✅。」

**验收**：`Copy-Item SMOKETEST_RUNBOOK.md SMOKETEST_RUNBOOK.txt -Force` 后 md≡txt；`Select-String -Pattern "判据④唯一未完项"` → 0；门禁 exit 0。

---

### R15 🟡 `SMOKETEST_RUNBOOK.md:52` 仍称「现状 appid 是 touristappid」= **事实已错**（R7 只改了手册，这是同一事实的第二处）（+`.txt`）

**证据**：`:52`「现状：`project.config.json` 的 `appid` 是 `"touristappid"`（游客模式占位）」，而实测 `project.config.json:3` = `"appid": "wx33c110dc57a9c8dc"`。`:54` 的**判据**（"`appid` 不等于 `touristappid`"）可以保留，它是校验式不是现状描述。

**修法**：`:52` → 「现状：`project.config.json` 的 `appid` **已是真值** `wx33c110dc57a9c8dc`（`project.config.json:3`）；若哪天它退回 `touristappid` 才需要替换。」
**验收**：同 R14（重生 `.txt` + 门禁 exit 0）。

---

### R16 🔵 重启键「判据④」的**第三处**说法未同步

**证据**：`★知识存储点_2026-09-10.md:59` 仍写「点④ 实为「**未完成**（待你建环境后替换 `env.js:14-15` 真实 ID）」」，与同文件 `:49`（今天刚改成 ✅ **dev 侧闭环**）矛盾。
**修法**：把该句尾改为「点④ 现状：**dev 侧已闭环**（`env.js:16` 已填真实环境 ID），仅 prod 待建环境后替换 —— 见 §1.1 判据④。」（保留前半段 P0/假绿的历史记载，**不重写历史**。）
**验收**：`Select-String "未完成（待你建环境后替换" → 0`；`Select-String "dev 侧已闭环" → ≥2`；门禁 exit 0（该文件在扫描面内）。

---

### R17 🔵 「同一事实残留」清尾：条件式排错行 + 悬案措辞（3 处，均可一起做）

| # | 位置 | 现状 | 改为 |
|---|---|---|---|
| a | `新手上云操作手册.md:192`（+`.txt`） | 排错行「AppID 还是 touristappid 或没实名」 | 「AppID 没填对或没实名」 |
| b | `下一步工序清单.md:255`（+`.txt`） | 排错行「AppID 是 touristappid 或没实名」 | 「AppID 没填对或没实名」 |
| c | `SMOKETEST_RUNBOOK.md:37`（+`.txt`） | 「一次性问清 A7 / A6 / require **三悬案**」 | 「三悬案已于 2026-09-14~09-15 云端实测答完（见本文件 §四前提终局结论）」 |

**理由**：这三处的 `touristappid` 现在是**不可能出现的诊断结论**（留着会让排错追到错方向）；「三悬案」也已随探针 v3 全部关闭。**验收**：三对 md≡txt 重生后一致；门禁 exit 0。

---

### R18 🔵【准予】把 `下一步工序清单` 纳入门禁 `DOC_PAIRS`（对应回执「未决 2」，我的裁决 = **同意做**，但要带变异验证）

**我核准的理由（有实测支撑）**
- 全仓库 `.md`+`.txt` 同名配对**只有 3 对**，`下一步工序清单` 是**唯一无门禁**的一对：
  ```
  SMOKETEST_RUNBOOK.md  ≡txt=True  [K12/K13]
  下一步工序清单.md      ≡txt=True  [⚠️ 无门禁]
  新手上云操作手册.md    ≡txt=True  [K12/K13]
  ```
  而 `check_error_codes.js:546` = `const DOC_PAIRS = ['SMOKETEST_RUNBOOK', '新手上云操作手册'];` —— **确实漏了它**。
- 风险不是假想：**R8 的 30→39 修正正是靠手工 `Copy-Item` 才没漂移**；一旦漏一次，门禁**看不见**，而 PDF/DOCX 又不守（见 R12）⇒ 三层都漏。

**修法**：`DOC_PAIRS` 加第三项 `'下一步工序清单'`（现有两份完全相同，加完**当场不会变红**）。
> 连带一处（不加会立刻制造新的「同一事实写两遍」）：`check_error_codes.js:558` 的汇总串写死了组名 ——
> `notes.push(\`文档派生一致性 : ${DOC_PAIRS.length} 组（Runbook / 新手上云手册 的 .txt ≡ .md，CRLF/BOM 已归一）\`)`，
> 加了第三项后这句会**少报一对**。请一并改为不写死名字的措辞（如「（`DOC_PAIRS` 各组 .md ≡ .txt，CRLF/BOM 已归一）」）。
**必须附变异验证**（否则就是"改门禁自证清白"）：**同一次命令序列内**注入 → 跑 → 还原 → 再跑：
```
Add-Content 下一步工序清单.txt "DRIFT-INJECTED"   # 注入
node specs/dev-specs/prototype/check_error_codes.js   # 期望 exit≠0 且点名 [K13] 下一步工序清单
Copy-Item 下一步工序清单.md 下一步工序清单.txt -Force   # 还原（用单源重生，勿 git checkout）
node specs/dev-specs/prototype/check_error_codes.js   # 期望 exit=0
git grep -c "DRIFT-INJECTED" -- 下一步工序清单.txt      # 期望 0（无残留）
```
**验收**：`DOC_PAIRS` 含 3 项 + 上面四步输出（含注入后**必须变红**的那一次）；`check_error_codes.js` 本身在 D/I/J 扫描面外（那三组只用 `.md+.txt`），改它不会自命中 —— 但 **F 组扫 `.js`**，注意别引入替换字符。

---

## §1.5 对回执「未决 3 项」的裁决

| 回执提问 | 我的裁决 | 依据 |
|---|---|---|
| **① `app.json` 的 `projectConfig.appid: touristappid` 怎么处理** | **删掉整个 `projectConfig` 块**；删后编译一次确认 | 这是**同一事实的第二处副本**（权威在 `project.config.json:3`）。且有**实证**：真 AppID 已在生效 —— 9/14–9/15 你在真云上把 `initDb`/`smokeTest` 都部署跑通了，说明工具/CLI**用的是 `project.config.json` 的值**，`app.json` 里那个字段是**惰性的**；删掉只会移除陈旧副本，不会改变生效值。⚠️ 我仍保留一句：**"没坏"不等于"删了不会坏"**，所以删后编译一次（此步我核不了，须你/WorkBuddy 实测）。 |
| **② 是否把 `下一步工序清单` 加进 `DOC_PAIRS`** | **准予** —— 见 **R18**（附强制变异验证） | 3 对里唯一的门禁盲区；R8 手工同步已证明风险真实 |
| **③ R5 用「横幅」代替「逐行现址列」** | **接受，不必返工** | 我实核了横幅**信息完备**：覆盖 11 份归档清单 + `inscode交付包/` + Module 四件套(`core/`) + `poc/` 仍在；且 §5.1 五行**已全部加 `core/` 前缀**。逐行重写整表=更高引入误差的风险，收益为零。 |

---

## §2 能力边界（本轮新增两条）

- **`.docx`/`.pdf` 我只能间接判**：`review/` 与仓库 `.docx` 我按 ZIP 解 XML 抽查了关键字，**能确证"有旧串"，但不能确证"没有新串"**（docx 把文本切成 run，跨 run 的词会被我漏掉；我剥标签后已大幅缓解，但仍非全文比对）。**要"文件级"确认，须在 Word/开发者工具里打开看。**
- **远端推送核不了**：`git ls-remote` 在本环境**超时卡死**；R13 只能依据**本地 `origin/dev` 引用**判断，不能替你说"远端确实没有"。
- **开发者工具行为核不了**（延续上一份）：R12 的 docx 呈现、`app.json` 删块后能否编译，都须你/WorkBuddy 实测。
- **`verify_all.js` 端到端跑不了**（沙箱禁管道型子进程）；我跑的是**单套件**（门禁、以及自写的索引表 diff 脚本）。

---

## 附 · 本轮我额外做的两项对抗性检查（回执里没有的）

1. **索引对照表 × 代码 机器 diff**（R8 的核心交付物是要拿去手工建索引的）：
   输出 `代码真相源 = 39 条（unique 10）` / `文档对照表 = 39 行（【唯一】 10）` → **逐条一致**（集合、索引名、唯一标记、字段与 ↑↓ 方向、行序全对），`exit 0`。**这是我最担心会出错的一处，结果干净。**
2. **回执有没有篡改我的正文**：用**逐字符前缀比对**（`仓库版.StartsWith(我的正文)`）+ 重启键**章节结构扫描**（查串行重放导致的重复/丢失）。结果：**只追加为真**、`## 0`~`## 12` 全在。响应了回执自述的「首轮写覆盖」风险。
