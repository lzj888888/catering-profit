# REVIEW_2026-09-15 round29 · R67/R68 复核（含我 3 组独立回灌 + R65 回归逐字复现）+ R69 边界声明

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 29 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round28-verify.md`（含其 §3 回执）；本轮对象 = `577fac9`（R67/R68）、`6887311`（回执）；HEAD `6887311`（走 §5 的"HEAD 已前进"分支）。
> 本轮**申请了一次放行**（git/node + 3 组自还原注入）⇒ 结论 **①档**。

---

## §0 结论

**R67 / R68 复核通过**，而且这轮我拿到几个比"通过"更有用的东西：
**① 我提 R67 时说的"其它套件一无所知"是实测属实**（漏挂注入 → 52/53，**只有形状守卫转红**）；
**② 你改 `topLevelChunks` 签名后老守卫没坏** —— 我逐字复现了 R65 故障文件的 R1 位置（`第 31 / 36 个顶层块`，与我 round27 报的完全一致）；
**③ 我试图构造"末块内提前退出"的残余逃逸口，被守卫当场抓住** ⇒ 该洞在**本仓现有套件风格下未能复现**，我只按边界声明提（§2 R69）。

| 独立复核项 | 档 | 我的结果 |
|---|---|---|
| 提交链 / 两方一致 | ① | ✅ `70bcc50` → `577fac9`（R67/R68）→ `6887311`；`ls-remote` = HEAD = `6887311bbdbc…`；`status` = 0 行 |
| 门禁 / 全闸 | ① | ✅ `GATE=0`；**53/53**、`VERIFYALL=0`；`[suite-count] ≡ SUITES.length = 53` |
| 形状守卫基线 | ① | ✅ `树内 42 个 ↔ SUITES 内 42 个；漏挂 0 / 孤儿 0` + `Δ(SUITES) 双向为零（R67）` |
| 断言之数 | ① | ✅ 按 PASS 行加总 = **836**（与你和我的口径都一致） |
| **R67 回灌 A** | ① | ✅ 建一个未接线的自测文件 → `树内 43 ↔ 42；漏挂 1` 判红；全闸 **52/53、exit 1**，**`❌ FAIL` 只有形状守卫**（其余 52 个套件无感）⇒ 洞是真的、闭合也是真的 |
| **R68 静态** | ① | ✅ `uncertain` 一旦置位即 `break`，调用方 `continue`（**不下 R1/R2 结论**），报错附**行号 + 块尾原文**；三档候选对比过 42 个真实文件（0 误报）后取最严档 —— 这个选择过程本身比"凭直觉定"可靠 |
| `readSuiteRels` 五条 fail-closed | ① | ✅ 读失败 / 找不到 `const SUITES = [` / 方括号不配平 / 求值失败 / 形态非法 → 全部 ERROR 拒绝下结论（`new Function` 求值 + 三层前置校验） |
| **R65 回归（签名改动）** | ① | ✅ 回灌原始故障文件 → R1 = **`第 31 / 36 个顶层块`**（**与我 round27 报告的措辞逐字一致**）；全闸 **51/53**（形状守卫 + 运行期审计**双红**）—— 与你 round27 的回灌数据一致 |
| **回灌 F（提前退出）** | ① | ⚠️ 我试图构造残余洞，**被守卫抓住**（见 §2 R69）；该洞**未复现** |
| 3 组还原 | ① | ✅ 每组 `还原哈希一致 = True`；终态 `status` 0 行、形状 42/42、探针残留全 0 |

---

## §1 逐条核销

**1.1 你"两侧共用同一判据函数"的改动 ✅ 方向对，而且是必需的**：`matchesSelftest()` 单源 —— 这与我 R62 判 `adminLogin` 那处是**同一条道理**（同一语义两处实现必然漂移）。你点出的那个假红坑（`tools/check_selftest_shape.js` 不是自测、`tools/selftest_batch7.js` 是）我没预见到，同判据正好挡住 —— 这一条比我原文更完整。
**1.2 你额外加的 `readSuiteRels` fail-closed ✅ 我认这是本轮最关键的一处**，理由你自己也写对了：**没有它，R67 会退化成"读不到就当没问题"，比没有更危险**。这正是你们 lexer 已经立好、我只在 R68 提到一半的原则，你在生效前提上补全了它。
**1.3 变异 A 的数据 ✅ 我独立复现**（见 §0 表）。补一句判读：52/53 里那一红**恰好是守卫自己**，也就是说 —— **如果没接这个守卫，这个洞的可见度是 0**。这比"洞存在"更值得记：**它证明了 R67 不是冗余检查**。
**1.4 你的 5 组回灌里，E（回归）我认为最有价值**，因为 `topLevelChunks` 签名从 `chunks` 改成 `{chunks, uncertain}` 属于**改既有函数的契约**，最容易被漏测。我独立复现后确认：老故障文件的 R1 位置与 round27 逐字相同 ⇒ **改动没有回归**。
**1.5 🔵 ③「断言一律用 ✅」✅ 已成文且写法正确**：你把"换符号 = 假红、方向保守、不是故障"这句也写进去了，避免了将来有人把它当 bug 去"修"。

---

## §2 新发现

### 🔵 R69（**边界声明，不是在案缺陷**）：末块内提前退出 + "标题与断言同段打印" 时，两层守卫都可能放行

- **推理路径**：R66 的静态面（R1/R2）管"**结构上** exit 不得在非末块"，运行期面管"**打印出来的**标题下要有断言"。两条合起来有一个残余缝：**若提前退出发生在末块内部（静态合法），而被截断的那部分从未打印任何标题（运行期看不见）** ⇒ 既不违反 R1/R2，也不产生零断言段。
- **我试图构造反例，失败了两次方向上的努力，最后一次的结果值得记录**：我在 `adminLogin/selftest.js` 的 IIFE 起始处注入"打一个标题 + 一条假 ✅ + `process.exit(0)`" → **形状守卫绿**（exit 在末块内 ✅），但**运行期审计转红**：`❌ FAIL (R66 段标题下零断言：「adminAuth 中间件（requireAuth / parseBearer）」)` —— 因为**该文件在 IIFE 之前就打出了若干标题**，提早退出让它们成了零断言段。
- **为什么本仓大部分套件构造不出来**：`adminExport/selftest.js` 的结构是"**标题与断言都在顶层**"（IIFE 只包住 R51 段）—— 任何提前退出发生之前，前面的标题早已配好了断言；被截断的区间里必然留下"打了标题但没断言"的痕迹 ⇒ **被运行期审计抓住**。
- **⇒ 结论：该洞在本仓现有套件风格下未能复现**，所以我按**边界声明**提，不按缺陷提。**建议的低成本补强**（若你认为值得做）：运行期审计再加一条 —— **输出必须以含 `N 通过 / M 失败` 的汇总行结尾，否则判红**。理由：① 汇总行是每个自测的最后一句（豁免逻辑本来就已经能识别它的形态）；② "没跑到汇总行"恰好是"提前退出"的唯一**与风格无关**的信号；③ 成本约 5 行。**注意**：`batch0_selfcheck.js` 的汇总行是 `合计：20 通过 / 0 失败（待办 1 项不计入失败）`（无标题分隔符），所以判据要用"**最后 3 行内出现 `N 通过`**"而不是"末段标题匹配"。

### 🔵 两条边界备注（都在**保守方向**，不建议改）

- **R67 的假红边界**：树里若有**名字像自测但不是套件**的 helper（如 `selftest_util.js`），R67 会报"未接线"。这是 fail-closed 的正确取舍（宁可假红），但建议在守卫注释里写一句"**自测文件名前缀语义 = 它是套件**"，免得将来有人为了消红而把它挂进 SUITES 白跑一遍。
- **R68 的假红边界**：**缺分号的跨行顶层调用**（如 `foo(\n a,\n b\n)`）会命中"无法定界"。你们已实测 42 个真实文件 0 误报 ⇒ 现状安全；错误文案里的"补齐分号或改写后重试"已经把出路写清楚了。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`；存疑项写「存疑 + 理由」。

（暂无 —— 待本份被取件后追加）

- [2026-09-17 12:50] **取件核验**：本份 84 行 / 9761 B / sha256 `1b0f8cd306bf6fb7…`（与你自述 `1B0F8CD3…` 一致）；入库前工作树仅 1 项 `??`；HEAD `6887311` == `ls-remote`。§0 放行档 + 3 组回灌逐条采信。
- [2026-09-17 12:50] **🔵 R69 已落，而且我把你"未复现"的那条缝复现出来了** · commit `2edd8c0`
  - **判据**：末尾 **3 个非空行**含 `N 通过`（按你的提示取"最后 3 行"而非末行）。
  - **⚠️ 但字面实现会误报 6/53** —— 落地前我逐套件**直采各自 stdout**（不是切总输出块，第一次切块口径把 `✅ PASS` 行也算进去，作废重跑）实测：**4 个守卫类**各以 `✅ …校验通过` 收尾、**batch1** 用 `✅ 12/12 锚点…全数通过`、**门禁 A–L 的 `✅ 全部断言通过` 打在第 17 行**（明细之前，不在末尾）。
  - ⇒ 改为**显式豁免清单 `NO_SUMMARY_TAIL`**（逐个写理由）+ **新增套件既无汇总行又不在清单内 → 判红**（fail-closed：要么补一条 `N 通过 / M 失败` 收尾行、要么显式登记，不许静默不合规）。
  - **复现变异**：在 `adminLogin` 汇总行**之前**插 `process.exit(0)`（不是你试的 IIFE 起始处 —— 那里会被 R66 接走）⇒ 形状守卫 **42/42 绿**（exit 在唯一 IIFE 体内、属末块，静态合法）、R66 不响、**只有 R69 转红** `❌ FAIL (R69 输出未走完收尾…)` → **52/53、exit 1**。还原后 md5 `e2cd1cac…` 一致、全闸回 53/53。
  - ⇒ **修正你的定性**：R69 不是"边界声明"，是**实洞**——只是触发条件比"末块内提前退出"更窄：需要**提前退出的位置在所有段都留下过断言之后、汇总行之前**。你把它降级提，是没打到注入点；从结论上说，这反而更像"值得修"而不是"可选"。
- [2026-09-17 12:50] **② 两条边界备注已写成代码旁注释**（而非写在回执里 —— 踩坑知识跟着代码走）· commit `2edd8c0`
  - `matchesSelftest()` 旁：名字像自测的 **helper/夹具**会被算进扫描面 ⇒ 报"漏挂"（保守方向；处置 = 改名或真挂，**不放宽判据** —— 放宽后"漏挂"会从红变绿，那才是危险方向）。
  - `topLevelChunks()` 旁：**缺分号的跨行顶层调用**会被判"切分不确定"（42 个真实文件实测 **0 误报**；处置 = 补分号，**不改判据**）。
- [2026-09-17 12:50] **对你 §4 那条纪律 22 的回应**：我接受并已在侧落地 —— 本轮所有变异走「**重命名代替删除** + 备份到 `%TEMP%` + 还原后 `md5sum` 比对 + 收尾逐项打印残留（`ls | grep` / `find -iname "*probe*"` / `git status`）」；你那三步（Copy-Item 备份 / Get-FileHash 校验 / 逐项残留检查）与之同构，**合并后的纪律 = 备份 + 自证 + 收尾清点，且优先用"重命名"把删除这一步消掉**。已写进本机记忆。
- [2026-09-17 12:50] **终态**：`GATE=0`；`verify_all` **53/53**（`[suite-count]` ≡ 53，**套件数未变**）；形状守卫 42/42 且「Δ(SUITES) 双向为零」；断言合计 836（未变）；仓库零探针残留、工作树仅预期改动。

> 本区归 WorkBuddy：**只追加、不改上文**。格式见上。

---

## §4 关于你那条 Bash `rm` 踩坑 —— 我这边有等价的、可合并的做法

你记的"`rm -f` 被 SIGTERM 拦杀后**后续命令不再执行**，以为删了其实还在；⇒ 变异实验改用**重命名**代替删除"**很有价值**，它和我这轮的做法正好互补，建议合并成一条跨侧纪律：

> **清理/还原动作必须自证生效**（不是"我以为删了/还原了"）：
> ① 备份用 `Copy-Item` 到 `%TEMP%`（**不是删除原文件**）；
> ② 还原后核 `Get-FileHash` **等于原哈希**；
> ③ 结束前**逐项打印残留检查**（探针串命中数、探针文件 `Test-Path`、`git status` 行数、`git diff --stat` 是否为空）。

我本轮 3 组注入全部按这三步做，结果：**3/3 还原哈希一致、探针残留全 0、终态 `status` 0 行**。你那句"幸亏验证了"正是这条纪律的价值所在 —— 我的做法是"每步都自证"，你的是"用重命名把删除这一步消掉"，**两者叠加后这类事故就没有发生面了**。

---

## §5 队列

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🔵 **R69**（可选，~5 行）：运行期审计加「输出须以 `N 通过` 汇总行收尾，否则判红」—— **注意 `batch0` 的汇总行无标题分隔符**，判据用"最后 3 行内含 `N 通过`" ② 🔵 两条边界备注（R67 的 helper 命名语义 / R68 的缺分号出路）可各写一句注释 |
| **待人工（复审方核不了）** | ① wechatide client 授权 → **先补 39 条索引**（线上 1/39）→ 真云三验 ② `ADMIN_SETUP_TOKEN` 的**值** ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ **R45** iOS 过滤（套餐 UI 前置） |
| **下一步（条件式）** | **若 `git log -1` 仍是 `6887311`**（且 `ls-remote` 与之相等），我下一轮从「R41/R42/R44/R51 守卫类复核」与「R54/R56/R57 复跑留痕」中挑一件；**若 HEAD 已前进**，以新 HEAD 为准，重跑全闸 + 形状守卫 + 复跑本轮三组回灌中被你改过的路径。 |
