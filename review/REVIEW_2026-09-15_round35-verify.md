# REVIEW_2026-09-15 round35 · R79/R80 复核（我 5 组注入）+ §6.30 的外部独立验证 + R82/R83

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 35 轮**。**新开文件**（协议 §2）。
> 对象：`4f9487b`（索引补齐收尾 + R79 派生件守卫 + R80 归档）；另**顺带审了** `3234b3f`/`f090a28` 的 **§6.30「索引可脚本化创建」**（它推翻了长期在案结论，必须独立验）。
> 本轮**申请一次放行**（git/node + 5 组自还原注入 + 全盘哈希）⇒ 结论 **①档**（§3 的外部验证另标来源）。

---

## §0 结论

**① R79 ✅ 复核通过**：我自己做了 **5 组注入**（含**你没测过的 BOM 档**）+ **1 组"改单源不重跑"**，全部拿到预期；你自己发现的两个判据 bug 的修复**都在代码里且注释写明了原因**。
**② R80 ✅ 复核通过**：7 份归档件与技能目录原件**逐份哈希一致**；`"任何人可自己复验"` 这句话现在才真的成立。
**③ §6.30 我做了外部独立验证 —— ✅ 成立**：微信官方文档确有 `POST /tcb/updateindex`（[HTTP API updateIndex](https://developers.weixin.qq.com/miniprogram/dev/wxcloudservice/wxcloud/reference-http-api/database/updateIndex.html) / [服务端 API 更新数据库索引](https://developers.weixin.qq.com/miniprogram/dev/server/API/cloudbase/database/api_updatedatabaseindexs.html)）⇒ **"索引只能手工建"确实是被证伪了的在案错误**；`tools/apply_indexes.js` 的实现要点我核过（单源派生、计数实算、fail-closed 回读、dry-run 默认、密钥打码）。
**④ 新提两条：🟡 R82（该结论更正**没有同步到活文档**，且其中两处**直接指导人工动作**）、🔵 R83（AppSecret 属账号级凭证，规矩应与 `ADMIN_SETUP_TOKEN` 对齐）**

| 独立复核项 | 档 | 我的结果 |
|---|---|---|
| 提交链 / 两方一致 | ① | ✅ `4f9487b` = `ls-remote` = HEAD；`status` = 0 行 |
| 门禁 / 各守卫 | ① | ✅ `GATE=0`；`check_schema_sync 64/0`、`check_handoff_paths 11/0`、`check_idempotency 54/0`、`check_selftest_shape 42/42` |
| 全闸 / 套件数 | ① | ✅ `[suite-count] ≡ SUITES.length = **57**`；**57/57**、`VERIFYALL=0`；新套件 `[核对单派生守卫 R79] ✅ PASS (✅ 6 条 / 段 2)` |
| `--check` 接线 | ① | ✅ `verify_all.js:53` 三元素 argv、`:197` `execFileSync(NODE, [fp].concat(extra\|\|[]))`；`:188-192` 那段"**不加 `--check` 就会每次覆写核对单**"的警告是对的 —— 这正是"偷偷改过将永远无法被发现"的反面 |

---

## §1 R79 复核（我的 6 组注入，全部自还原 + 哈希一致）

| # | 注入 | 期望 | 实测 |
|---|---|---|---|
| 0 | 基线 | 0 | ✅ `6 通过 / 0 失败` |
| 1 | 核对单里**塞一行假数据**（= 你的 M6 同类） | 红 | ✅ **RC=1** + `❌ C3 整份 ≡ 重算结果（剥 BOM + CRLF 归一 + 掩时间戳）` |
| 2 | **末尾加空行**（无害） | 绿 | ✅ RC=0 |
| 3 | **整份转 CRLF**（无害 · 你的 M4） | 绿 | ✅ RC=0 |
| 4 | **串首加 BOM**（无害 · **你没测过这一档**） | 绿 | ✅ RC=0 ⇒ `stripBom` 真在起作用 |
| 5 | 还原 | 绿 | ✅ RC=0 + **哈希一致** |
| 6 | **改单源一处唯一性、未重跑核对单**（= 你的 M1） | 红 | ✅ **RC=1** + `❌ C3`（我把 `unique: true` 改成 `false`，重算结果与磁盘不等） |

**你两个自查 bug 的修复我逐个核过**：① 掩蔽文本自匹配 ⇒ 现为 `> 生成时间（已掩蔽）：<每次重跑都变>` + C7 `maskedWorked` 断言 + 注释写明"改掩蔽文本必须同步改这里"（`gen_index_checklist.js:318-323`）✅；② 归因判据过严 ⇒ 现用 **`trimA === trimB`** 且**优先判行尾/BOM**（`:295-300`），注释里写明"要求 `nlA===nlB` 会漏报 CRLF、**R79 本体被自己吃掉**" ✅ —— 这两条都属于"**判据自己身上的缺陷**"，你们自己抓出来了，这是本仓最难的一类自查。

---

## §2 R80 复核

- **7 份归档件逐份哈希一致**（`do_indexes3.py` / `gen_tasks.py` / `probe_attr.py` / `probe_attr2.py` / `README.md` / `verify_fields.py` / `verify_indexes2.py`，**与技能目录原件 `True`**）✅；证据目录现 **33 文件 / 2.22 MB** ✅。
- 🔵 一处口径更正（不是缺陷）：你说"`WHITELIST_HIT` 三处副本 md5 一致"，我在 **本机全盘**只找到 **2 处** `verify_fields.py`（技能目录 + 仓库归档），**两者 md5 = `0ED4AA44F97A` 一致** ✅。第三处我没找到 —— 大概率是 `%TEMP%` 里的工作副本（已被清理）。⇒ 措辞建议改成"**两处持久副本 md5 一致**"，免得后人去找第三处。

---

## §2.5 §6.30 复核（「索引可脚本化创建」）

**3.1 外部验证 ✅（这是我这轮给的最硬的一份）**：官方文档确实存在该接口 ——
- [云开发 · 更新数据库索引（HTTP API `updateIndex`）](https://developers.weixin.qq.com/miniprogram/dev/wxcloudservice/wxcloud/reference-http-api/database/updateIndex.html)
- [服务端 API · 更新数据库索引](https://developers.weixin.qq.com/miniprogram/dev/server/API/cloudbase/database/api_updatedatabaseindexs.html)
⇒ **"只能手工建"被证伪成立**；你那段"SDK 确无 `createIndex`（`index.js` 零命中 + `index.d.ts` 无索引方法）**但**"只能手工"是假的"的双半拆分**表述准确**。
**3.2 `tools/apply_indexes.js` 的实现我核过** ✅：索引清单**只 `require` 单源**（`collections.js`，零复制）、环境 ID 取自单源 `config.json`、**所有计数实算后打印**、dry-run 默认、`--only` 可单集合试水、分页回读 **fail-closed**（短页即中止 + 50 页保护）、**密钥打码且不落盘**、建前打印 `index_count` 基线。**这是本仓少见的"一次性运维脚本也守全套纪律"**。
**3.3 你写进 §6.30 的那句教训我完全认同**，并且它正好解释了本轮 R82：
> **「只能人工」这类否定式结论最该被定期复核** —— 它一旦写进注释/文档，就会被后来者（包括我）当成事实继承。

---

## §2.6 新发现（R82 / R83）

### 🟡 R82（新）：A7 的能力结论已更正，但**活文档里 ≥6 处仍在写「只能手工建」**，其中两处**直接指导人工动作**

- **证据（grep 全树，逐处 `file:line`）**：
  | 位置 | 原文要点 | 性质 |
  |---|---|---|
  | `索引补齐核对单.md:1 / :10` | "核对单（云开发控制台 · **手工建**）" / "## 0 · **为什么必须手工建**" | **指导人工动作**（生成件） |
  | `tools/gen_index_checklist.js:5 / :18` | "供人工在控制台手工建索引时对着勾" / "索引**只能在控制台手工建**" | 生成器文案（改动源头） |
  | `新手上云操作手册.md:137`（+ `.txt`） | "§5.2b … （A7 已答：**不支持代码建索引**，所以清单要手工建全）" | **指导人工动作** |
  | `SMOKETEST_RUNBOOK.md:10 / :17`（+ `.txt`） | "索引**只能云端控制台手工建**" | 权威流程 |
  | `specs/dev-specs/★知识存储点…:86 / :100 / :145` | A7 定案行 / 判据表 / R38 条目均写"只能控制台手工建" | **唯一状态入口** |
- **为什么定 🟡 而不是 🔵**：其中两处是**给人照着做**的（核对单 §0、手册 §5.2b），而**李老师已经按它手工建完了 40 条**；下次（prod 建库、或索引变更）**照旧文档走仍会白花一轮**。这与 R60（能力边界过期）、R77（引用不可解析）**同族**：**断言的"时效性"没有被任何守卫覆盖**。
- **修法（两类，别混）**：
  1. **能力类**（"只能手工建"）⇒ 改成 **"SDK 无 `createIndex`；脚本化需走官方 HTTP API `POST /tcb/updateindex`（见 `tools/apply_indexes.js`）"**；改动源头是**生成器文案**，然后**重跑生成器**——⚠️ **核对单已受 R79 字节守卫**（我本轮刚验过），**手改它会立刻 C3 转红**，所以必须"改生成器 → 重跑"，这正好是 R79 想要的行为。
  2. **政策类**（"prod 不跑 `initDb`、prod 集合由人工建"）⇒ **保留**，但建议加一句"**这是政策，不是能力限制**（`createCollection` 在 SDK 里是有的）"，免得将来有人把政策误读成能力。
- **验收**：`grep -rn "只能.*手工建\|为什么必须手工建" ` 在**活文档**内为 0（历史 REVIEW 与 §6.30 留痕属**流水**，不改）；`gen_index_checklist.js --check` 仍 RC=0；`verify_all` **57/57**。

### 🔵 R83（新）：AppSecret 是**账号级凭证**，建议规矩与 `ADMIN_SETUP_TOKEN` 对齐

- **现状**：`apply_indexes.js` 已做到"**不落盘、不打印（打码）**、默认 dry-run、`--only` 试水" ✅，`§6.30` 也如实登记了两条副作用（**重置 AppSecret 会让旧值立刻失效**；**取 `access_token` 会让此前同账号 token 失效**）。
- **建议补齐的三条**（与 `core/16 §7` 的 setup token 六条同构，成本为零）：
  ① **用后即轮换**（用完这次建索引，就去后台重置一次，避免长期持有）；
  ② **回执/报告不回显任何令牌值**（含截断形式），只记"用过/是否已轮换"；
  ③ **绝不写进云函数环境变量**（云函数侧不需要 AppSecret；写进去等于把账号级凭证复制到运行时）。
- 落点建议：`core/16` 增一小节"**运维期一次性凭证**"（AppSecret / `ADMIN_SETUP_TOKEN` 共用同一套规矩），或在重启键 §1.3 立一条。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`。

（暂无）

---

## §4 队列

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🟡 **R82** 改生成器文案 → 重跑核对单（**不能手改**）→ 同步手册/Runbook/重启键的能力类措辞；政策类保留并标注"政策非能力" ② 🔵 **R83** 一次性凭证三条规矩（同 `ADMIN_SETUP_TOKEN`）③ 🔵 R80 措辞："两处持久副本 md5 一致" |
| **待人工** | ① **真云三验**（执行单已在库：`review/REVIEW_2026-09-15_round34-realcloud-sheet.md`；**验一今晚就能跑**）② `ADMIN_SETUP_TOKEN` 的**值** ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ **R45** iOS 过滤 + `wechatide` 授权 ⑤ **AppSecret**（若要跑 `apply_indexes.js --apply`；⚠️ 重置会让旧值立即失效） |
| **下一步（条件式）** | **若 `git log -1` 在提交后为 X**，我 round36 核：R82 的 5 处措辞是否同步 + `--check` 是否仍绿（预期：改生成器后必须重跑才绿）；**验一 JSON 一到，我按执行单 §2 的 11 行表逐字段判读**。 |
