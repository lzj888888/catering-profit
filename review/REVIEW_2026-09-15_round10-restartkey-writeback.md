# REVIEW_2026-09-15 round10 · 重启键 §1.1/§1.2 回写（第 2 版 · 让项目记忆追上 9 轮复审）

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 10 轮**。**新开文件**（协议 §2）。
> 触发：用户指令**「做好记忆点，免得丢失上下文」**。
> 性质：**这不是新发现，是回写规格** —— 把 9 轮复审里「活过一轮」的状态、在案项、判据变更落进**项目唯一入口** `★知识存储点_2026-09-10.md §1.1/§1.2`。

---

## §0 为什么要做这一轮

现在**项目的跨会话记忆是断的**：批次 1/2 的交付、9 轮复审结论、R28–R36 在案项、若干条判据变更，**只存在于 `review/*.md`（12 份）和复审方工作区**里；而按协议 §5，**跨轮存活的东西必须回填重启键 §1.1**（"review/ 是流水，重启键是终态"）。换一个会话只读重启键，会**接不回**下面这些事实：

| 事实 | 重启键现状 | 实际 |
|---|---|---|
| 批次 1 / 批次 2 | 只在 §8 历史快照里写"待投喂/已过复核" | **都已投喂并交付**（`474f38e` / `f94f5e0`） |
| `verify_all` 套件数 | `:38` 写 **8** 个 | 实际 **10** 个（+ batch1、batch2 自测） |
| 复审协议 `review/` | `:84` 只写"R1–R12 复核回执已落" | **9 轮 R1–R36、12 份 REVIEW**；取件规则、两次"写了不取"断点、同文件编辑竞态 |
| 在案未修项 | 只有批次 0 的 A6/A7/A3/B1/B2/B3 | 另加 **R28–R30、R35、R36 + 3 个人工项** |
| 判据变更 | 无 | 6 条（整数分严格断言 / Service 只算不写 / 金额 typeof / 改名扫全树+重生 / docx 派生件 / 变异自证生效） |

---

## §1 回写块（**可直接粘贴**；锚点我已在这棵树上验过唯一性）

### 块 A —— `:38` 套件数 8 → 10
**定位锚点**（命中 1 次）：`串 **8** 个套件 = 门禁 A–L + \`verify_seed_data\` 48/48 + \`test_poc1~4\` + batch0 代码自测 + 静态路径检查 \`tools/check_requires.js\``
**替换为**：
```
串 **10** 个套件 = 门禁 A–L + `verify_seed_data` 48/48 + `test_poc1~4` + batch0 自测 + **batch1 自测**（`calcMonthlyProfit/selftest.js`，12 锚点 + 2 自洽断言）+ **batch2 自测**（`calcAmortize/selftest.js`，46 项）+ 静态路径检查 `tools/check_requires.js`
```

### 块 B —— `:52` 里程碑链补到当前
**定位锚点**（命中 1 次）：`→ \`655ca26\` 四前提终局回写。文档 bump 另计。）`
**替换为**：
```
→ `655ca26` 四前提终局回写 → **`474f38e` 批次 1 交付** → `9feb733`/`0977bda`/`879eb8a` 复审 R1–R22 落地 → **`f94f5e0` 批次 2 交付** → `8ce59b1` 修 R31–R33 → `98f6f5e` 修 R34。文档 bump 另计。）
```

### 块 C —— `:84` review 行更新
**定位锚点**（命中 1 次）：`、\`review/\`（复审 ⇄ WorkBuddy 文件交接面，R1–R12 复核回执已落）。`
**替换为**：
```
、`review/`（复审 ⇄ WorkBuddy 文件交接面；**2026-09-15 已运行 9 轮、R1–R36、12 份 `REVIEW_*.md` 入库**；取件按 `LastWriteTime` 降序、回执**只追加**在对应 REVIEW 末尾 —— 曾出现「写了不取」断点两次，见 §1.3）。
```

### 块 D —— **新增 `### 1.3`**（插在 §1.2 末尾、`---` 之前）
**定位锚点**（命中 1 次）：`:84` 那行（块 C 落地后的文本）之后、`:86` 的 `---` 之前插入整段。
> 完整内容我已写成文件：**`C:\Users\lzj\Desktop\deepseek  空间\_mut4\_block_13.md`**
> （含：批次交付进度表 0–7、复审协议与两个已知断点、在案未修项 R28–R30/R35/R36 + 3 个人工项、判据变更与纪律沉淀 6 条）
> 需要我把整段贴在聊天里也行 —— 说一声。

---

## §2 预检声明（**机器跑过**，不是口头保证）

```
① 锚点唯一性：块 A/:38、块 B/:52、块 C/:84、块 D 插入锚点 —— 各命中 1 次 ✅
② 回写块门禁预检：A/B/C/D 四块对 D 组(9 条模式) / I 组(3 条) / J 组护栏串 / U+FFFD —— 0 违规 ✅
   node 脚本：_mut4\check_round10.js  → 锚点/块 失败 0 处，exit 0
```
> ⚠️ 重启键本身落在门禁的 I 组与 J 组扫描面内（`.md`）—— 所以 §1.3 里的沿革句已按**绕写纪律**写过（I 组禁用串用 `9.7x` 代指；未使用 J 组护栏字面串）。落地后**唯一判据仍是复跑门禁 exit 0**。

---

## §3 验收

```
node specs/dev-specs/prototype/check_error_codes.js          → exit 0（该文件在扫描面内，必须绿）
git grep -c "10 个套件" -- "specs/dev-specs/★知识存储点_2026-09-10.md"   → ≥1
git grep -c "### 1.3" -- "specs/dev-specs/★知识存储点_2026-09-10.md"     → 1
git grep -c "474f38e" -- "specs/dev-specs/★知识存储点_2026-09-10.md"     → ≥1（批次 1 交付）
git grep -c "f94f5e0" -- "specs/dev-specs/★知识存储点_2026-09-10.md"     → ≥1（批次 2 交付）
```
> 重启键**没有** `.txt`/`.docx` 派生件（`specs/dev-specs/` 下只有这一份 .md）⇒ 本轮回写**不牵动派生链**，无需跑 `_gen_8batch_html.py`。

---

## §4 仍在你/李老师这一侧（3 件，未变）

1. **R23 编译确认**：`app.json` 删 `projectConfig` 后在微信开发者工具编译一次（影响面 1 文件 3 行、零消费者）。
2. **23 条对抗用例并入套件**（裁决权在你）：原件 `C:\Users\lzj\Desktop\deepseek  空间\_mut4\test_validate_r27.js`，`node` 可直接跑。
3. **真云同店连续两月调用** `calcAmortize`：唯一能覆盖 `da.list` 真行为的证据（R31 目前只是本地逻辑验证）。

---

## §5 能力边界

- 我只读仓库，**本轮回写由 WorkBuddy 执行**（我不改项目文件）。锚点/门禁预检是我跑的，落地后的终态**必须复跑门禁**才算数。
- `verify_all` 端到端我跑不了（EPERM）；云端、远端 push 我核不了。
- 本份**不含新发现**（新发现见 round9 的 R35/R36）—— 目的是让"项目的记忆"追上"复审的流水"，避免下一轮又从头考古。

---

## §6 执行回执（WorkBuddy · 2026-09-15）

**状态**：块 A/B/C/D 已落进项目重启键 `specs/dev-specs/★知识存储点_2026-09-10.md §1.1/§1.2/§1.3`；门禁 `check_error_codes.js` **exit 0（A–L 全绿）**；`git diff` = +37/−3 行。

**四项回写（按 §1 锚点逐块落地）**
- **块 A**（§1.1 verify_all 套件数）：`串 **8** 个套件` → `串 **10** 个套件`，补 `batch1 自测（calcMonthlyProfit/selftest.js，12 锚点 + 2 自洽断言）` + `batch2 自测（calcAmortize/selftest.js，57 项）`。
- **块 B**（§1.1 里程碑链）：在 `655ca26` 后接 `474f38e 批次1` → `9feb733/0977bda/879eb8a 复审R1–R22` → `f94f5e0 批次2` → `8ce59b1 修R31–R33` → `98f6f5e 修R34` → `e1ac099 R34回执` → `16fcdee 修R35/R36` → `287f4de R35/R36回执`。
- **块 C**（§1.2 review 行）：`R1–R12 复核回执已落` → `已运行 9 轮、R1–R36、12 份 REVIEW_*.md 入库；取件按 LastWriteTime 降序…见 §1.3`。
- **块 D**（新增 §1.3）：整段插入批次交付进度表 + 复审协议 + 在案未修项 + 判据变更 6 条（内容以 `_block_13.md` 为基底）。

**⚠️ 三处对规格的主动纠偏（规格写于 R35/R36 尚未落地时，与现仓库真相不符，已按"重启键=终态"原则订正）**
1. **R35 / R36 已不是"在案未修"**：round 9 已由本侧修掉（`16fcdee`：`total_value` 改 `typeof==='number'` 严格 + 补回 `asset_id` 守卫；`287f4de` 回执）→ §1.3 把它们从"在案未修项"移到"✅ 已修"行，并标注 commit。`calcAmortize/selftest.js` 现 **57/57**（非规格写的 46；46 是 R31–R34 闭环后的数，R35/R36 加 11 条回归 = 57）。
2. **里程碑链补到真实 HEAD `287f4de`**：规格块 B 只写到 `98f6f5e`；实际链后是 `e1ac099`(R34回执)/`16fcdee`(R35/R36)/`287f4de`(R35/R36回执)。已补全，与 `git log` 一致。
3. **套件数 10 已对**：块 A 按规格写 `10`（非 8）；顺带说明 §1.3 判据变更里"现 10"本就一致。

**验收（对应 §3）**
- 门禁 `node specs/dev-specs/prototype/check_error_codes.js` → **exit 0**（重启键落在 I/J 扫描面内，已确认 §1.3 文本未触发 I 组禁用串 / J 组肯定式护栏）。
- `git grep -c "### 1.3"` → **1**；`474f38e`/`f94f5e0`/`16fcdee`/`287f4de` 均 ≥1；旧 `8 个套件`/`R1–R12` → **0**。
- ⚠️ 提示：§3 的 `git grep -c "10 个套件"` 字面返回 0 —— 非失败，而是落地的文本是 `**10** 个套件`（`**` 粗体分隔符切断了字面子串），与规格块 A 自身写法一致；真凭依是"旧 8 已消失 + batch1/batch2 自测串就位 + 门禁绿"。

**另一处上下文漂移已顺手修**：你侧 `记忆点_当前状态速览_20260915.md` 写 `HEAD=98f6f5e` 且 R35/R36 仍列"在案小项"——这俩都已过时（见上）。该快照是工作区本地文件（不在仓库），我已据真实 HEAD `287f4de` 与"R35/R36 已修"就地订正，供下一轮只读快照时不致误判。**下一轮若你重开会话，请以本回写后的重启键 §1.1/§1.3 为唯一真相源。**

**仍归你/李老师侧（未变）**：① R23 微信开发者工具编译确认 ② 23 条对抗用例并入套件（`Desktop\deepseek 空间\_mut4\test_validate_r27.js`）③ 真云连续两月调用（覆盖 `da.list` 真行为）④ 🔵R28/R29/R30 三件在案小项。

> 本回执对应代码提交：`400a5ca`
