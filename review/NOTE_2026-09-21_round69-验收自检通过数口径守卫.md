# NOTE_round69 · 2026-09-21 自驱动巡检

> 仓库 `C:\Users\lzj\WorkBuddy\Claw\catering-profit`。本轮起点 HEAD `463002d`（≡ round68）。
> 判据一律回源实扫/实跑，**不采信任何自述（含我方上一轮）**。

---

## 1. 探针（唯一入口）

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | `inflight=0`、`message_count=1465`（**十六连持平**）、`idle_sec≈211594`（≈58.8h）、`permission=auto`、无 pending 审批 | **idle**，无待批、无在途 |
| DeepSeek Harness | `hwnd=855322`、`rect[0]=-32000` | **最小化**；结合 round50~68 连续 17 轮「后端不可用」（`/api/*` 全 404 + 上游 `api.deepseek.com` 401 + `web.log` mtime ≈ uptime）且窗口会话为「餐饮闭店决策指标模型咨询」与仓库**上下文不匹配** ⇒ **不投递**（投了是上下文污染，且它跑不动）。**无人值守不擅自动 GUI**（抢焦点会毁并发方固定坐标点击） |

## 2. 🔴 检出第三个写入方（**不是 InsCode**）

开局 `git status` 出现 3 个我方未动过的 `M`，随后涨到 **6 个**，mtime 呈稳定 2–3 分钟节奏：

```
01:49:52  miniprogram/i18n/terms.js
01:50:15  specs/dev-specs/i18n/terms.js
01:53:37  pages/month/input.js
01:56:07  pages/month/input.wxml
01:59:02  pages/month/input.wxss
02:04:40  tools/selftest_batch8b.js
02:06:03  (terms.js ×2 + input.wxml 再次改写)
```

归属三条独立证据：
1. **排除 InsCode**：探针 `inflight=0` 且 `message_count` 十六连持平 —— InsCode 写码必然有 inflight 或消息数增长；
2. **排除我自己**：本轮操作目标与上述文件零交集，且我方变异已全部还原；
3. **外部工具特征**：git 报 `LF will be replaced by CRLF`，实算 6 文件 `CRLF=0`（纯 LF），与 round53/56 的第三方写入同特征。

⇒ 处置：按 §0.7 **降级只读旁观** —— 不点 GUI、不跑 `cli`、**不 `git add -A`**、不提交他人文件。
对方于 **02:14:24 提交 `9a329ab`**，内容 = 李老师 01:34 需求的「堂食收入录入改造（方案 A 严格互斥）」。

### 2.1 独立复核 `9a329ab`（不采信其自述）

- 改动面：terms.js 双副本 + `pages/month/input.{js,wxml,wxss}` + `tools/selftest_batch8b.js`。
- **未弱化守卫**：`selftest_batch8b.js` 是**净新增 G 组**（`@@ -110,6 +110,51 @@`，无删除断言）。
- **我方独立变异（M7）**：把 `TERMS.ledger.income[dine_in].items` 退回旧 5 项（现金/微信支付宝/储值消费/团购券核销/企业挂账消费）
  ⇒ `tools/selftest_batch8b.js` **rc=1 转红**；还原后 **69/0 绿** ⇒ 其 G1 守名判据是**真判据**，非假绿。
- 对方诚实标注 4 项未落，其中 **④ `init_db.js` 系统字典仍是旧 5 项**（前端默认行 7 项 vs 后端字典 5 项不一致）——
  本轮**未代修**（属对方在途决策面，且要李老师定），如实挂待办。

## 3. 🟢 同族病第 13 例：验收自检通过数 41 vs 实跑 48

### 3.1 怎么找到的

队列空 ⇒ 正事 = 防回潮扫描。按 round67 教训（「越往上线前人工操作清单方向找，后果越硬」），
对 `core/` 与 `上线材料_*` 逐个查「被守卫引用次数」（`grep -rl` 于 `tools/` + `prototype/`）：

```
0 次  11_微信审核自查清单 / 13_上线前查缺补漏 / 14_种子数据与验收脚本规范 / 16_后台鉴权规范
```

选 `14_种子数据与验收脚本规范.md` —— 它是《02_模拟测试数据集》（**验收唯一标准**）的机器可消费配套件。
回源实跑 `node specs/dev-specs/prototype/verify_seed_data.js`：

```
验收自检结果：48 通过 / 0 失败      ← 实算
```

而文档自述 **41**，且 `grep -rln "验收自检\|48 通过" tools/` ⇒ **零命中** ⇒ **零守卫**。

### 3.2 漂移面（实扫 5 处，全部当前态陈述）

| 位置 | 原文 | 真值 |
|---|---|---|
| `core/14:24` | `**41/41 全绿**` | 48/48 |
| `core/14:83` | `验收自检结果：41 通过 / 0 失败` | 48 通过 / 0 失败 |
| `★知识存储点:951` | `当前本机跑通 41/41` | 48/48 |
| `BRANCH_STRATEGY:41` | `verify 41/41 全绿` | 48/48 |
| `delivery/inscode喂投包_8批:144` | `当前已本机跑通：41/41 全绿` | 48/48 |

**最刺眼的证据**：`★知识存储点:38`（§1.1 一键校验入口）**早就写着 `verify_seed_data 48/48`**，
而**同一文件 :951 仍写 41/41** —— 同文件内部自相矛盾；`verify_all.js:299` 的收尾注释也早写着 48。
即「改了一处、漏了五处」的人工更正盲区（与 round64 红线 12 vs 11 同型）。

### 3.3 后果（为什么必须守）

「期望通过数」是**下界保护**。写成 41 ⇒ 断言从 48 掉回 41 时文档仍说「41 通过即符合预期」
⇒ **7 条断言静默丢失而验收照旧判绿**。反向也伤人：验收人看到实际 48 而文档写 41，先当异常查一轮（虚惊）。

### 3.4 已做

1. 立**口径单源**（唯一声明处）：`core/14` 新增语义标记 `🔢 验收自检口径（唯一声明处）… 本机实跑 **48** 通过 / **0** 失败`；
2. 修 5 处陈述为 48（喂投包改后**重跑 `delivery/_gen_8batch_html.py`** 重生派生件，实扫残留 41 归零）；
3. 新增 **`tools/check_acceptance_counts.js`（R105，13 断言）**，SUITES **77 → 78**，重启键两处套件数同步；
4. 门禁 **78/78 RC=0** + A–L **RC=0**。

### 3.5 判据三条腿（可复用模板）

- **① 实算单源 = 真跑脚本**（`execFileSync` 跑 `verify_seed_data.js` 后正则取 `验收自检结果：N 通过 / M 失败`）
  —— 不抄任何字面量，**fail-closed**（解析不到即红）。
- **② 唯一声明处 ≡ 实算**：「验收自检口径（唯一声明处）」语义标记 + `**N** 通过 / **M** 失败`，恰 1 处（删即红）。
- **③ 全仓当前态引用 ≡ 实算**：两种句式**分别归一**后再比 ——
  `N 通过 / M 失败` → pass=N, fail=M；`N/M 全绿` → pass=N, **fail=M-N**（第二个数是总数不是失败数）。
  锚点（`verify_seed_data`/`verify`/`验收自检`/`验收套件`/`seed_data`）须**就近 ≤120 字符**；
  够不着 → **弱面只 ⚠️ 明示不判红**（坑⑯）。HIST 行（`git commit` 示例等）放行并配**前提守卫**。

## 4. 双向变异回灌（8 组，0 异常）

| # | 变异 | 期望 | 实得 |
|---|---|---|---|
| M1 | 声明 48→41（原缺陷回归） | 红 | rc=1 ✅ |
| M2 | 引用面回 41/41（**换另一文件**，证不止守一处） | 红 | rc=1 ✅ |
| M3 | 删唯一声明处（fail-closed） | 红 | rc=1 ✅ |
| M4 | **正确实现换等价句式** `48/48 全绿`→`48 通过 / 0 失败` | **绿（不错杀）** | rc=0 ✅ |
| M5b | 扫描面根路径写错（前提守卫非恒真） | 红 | rc=1 ✅ |
| M6 | HIST 排除词表清空（排除面非恒真） | 红 | rc=1 ✅ |
| M7 | 对方渠道退回旧 5 项（独立复核 `9a329ab`） | 红 | rc=1 ✅ |

证据：`review/evidence/selfdrive_20260921_r69/mutation_matrix_r69.txt`。

## 5. 🆕 本轮新坑（两条，写完就踩）

- **坑⑲：同一文件并行发多条 `Edit`，只有最后一条落地**（其余报 success 但不生效）。
  本轮 4 条并行 Edit 只落了 1 条，表象是「改了却没生效、输出仍是旧格式」，极易往「判据写错」方向查。
  ⇒ **同文件 Edit 必须串行，且每条发完回读（`grep`）确认落地**。
- **坑⑳：`git checkout --` 还原在 autocrlf 仓里是「反向有损」的** ——
  把 LF 文件还原成 **CRLF**，直接打破 `K11 terms.js 双副本逐字一致`（另一副本仍是 LF）。
  既有铁律 #8 只防了「Python 写回把 CRLF 改 LF」这一个方向。
  ⇒ **变异还原后必须核行尾**，与同族文件归一；必要时用 `git show :<path>` 字节级回写，
  并用 `git add`+`git reset` 复位 stat 缓存（内容零变化）。

## 6. 溢出 / 待办（未修，如实标注）

1. 🟡 **`init_db.js` 系统字典仍是旧 5 项** vs 前端默认行 7 项（`9a329ab` 自述④）—— 待李老师定，本轮未代修。
2. 🟡 4 份前轮守卫仍是**只扫 index**（坑⑱ 同盲区）：`check_collection_perms` / `check_quota_limits` /
   `check_suite_count_claims` / `check_suite_coverage` —— 不代修，待裁决。
3. 🟡 零守卫引用、本轮未及处理：`11_微信审核自查清单`（其 §2「硬红线清单 6 项」vs §6「勾选清单 11 条」
   自称「与待办表一致」，属**提审前逐条勾**的人工面）、`13_上线前查缺补漏`、`16_后台鉴权规范`。
4. 🟡 并发方 1000+ 过程 PNG 仍未处理（不代提交不代删）。
5. 🟡 dsh 第 18 轮不可用 ⇒ 李老师四选一（推荐 **D 允许重启 dsh web**）仍未定，round39 等 5 份 NOTE 复审仍悬。
6. 🟢 R86 超时：按 round50 三方一致结论**不再回读**（本轮未实测，记存疑）；**建 prod 时必须重走定值与回读**。

## 7. 回执

- [2026-09-21 2026-09-21 02:37] R69-a **已落** · 探针：InsCode idle（`msg=1465` 十六连持平 / inflight 0）、dsh 最小化 ⇒ 按 17 轮不可用 + 上下文不匹配**不投递** · 证据 `probe_r69.json`
- [2026-09-21 2026-09-21 02:37] R69-b **已落** · 检出第三写入方（非 InsCode）：mtime 6 文件 2–3 分钟节奏 + 纯 LF + 探针排除 · 按 §0.7 降级只读旁观，未动他人文件
- [2026-09-21 2026-09-21 02:37] R69-c **已落** · 独立复核 `9a329ab`：净新增 19 条断言未弱化；M7 变异 rc=1 转红、还原 69/0 绿 · 证据 `selftest_batch8b_after_restore.txt`
- [2026-09-21 2026-09-21 02:37] R69-d **已落** · 同族病第 13 例闭环：实跑 48 vs 文档 5 处 41；立单源 + 修 5 处 + 重生派生件 + 新增 R105（SUITES 77→78） · 门禁 78/78 RC=0、A–L RC=0
- [2026-09-21 2026-09-21 02:37] R69-e **已落** · 双向变异 8 组 0 异常（含 M4 不错杀、M5b/M6 前提非恒真）· 证据 `mutation_matrix_r69.txt`
- [2026-09-21 2026-09-21 02:37] R69-f **未落** · R86 超时本轮未实测（按 round50 三方一致结论不再回读）· 理由：人工面 + 通道留给新活
- [2026-09-21 2026-09-21 02:37] R69-g **存疑** · dsh 后端状态未实探（最小化 + 无人值守不抢焦点）· 理由：§0.7 抢焦点会毁并发方固定坐标点击
