# NOTE · 2026-09-20 round64 自驱动巡检 —— 红线复核条数口径（同族病第 8 例）

> 执行方：WorkBuddy（巴迪）· 无人值守自驱动轮次。仓库 `C:\Users\lzj\WorkBuddy\Claw\catering-profit`。
> 判据：**回源实扫**，不采信任何一方的自述（含我方上一轮）。证据目录 `review/evidence/selfdrive_20260920_r64/`。

## 1. 探针（两个外部代理）

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | SQLite `inflight=0`、`message_count=1465`（**十一连持平**）、`idle_sec≈183288`（≈50.9h）、`approval_audit` 末行 `approved_once` | **idle**，无待批 ⇒ 不点击、不投喂 |
| dsh（:3080） | 窗口 hwnd=855322 存在，OCR 见「给智能体发消息」输入框，但**会话名 = 「餐饮闭店决策指标模型咨询」** | **上下文不匹配** ⇒ 第 13 轮不投递（非同一主题，投了是上下文污染） |

并发写入方判定（**四连**）：进程采样两轮 `HITS 0`（脚本文件化 + 排除自身 PID）、门禁跑完后回读
`review/evidence/` mtime 仍为 16:52（我方 round63 留下）、他人 `M`=0、`git log` HEAD `c57d14a` ≡ 上轮
⇒ **无并发方，本轮可正常写**。

## 2. 本轮核心：同族病第 8 例 —— 红线复核 12 条，三处文档仍写 11 条，零守卫

### 2.1 实扫（不采信任何自述）

`specs/dev-specs/core/04_核对清单.md` §C「红线复核表（一票否决，任一不符即打回）」实算 **12 行**
（末行「操作必留痕」）。而该文件的校订行早在 2026-09-12 就明写「**原写 11 条红线 / AD-1~AD-21，漏计 1 条红线**」
—— 更正**只落在 04 这一处**。对 `git show HEAD:<文件>` 回扫，另有 **3 处仍写 11 条**：

| 文件 | 行 | 原文（修复前） |
|---|---|---|
| `core/05_审计单.md` | 149 | `- 11 条红线复核：通过 ____ / 11` |
| `core/06_工程治理与运维规范.md` | 313 | `\| **v1.0.0** \| 提审发布 \| **22 项验收全绿 + 红线复核 11 条全过** \|` |
| `写码阶段启动执行手册_v1.0.md` | 85 | `**阶段 5**：… + 红线复核 11 条` |

原始证据 `review/evidence/selfdrive_20260920_r64/redline_prescan.txt`（`git show HEAD:` 实扫，非记忆）。

**后果不是文字瑕疵**：`06` 那一行就是 **v1.0.0 提审发布判据** —— 照它执行会**少查 1 条红线（操作必留痕）就放行**，
而「操作必留痕」对应 01 铁律 9（`audit_log` 追溯），正是当初专门补进去的那一条。

### 2.2 与前面 7 例同构

R59「套件数」/ round61「重启键套件数」/ R98「云函数清单」/ R99「隐私政策占位符」/ 本例
⇒ 同一模式：**同一事实在多处人工抄写，更正只改到一处，零机器校验**。
本例还多一层讽刺：更正说明（04 校订行）**自己就写着**「漏计 1 条」，却没人去其它文档找那 1 条。

### 2.3 顺带核实（**无缺口**，实扫为证，不是"顺带看了一眼"）

- §B「22 项验收清单」：实际编号 `1..22` 唯一数 **22**，无缺号重号 ✅
- §C 红线表：**12** 行（与 04 交付判据行的「12 条红线全过」一致）✅
- AD 清单：表格行 **26** 条、`AD-1..AD-26` 无缺号 ✅（下条为**溢出观察**，本轮**未**做成守卫）
- `git ls-files` 临时/调试件命中 **0**（剔除证据目录后）✅；33 个 `check_|verify_|selftest_|test_*` 脚本
  **零**未挂 SUITES ✅（round60 元守卫持续生效）

#### 🟡 溢出观察（**未修、未立守卫，待裁决**）：AD-1~AD-26 只有 6 条有机械判据

`tools/selftest_ad_gates.js`（24 条断言）里**只点名了 AD-15 一个编号**；逐条比对后确有机械覆盖的仅
**AD-3（G1/G2 字号·触控）/ AD-12（G4 热更新）/ AD-15（G5 头像昵称）/ AD-18（G7 场景值）/ AD-22（G3 adjust-position）/ AD-26（G6 触觉反馈）**，
其余 **20 条（含文档自称高危的 AD-23/AD-24）只有人读清单、无机器判据**，依赖真机实测。
本轮**不**把它们登记成"有判据"（那等于谎报覆盖），仅如实记录。是否补判据 / 是否接受"真机项"属李老师决策。

## 3. 处置（修陈述 + 立单源 + 落守卫，**不动判据**）

1. **立单源**：`core/04` §C 标题下新增语义标记行
   `🔢 **红线全集口径（唯一声明处）**：本表共 **12** 条（一票否决，末行为「操作必留痕」）`。
2. **修 3 处陈述** 11 → 12（05 审计单 / 06 提审发布行 / 手册阶段 5）。改的是**外部文档**，扫描规则未动。
3. **新建守卫** `tools/check_redline_inventory.js`（R100，11 条断言，挂 SUITES **第 73** 项）：
   - P1 扫描面 fail-closed（文件非空 + `.md` 数 ≥20）
   - P2 语义标记可解析（fail-closed）+ 唯一声明处在 04 内恰 1 行
   - P3 **声明 N ≡ §C 实算行数**（双向：改表 / 改声明都红）
   - P4 每条三字段非空
   - P5 全仓当前态声明（`X 条红线` / `红线复核 X 条`）≡ N
   - P6 **排除面前提**（确有历史/校订类陈述被放行）
   - P7 命中数 ≥3（正则失效即零命中假绿 ⇒ 红）
   - P8 唯一声明处不扩散
4. **套件数三处同步**：`verify_all.js` 头注 72→73、重启键 §1.1 行 72→73、重启键「套件数会漂」行 72→73（+73 演进条目）。

🔴 **为什么不做裸扫**（round62 坑⑭ / round63 第三次复现）：裸扫 `N 条红线` 会当场**误杀 04 的校订说明行**
（「原写「11 条红线…」，漏计 1 条红线」＝ 否定式/历史式引用，不是当前口径）⇒ 改为
「口径编码进文本（语义标记单源）+ 散落引用行仅在**行内含历史/校订标记词**时放行 + 前提守卫证明排除生效」。

### 3.1 变异回灌（双向，7 组全如期）

| 组 | 变异 | 预期 | 实测 | 还原 |
|---|---|---|---|---|
| M1 | 06 提审行改回 11（第 8 例回归） | 红 | **红**（P5：`06:313 写 11`） | 绿 |
| M2 | 05 换措辞 `红线复核 **12** 条：通过 ____ / **12**` | 绿（不错杀） | **绿** | 绿 |
| M3 | 删 04 单源标记行 | 红 | **红**（P2-① 零命中 / P2-② 0 行 / P3-② N=-1） | 绿 |
| M4 | §C 表删一行（12→11） | 红 | **红**（P3-② 声明 12 / 实算 11） | 绿 |
| M5 | 声明数字改 13 | 红 | **红**（P3-② + P5 三处写 12） | 绿 |
| M6b | 抹去校订行的历史上下文（原写/漏计/校订 三词） | 红 | **红**（P5：`04:269 写 11`、`写 1`） | 绿 |
| M7 | 扫描面路径写错 | 红 | **红**（P1-② 0 个 / P6 0 行 / P7 0 处） | 绿 |

> 首次 M6 报 GREEN（**不符预期**）：只替换了「原写」，**「漏计」仍在** ⇒ 仍被 HIST 放行。
> 按 §0.3 ⑦（每写一条排除，就要守它的前提）补 M6b 三词全抹 ⇒ 转红。**这不是守卫失效，是变异没做到位**。
> 变异前置已按 §0.3 ⑩ 先 `git add` 全部改动并确认 `git diff --stat` 为空，故还原未抹掉本轮工作。

## 4. 门禁

| 项 | 改 specs 前 | 改 specs 后 |
|---|---|---|
| `node verify_all.js` | **72/72 RC=0** | **73/73 RC=0**（`[redline-inventory] ✅ PASS (✅ 11 条 / 段 2)`） |
| `node specs/dev-specs/prototype/check_error_codes.js`（A–L） | RC=0 | **RC=0**（K 组投喂链派生一致仍绿 ⇒ 本次改的 04/05/06/手册不在投喂派生面内，无需重生成） |

## 5. 回执（`- [时间] R64 已落/未落/存疑 · 证据`）

- [2026-09-20 18:05] R64 **已落** · 探针两代理状态 · `probe_agents.py` → InsCode idle（msg 1465 十一连持平、inflight 0、无待批）/ dsh 窗口在但会话上下文不匹配 · 证据 `probe_agents.json`
- [2026-09-20 18:06] R64 **已落** · 并发方四连判定 · 进程采样 ×2 HITS 0 + 门禁后 mtime 静止 + HEAD `c57d14a` ≡ 上轮 + `M`=0 ⇒ 无并发方
- [2026-09-20 18:08] R64 **已落** · 防回潮扫描 · `git ls-files` 临时/调试件剔除证据目录后 **0** 命中；33 个判据脚本 **0** 未挂 SUITES
- [2026-09-20 18:10] R64 **已落** · 第 8 例实扫 · §C 表 12 行 + `git show HEAD:` 回扫抓到 05:149 / 06:313 / 手册:85 三处仍写 11 · 证据 `redline_prescan.txt`
- [2026-09-20 18:12] R64 **已落** · 修 3 处陈述 11→12 + 立单源标记 · 证据 `fix_diff.txt`（`git diff --cached`，仅数字变更）
- [2026-09-20 18:15] R64 **已落** · 新建守卫 `tools/check_redline_inventory.js`（11 断言）· 首跑 11/11 RC=0 · 证据 `redline_guard.txt`
- [2026-09-20 18:16] R64 **已落** · 挂 SUITES 第 73 项 + 套件数三处同步（头注 / 重启键 §1.1 / 重启键会漂行）· `[suite-count] ≡ 73`
- [2026-09-20 18:18] R64 **已落** · 双向变异 7 组全如期（含 M2 不错杀、M6b 排除面前提）· 证据 `mutation_matrix.txt`
- [2026-09-20 18:21] R64 **已落** · 门禁改后 **73/73 RC=0** + A–L **RC=0** · 证据 `gate_after_73.txt` / `alcodes_after.txt`
- [2026-09-20 18:22] R64 **已落** · 证据落盘并 `ls` 回读 · `review/evidence/selfdrive_20260920_r64/` 共 8 个文件
- [2026-09-20 18:23] R64 **未落（有理由）** · **不投喂 InsCode / 不投递 dsh** —— InsCode 无待批且投喂已收官（无批次 8）；dsh 会话是「餐饮闭店决策指标模型咨询」，与仓库复审**非同一上下文**（投了是上下文污染）。dsh 已连续 13 轮未投递，根因侧证（`/api/*` 404 + `web.log` mtime≈uptime）指向**方案 D：允许重启 dsh web**，仍待李老师拍板
- [2026-09-20 18:23] R64 **未落（有理由）** · 并发方遗留 **1033 个**过程 PNG（`review/evidence/r86_timeout_20260919/`）**不代提交、不代删**，归属待李老师裁决（round58 起已连续 6 轮挂起）

## 6. 队列状态（§6）

①A6b 代码层兜底 ✅ / ②R91 UI 缺陷 ✅ / ③AD 适配 G1–G8 复验 ✅（`ad-gates` 在 73/73 内通过）/ ④R86 超时值 ✅（人工面，按 round50 三方一致结论不再回读；**提醒：建 prod 须重走定值与回读**）/ ⑤14 页真数据走查 ✅
⇒ **队列仍空**；本轮正事 = 防回潮扫描 ⇒ 抓出第 8 例并做成守卫。下一轮继续按 §0.10 防回潮扫描。

## 7. 待办（需李老师）

1. dsh 四选一（放开写+node / 改只读判据 / 退场 / **允许重启 dsh web**）—— 已 13 轮悬空。
2. 并发方 1033 个过程 PNG 归属裁决。
3. 隐私政策**按 6 处**填空（旧清单会漏 2 项，直接卡提审）。
4. 溢出观察：AD-26 条里 20 条只有人读清单无机械判据（含高危 AD-23/AD-24）⇒ 是否补判据 / 接受"真机项"。
