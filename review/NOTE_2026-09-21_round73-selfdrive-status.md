# round73 自驱动巡检 · 状态与复核（2026-09-21 10:18–10:5x）

> 本件只记本轮**实跑实读**到的结论；凡计数/口径类，一律回源实扫或真跑，不采信任何一方自述（含我方上一轮）。

## 0. 探针（唯一入口 `probe_agents.py`）

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | `inflight=0`；`sessions.message_count=1465`（与 round70 持平）；`idle_sec≈241946`（约 67h）；模型 `deepseek-v4-flash`；`last_approval` 末行 `approved_once` | **idle** |
| dsh | 窗口 hwnd 855322，`rect=[-32000,…]` | **minimized** |

- InsCode idle 且 **8 批投喂已收官**（无「批次 8」可投）⇒ **不投喂**。
- dsh 最小化；按 round50/52 的根因结论（长跑进程会话加载超时，`web.log` mtime ≈ uptime）与「会话上下文与仓库复审不匹配」两条独立理由 ⇒ **本轮不投递**（无人值守不擅自动 `taskkill` 重启李老师窗口）。
- 两个窗口均无 error/额度命中 ⇒ 无需换模型。

## 1. 并发写入方：第三个（非 InsCode，round70 同型）

- `git log` 出现**我方未复核过的新 commit** `523d9b4`（09:55:35）⇒ 先只读旁观，判停手四连通过后恢复写入：
  ① 进程采样两次（10:21 / 10:47）**HITS 0** ② 门禁跑完后回读 mtime（除我方变异文件外无变化）③ `git log` 无新 commit ④ 跨分钟采样无写入。
- 归属判据：探针 idle 且 `message_count` 未涨 ⇒ 排除 InsCode；其余 `??` 文件（`_mut_r71/_mut_r72/_bak_r70/_restore_r72.js/_tmp_commit_r72.txt`）非我方产物。

## 2. 独立复核 `523d9b4`（round71/72 堂食渠道全集常在 + 装配单一入口）

**改了什么**：新增 `utils/dineChannels.js::normalizeDineRows`（预设全集常在 + 金额按名回填 + 自定义留末尾 + 幂等）；
`pages/month/input.js` 的 `decorateDineRows` 转调该唯一装配口，5 条产出路径收口；`addRow` 带 `custom:true`；
`buildItems` 只提交填了金额的行；`input.wxml` 预设行不给删除键；新增 `dineDetailRows` 快照解决分项↔快速 round-trip 丢值。

**复核判据（实跑）**：`node tools/selftest_batch8b.js` → **104 通过 / 0 失败 RC=0**；`verify_all.js` **79/79 RC=0**；`check_error_codes.js`（A–L）**RC=0**。

**双向变异 11 + 3 组**（证据 `review/evidence/selfdrive_20260921_r73/mutation_523d9b4_r73*.txt`）：

| 组 | 变异 | 期望 | 实得 |
|---|---|---|---|
| M1 | 金额改「首个非空即取」（不再按名回填） | 红 | 红 ✅ |
| M2 | 取消预设全集常在（`presets=[]`） | 红 | 红 ✅ |
| M3 | 保存不再过滤空行 | 红 | 红 ✅ |
| M4 | `addRow` 丢掉 `custom:true` | 红 | 红 ✅ |
| M11 | `decorateDineRows` 不再转调唯一装配口 | 红 | 红 ✅ |
| M5b | 预设行恢复给删除键（**精确锚点**） | 红 | 红 ✅ |
| M9b | 页面写死**完整**渠道名 | 红 | 红 ✅ |
| M7 | 删除键换等价写法 `r.fixed === false` | 绿 | 绿 ✅ |
| M8 | `filter` 换等价写法（保留金额判据） | 绿 | 绿 ✅ |
| M9c | 注释里出现渠道全名 | 绿 | 绿 ✅ |
| M10 | 装配口内部等价改写（重命名 + 换注释） | 绿 | 绿 ✅ |

> 两条首轮 MISMATCH 是**我方变异打偏**，不是守卫失守：M5 首轮把 `wx:if="{{!r.fixed}}"` 打到了**同一文件第 73 行** 的
> 名称输入框（先于 78 行的删除键）⇒ 判据本就不该红；M9 首轮只写了「挂账」两字，而清单全名是「个人/单位挂账消费」
> ⇒ 判据匹配的是全名，也不该红。**定式：报 MISMATCH 先确认锚点命中位置与「口径全名」，别先改判据。**

🟡 **判据强度缺口（复核他人守卫必做，未代修、归并发方 round74）**：
`G10g` 只断言「`decorateDineRows` 内部转调装配口 + 页面 require 了模块」，守不住**新增另一条绕过装配口的产出路径** ——
变异 M6（在 `input.js` 里新增一个自己 `map` 拼装堂食行的方法）⇒ **判绿**。与 round55 `c24e0a2`、round59 设置页开关同族：
**「变异转红」证明不了「守得住意图」**。守的是「这个函数转调了」，不是「所有产出路径都经它」。

## 3. 队列五项：逐条实跑复核（非采信自述）

| 项 | 判据 | 结论 |
|---|---|---|
| ① A6b 代码层兜底 | 无新改动；随门禁通过 | 闭环 |
| ② R91 UI 缺陷 | `node tools/selftest_ui_fix.js` → **31 通过 / 0 失败** | 闭环 |
| ③ AD 适配 G1–G8 | `node tools/selftest_ad_gates.js` → **24 通过 / 0 失败** | 闭环 |
| ④ R86 超时定值 | 按 round50 三方一致（42/42）结论**不再回读** | **存疑（本轮未实测）**，提醒：建 prod 须重走定值与回读 |
| ⑤ 14 页真数据走查 | 无新改动；随门禁通过 | 闭环 |

## 4. 本轮正事：同族病第 15 例 —— 套件断言数（人工面）≡ 实跑，零守卫

**查法**：沿用 round67 教训（优先扫「上线前人工操作清单 / 带表格的核对单」），对 `core/` 与 `上线材料_*` 逐个
`grep -rl <文件名> tools/ prototype/` 数**被守卫引用次数** ⇒ `11_微信审核自查清单` 为 0。核查其「硬红线 6 项 vs 勾选 11 条」
经实读是**两张不同清单**（非同一口径），**不构成漂移**（未据以造例）。转而逐个**实跑**各套件取断言数 ⇒ 抓到真漂移：

| 套件 | 文档所记 | 实跑 | 漂移 |
|---|---|---|---|
| `tools/selftest_ui_fix.js` | 27 | **31** | ✅ 有 |
| `tools/selftest_batch8b.js` | 48 | **104** | ✅ 有 |
| `tools/selftest_batch8c.js` | 56 | **57** | ✅ 有 |
| `selftest_ad_gates` / `check_ios_pay` / `check_env_ready` | 24 / 6 / 11 | 24 / 6 / 11 | 无 |

**为什么必须守**（与 round69 第 13 例同型）：断言数是**下界** —— 文档写 27 而实跑 31 时，删掉 4 条断言文档仍说符合预期
⇒ 4 条断言静默丢失而门禁照旧判绿。R105 只守住了 `verify_seed_data` 一个脚本，其余套件仍是人工面。

**已落**：
1. 修当前态陈述 4 处（重启键 §1.1 的 27→31 / 48→104 / 56→57；`core/13:145` 的 27→31）；
   演进链里的旧值（重启键 :652「新增 …48 条」）**按只增不改保留**，靠 HIST 放行 + 前提守卫证明排除生效。
2. 在重启键立单源声明行（一行列出 6 个套件的 `key`=N，均 ≡ 实跑）。
3. 新增 `tools/check_suite_assert_counts.js`（**R107**，14 条断言 / 7 段），SUITES **79 → 80**，三处套件数同步（R97 自动校验）。
4. 判据四条腿：① **实跑**各套件解析 stdout（不抄字面量，rc≠0 即红）② 唯一声明处（CJK 文件名按前缀实找，避坑⑪）
   ③ 声明 ≡ 实跑逐 key ④ 两道**前提守卫**（受守集合 ≥6；历史面确有旧值命中 ⇒ 排除面打错即红）+ A7 单源不扩散 + W 弱面只明示。
5. 弱面收窄：「新增 N 条」是**增量口径**不是全集 ⇒ 跳过（坑⑭ 同族，裸扫必误杀）。

**双向变异 10 组 0 异常**（证据 `mutation_r107_r73.txt`）：M1 声明写回 27 / M2 写成 48 / M3 删 key / M4 实跑路径写错 /
M5 受守集合改小 / M6 历史面路径写错 / M7 声明扩散到第二份 md / M8 删声明行（fail-closed）⇒ **全转红**；
M9 声明换措辞（加空格）/ M10 历史措辞旧值陈述 ⇒ **仍绿（不错杀）**。

## 5. 回执

- [2026-09-21 10:2x] R73-① 探针与并发方判定 · 已落 · 证据：`probe_agents.py` → InsCode idle / dsh minimized；进程采样 10:21、10:47 两次 HITS 0 · commit 本件
- [2026-09-21 10:3x] R73-② 独立复核 `523d9b4` · 已落 · 证据：batch8b 104/104 RC=0 + 双向变异 14 组（11+3）· 见 §2
- [2026-09-21 10:4x] R73-③ 同族病第 15 例做成守卫 R107 · 已落 · 证据：`node verify_all.js` → **80/80 RC=0**；A–L RC=0 · commit 本件
- [2026-09-21 10:4x] R73-④ 队列①~⑤ 复核 · 已落（④ 存疑：R86 本轮未实测，按 round50 三方一致结论不回读）· 证据：ui_fix 31/31、ad_gates 24/24
- [2026-09-21 10:5x] R73-⑤ 投喂/投递 · **未落** · 理由：InsCode 8 批已收官无下一批；dsh 最小化 + 后端根因未除 + 会话上下文与仓库复审不匹配（投递＝上下文污染）· 需李老师裁决（方案 D 重启 dsh）
- [2026-09-21 10:5x] R73-⑥ 并发方悬空产出（`_mut_r71/_mut_r72/_bak_r70/_restore_r72.js/_tmp_commit_r72.txt`、1033 张过程 PNG、`review/evidence/selfdrive_20260921_r71/`）· **未落** · 理由：归属并发方，不代提交不代删
- [2026-09-21 10:5x] R73-⑦ G10g 强度缺口（M6 判绿）· **未落** · 理由：他人判据本体不代修，挂并发方 round74 待办

## 6. 待办（下轮优先）

1. **`G10g` 强度缺口**：新增绕过唯一装配口的产出路径不被抓（M6 判绿）⇒ 归并发方 round74。
2. **dsh 方案四选一**（round50 起悬而未决，推荐 D 重启）：未定则每轮复审缺口继续累积。
3. 建 prod 时**重走 R86 定值与回读**（`config.json::timeout` 不被平台采纳）。
4. 隐私政策按 **6 处**占位符填写（李老师面）。
5. 坑⑱ 的 4 份前轮守卫仍只扫 index（`check_collection_perms` / `check_quota_limits` / `check_suite_count_claims` / `check_suite_coverage`）：待裁决是否我方代修。
6. `init_db.js` 系统字典仍旧 5 项 vs 前端 7 项（并发方自述未落，未代修）。
