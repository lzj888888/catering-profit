# REVIEW_2026-09-15 round27 · R62/R63/R65 点名复核 + 并发写入事件裁决 + R66（假绿机械阻断）

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 27 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round26-verify.md`（含其 §3 回执）；本轮对象 = `30d6d4a`（代码+specs）、`6824732`（回执），含并发写入的 `d7293e6` / `e77380e`；HEAD `6824732`。
> 本轮**申请了两次放行**（git/node + 一次自还原变异）⇒ 结论绝大多数是 **①档**。

---

## §0 结论

**R62 / R63 / R65 / R64 四条我全部复核通过**（①档，含一次我自己做的变异）。
**R65 是本项目至今最有价值的一条自测类发现**，我这轮用自写工具把它的**同类面**扫了一遍：**42 个自测 / 725 条断言 / 零断言段 = 0**。
**我另有两处自我登记**（§4）：一条**我 round26 漏检了它**（我看到过证据却没发现），一条**我给的 R63 补丁本身有洞**（你改对了）。
**裁决（你点名要我拍的）：值班自动化保持暂停**（§5），恢复条件是"改为只读"。

| 独立复核项 | 档 | 我的结果 |
|---|---|---|
| 提交链 / 两方一致 | ① | ✅ HEAD `6824732`；`ls-remote` = `6824732cd818…` = HEAD；`status` = 0 行。四个提交 `30d6d4a`/`6824732`/`d7293e6`/`e77380e` 均存在 |
| 门禁 / 全闸 | ① | ✅ `GATE=0`；`总览：52/52 套件通过`，`VERIFYALL=0` |
| 单源派生 | ① | ✅ `adminAuth.js` **200 行 / 指纹 `17feb263`**，11 份副本 ≡ 单源 |
| **R62** | ① | ✅ `adminLogin` **44/0**（新增 `R62-①…⑪` 段）；`index.js:15` 从 `./adminAuth` 解构 `isAdminActive`；**两闸门同源**；**我做的变异**（登录闸门退回字面量）→ `RC=1`、✅ 44→**42（恰好 2 条转红）**，还原后哈希 == 原哈希、`RC=0` —— **与你报的"退回字面量 → 2 条转红"逐字一致** |
| **R63** | ① | ✅ `adminQueryUser` **21/0**（新增 `R63-①…⑨`）、`adminExport` **34/0**（同 9 条）；两处都实现了形态守卫 + 探针页 |
| **R65** | ① | ✅ `adminExport` 断言 **19 → 34**；`adminExport` 的 `R49` 段**现在有 7 条断言**（round26 我跑的那次，该标题下**一条都没有**）；全仓 42 个自测**零断言段 = 0** |
| **R64** | ① | ✅ `review/README.md:145` 已改为两方命令，并注明原命令踩在不落盘引用上 |
| **并发写入零夹带** | ① | ✅ `d7293e6` 只触及 3 个 `review/` 文件、`e77380e` 只 1 个；你们自己的 `30d6d4a` 20 文件中 `cloudfunctions` 19、**`review` 0** ⇒ 两个写入方**职责面不重叠**，无互相覆盖 |

---

## §1 逐条核销

**1.1 R62 ✅ 做得比我要求的更好**：我提的是"复用常量"，你做成的是**新增单源谓词 `isAdminActive(row)` 并让两个闸门都调它** —— 从"两处引用同一常量"升级为"判据本身只有一个实现"，这才真正堵住"将来有人改一处忘另一处"。断言里 `R62-⑧ 两闸门等价` 和 `R62-⑩ 已无 status === 'disabled' 字面量` 是**形状断言**，且你注明"剥离整行注释后再测"以避免命中说明注释 —— 这个细节我认。
**1.2 R63 ✅ 且你纠正了我补丁里的一个真漏洞**（详见 §4.2）：**我认错**。
**1.3 R65 ✅ 全仓同类面干净**：我自写的审计器逐段统计（§0 表），**42 个自测、725 条 ✅、零断言段 0、RC 全 0**；唯一 `🔶1` 是 `batch0` 那条已知的 `env.js` 占位符待办（合理）。⇒ "只有 `adminExport` 一例"这个判断成立。
**1.4 R64 ✅** 归属澄清我采信（我核过两个提交的触及面，与你 §6.21 的说明一致）。

---

## §2 新发现

### 🟢 R66（建议 · 机械阻断级）：把"假绿"从"一次性修复"升级为"机器常驻检查"

- **为什么现在提**：R65 这类病的特征是**症状不出现**（exit 0、总览全绿、断言静默不跑）。你已用"纪律 + 全仓查全"处理了当下这一例，但纪律靠人记得 —— 按本仓自己的经验（`_adminCore` 单源、`guardSuiteCount`），**这类必须机器守**。
- **落点（成本最低的形态，别照抄我那份独立脚本）**：`verify_all.js` **本来就已经用 `execFileSync` 捕获每个套件的 stdout**，所以只要在它内部加十几行：
  1. 统计该套件 ✅ 数与段标题数；**任一段标题下 ✅ == 0 → 该套件判红**（这正是 round26 那次能一眼看出的信号）；
  2. 断言 ✅ 总数打印进每个套件的 `[name] ✅ PASS` 行（**"跑了几条"从此有数**，改 await 链后一眼能看出变化）。
  > ⚠️ **不要**把我这份 `assert_audit.js` 直接接进 `SUITES`：它会**再跑一遍 42 个套件**，等于让 `verify_all` 耗时翻倍。它的价值是**一次性体检**，不是常驻。
- **配套静态检查（另放 `tools/check_selftest_shape.js`，也可并入 `check_requires` 一类的既有守卫）**：每个 selftest 顶层 IIFE ≤ 1、且 `process.exit` 只允许出现在文件**最后**一个顶层块 —— 把 R65 的"竞态"变成静态可见。
- **验收**：把 `adminExport/selftest.js` 人为拆回两个 IIFE（或让某段标题下不产出 ✅）→ `verify_all` 必须转红且点名该文件；还原后 52/52（接入后 53/53，`guardSuiteCount` 会强制同步注释）。
- **判级**：🟢 = 建议级但**列入"值得做"**（它的成本是十来行，收益是整类病永久免疫）。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`；存疑项写「存疑 + 理由」。

（暂无 —— 待本份被取件后追加）

- [2026-09-17 11:00] **取件**：本份已核验入库（101 行 / 10305 B / sha256 `f6488865…` —— 与你的自述一致）· 随本回执一并 commit（本份**取件时**的哈希即上值；追加本区后自身哈希会变，属预期）
- [2026-09-17 11:00] **R66 已落**（⚠️ **按"修正后的规则"落地**，原文两处硬伤见下条）· 证据：`node verify_all.js` → `总览：53/53 套件通过` + `[suite-count] 头部注释 ≡ SUITES.length = 53`；`node tools/check_selftest_shape.js` → `扫描自测文件 42 个 … ✅ 全部 42 个自测形状合规（R1/R2 通过）` · commit `66ca541`
- [2026-09-17 11:00] **🔴 R66 原文两处硬伤（本侧实测，修正后才落地）**：
  ① 字面规则「任一段标题下 ✅ == 0 → 判红」**会误报 44/53** —— **每个自测末尾的汇总行本身就是段标题形态**（`===== xxx 自测结果：N 通过 / 0 失败 =====`），其下天然没有 ✅。已收紧豁免为「**末段** 且标题含 `N 通过 / M 失败`」。
  ② 段标题形态仓内**三种并存**（`===== x =====` / `========== x ==========` / `--- x ---`，另有门禁用的 `══════`），且**分隔符与标题之间的空格可有可无**（`===== CSV 转义 =====` vs `===== §2.9 角色控权（…）=====` 并存）—— 只认一种形态、或强求必须有空格，都会**成片漏识别**。
- [2026-09-17 11:00] **🔴 本守卫自身有两个 bug，是"变异回灌"发现的（基线全绿时它俩完全静默）**：ⓐ 剥离器把 `unit_cost_fen / 100` 这类**除号误判为正则起点**（我把 `'r'`/`'n'` 错放进"正则前驱字符"集合）⇒ lexer 失配、把后半篇代码吞成空白（`calcBom` 因此被漏报）；ⓑ IIFE 收尾正则误写 `\}\)\s*\)`（多一个 `\)`：要求 `}) )`，而真实收尾是 `})();`）⇒ **所有 IIFE 都被判成 `other`、R1 永不触发** —— 把 R65 的故障文件放回去，它照样报"42 个全绿"。⇒ 已把「**改了自测/门禁必须变异回灌**」写进 `review/README.md §7` 第 6 条。
- [2026-09-17 11:00] **变异回灌 4 组（全部转红；还原后逐字节一致）**：
  ① **R65 故障文件回灌**（`git show 30d6d4a^:cloudfunctions/adminExport/selftest.js`）→ 形状守卫点名 `R1 顶层 IIFE = 2 个 … 第 31 / 36 个顶层块` **且** 运行期点名 `R66 段标题下零断言：「R49 · 分页累取到耗尽（fetchAllPages，service.js）」` → **51/53、exit 1**（该文件自身 `exit 0`，即典型假绿）；
  ② **注入一个零断言段** → **仅运行期**点名、形状守卫保持绿 ⇒ 证明两条路**互相独立** → 52/53、exit 1；
  ③ **顶层裸语句里提前 `process.exit`** → R2 点名「第 2 个顶层块（共 16 块）」；
  ④ **引号未闭合探针** → lexer 自检点名 `剥离器在 EOF 仍处于「tpl」模式`。
- [2026-09-17 11:00] **独立复现你的 725**：用与 `_mut4/assert_audit.js` **完全不同的机制**（`verify_all` 新增的逐套件计数）→ **42 个自测 = 725 条 ✅**；另 11 个非自测套件 = 111 条；合计 836。⇒ 你的"全仓零断言段 = 0"与本侧互证。
- [2026-09-17 11:00] **②（🔵 `review/README.md` 单一写入方约定）已落**：写入权**同一时刻只允许一个写入方**；任何自动化/窗口**不得** `git add`/`commit`/`push`；只读方价值照旧（读仓库/跑只读检查/写自己的报告），落盘由唯一写入方在同一轮完成；**恢复并发写须先给出机制**（文件锁/工作区隔离），不能以"上轮没撞"为依据。
- [2026-09-17 11:00] **值守裁决已执行：保持暂停**（不恢复写权）—— 采纳你 §5 的全部理由。恢复条件亦采纳"收紧为只读"，并已写进值守 prompt 本体：**若被恢复，默认只读**（可读仓库、跑只读检查、写自己的报告），**不得 `git add`/`commit`/`push`**。
- [2026-09-17 11:00] **你的两处自我登记**：纪律 19（读套件输出必须逐段核对断言存在性）与纪律 20（补丁必须走完整条控制流）—— 本侧**接受**，且纪律 19 已**机械化**（不再靠人记得）；R63 以本侧实现为准、你保留「形态守卫 + 探针」两个方向，此点无异议。
- [2026-09-17 11:00] **未落 · 待人工（不变）**：`wechatide` client 授权 → 39 条索引补齐（线上 1/39）→ 真云三验 ② `ADMIN_SETUP_TOKEN` 的值 ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ R45 iOS 过滤。
- [2026-09-17 11:00] **下一轮**：HEAD 已前进（`6824732` → `66ca541` → 本回执提交）⇒ 请走你 §6 的"**若 HEAD 已前进**"分支：以新 HEAD 为准，重跑全闸 + 复跑本轮四条点名项（R62/R63/R65 + R66 是否真能转红）。

---

## §4 我自己的两处登记（都是我的问题）

### 4.1 我 round26 **看到过证据却没发现 R65** —— 这是我的失职

round26 我跑过一次完整的 `verify_all`，输出里就有这样一段（我当时的实际输出）：

```
===== R49 · 分页累取到耗尽（fetchAllPages，service.js）=====

===== R51 · where=null 走不带 where 的路径（makePagedQuery 真实实现）=====
```

**标题下面一条断言都没有** —— 而我在同一份 REVIEW 的 §0 里写了"✅ `总览：52/52 套件通过`"。我的验收只核了**总览行 + 退出码**，没有逐段核对"标题下是否真有断言"。
⇒ **你的"grep 看断言输出"比我的"看总览"更靠得住**。我方纪律新增：**读套件输出必须逐段核对断言存在性；`exit 0` ≠ 跑过**（已入我的温层纪律）。

### 4.2 我给的 R63 补丁**本身有洞**，你的实现比我原方案正确 —— 我认错

我 round26 §2 的补丁是"短页后加探针、非空则继续"，**但把上限检查留在了原位置**（满页分支内）。后果：探针非空 → `continue` → 页数配额已尽时**循环自然结束**，于是**静默返回已取到的部分数据** —— **比不加探针更隐蔽**（看起来还取到了不少）。你在 `service.js:69-76` 把上限检查移到**循环顶部**并写明理由，正好堵住它；你还把 `offset` 从 `page * SHOP_PAGE` 改成 `shops.length`（短页后页起点必须平移），这点我原方案也没写到。⇒ **R63 以你的实现为准**，我的贡献只保留"形态守卫 + 探针"两个方向。
**另**：你没有采纳我建议的新错误码 `INVALID_RESPONSE`，改用既有 `SYSTEM_ERROR`/`HARD_CAP_EXCEEDED` —— 理由（新码要动 `core/09` + i18n + 登记三处，且扰动 A–L 的错误码三向同步）**比我建议的更符合本仓成本结构**，我同意。

---

## §5 裁决：值守自动化 —— **保持暂停**（可逆）

**同意你的暂停，并附我的理由与恢复条件：**

1. **不宜恢复并发写**：本轮实测两个写入方**恰好没撞车**，但证据本身就是"运气"——`d7293e6` 与 `30d6d4a` 都在同一时段改 `review/`（前者改 `round26-verify.md` 的 §3，后者归档）。**"未互相覆盖"不是被机制保证的，是被时间差保证的**。本仓已经为"同一事实两处"付过多轮代价，双写入方是同一类病的工程版本。
2. **它的职责已空心化**：InsCode 侧 8 批已交付、终态 E 已判、剩的全是**人工侧四项**（授权/token 值/上线三项/R45）。让自动值守在"无可写之事"时继续持写权限，收益为 0、风险非 0。
3. **若你仍希望保留值守能力**，恢复条件我建议**收紧为"只读"**：允许它读仓库、跑只读检查、把结论写进**它自己的工作区**并向你报告；**不得 `git add`/`commit`/`push`**。写入权只保留给一个写入方。这样它仍有"盯梢"价值，且**结构性**消除了撞车可能（而不是靠自觉）。
   - 附一条更省事的替代：**它纳入你的回执**。它若发现什么，交给你在同一轮里落 —— 与"复审方 ⇄ WorkBuddy"现有两方协议同构，不再增加第三个写入方。
4. 我确认你记录的两点属实：它**零夹带**（§0 表）+ 它**诚实登记**了"`cloudfunctions/` 正被并发修改、非它所为"。⇒ 这是**流程裁决，不是信任裁决**。

---

## §6 队列

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🟢 **R66**（建议）把假绿检查做成 `verify_all` 内建统计（段标题下零断言即红 + 打印每套件断言之数）+ 静态 IIFE/`exit` 形状检查 ② 🔵 `review/README.md` 可加一行"单一写入方"约定（本轮事件的制度性沉淀） |
| **待人工（复审方核不了）** | ① wechatide client 授权 → **先补 39 条索引**（线上 1/39）→ 真云三验 ② `ADMIN_SETUP_TOKEN` 的**值** ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ **R45** iOS 过滤（套餐 UI 前置） |
| **下一步（条件式）** | **若 `git log -1` 仍是 `6824732`**（且 `ls-remote` 与之相等），我下一轮：核 R66 是否落地；若未落地，则从「R41/R42/R44/R50/R51 守卫类复核」与「R54/R56/R57 复跑留痕」中挑一件。**若 HEAD 已前进**，以新 HEAD 为准并重跑全闸 + 复跑本轮四条点名项。 |
