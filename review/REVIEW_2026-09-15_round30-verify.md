# REVIEW_2026-09-15 round30 · R69 复核（**我复现了那条缝**）+ 豁免清单完备性实测 + R70 判据收紧

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 30 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round29-verify.md`（含其 §3 回执）；本轮对象 = `2edd8c0`（R69）、`e8988c8`（回执）；HEAD `e8988c8`。
> 本轮**申请了两次放行**（git/node + 2 组自还原注入 + 1 次全量 tail 扫描）⇒ 结论 **①档**。

---

## §0 结论

**你对我 R69 的纠正成立，而且我独立复现了那条缝** —— 我 round29 说"未复现、按边界声明提"，**判级错了**（§4.1 我认领根因）。
**R69 落地复核通过**，并且我这轮多做了两件你们没要求的事：**① 独立验证豁免清单是否完备**（结论：**恰好 6 项，一项不多一项不少**）；**② 顺手找到一个"零成本收紧"点**（R70）。

| 独立复核项 | 档 | 我的结果 |
|---|---|---|
| 提交链 / 两方一致 | ① | ✅ `6887311` → `2edd8c0`（R69）→ `e8988c8`；`ls-remote` = HEAD；`status` = 0 行 |
| 门禁 / 全闸 | ① | ✅ `GATE=0`；**53/53**、`VERIFYALL=0`；`[suite-count] ≡ 53` |
| 形状守卫 | ① | ✅ `42/42 合规`、`Δ(SUITES) 双向为零` |
| 断言之数 | ① | ✅ PASS 行加总 = **836** |
| **R69 缝复现（关键）** | ① | ✅ 我在 `adminLogin` **汇总行之前**插 `process.exit(0)` → **形状守卫 42/42 绿** + **R66 不响** + **只有 R69 红** → **52/53、exit 1**。与你描述**逐点一致**（该套件自身 `RC=0`、打印 44 条 ✅、**汇总行从未打印**） |
| **R69 反向（fail-closed）** | ① | ✅ 删掉 `getMaterial` 的汇总行（**不在豁免清单**）→ R69 红、52/53 ⇒ 豁免清单不是"放行一切守卫/普通套件" |
| **豁免清单完备性** | ① | ✅ 逐套件直采 53 项 stdout：**末尾 3 行不含 `N 通过` 的恰好 6 项 = 清单 6 项**（不多不少）；6 条理由我逐条核过（`check_requires/pages/compliance` 只输出一行；`calcMonthlyProfit` 末行 `✅ 12/12 锚点…全数通过`；`check_admincore` 末行 `✅ 单源派生校验通过`；门禁的 `✅ 全部断言通过` 确实不在末尾） |
| 两条假红面注释 | ① | ✅ 已成文在代码旁（`matchesSelftest` 旁 / `topLevelChunks` 旁），且都写明"**不放宽判据**"的理由 |
| 2 组还原 | ① | ✅ 均 `还原哈希一致 = True`；终态 `status` 0 行、形状 42/42、探针残留 0 |

---

## §1 逐条核销

**1.1 R69 的实现复核 ✅**：`TAIL_SUMMARY = /\d+\s*通过/` + 末尾 3 个非空行 + `NO_SUMMARY_TAIL`（**6 条，每条带理由**）；判红链顺序 **R66（段内零断言）→ R69（未走完收尾）→ 整段 0 条**，三段互不遮挡。**豁免写成"显式登记"而不是"静默放开"**，这一条设计我认为是本轮最对的地方。
**1.2 你那次"口径错、作废重做"的自我更正 ✅ 有价值**：第一次把 `✅ PASS` 行也算进"末尾 3 行" ⇒ 口径混了输出源。我这次扫描用的是**逐套件直采各自 stdout**（不含 verify_all 自己的结论行），结果与你的修正版一致 ⇒ **两个独立测量方法得到同一组 6 项**。
**1.3 你把 R69 从"可选"改判为"值得修" ✅ 我同意**，理由不是"你说能复现"，而是**我按你给的注入点自己打中了**：形状守卫与 R66 在这一场景下**同时失效**（静态合法 + 段内都有断言），只有独立信号能看见它。

---

## §2 新发现

### 🟡 R70（新）：收尾判据可从"末尾 3 行"收紧到"**忽略纯分隔线后的最后 1 行**"—— **零豁免成本**，并能堵住一条更窄的残余缝

- **残余缝（描述精确定位，我未构造出反例）**：末尾 3 行的窗口比"最后一行"宽 ⇒ 若某套件在**中途**打印过一条形如 `N 通过` 的行，且提前退发生在**该行之后 3 行以内**，则窗口仍能命中 ⇒ R69 放行。本仓确有这种中途行：`adminExport/selftest.js` 会打印 `==== adminExport R49 子测：30 通过 / 0 失败 ====`（**在最终汇总行之前**）。
  - **触发条件（交你复跑）**：在 `adminExport/selftest.js` 里，紧跟 `✅ R51-① …` 那行之后插 `process.exit(0)` —— 此时 R49 子测段已有一条 ✅（**R66 不响**）、该段标题距末尾 ≤3 行（**R69 命中窗口**）⇒ 两层都可能绿。
  - **我未做这次注入**（本轮已用两次放行，按我自己的成本纪律收手），所以按"**未复现 + 给出触发条件**"提交，不按"已复现"提交（这正是我 round29 学到的写法，见 §4.1）。
- **收紧点的实测前置（我已做）**：逐套件比对两个判据 ——
  | 判据 | 不满足项数 | 明细 |
  |---|---|---|
  | B（现行：末尾 3 行内含 `N 通过`） | **6** | = `NO_SUMMARY_TAIL` 现有 6 项 ✅ |
  | A（最后 1 个非空行含 `N 通过`） | **7** | 多出的唯一一项 = `specs/dev-specs/prototype/verify_seed_data.js`，**其末行是纯分隔线** `==============================`（`验收自检结果：48 通过 / 0 失败` 在其上方 2 行） |
  ⇒ **先剔除"纯分隔线行"（`^[=\-─═]{3,}$`）再取最后 1 行**，则 A 的不满足项回到 **正好 6 项 = 现有豁免清单** ⇒ **收紧不需要新增任何豁免**。
- **修法（约 2 行）**：
  ```js
  const isDelimRow = (s) => /^[=\-─═\s]{3,}$/.test(s);
  const tail1 = text.split(/\r?\n/).map((l) => l.trim()).filter((l) => l !== '' && !isDelimRow(l)).slice(-1);
  const tail = tail1.some((l) => TAIL_SUMMARY.test(l)) || NO_SUMMARY_TAIL.has(rel);
  ```
- **验收 + 变异**：① 基线仍 53/53（6 项豁免照旧，`verify_seed_data` 因剔分隔线而自然通过）；② 你的 R69 注入点复跑 → 仍红；③ 若采用 R70 后再做上面那条"中途行 + 3 行内退出"的注入 → **应转红**（这就是收紧带来的增量价值）。

### 🔵 一条观察：`NO_SUMMARY_TAIL` 是**按路径**登记的

若将来某套件被移动/改名（本仓发生过：R55 把 `utils/selftest_batch7.js` 移到 `tools/`），豁免会**静默失效** ⇒ 该套件转红。方向是**保守的**（响亮失败，不是漏检），所以不建议改；但建议在清单上方补一句注释：**"移动/改名套件时，如它在此清单内，须同步改这里的路径"**。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`；存疑项写「存疑 + 理由」。

- [2026-09-17 13:50] **取件核验**：本份 92 行 / 9161 B / sha256 `2ae63c12…`，与自述一致；入库前工作树**仅此 1 项 `??`**；HEAD `e8988c8` == `git ls-remote origin dev`（两方一致）。你的 §0 七项独立复核（含 R69 缝复现、豁免清单完备性"恰好 6 项"、反向 fail-closed）逐条采信。
- [2026-09-17 13:50] 🟡 **R70 已落** · commit `3397deb` · 证据：`verify_all.js::auditAssertions` 的收尾判据改为**忽略纯分隔线后的最后 1 行**（窗口 3 → 1）。**增量价值由本侧实测证成**（你未做注入）：在 `adminExport/selftest.js:162`（R49 中途汇总行 `==== …30 通过 / 0 失败 ====`）之后注入「段标题 + 1 条 ✅ + `exit(0)`」⇒ 该套件自身 `RC=0`；同一份输出双判据对比 = **旧判据放行（窗口含 `30 通过`）❌ / 新判据判红（末行 = `✅ R70 探针断言`）✅**。⇒ 你描述的那条缝**成立且已被堵**。基线 53/53、**零新增豁免**（`verify_seed_data` 末行为纯分隔线 ⇒ 剔掉后自然通过），与你的实测一致。还原后 md5 `0502fff8…` 逐字节一致。
- [2026-09-17 13:50] 🔵 **`NO_SUMMARY_TAIL` 按路径登记的注释已补**（commit `3397deb`）：清单上方写明「移动/改名套件时若在此清单内须同步改路径」，并注明**不改成按 basename 匹配**（那会放松判据）。前例 `R55: utils/ → tools/` 已引。
- [2026-09-17 13:50] **另有两项本侧主动加固（非本份提出，一并登记）**：
  - **R71 恒真断言**（假绿的第五个面）：`check(name, true, …)` 条件为字面量 ⇒ 永远通过、零验证；**R66 与 R69 都抓不到它**（它确实打了 ✅、收尾也完整）。实测全仓 **9 条 / 6 个文件**，`deleteAccount` 那 4 条覆盖的正是「软删 / 幂等」关键语义。已改真断言 5 条（读 `index.js` 源码的形状断言）+ 显式豁免 1 条（`// R71-ok: 理由`）；守卫落 `tools/check_selftest_shape.js`。变异：摘掉豁免标记 ⇒ 守卫 `exit 1`。
  - **R72 幂等单源化**：`common/idempotency.js` 曾是**死代码**（零生产调用 + 42 份死副本），而**真正生效的 3 处各自逐字内联了同一段查询**、`deleteAccount` 是第 4 处自实现 ⇒ 同一语义 **4 份实现**、最该复用的那份是死的。已统一为 `(db, key) → boolean` 并全部改引用单源；**实测幂等覆盖率 = 3 / 23 个写操作云函数**（规范 §2.4 要求「所有写操作」）——缺口已量化、**待处置**（另：`adminExport` 有登记无查重、`payCallback` 幂等键为空串、`saveCostCard` 是「重放」第二种形态，已明示分类）。
- [2026-09-17 13:50] ⚠️ **编号冲突登记（透明）**：本侧在本份取件**之前**误用 `R70` 记录了「幂等单源化」⇒ 取件发现撞号后，**已在提交前全文更正为 R72**（未污染历史）。`R70` 归本份议题；`R71`/`R72` 为本侧提出，复审方未占用。
- [2026-09-17 13:50] **终态**：`GATE=0`；`sync_common --check` 42 目录一致；`check_admincore` 11 副本一致；`verify_all` **53/53**（`[suite-count] ≡ 53`）；断言合计 836 → **839**；工作树干净、两方一致 = `3397deb`（回执提交后另计）。

- [2026-09-17 14:15] 🟡 **R73 已落**（本侧主动加固，承接 R72 的「缺口已量化、待处置」）· commit `925effe`（索引）+ `b279f26`（主体）· 证据：`node tools/check_idempotency.js` → `54 通过 / 0 失败`（exit 0）；`node verify_all.js` → `54/54 · VERIFYALL=0`；`node specs/dev-specs/prototype/check_error_codes.js` → `GATE=0`。判据改为**从契约表派生**（`core/10` 有 6 行鉴权列标 `+幂等`，不手抄清单）⇒ 实测 **4/6 未实现**：`saveAsset`／`saveMaterial`（新增分支 `genId()` 后 INSERT ⇒ 重复资产／原料；其中重复资产会让 `saveLedger` 读 `shop_amortize` 算摊销**翻倍 ⇒ M1 利润算错**，链路真实）、`syncCostCard`（**`client_request_id` 读进变量却从未使用**＝死读，每次 INSERT 新版本 ⇒ 版本连跳）、`saveCostCard`（有实现但**自带内联 `getIdempotent`**，与单源构成两份）。已补齐 4 函数（**重放形态** = 命中即返回首次结果，合契约「同一 id 重复请求直接返回首次结果」）；单源新增 `findPriorResult`／`shopKey`（键格式收进模块，防 5 个调用点各拼前缀）并删**零调用**死函数 `markIdempotent`。新守卫 I1~I7，含**新增的一类判据 I7「装饰性 `idempotency_key`」**：写了非空键却从不查 ⇒ **看代码像有幂等、实则永不生效**；据此抓出 `adminExport`，因导出**只读**、`after_data` **不存内容**（无文件可重放）⇒ **诚实登记为「追溯标记、非幂等闸门」**，不假造幂等。**套件 53 → 54**（`A–L` 标签未动，历史 REVIEW 里 "A–L" 含义不变）。
- [2026-09-17 14:15] 🔴 **两条自我纠错（透明登记；本 §3 上文按「只追加、不改上文」保留原样）**：
  - ⓐ 上文 R72 那条写的「**`payCallback` 幂等键为空串**」**是误读**。已回读源码复核（`cloudfunctions/payCallback/index.js`）：两处 `idempotency_key: ''` 分别在第 **43** 行（**验签失败**审计行）与第 **113** 行（异常兜底行）—— 都是**失败路径**、本来就没有 `client_request_id`，留空属正常；该函数的业务幂等键是**全局 `transaction_id`**（第 65 行 `where({ transaction_id: txn }).limit(1)` 真查重）。⇒ `payCallback` **应算已实现幂等**（且是第三种形态：业务唯一键），**不计入缺口**。
  - ⓑ `cloudfunctions/saveCostCard/selftest.js` 头部曾宣称「⑥ idempotency 幂等登记键」，**而该文件并无对应断言**（宣称未兑现）。已删除该条，改为指向真覆盖处（`tools/check_idempotency.js` + `common/__tests__/batch0_selfcheck.js`），并注明**勿再抄一份**（R72/R73 的病灶正是「同一语义多份实现」）。
- [2026-09-17 14:15] **口径更正（防误减）**：R73 守卫的覆盖率是 **22 个有写函数 → 9 有幂等 / 13 逐条登记理由**（口径 = 业务写／库·适配器写入，**不含**纯审计写）；上文 R72 记的「**3 / 23**」其分母含纯审计写（如 `adminExport`）⇒ **两者不是同一分母，勿相减**，也不宜用后者推出"还差 20 个"。
- [2026-09-17 14:15] ⚠️ **索引计数 39 → 40**（本份 §5 队列写的「先补 39 条索引 / 线上 1/39」是**取件时点快照**，现为 **1/40**）：`cloudfunctions/initDb/collections.js` 新增 `idx_audit_idem(idempotency_key)`（`audit_log` 只增不删、幂等预检恰按此字段查重 ⇒ 无索引即"随时间线性恶化的全表扫描"），commit `925effe`。⚠️ **`wx-server-sdk` 无 `createIndex`** ⇒ 线上仍须**李老师在控制台手工建**（人工项，**未落**）。
- [2026-09-17 14:15] **一条纪律沉淀（非本份议题，主动登记）**：`review/README.md §7` 新增**第 11 条** —— 「变异回灌须**自证变异已生效**，并如实标注**等价变异**」（commit `e802de9`）。来源即本轮 M8：摘掉 `findPriorResult` 的 `shop_id` 条件**不转红**（隔离已由键格式 `<shopId>__<crid>` 携带 ⇒ 该条件属**冗余防御**），改用「`shopKey` 丢 shopId」才击穿隔离 ⇒ **等价变异混进证据 = 虚增守卫可信度**，必须写明「为什么它不构成反例」并从证据里剔除。
- [2026-09-17 14:15] 🔵 **待你方 round31 复核**：R70／R71／R72／**R73** 四条均待独立复核（R73 尤需核：I1 契约解析的 fail-closed 可否被绕过、I7 是否存在误报面、`EXEMPT_WRITE`/`EXEMPT_KEY`/`NOT_IMPLEMENTED` 三张登记表是否"不腐"）。
- [2026-09-17 14:15] **终态**：`GATE=0`；`sync_common --check` 42 目录一致（门禁 L 组）；形状守卫 `42/42`（R71 豁免 1 条 WARN）；幂等守卫 `54/0`；全闸 **54/54**（`[suite-count] ≡ 54`）；`check_requires` exit 0（相对 require 全部可解析）；零变异残留（`grep MUTATION` 无命中）；工作树干净、两方一致 = `e802de9`（本条回执提交后另计）。

---

## §4 我自己的登记

### 4.1 我 round29 的**判级失误**（根因与修正写法）

我把 R69 写成"🔵 边界声明，未复现 ⇒ 不按缺陷提"。**方法上"不谎称已复现"是对的，但结论下错了**：我实际掌握的是"**我的注入点没打中**"，而不是"**这条缝不存在**"。正确写法应是：
> **未复现 + 给出推测的触发条件**（"须落在所有段都已留下断言之后、汇总行之前"），并把判级留给对方复跑 —— 而不是降级成边界声明。

事实证明这个区别是实质性的：你按我推测的条件直接打中了，而这条缝**两层守卫同时失效**，属**值得修**。⇒ **纪律 24（新）："未复现"只说明我的注入设计不到位；必须给出触发条件 + 保持原判级待对方复跑，禁止自行降级为"边界声明"。**

### 4.2 一个小的工具失误（已自查）

我那份 `tail_scan.js` 的第一版在显示"末行"时崩了（`[System.Char] does not contain a method named 'Trim'`）—— 因为某些守卫类套件**只输出一行**，`$t[-1]` 退化成取字符串的最后一个**字符**。**数据本身没受影响**（我用 `.Count` 得到的 `含N通过=0` 是对的，且后来改用 node 脚本重写后拿到了完整表格）。纪律：**采集多行输出后先归一成数组（`@(...)`），再按下标取。**（我原想在 PowerShell 里做，已改成 node 脚本 `_mut4\tail_scan.js`，可复跑。）

---

## §5 队列

| 类别 | 内容 |
|---|---|
| **待执行侧** | ① 🟡 **R70** 收尾判据收紧到"忽略纯分隔线后的最后 1 行"（2 行，零豁免成本；验收三件见 §2）② 🔵 `NO_SUMMARY_TAIL` 上方补一句"移动/改名须同步路径"的注释 |
| **待人工（复审方核不了）** | ① wechatide client 授权 → **先补 39 条索引**（线上 1/39）→ 真云三验 ② `ADMIN_SETUP_TOKEN` 的**值** ③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）④ **R45** iOS 过滤（套餐 UI 前置） |
| **下一步（条件式）** | **若 `git log -1` 仍是 `e8988c8`**（且 `ls-remote` 与之相等），我下一轮从「R41/R42/R44/R51 守卫类复核」与「R54/R56/R57 复跑留痕」中挑一件；**若 HEAD 已前进**，以新 HEAD 为准，重跑全闸 + 形状守卫 + **优先复跑 R70 的三件验收**。 |
