# REVIEW_2026-09-15 round16 · 批次 3 提交复核 + R38 验收通过 + 2 件未收尾

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 16 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round15-batch3.md`（节点②验收 + 索引裁决 A′）；本轮对象 = `d8fbba6`（116 文件）+ 工作区未提交的 R38 用例。

---

## §0 结论

**我上一轮提的东西，主体全部落地且质量很好**（索引按 A′ 落地、文档两处副本 + 派生 txt 同步、`.atomcode` 双保险、行尾未翻动、17/17 套件）。
**R38 的验收（我最在意的那条）通过了** —— 现在改生产代码为"逐行 round"，套件会**转红**。
但**两件没收尾**：① **R38 用例还没提交**（改动只在工作区）；② **重启键 §1.3 没记 R38**。另**本轮仍无回执**（第三次）。

---

## §1 逐项复核（我的独立实测）

| 项 | 结果 |
|---|---|
| 提交/推送 | ✅ `d8fbba6` = HEAD = `origin/dev`；**116 文件**（7 函数 105 + `verify_all.js` + `collections.js` + 文档/txt + `.gitignore`/`project.config.json` + 我的两份 review） |
| **索引 A′** | ✅ `collections.js:59-63` 已是 `idx_card_code_version` unique `(shop_id, card_code, version)`，且**带了我给的 R38 理由注释**；索引 **39 / unique 10** 不变 |
| **文档两处副本** | ✅ `新手上云操作手册.md:141` → `shop_cost_card.(shop_id,card_code,version)`；`下一步工序清单.md:162` → `idx_card_code_version \| shop_id ↑, card_code ↑, version ↑ \|【唯一】` |
| **派生 txt** | ✅ 三对 `.md ≡ .txt` 哈希相同（改完 `.md` 确实重生了） |
| **旧名残留** | ✅ 活跃文件里 `idx_card_code` **0 命中**（仅存于我这两份 review 的历史记录里，按协议不改） |
| **`.atomcode` 双保险** | ✅ `.gitignore` 含 ✅ `packOptions.ignore` 含 ✅ **未被误提交**（`git ls-files .atomcode` 空） |
| **行尾翻动** | ✅ `cx_money.js`/`cx_utilTime.js` **未进本提交**（`git show --stat HEAD -- <这两文件>` 无输出），工作区也不再显示它们为 modified |
| 套件 | ✅ `SUITES = 17` 且头部注释同步（"批次0~3"）；**我逐个跑 17/17**；门禁 **exit 0** |
| 我的两份 review | ✅ `round14`/`round15` 均已入库 |

### R38 验收（我上轮设的判据：**改生产代码必须转红**）—— ✅ 通过

```
基线（含未提交的 R38 用例）→ 29 通过 / 0 失败，exit=0
把 calcBom/service.js 的 lineNetCostYuan 变异成"逐行 round 到分" →
  ❌ R38：高精度累积 → unit_cost_fen = 2498（非 2499）  (=2499分)
  ❌ R38 变异：逐行 round 会得 2499 ≠ 规范值 2498
  ❌ R38 总成本即明细合计
  → 25 通过 / 4 失败，exit=1     ✅ 判据真的有鉴别力
```
**记一功**：它把我要求的"变异回验"**做进了用例本身**（`:133-136` 在测试里自算逐行值并断言 `2499 ≠ 2498`）—— 与 R33 那个 `base×N ≠ 原值` 的技巧同思路：**用测试自己证明判据有鉴别力**，不依赖外部变异。这个范式值得留在项目里。

---

## §2 两件未收尾

### R39 🟡 **R38 的用例还没提交**
- 证据：`git status` = ` M cloudfunctions/calcBom/selftest.js`（工作区改动）。⇒ **`origin/dev` 上的 `calcBom/selftest.js` 仍是 25 断言的旧版**，别人 clone 拿到的还是"没有防逐行 round 用例"的版本。
- 修法：提交这一个文件（可与后续小改一起），提交信息里点明 `R38 用例`。
- 验收：`git show HEAD:cloudfunctions/calcBom/selftest.js` 里能搜到 `2498`；`git status` 干净。

### R40 🟡 **重启键 §1.3 没记 R38**
- 证据：`★知识存储点_2026-09-10.md` 里搜 `R38`/`idx_card_code_version` → **0 命中**（R28–R30/R37 都记了，R38 漏了）。
- 为什么该记（按协议 §5）：**索引口径变更 + 多版本模型约束 + 判据缺口补洞**，三件都属"活过一轮"，且**下线索引不在代码里**（A7：只能控制台手工建）—— 换会话只读重启键的人，必须知道"云端该有 `idx_card_code_version`、不是 `idx_card_code`"。
- 修法（建议一并写进 §1.3 在案/已清区）：
  ```
  - ✅ R38 已清（round 15/16）：① 索引由单列唯一 `idx_card_code(card_code)` 改为**复合唯一 `idx_card_code_version(shop_id,card_code,version)`**（多版本模型；顺带充当 saveCostCard 非原子读写的并发守卫）—— ⚠️ **A7 决定它只能控制台手工建；若已建过旧索引须先 drop**。
    ② 补"防逐行 round"用例（反例 `3×{quantity:250,net_unit_cost:333}` → 断言 2498；逐行 round 得 2499 会转红）。
    ③ 连带已同步：`手册:141` + `工序清单:162` + 两处 `.txt` 派生件。
  ```

---

## §3 关于「回执」：这已是第三次（`round13`、`round15` 均无追加）

- 事实：本轮 `round15` 正文 **7363 → 7363 字符**，**无追加**。
- 与上轮同理，**我不判为断点**（本轮动作在 `d8fbba6` 里完整可见：116 文件、索引、文档、忽略项）。
- 但既然成了**模式**，建议把"追加回执"从"记得就写"变成**提交前自检清单的一项**（你们已经有"提交前看 `git diff --cached --stat`"这类动作，加一行即可）。回执的不可替代性在于：**提交说不清"哪条没做、为什么"**——本轮就正好有两条没做（R38 未提交、§1.3 未记 R38），若你写了回执，这两条本来会被你自己先发现。
- 最小格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`

---

## §4 队列现状

| 类别 | 内容 |
|---|---|
| **待办** | **R39** 提交 R38 用例｜**R40** 重启键记 R38 |
| **人工/用户侧** | ① 23 条对抗用例是否并入套件（原件 `_mut4\test_validate_r27.js`）② 真云同店连续两月调用 `calcAmortize` ③ 真云跑一次 `smokeTest`（验 R29 `commonShape`）④ **云端确认索引**：若 `idx_card_code` 已建过 → drop 后建 `idx_card_code_version` ⑤ 上传留提审前 |
| **下一步** | 批次 4（页面闭环）可投喂；但建议先把 R39/R40 收掉（都很小），并确认 ④ 的云端索引状态——**批次 4 会真正驱动成本卡多版本写入** |

## §5 能力边界

- **云端索引现状我核不了**（控制台状态）；A7 已定案"代码建不了索引" ⇒ `idx_card_code_version` 必须在控制台手工建。
- 开发者工具/云端/远端 push/`verify_all` 端到端 —— 一如既往核不了（我用"逐个跑 17 个成员"替代）。
- 本轮我对仓库**只读**（唯二写入 = 本份 REVIEW 与其副本）；变异全在工作区副本上做，且**先验拷贝数=15、再验基线 29/29 绿，才施变异**（沿用上轮踩坑后的自证步骤）。
