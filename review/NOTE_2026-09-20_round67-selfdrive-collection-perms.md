# round67 自驱动巡检 —— 集合权限矩阵口径守卫（同族病第 11 例）

- 时间：2026-09-20 22:40 – 23:0x
- 仓库：`C:\Users\lzj\WorkBuddy\Claw\catering-profit`，上轮 HEAD `deb77c4`（== round66 末）⇒ 无未复核新 commit
- 探针：InsCode `idle`（`message_count` **1465 十四连持平**、idle 55.6h、`inflight=0`、无待批）；
  dsh 窗口在但会话是「餐饮闭店决策指标模型咨询」**上下文不匹配** ⇒ 不投递（第 16 轮未投递）
- 并发写入方：**无**（进程两采 0/0 + 门禁后 mtime 静止 + `git log` 无新 commit + `M`=0）

## 1. 队列状态（逐条实跑复核，非采信自述）

①～⑤ 与 round66 结论一致，**无一回潮**：HEAD ≡ 上轮 ⇒ A6b / R91 / AD / R86 / 14 页走查**不重跑**；
本轮实跑 AD 24/24（rc=0）与 UI 31/31（rc=0）作抽查（证据见 `review/evidence/collection_perms_20260920/`）。
队列空 ⇒ 本轮正事 = **已入库文件防回潮扫描**（技能 §0.10）。

## 2. 本轮抓到的缺口：集合权限矩阵 ≡ 单源，零守卫

### 2.1 事实
- 单源 = `cloudfunctions/initDb/collections.js::COLLECTIONS`，node 实跑 **25 张**（`require` 实算，非肉眼抄）
- 人工面 = `specs/dev-specs/core/15_集合权限矩阵.md` §2「全量集合权限矩阵（25 张）」25 行表格
- **实扫**：`tools/` 与 `specs/dev-specs/prototype/` 对 `集合权限矩阵|core/15` **零命中** ⇒ 该表与单源**零守卫**
- `check_schema_sync.js`（R74）的 S1~S5 只守：集合清单 `collections.js ↔ init_db.js` 镜像 / 索引清单 /
  单源结构不变量 / **三份文档**（`新手上云操作手册` · `下一步工序清单` · 重启键）的写死计数 / 工序清单对照表
  ⇒ **不看 `core/15` 一个字**

### 2.2 后果（比前 10 例硬）
集合权限是**安全边界**口径：AD-9「客户端不直连 DB」，店算默认安全态 = `仅管理端可读写`
（微信云开发默认却是「仅创建者可读写」）。加/删一个集合时：
- R74 会要求同步镜像与文档计数 ⇒ **绿**；
- 但 `core/15` **静默过期** ⇒ 新集合留在默认「仅创建者可读写」⇒ **客户端可绕过云函数直连越权**。

前 10 例是"文档陈述错"，本例是**安全边界错**；同病仍是「人工陈述面 ≡ 实然，零守卫」。

### 2.3 已落
1. 在 `core/15` §2 立口径单源（唯一声明处）：
   `🔢 集合权限口径（唯一声明处）：本表共 **25** 张集合，与 collections.js::COLLECTIONS 逐条一致；权限模式一律 **仅管理端可读写**（AD-9）`
2. 新增 `tools/check_collection_perms.js`（**R103**，20 条断言），SUITES **75 → 76**

## 3. 判据（P1~P9，全部 fail-closed）

| 编号 | 判据 |
|---|---|
| P1 | 扫描面：git ls-files 须 `core.quotepath=false`，路径含 `\NNN` 转义即红（坑⑪） |
| P2 | 单源 COLLECTIONS 可解析 / 非空且元素唯一 / 命名合法 |
| P3 | 唯一声明处**恰 1 处**且数字 ≡ 单源（fail-closed） |
| P4 | **三方**：声明数 ≡ §2 表格实算行数 ≡ 单源长度 |
| P5 | 集合**逐条双向**（缺 / 多 / 表内重复都红） |
| P6 | 权限模式 ∈ 白名单（`仅管理端可读写`；例外须 `by/date/reason` 显式登记 + **双向防腐**无僵尸条目） |
| P7 | 序号连续 1..N 无重号跳号 |
| P8 | 三道**前提守卫**：表格行解析 ≥1 / 字段无空 / 白名单命中 ≥20 / 弱面扫描面有效 |
| P9 | 唯一声明处不扩散（其它 .md 不得再自称单源） |

- **弱面（只明示不判红）**：`specs/**/*.md` 里「N 张集合」与单源不符者打印 ⚠️。
  依据坑⑭/⑯：裸扫必误杀（「dev 只有 7 个集合」= 显示截断、「15 集合」= 子集）。
  变异 M2 现场实证弱面有效：`core/13:61 写 25（单源 26）` 被打印出来。

## 4. 双向变异回灌（10 组，0 异常）

证据 `review/evidence/collection_perms_20260920/mutation_matrix_r67.txt`（逐组输出 `mut_M*.txt`）。
变异前已 `git add` 本轮改动（铁律⑩），还原一律 `git checkout --`。

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| M1 | 声明 25→26 | 红 | ✅ rc=1 |
| M2 | 单源加一张集合 `shop_probe_tmp` | 红 | ✅ rc=1 |
| M3 | 表格删末行 `admin_login_log` | 红 | ✅ rc=1 |
| M4 | 权限模式改「所有用户可读，仅创建者可写」 | 红 | ✅ rc=1（P6） |
| M5 | 删唯一声明处 | 红 | ✅ rc=1（fail-closed） |
| M6 | 序号跳号 25→26 | 红 | ✅ rc=1（P7） |
| M7 | **换措辞**「本表共**登记** 25 张集合」 | **仍绿** | ✅ rc=0（不错杀） |
| M8 | **表格两行互换**（顺序无关） | **仍绿** | ✅ rc=0（不错杀） |
| M9 | 守卫扫描面路径写错 | 红 | ✅ rc=1（P1-②） |
| M10 | 单源集合改名 `shop_switch_v2` | 红 | ✅ rc=1（P5 双向） |

- **M5 首轮 `NOT_APPLIED` 是变异脚本 bug**（`if DECL in t` 走了空替换分支、fallback 没执行），
  **不是 CRLF 坑⑨**（DOC 是 LF，且其它 9 组锚点全命中）⇒ 判别法成立：先看锚点在不在，再看行尾。
- M7 能绿是因为声明解析正则写成 `共[^0-9]{0,6}?\*{0,2}(\d+)\*{0,2}\s*张集合`，容得下「共**登记** 25 张」这类措辞变化
  （否则「换措辞即解析失败」会变成 fail-closed 误杀）。

## 5. 门禁（改 specs 前后各跑，取 rc 不走管道）

| 项 | 结果 |
|---|---|
| `node verify_all.js` | **76/76** 套件通过，rc=0（×2） |
| `node specs/dev-specs/prototype/check_error_codes.js` | rc=0（×2） |
| `node tools/selftest_ad_gates.js` | AD **24/24**，rc=0 |
| `node tools/selftest_ui_fix.js` | UI **31/31**，rc=0 |
| `node tools/check_collection_perms.js` | **20/20**，rc=0 |

套件数三处同步（R97 自证）：`verify_all.js` 头注 75→76、重启键 §1.1 行、重启键「套件数会漂」行（含演进链第 76 条）。

## 6. 回执

- [2026-09-20 22:43] R67 探针已落 · 证据：`probe_agents.py` → InsCode idle（msg 1465 十四连持平、无待批）；dsh 窗口在但上下文不匹配 · commit 待回填
- [2026-09-20 22:45] R67 门禁基线已落 · 证据：`node verify_all.js` → 75/75 rc=0（改 specs 前）· commit 待回填
- [2026-09-20 22:50] R67 并发方判定已落 · 证据：进程两采 0/0（采样脚本 .py 化并排除自身 PID）+ mtime 静止 + `git log` 无新 commit + `M`=0 → 无并发写入方 · commit 待回填
- [2026-09-20 22:52] R67 缺口定位已落 · 证据：`grep -rn "集合权限矩阵\|core/15" tools/ specs/dev-specs/prototype/` → **零命中**；`collections.js` node 实跑 → 25 张 · commit 待回填
- [2026-09-20 22:54] R67 口径单源已落 · 证据：`core/15` §2 新增「集合权限口径（唯一声明处）」行 · commit 待回填
- [2026-09-20 22:55] R67 守卫已落 · 证据：`node tools/check_collection_perms.js` → 20/20 rc=0 · commit 待回填
- [2026-09-20 22:57] R67 SUITES 已落 · 证据：`node verify_all.js` → **76/76** rc=0（R92 首跑拦「未入库」⇒ `git add` 后转绿，属预期）· commit 待回填
- [2026-09-20 22:59] R67 双向变异已落 · 证据：`_mut_r67.py` → **10 组 0 异常**（8 转红 + M7/M8 不错杀），`mutation_matrix_r67.txt` · commit 待回填
- [2026-09-20 23:00] R67 A–L/AD/UI 已落 · 证据：`check_error_codes.js` rc=0 · `selftest_ad_gates.js` 24/24 · `selftest_ui_fix.js` 31/31 · commit 待回填
- [2026-09-20 23:00] R67 **未落（人工面）**：dsh 复审 —— 上下文不匹配（会话是「餐饮闭店决策指标模型咨询」）且后端自启动零业务写入 ⇒ 不投递；round39 等 5 份 NOTE 复审仍悬，待李老师四选一 · commit —
- [2026-09-20 23:00] R67 **未落（待裁决）**：1033 个并发方过程 PNG 不代提交不代删 · commit —

## 7. 待下轮

1. **李老师四选一**（dsh 放开写+node / 改只读判据 / 退场 / 允许重启 dsh web）未定 ⇒ round39 等 5 份 NOTE 复审仍悬
2. 并发方 1033 个过程 PNG 待定（不代提交不代删）
3. 建 prod 时提醒重走 R86 定值与回读 + 替换 `ENV_MAP.prod` 占位符
4. **提醒李老师：隐私政策按 6 处填**（记 4 处会漏 2 项，直接卡提审）
5. 🟡 **已知覆盖缺口（如实标注，不谎报）**：`specs/**/*.md` 里若有人写「本小程序共 26 张集合」而**不带**全集语义锚点，
   本守卫只会在弱面 ⚠️ 打印、**不判红**（与 round65 Q6 同族，宁可少判红不可误杀）；硬判据是 P3/P4/P5（声明 + 表格逐条双向）。
6. 🟡 行尾混用（坑⑰）：`core/09`/`11`/`12`/`15`/`16` 等一批 `.md` 是 LF，其余是 CRLF。
   本轮**未改**（`core/15` 改动前即为 LF，与同批文件一致），仅登记。
