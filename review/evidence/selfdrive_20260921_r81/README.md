# round81 自驱动巡检证据（2026-09-21 22:39–23:0x）

## 一句话结论
探针：InsCode **idle**（msg 1465 二十三连持平 / idle≈79.6h）、dsh **窗口缺失** ⇒ 不投喂不投递。
停手四连通过 ⇒ 无并发写入方。队列 ①~⑤ 复验仍闭环（④ R86 人工面**未实测**，记存疑）。
队列空 ⇒ 正事 = 防回潮扫描 ⇒ 抓出**同族病第 19 例**：`core/11_微信审核自查清单.md` §2
「微信审核硬红线清单」（6 条，自称「权威计数」、提审前逐条勾的面）**全仓零守卫**
⇒ 立单源声明 + 新增守卫 `tools/check_audit_redlines.js`（R112，15 断言），SUITES **84→85**，
门禁 **85/85 RC=0**，双向变异 **10 组 0 异常**。

## 文件清单
| 文件 | 内容 |
|---|---|
| `gate_r81.txt` | 首次门禁（84/84，改动前基线） |
| `gate_final.txt` | 挂套件后首跑 —— 84/85，`C2-③ 重启键「套件数会漂」文档 84 / 实算 85`（R97 自动抓出） |
| `gate_final2.txt` | 修正现值后 —— **85/85 RC=0** |
| `error_codes.txt` | 门禁 A–L（`check_error_codes.js`）RC=0，末条 `✅ K11 i18n 双副本逐字一致` |
| `ad_gates.txt` | 上线前 AD 缺口自测 **24 通过 / 0 失败** |
| `ui_fix.txt` | 批次 8 UI 修复自测 **38 通过 / 0 失败** |
| `scan_refs.txt` | 防回潮扫描：`specs` 全树 .md 被 `tools/`+`prototype/` 引用次数（零引用 3 份） |
| `proc_scan.txt` | 进程双采（22:44:09 / 22:45:27，排除自身 PID）⇒ 仓内相关进程 **0 / 0** |
| `_all_scripts.txt` / `_suites.txt` | `tools/` 下 37 个 check_/verify_/selftest_ 脚本 vs SUITES 引用 ⇒ **未挂 0 个** |
| `r112_first.txt` | R112 首跑 **15 通过 / 0 失败** |
| `mutation.txt` | 双向变异 10 组结果（含每组命中的 ❌ 判据行 + 还原后字节比对 ALL_OK） |

`.py` 为取证脚本（沿用前轮做法：不入库、不删，等李老师定）。

## 停手四连（round55 定式）
1. 跨分钟 mtime 无变化 —— 最近写入 21:31（证据目录），采样式 22:41，间隔 ≥1h；
   门禁跑完（22:58）再回读 `terms.js` / `core/13` / 重启键 mtime 均**未变**。
2. 进程两采：`HIT(仓内相关, 排除自身 PID)` = **0 / 0**。
3. 门禁后回读 mtime —— 无变化（见上）。
4. `git log -3` 未见新 commit，HEAD 仍 `2bf88f1`（≡ round80 收口）。
⇒ 无并发写入方，恢复写入权限。

## 同族病第 19 例 · 实扫证据（不采信任何自述）
- §2 清单实算 **6** 条（`> N. ` 形式，序号 1..6 连续）。
- `grep -rn "硬红线" --include=*.md/.txt/.js`（排除 `review/`）⇒ **仅 1 处命中** = 该清单标题行本身
  ⇒ **计数 6 未在任何第二处出现**，当前**零漂移**。
- `grep -rln "11_微信审核自查清单|微信审核自查" tools/ specs/dev-specs/prototype/ verify_all.js` ⇒ **零命中**；
  `grep -rln "硬红线" tools/ specs/dev-specs/prototype/` ⇒ **零命中**。
- 既有的 R100 `tools/check_redline_inventory.js` 的 `LIST_REL` 写死为 `core/04_核对清单.md`
  ⇒ **不看 core/11 一个字**。

⚠️ **「缺守卫」与「已违规」分开报**（round55 纪律）：本轮实扫证明当前**无漂移、无错误数据**；
本守卫做的是**防复发** —— 今后任一侧改数字都会当场判红，不再靠人记。

🟡 **溢出（未修、不代裁决）**：`core/11:44` 自称「与待办表(§6)一致」，而 §2 是 6 条、§6 勾选是 11 条
（round73 已实读判定：两张不同清单，不构成漂移）。本守卫只做**弱形式**校验（§6 条数 ≥ 硬红线条数，A7-②），
**不代定义两套清单的映射** ⇒ 归弱面只明示 + 待李老师确认措辞。
