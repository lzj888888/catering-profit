# -*- coding: utf-8 -*-
p = 'specs/dev-specs/core/13_上线前查缺补漏_决策与待办总览.md'
d = open(p,'rb').read()
nl = b'\r\n' if d.count(b'\r\n') == d.count(b'\n') else b'\n'
text = """
---

## 🟡 待裁决 · M1 开发规范 M1.6「四红线 + 保本达成率红绿灯」在代码中零实现（round80 防回潮扫描发现）

**两侧事实（均为回源实扫，非采信自述）**：

- 写码基线 `specs/dev-specs/core/开发规范v1.0_ModuleM1_月度盈利核算.md` §M1.6 立了**四红线**
  （房租占比 ≤15% / 人工占比 ≤20% / 菜品毛利率 ≥55% / 食材损耗率 ≤5%）与**保本达成率红绿灯**
  （<70% 绿 / 70–100% 黄 / >100% 红），并注明「与豆包口径待统一，见 M2」。
- 实扫代码侧：`grep -rn "预警|warn|达红|红灯|保本达成|占比|红线|risk|level|alarm|threshold|健康|诊断"`
  于 `miniprogram/`（js/wxml/json）与 `cloudfunctions/` ⇒ **零命中**；
  前端仅存 M2 的两个词条（`terms.js` `breakEvenMonthly` / `breakEvenDaily`），**与经营红线无关**。
  ⇒ **阈值与红绿灯当前均未在小程序与云函数里落地**。

**为何本轮不代修 / 不代实现**：

- 「补实现」= 新增功能，属**范围决策**，按团队约定不代选方案；
- 「删规范」= 改写码基线，同样需李老师拍板。两条路线本轮都不动。

**已做的防扩散（round80 已落）**：四红线阈值此前**零守卫**（`tools/` + `prototype/` 对两份模块开发规范
引用次数实测 **0/0**）。已在 M1 规范 §M1.6 立单源声明行，并新增守卫 `tools/check_redline_thresholds.js`
（R111，18 断言，SUITES 83→**84**）：声明 ≡ §M1.6 表格（双向逐项）+ M3 规范引用行 ≡ 声明（双向逐项）
+ 单源不扩散 + 三道前提守卫。⇒ 今后**任一侧改数字都会当场判红**，不再靠人记。
⚠️ 裸扫「房租/人工/毛利/损耗 + 数字%」会误杀 **49 处**合法口径（S3 测试数据 65.625%、用例「损耗 5%」、
`PRODUCT_PLAN` 自述的另一套警戒线）⇒ 判据只走语义标记单源，弱面仅打印明示、不判红。

**待李老师二选一**：
① **补实现** —— 在月度结果页按四红线做占比预警与红绿灯（需先确认阈值是否沿用 15/20/55/5）；
② **确认已裁剪** —— v1.0 不做经营预警，则在 M1 规范 §M1.6 标注「v1.0 不实现、排 v1.1」，避免写码方照基线空转。
"""
open(p,'wb').write(d + text.replace('\n', nl.decode()).encode('utf-8'))
d2 = open(p,'rb').read(); print('写后 CRLF=', d2.count(b'\r\n'), 'LF=', d2.count(b'\n'))
