===== R113 · 限流阈值口径守卫 =====
  ✅ A1-① 代码单源存在且非空 —— cloudfunctions/common/rateLimit.js · 808B
  ✅ A1-② 声明文档存在且非空 —— specs/dev-specs/core/09_统一错误码表.md · 9892B
  ✅ A1-③ 扁平副本数 ≥ 40（副本扫描面失效即红） —— 42 份
  ✅ A1-④ 扫描面 .md 数 ≥ 25（排除 review/，路径写错即红） —— 29 个
  ✅ A2-① 语义标记「限流阈值口径」可解析出次数 N（fail-closed） —— N=60
  ✅ A2-② 声明内可解析出窗口 W（ms，fail-closed） —— W=60000
  ✅ A2-③ 「唯一声明处」在 core/09 内恰 1 行 —— 1 行
  ✅ A3-① 单源可解析出 MAX_WRITES / WINDOW_MS（fail-closed） —— MAX_WRITES=60 / WINDOW_MS=60000
  ✅ A3-② 声明次数 N=60 ≡ 单源 MAX_WRITES=60 —— 声明 60 / 实算 60
  ✅ A3-③ 声明窗口 W=60000 ≡ 单源 WINDOW_MS=60000 —— 声明 60000 / 实算 60000
  ✅ A3-④ 窗口恰为一分钟（60000ms，「次/分钟」口径自洽） —— 60000 ms
  ✅ A4 全部 42 份副本 ≡ 单源（防只改一处副本） —— 42 份同值 60/60000
  ✅ A5-① 当前态声明全部 ≡ 60（共 4 处，HIST 放行 0 行） —— 4 处一致
  ✅ A5-② 当前态声明命中数 ≥ 3（正则/扫描面失效即零命中假绿） —— 4 处
  ✅ A6-① 前提：扫描面确含声明文档（自指排除没误伤） —— specs/dev-specs/core/09_统一错误码表.md
  ✅ A6-② 前提：副本集确含已知函数目录（副本面没打错） —— adminExport,adminGrantEntitlement,adminInit
  ✅ A7 唯一声明处不扩散（其它 .md 同行不得同时自称单源 + 本口径） —— 仅 core/09 一处

===== W 弱面（只明示不判红）=====
  ⚠️ core/16:48 的维度（IP + admin_id，admin 后台）与单源（openid，C 端）是**两套场景**
     （round82 实读判定：不是漂移；单源注释明写「批次 0 §2.2.5」，与 core/09 表格行口径一致）
     ⇒ 弱面只打印；硬判据是 A3（声明≡单源）、A4（副本≡单源）、A5（全仓引用≡N）。
  ❌ W-① 弱面已打印（坑⑭/⑯ 印证：裸扫数字必误杀） —— admin 维度描述命中=false / 当前态声明 4 处

===== 限流阈值口径守卫结果：17 通过 / 1 失败 =====
