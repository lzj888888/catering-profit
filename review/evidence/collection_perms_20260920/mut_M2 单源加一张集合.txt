  ✅ P1-① 扫描面非空且无八进制转义（CJK 路径不得被静默跳过） —— 1321 个文件 / 转义 0
  ✅ P2-① 单源 COLLECTIONS 可解析 —— cloudfunctions/initDb/collections.js
  ✅ P2-② 单源非空且元素唯一 —— 26 张 / 去重 26
  ✅ P2-③ 单源命名合法（小写下划线） —— 全部合法
  ✅ P1-② 权限矩阵文档可读 —— specs/dev-specs/core/15_集合权限矩阵.md
  ✅ P3-① 唯一声明处恰 1 处 —— 命中 1 处
  ✅ P3-② 声明可解析出集合数（fail-closed） —— 声明 25
  ❌ P3-③ 声明数 ≡ 单源长度 —— 声明 25 / 单源 26
  ✅ P8-① 表格行解析成功（fail-closed） —— 25 行
  ✅ P8-② 表格字段无空（序号/集合/权限模式） —— 无空字段
  ❌ P4 声明数 ≡ 表格行数 ≡ 单源长度（三方） —— 声明 25 / 表格 25 / 单源 26
  ❌ P5-① 单源每张集合都在权限矩阵里登记 —— shop_probe_tmp
  ✅ P5-② 权限矩阵无单源之外的集合 —— 无多余
  ✅ P5-③ 表格内集合无重复行 —— 无重复
  ✅ P6 权限模式 ∈ 白名单（默认安全态 `仅管理端可读写`，例外须显式登记） —— 全部 仅管理端可读写
  ✅ P6-b 豁免双向防腐（无僵尸条目） —— 无豁免
  ✅ P7 序号连续 1..N 且无重号 —— 1..25
  ✅ P8-③ 白名单命中 ≥ 20（列错位即零命中假绿） —— 25 行
  ✅ P9 唯一声明处不扩散（其它 .md 不得再自称本口径单源） —— 仅 specs/dev-specs/core/15_集合权限矩阵.md 一处
  ✅ P8-④ 弱面扫描面有效（确有「N 张集合」陈述被扫到） —— 5 处
  ⚠️ 弱面（与单源不符，只明示不判红，可能是历史/子集/截断）5 处：
     specs/dev-specs/core/10_云函数清单与接口契约.md:28 写 25（单源 26） —— | `initDb` | **一次性**建集合+索引+种子数据（**仅 dev 环境部署**，含 env 门禁；prod 25 张集合在控制台建；**禁止部署 prod 是政策、不
     specs/dev-specs/core/13_上线前查缺补漏_决策与待办总览.md:61 写 25（单源 26） —— ⇒ **验收必须回读**：跑完要**回读 25 张集合清单 + 种子数据是否齐**，**不看返回值、不看界面「测试结果：成功」**。
     specs/dev-specs/delivery/inscode喂投包_8批_自包含完整版.md:127 写 25（单源 26） —— - **建库云函数 `initDb`**：把 §2.4 的 **25 张集合 + 索引 + 种子套餐/权限** 落地为可运行脚本 `prototype/init_db.js`，部署
     specs/dev-specs/★知识存储点_2026-09-10.md:546 写 25（单源 26） —— - ✅ **R38 已清（round 15 裁决 / round 16 验收）：① 索引由单列唯一 `idx_card_code(card_code)` 改为复合唯一 `idx_c
     specs/dev-specs/★知识存储点_2026-09-10.md:703 写 25（单源 26） —— | `specs/dev-specs/prototype/init_db.js` | **⭐ 一次性建库云函数**：25 张集合 + 索引 + 种子套餐/权限；部署为 `initD

===== 集合权限矩阵口径守卫结果：17 通过 / 3 失败 =====
