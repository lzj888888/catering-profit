# 23 条入参对抗用例并入 + 变异回验（2026-09-16）

## 做了什么（round18 §2 裁决① / round19 §4 裁决的执行）

把复审方那份独立资产 `_mut4/test_validate_r27.js`（23 条：A1–A13 标量 / B1–B2 库存 / C1–C7 明细 / D1 可诊断性）
**并入** `cloudfunctions/calcMonthlyProfit/selftest.js`，**不另建套件**。

- **去重**：原 R27/R30 那 7 条（标量字符串 / 库存字符串 / 明细字符串 / 合法整数 / 并存相等 / 并存不等 / 只传 camel）
  是该 23 条的**子集**，已由 23 条取代 ⇒ **覆盖未减**（逐条对照见 `selftest.js` 内 A/B/C/D 分组注释）。
- **统一分区**：全部收在 `===== 入参对抗（validate）=====` 一个标题下，每条带 `[入参 <id>]` 前缀 ⇒
  跑挂时一眼区分"入参面"还是"锚点面"（失败归因）。
- **动态计数**：末行汇总由 `INPUT_CASES.length` 生成，不写死数字（R21 纪律：改用例数不必再改汇总）。
- **断言形态**：每条是 `{ id, desc, run: () => 实得, exp }`，`run()` 里 `try/catch`（抛异常也记红，不静默）。

## 变异回验（把"用例有鉴别力"这件事实测出来）

原始输出见同目录 `23条并入_变异回验_20260916.txt`。摘要：

| 阶段 | 动作 | selftest | verify_all |
|---|---|---|---|
| 基线 | 无 | **EXIT=0**，`23 条入参对抗用例全数通过` | EXIT=0 |
| 变异 | `validate.js` `cleanItems` 的 `hasCamel` 判定**退一档**：`if (hasCamel)` → `if (hasCamel && amountFen !== amount_fen)`（即退回 R30 之前的"相等即放行"宽松行为） | **EXIT=1**，`[入参 C4] … → OK ❌ 期望 INVALID_PARAM`，`存在未通过项：1 处` | **EXIT=1** |
| 还原 | `git checkout -- validate.js`（`git diff --stat` 空） | EXIT=0 | EXIT=0（`17/17 套件通过`） |

**结论**：① 这 23 条**确实有鉴别力**（不是恒绿的装饰性断言）——变异点正落在 C4，
且**只有 C4 转红**（C5「并存且不等」在旧行为下也拒，故不动，说明断言精确而非连坐）；
② 该分区**真的在门禁里**（`verify_all` 同步转红）—— 这正是 round18 §4 说的"避免孤立失败面"被实测掉了。
