# 证据 · 自驱动巡检 round42（2026-09-19 12:27–12:5x）

采集时刻：2026-09-19 12:31–12:45（本目录所有文件的采集均在上述区间内）
性质声明：本节**非纯读** —— 本轮包含一次 git 提交与推送（见「例外声明」）。除此之外本轮**无任何云侧写入**。

## 例外声明（E1）
本轮**不点 GUI、不跑 `cli`、不部署**（检出并发写入方，见主 NOTE）。唯一的写入动作是
`git add <我方路径> && git commit && git push origin dev`，属**仓库侧提交**，**非云侧写入**。

## 文件清单
| 文件 | 采集时刻 | 说明 |
|---|---|---|
| `probe_20260919_1240.txt` | 12:40 | `probe_agents.py` 原始输出（InsCode 走 SQLite / dsh 走窗口+OCR） |
| `dsh_855322.png` | 12:33 | dsh 窗口截图（hwnd 855322，rect 852,105–1764,924；`screenshot_window`，**未抢焦点**） |
| `ocr_dsh_855322.txt` | 12:3x | 对上图的 OCR 全文（OCR 只用于定位与粗判，**不作机器判据**） |
| `README.md` | 12:45 | 本说明 |

## 时刻序（E2）
采集时刻 12:31→12:45；本目录无「前置动作时刻」声明 ⇒ E2 规则不适用（不虚构前置时刻）。
