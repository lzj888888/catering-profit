# round65 · R101 功能页面清单口径守卫 —— 双向变异回灌矩阵

守卫：`tools/check_page_manifest.js`（17 条断言，SUITES 第 74 项）
还原方式：一律 `git checkout -- <file>`（本轮改动已先入 index，符合铁律⑩）
还原后基线复核：`RESTORED_BASELINE_RC=0`

| # | 变异 | 预期 | 实测 | 判定 | 首条红 |
|---|---|---|---|---|---|
| M1 | 提审材料口径声明 14 → 13 | 红 | RED | OK | `Q3 声明 N=13 ≡ app.json::pages 实算 14` + `Q6（强锚点面 8 处）` |
| M2 | 正确实现**换措辞**（声明句改写，数字仍 14） | 绿 | GREEN | OK | —（证明判据不绑字面） |
| M3 | 删掉 §4 表格最后一行登记 `pages/mine/index` | 红 | RED | OK | `Q4-② app.json 每条页面都在 §4 登记 —— pages/mine/index` |
| M4 | `app.json::pages` 追加 `pages/probe/index`（代码改、材料没跟） | 红 | RED | OK | `Q3 声明 14 / 实算 15` + `Q4-②` |
| M5 | 弱面新增一条无关页数「共 **9** 页（导出分页）」 | 绿 | GREEN | OK | —（证明不错杀分页页数） |
| M6 | 删掉口径声明行 | 红 | RED | OK | `Q2-① 零命中` + `Q2-② 0 行` + `Q3 N=-1`（fail-closed） |
| M7 | `SCAN_DIR` 改错路径（扫描面扫空） | 红 | RED | OK | `Q1-④ 0 个` + `Q7 0 行` + `Q8`（扫空 = 零覆盖） |
| M8 | 把子集页码「7 页内联色」挪到 `app.json` 旁（≤30 字符） | 红 | RED | OK | `Q6 …:311 写 7`（**证明「锚点就近」不是恒真**） |

结论：
- **能抓错**：6 组（M1/M3/M4/M6/M7/M8）全转红，且每条都**点名到具体文件:行/具体值**，不是笼统失败。
- **不错杀**：2 组（M2 换措辞、M5 无关页数）保持全绿。
- **还原**：8 组全部 `git checkout --` 还原成功，末次基线 RC=0。

## 首跑误杀记录（判据被收窄的实证，不是"改代码迎合"）

守卫 v1 裸扫「数字+页」，首跑（证据 `guard_v1_firstpass_2falsepositive.txt`）当场误杀两条**合法的不同语义**：

| 位置 | 原文 | 真实语义 | 处置 |
|---|---|---|---|
| `core/13:77` | 「1500 条 ≈ **15 页**往返」 | **导出分页的页数**，与小程序页面数无关 | 弱面 ⚠️ 明示 |
| `★知识存储点:311` | 「**7 页**内联色」 | **子集**（有内联色的 7 个页面） | 弱面 ⚠️ 明示 |

⇒ 判据改为**「全集语义锚点就近（±30 字符）」两条腿**：锚点在数字附近才判红，否则只打印 ⚠️ 明示、不判红。
⇒ **M2/M5 证明收窄后仍守得住意图；M8 证明「就近」判据本身有机械力（不是恒真）**。
⇒ 已知覆盖缺口（如实标注，不谎报）：若有人写「本小程序共 13 页」而全句无任何全集锚点，Q6 抓不到，
   只出现在 ⚠️ 明示列表供人读；真正的硬判据是 Q3（单源声明）与 Q4（表格逐条双向）。
