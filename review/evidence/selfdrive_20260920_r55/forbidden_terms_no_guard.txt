# round55 独立发现 —— TERMS.forbidden 是「存在但从未被执行」的权威判据（第 2 例）

时间：2026-09-20 01:44 CST
方法：全仓 grep + 用表自身实扫（不采信任何自述）

## 1. 判据存在性（有）

- miniprogram/i18n/terms.js:105  `forbidden: [ ... ]`  13 条（14 个词条）
- specs/dev-specs/i18n/terms.js:105  同表副本（K11 双副本逐字一致，门禁守得住）

词条：投资回报 / ROI / 回本周期 / 收益率 / 利润率 / 盈利 / 赚钱 / 躺赚 /
      投资 / 理财 / 股 / 付费 / 订阅 / 会员费
其中「盈利 / 赚钱 / 躺赚」带 exception 字段（round54 2026-09-20 为模块名「月度盈利核算」显式豁免）

## 2. 判据执行性（无）—— 关键缺口

```
grep -rn "forbidden" --include=*.js . | grep -v node_modules | grep -v ^./review/
  ./miniprogram/i18n/terms.js:17    （自身注释）
  ./miniprogram/i18n/terms.js:105   （自身定义）
  ./specs/dev-specs/i18n/terms.js:17  （自身注释）
  ./specs/dev-specs/i18n/terms.js:105 （自身定义）

grep -rln "forbidden" tools/ specs/dev-specs/prototype/   =>  零命中
grep -nE "term|禁词|forbidden|word" verify_all.js          =>  零命中（SUITES 无术语/禁用词套件）
```

⇒ 全仓**没有任何守卫脚本读取这张表**；`verify_all.js` 的 67 个套件里也没有术语套件。
⇒ 「门禁 67/67 全绿」这句话**不包含任何禁用词校验**。

## 3. 实扫后果（证明缺口有多实际）

### 3.1 粗扫（.js/.wxml/.html 全文，含注释）
扫描 54 个文件 → 命中 26 处，全部是**代码注释**里的「付费 / 订阅」
（如 `pages/card/index.js:2`、`utils/paywall.js:1`）。
⇒ 这张表若直接当守卫用会**大量误报**——这大概正是它从未被挂成守卫的原因。

### 3.2 精扫（去 `<!-- -->` 注释 + 去标签，只留用户可见文案）
扫描 15 个 wxml/html → 命中 **1 处**：
```
admin-h5/index.html | + ' ' + (u.tier === 'paid' ? '付费' : '免费') + ' ' + ...
```
⇒ 该处是**内部管理后台 H5**，不提交微信小程序审核 ⇒ 不构成审核风险。
⇒ 小程序 pages/*.wxml 的用户可见文案**当前零命中**＝暂无实际违规。

## 4. 结论

- 现状：**无实际违规**，但**无机械保障**。
- 风险：将来谁在 wxml 里硬编码「赚钱 / 躺赚 / 收益率」，67/67 全绿照样放行，
  直到提审被金融类目红线打回才发现。
- 与 round53 抓到的 `tools/verify_docx.py` 同族（权威判据从未登记 SUITES）。
  本仓判据缺口家族已确认 2 例：**「判据存在 ≠ 判据被自动执行」**。

## 5. 本轮处置：不补挂（记「未落 + 理由」）

理由：检出**并发写入方仍在活跃**（01:35:15 改 delivery MD、01:39:39 重生派生件）。
补挂守卫需改 `verify_all.js` 的 SUITES（67→68），会：
  ① 与对方抢写同一批文件（违反铁律「一次只允许一个写入方」）；
  ② 触发 R92（套件须已入库）/ R66（每段至少一个 ✅）自校验，
     让对方在中间态看到非自身原因的门禁红，污染其归因。
⇒ 列入下轮待办：**先设计一个只扫「用户可见文案」的判据**（避免 3.1 的注释误报），
   再挂 SUITES，并做变异回灌（wxml 注入「躺赚」须转红）。
