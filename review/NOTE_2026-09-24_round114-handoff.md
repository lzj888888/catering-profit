# NOTE · round114 交接记录（接手入口）

> **快照时刻**：2026-09-24 07:3x（本机）
> **现状**：round114 **已收官**。`catering-profit` HEAD = `ab1474b`（分支 `dev`，**已推**，两侧一致）；
> 工作树**干净**（`git status --porcelain -uall` = **0 行**）；门禁 **101/101 · RC=0**；
> `calcSandbox` **已上云**（`success=true · filesCount=16`）；**⚠️ 前端页面尚未上传体验版**（见 §5）。
> **轮次回执**（详细记录，本文件是它的"接手索引"）：
> `review/NOTE_2026-09-24_round114-m2-revenue-anchor-m1-plan-and-r128.md`

---

## §1 这个文件是干什么的

李老师的原话是「**做好上下文衔接记录**」。本项目此前已有四层上下文载体，但**没有一份是"接手索引"**——
新会话要读 5~6 个地方才能拼出"现在在哪、下一步做什么"。本文件补的就是这个（**只做索引与汇总，不复制细节**）。

**⚠️ 单一源纪律**：本文件**不引入第二处事实**，每条都指向唯一细节源。
真值顺序：`specs/dev-specs/★知识存储点_2026-09-10.md §1.1`（唯一状态节）→ 本文件（接手索引）→ 各 NOTE。

## §2 接手必读（按顺序，约 10 分钟）

| # | 读什么 | 得到什么 |
|---|---|---|
| 1 | `specs/dev-specs/★知识存储点_2026-09-10.md` **§1.1** | **唯一"当前状态"**。含门禁 A–L 说明、101 套件入口、断言数声明行、仓外备份位置、round75 起的逐轮留痕 |
| 2 | `review/README.md`（尤其 §1 目录约定 / §2 双向规则 / §7 单一写入方） | 交接协议 + **写入权规则**（同一时刻只允许一个写入方） |
| 3 | 本文件 §3~§6 | 本轮遗留 + 待拍板 + 下一步候选 |
| 4 | `review/NOTE_2026-09-24_round114-*.md` | 本轮的完整来龙去脉（含 P0 缺陷、4 个自踩坑） |
| 5 | 工作区记忆（**仓外**）：`C:\Users\lzj\WorkBuddy\2026-09-08-22-08-11\.workbuddy\memory\` | `2026-09-24.md`（当日流水）+ `MEMORY.md`（≤3000 字精炼）+ `PITFALLS.md`（细则） |

> 🔴 **别读错**：`~\.workbuddy\memory\` = 云端画像缓存（自动注入的 `<uuid>_memory.md`），**不是本项目报告**；
> `~\.workbuddy\MEMORY.md` = 跨项目用户画像，也不是。两个都是诱饵。

## §3 本轮（round114）遗留 —— 未清事项

| # | 事项 | 为什么没做 / 卡在哪 | 谁定 |
|---|---|---|---|
| 1 | **前端页面尚未上传体验版** | 云函数部署**不覆盖前端**；本轮改了 `pages/sandbox/*`（锚点输入行 + 参考金额）⇒ 真机看不到。上传动作需在微信开发者工具里做；出码则走 `miniprogram-preview-qr`（⚠️ 李老师已明确"不要再自动弹二维码"） | 李老师一句话即可 |
| 2 | **其它云函数未重新部署** | `cloudfunctions/*/cx_indicatorRef.js`（42 份派生副本）同批更新，但本轮新增的 `suggestAmounts` 是**纯新增**、不改旧行为 ⇒ 不部署不影响正确性。下次这些函数有改动时一并带上去即可 | 无需单独动作 |
| 3 | **既有守卫缺口（本轮只关掉一条）** | 「术语键**存在但值被掏空**」本轮已在 R128 内关掉（C-③），但**同类缺口在别处仍在**：`check_page_terms`（R124）只查"引用即存在"、**不查值非空** | 待排期（技术债，非裁决项） |
| 4 | **M1.6 四红线在代码中零实现** | 规范 M1.6 定义了「四红线 + 保本达成率红绿灯」，代码侧零实现。已登记在 `core/13_上线前查缺补漏_决策与待办总览.md` | 见 §4-A⑤ |

## §4 待李老师拍板（**共 9 项**，细节源见各条）

### A 组 · M1 结果页转型（**已登记·未动工**，细节源 `review/PLAN_2026-09-24_M1结果页转型_指标对照与整改方向.md` §5）

| # | 待定项 | 为什么关键 |
|---|---|---|
| A① | **分母用哪个营业额口径** | M1 收入是**权责发生制**，但"实收现金"另有口径 ⇒ 占比类指标用哪个做分母，**直接决定评级结果** |
| A② | **整改处方库增删** | 计划里草拟了 6 行（房租/人工/毛利率/能耗/平台费/管理费）。**处方是李老师的经营经验，我编不出来** |
| A③ | **时间窗口径** | 草拟 5 类（人工 1 月 / 能耗 1 月 / 毛利率 1~2 月 / 平台费率 1~3 月 / 房租 6~12 月），每条都带"前提" |
| A④ | **是否也做「趋势」** | 单月对照 vs 多期趋势（本月 vs 上月 vs 近 3 月），**两个工作量级** |
| A⑤ | **与 M1.6 四红线的关系** | M1.6 那条红线**代码零实现**；本计划是否一并落地（决定 §3-4 的处置） |

### B 组 · round111 遗留 4 条（细节源见 `review/NOTE_2026-09-23_round111-*.md` 与 `PLAN_2026-09-23_round111_*`）

| # | 待定项 |
|---|---|
| B① | **易耗品口径**（1.5~2% 那条怎么定） |
| B② | **净利率 5~15% 是否分档** |
| B③ | **水电与燃气是否拆两格** |
| B④ | **「三项刚性」是否引入代码**（round111 已撤回一次承诺，等值定义在权威参数库说明行） |

> 已拍板、**不要再重新讨论**的：**闭店决策模型以后单独做，暂不做成小程序第四个模块**（李老师原话）。

## §5 🔴 接手时必须知道的三件事

1. **M2 锚点的产品红线**：填「预计月营业额」后给出的参考金额**只进 placeholder（灰字起点）、绝不进用户输入值**。
   理由：M2 是前瞻沙盘，参考值来自行业平均；自动填值 ⇒ 用户不核对就得到一份"自证的合理"，**比不给还坏**。
   **这条已有机器守卫**（R128 `tools/check_m2_ref_not_prefill.js`，16 条）—— 改前端时别踩。
2. **round113 的教训（本轮才查明）**：上一轮把参考带"前移到填表页"，但**校验层还卡着"固定支出至少填一项"**
   ⇒ 空表单被拒 ⇒ **新功能真机零可见**，而门禁 100/100 全绿（因为**没有套件跑过"空表单"这条路径**）。
   ⇒ **门禁绿 ≠ 新功能的入口可达**。上线前要有"从零开始点一遍"的路径覆盖。
3. **前端四守卫 + 本轮新增**：R122（`.js` 编译）/ R123（`.wxml` 结构）/ R124（术语键）/ R127（零成本率文案面）
   / **R128（参考值不预填）**。改 `pages/` 或 `terms.js` 时**两侧核对**（`miniprogram/i18n/terms.js` ≡ `specs/dev-specs/i18n/terms.js`，改后 `cp` + `diff -q`）。

## §6 下一轮候选动作（**建议顺序**，都未做）

1. 🔴 **让本轮改动真机可见**：上传体验版（或按李老师要求出预览码）——**否则 M2 锚点等于没上线**。
2. **待拍板 9 项**收敛（§4）——口径没定就改 = 白改一遍，还要重跑门禁 + 重新部署。
3. M1 结果页转型**实现**（口径定了之后，一次性做完）。
4. §3-3 的守卫缺口补面（技术债，无阻塞）。
5. 常规自驱动巡检（若仍有投喂/复核窗口在跑）。

## §7 本轮备份与云端位置

| 件 | 位置 | 自证 |
|---|---|---|
| 仓外 zip（review/ + 仓根一次性脚本 + 工作区记忆 + 云端记忆缓存 + 工作区脚本） | `C:\Users\lzj\WorkBuddy\Claw\_archive\catering-profit_backup_2026-09-24_r114.zip` | 2140 件 · CRC 全通过 · 解压体积**逐字节一致** · 抽验 6/6 md5 一致 |
| 同名清单（**含逐件 md5**） | 同目录 `.list.txt` | 行数 = 文件数 |
| 全仓 git bundle | 同目录 `catering-profit_full_2026-09-24_r114.bundle` | `git bundle verify` → complete history |
| 代码云端 | GitHub `lzj888888/catering-profit` @ `dev` = `ab1474b` | `ls-remote` ≡ `rev-parse HEAD` |
| **记忆云端** | GitHub `lzj888888/workbuddy-memory` @ `main` | 记忆目录本身即 git 仓，SSH 推送；另有每日 23:00 自动同步 |

---

## §8 本文件**没有**做的事（防下一轮误判）

- **没有移动/改名/删除任何文件**（本仓纪律：不改历史；移动会打断历史文本里的旧路径引用）。
- **没有把工作区记忆复制进仓库** —— 记忆的单一源在仓外，复制一份进来就是"同一事实写两遍"（`review/README.md §1`）。
- **没有动 `specs/` 的口径数值**（本轮只登记，不改口径）。
- **没有替李老师做任何口径选择**（§4 九项全部并列呈现，等他拍板）。
- **没有 `git clean` / 瘦身历史**（属另一档授权，需先问）。
