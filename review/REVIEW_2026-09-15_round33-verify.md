# REVIEW_2026-09-15 round33 · 索引补齐终审（40/40）+ R77/R78 复核 + **我对 R77 定性的撤回** + R79/R80

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 33 轮**。**新开文件**（协议 §2）。
> 对象：`051b708`（R77/R78 + `check_handoff_paths`，已在上一轮核过）+ **本份重点 = 未提交的索引补齐批次**（4 文件 + `review/evidence/index_buildout_20260917/` 26 文件 2.18 MB）。
> 本轮**申请放行**两次（git/node/python + 在**未提交状态**下跑门禁与全部守卫）⇒ 结论 **①档**。

---

## §0 结论

**① 未提交批次：准予提交。** 我在**未提交状态下**实测全绿 —— `GATE=0`、`check_schema_sync 64/0`、`check_handoff_paths 11/0`、`check_idempotency 54/0`、`check_selftest_shape 42/42`、**`verify_all 56/56`**、`verify_docx rc=0`；改动面正是你说的 4 文件（`+82/−14`）+ 1 个未跟踪证据目录（**26 文件 / 2.18 MB**）。
**② `051b708`（R77/R78）复核通过**，且**报告目录我这次真的读到了**：10 份日报 + `MEMORY.md`；`2026-09-16.md` **473 行 / 67428 B**、`2026-09-17.md` **87676 B / 758 行 / 17:12:46**（**与你自述逐字一致**）。
**③ 我撤回 R77 的定性**（不是修正，是撤回）：我写的「报告链**事实中断**」「报告 vs 回执**结构性不可核**」**两句都是假的**。详见 §1.3 —— 这是**我的**错误，且正是你新立那条纪律的**实例**。
**④ 新提两条：🔵 R79（新增派生件守卫必须归一 CRLF，否则提交后立刻假红 —— 证据就在我这次 `git status` 的输出里）、🔵 R80（核对脚本没归档：证据进了仓库，产生证据的 7 个脚本还在本机技能目录）**；并对你抛出的盲区给出**裁定**（§2.4）。

---

## §1 R77 / R78 复核与我方撤回

**1.1 R77 的修法 ✅ 复核通过**：`tools/check_handoff_paths.js` **11 通过 / 0 失败**；**P4 解析出真目录** `C:\Users\lzj\WorkBuddy\2026-09-08-22-08-11\.workbuddy\memory` 并**断言其真实存在** ✅；**P4c 要求 4 份活文档都给出可解析绝对路径** ✅ —— 「引用位置必须可解析」这条从此是**机械**的。P1 锚点、诱饵标注（`~\.workbuddy\memory` = 云端画像缓存 / `~\.workbuddy\MEMORY.md` = 跨项目画像）都在文本里 ✅。
**1.2 R78 ✅** 重启键 A6 已补同源第二处 + 双向回指（我 grep 到 `:168` ⑪ 那段）✅。
**1.3 🔴 我撤回 R77 的定性（我自己认错，列冷层留痕）**：
- 我写的事实（三条候选路径的 `Test-Path` 结果）**都对**；但我的**结论错了两处**：① 「报告链事实中断」—— **假**，真目录一直在按日更新；② 「结构性不可核」—— **假**，按绝对路径直读即可核（我这次就读到了）。
- **根因两条，都在我**：**(a) 我没在仓内 grep 路径字符串** —— 一 grep 就到 `新手上云操作手册.md:250` 的绝对路径，而**那行在我提 R77 之前就已存在**（你本轮说它是"扫出的既有副本"，我复核属实）；**(b) 我把两个同名文件当成同一个**：拿 `~\.workbuddy\MEMORY.md`（跨项目画像，35113 B）去比你说的 `13747 B`（那是**本项目** `MEMORY.md`，现 24657 B），并用这个不吻合**支撑**"报告链中断" ⇒ **我自己的纪律 5/12（计数/口径必须限定范围）又犯一次**。
- ⇒ 你新立那条「**不可解析的引用会把『我核不了』升级成『你链断了』**」—— **我就是那个升级者**。**R77 的价值仍在修法**（路径可解析 + 点名诱饵 + 守卫存在性），但**定性部分是我冤枉了你**，此处正式撤回。

---

## §2 索引补齐终审（40/40）

**2.1 我独立核到的（①档，均在仓库内可复现）**：
- `10_单源dump.json` 里我**逐个数了 40 个 `idx_*` 名称**，**含 `idx_audit_idem`** ✅（与 `collections.js` 单源的 40 条一致）；
- 证据包结构齐整：`01_建库日志`、`02_名称核对_25集合`、`03_字段组成核对_15集合`、`04_逐条结果.json`、**15 张 `索引管理_<集合>.png`**、`05_三处报红_人工复核.png`、`06_复合与唯一_实测_shop_monthly_account.png`、`07_加减行图标_第1行加_末行删.png`、`08_sort字段_OCR稳定误读_放大佐证.png`、`09_sort_OCR穷举对照.txt` ✅；
- `09_...txt` 把"12 种读法全部读成 `so戊`、归一化相似度 **0.67 < 0.86**"写成了可复算的台账 ⇒ **白名单不是拍脑袋，是有据的**；且 `_README.md:47-48` 的"唯一性用**像素级绿点**判据（不经 OCR）"这一手我特别认 —— **能绕开 OCR 的地方就别用 OCR**。
- `_README.md:20` 那句自我更正（"初稿写 16 → 13，**汇报层是最后漂移源**"）我认为是**本轮最值钱的一句话**，建议收进重启键 §1.3。

**2.2 白名单属"放宽性设定"，我按纪律 12 核了三件事** ✅：① **不放宽全局阈值**（只放 `idx_plan_enabled.sort←'so戊'` 这一条，逐条点名）；② **命中必须输出点名**（`（1 项走 OCR 变体白名单: …）`）；③ **做了变异回灌**（清空 ⇒ `RC=1` + `❌ 缺字段 sort`）。三件齐了。
**2.3 建议补一条（🔵 小项，与你们 R76 的做法对齐）**：白名单也要**"不腐"** —— 若哪天 OCR 修好、`so戊` 不再出现，该条目应**转成提示要求清理**（而不是静静留着）。你们在 `RESTART_EXEMPTS` 已经这么做了（"仍须命中"），这里照抄即可。
**2.4 对你抛出的盲区（核对单是派生件但不在任何守卫扫描面内）——我的裁定：走方案②（生成器 `--check` 挂 `SUITES`，套件 56 → 57）**，理由与三条硬要求：
- **为什么不是方案①（纳入 S4 扫描面）**：S4 的语义是"文档里**写死的计数** == 实测"，它只能覆盖核对单里的**几处数字**，覆盖不了**逐行内容**；而核对单是**整份派生件**，唯一能覆盖"整份"的做法是**用生成器重算一遍与磁盘比对** —— 这正是本仓已验证的模式（`check_admincore` R50 单源派生、K/K12/K13 派生链）。
- **(a) 必须归一 CRLF/BOM 再比**：**证据就在我这次的 `git status` 输出里** —— 四条 `warning: … LF will be replaced by CRLF the next time Git touches it`（含 `索引补齐核对单.md`）。不归一，**提交后第一次 checkout/clone 就会假红**。K 组已经吃过的教训（重启键 `:34`："比字节前须归一 CRLF"）**照抄即可**。
- **(b) fail-closed**：读不到单源 / 读不到核对单 ⇒ **判红且不下结论**，并打印实际查找路径（照你们 dump 的处理方式）。
- **(c) 变异回灌三件套**：① 改单源不重跑 ⇒ 红；② 重跑生成器 ⇒ 绿；③ **只改行尾 / 末尾加空行 ⇒ 必须保持绿**（这条是"归一在起作用"的反向证据，缺席就等于没验）。
- ⚠️ 套件数 `56 → 57` ⇒ **三处人工面**（`verify_all` 头部注释 + 重启键两处）照旧同步。

---

## §2.5 新发现

### 🔵 R79（新）：新增"派生件守卫"时必须**归一 CRLF/BOM**，否则**提交即刻假红**

- **证据（①档，我本轮实测）**：`git status`/`git diff` 在待提交的 4 个文件上全部打出 `LF will be replaced by CRLF the next time Git touches it`（含 `索引补齐核对单.md`、重启键、`gen_index_checklist.js`、`md2docx_portrait.py`）。
- **为什么值得单列**：核对单现在是**逐字节派生件**，而本仓工作副本是 LF、入库后 git 会转 CRLF ⇒ 若 `--check` 直接 `readFileSync` 比对，**入库后第一次拉取就红**（而"本地全绿"会让人以为没问题）。本仓 K 组早已立过同款纪律（重启键 `:34`：**比字节前须归一 CRLF**），这里只是**新入口复用**。
- **修法**：`--check` 比对前统一 `replace(/\r\n/g,'\n')` + 剥 BOM，并对"行尾差异"补一条**应为绿**的反向变异。**验收**：提交后 `git checkout -- .`（或重新 clone）再跑 `verify_all` ⇒ 必须仍 57/57。

### 🔵 R80（新）：产生证据的**脚本没归档** —— 证据进了仓库，工具还在本机技能目录

- **证据（①档）**：`_README.md:29-34` 写"脚本（本次**已归档**，防 `%TEMP%` 被清）"，但其路径是 `C:\Users\lzj\.workbuddy\skills\win-desktop-control\scripts\examples\cloudbase_index\` —— 我实测**存在且 7 个文件齐全**（`do_indexes3.py` / `verify_indexes2.py` / `verify_fields.py` / `gen_tasks.py` / `probe_attr.py` / `probe_attr2.py` / `README.md`），但**它在仓库外、不随 git 走**。而 `_README.md:3` 声称"**任何人可按 §5 自己复验**"。
- **为什么这与 R77 是同一个病**：你在**同一个理由**下把 dump 归档进了仓库（"`%TEMP%` 里别人机器上不存在 ⇒ 开箱即崩"），但**同一行下面的脚本没做同样处理** ⇒ 换机/换人/半年后复验时，**证据在、工具没了**。
- **修法（10 分钟）**：把那 7 个文件复制进 `review/evidence/index_buildout_20260917/scripts/`（`review/` 已在 `packOptions.ignore` 内 ⇒ **进 git 不进小程序包**，与既有约定一致），并在 `_README.md` 注明"技能目录里的是**工作副本**，仓库内这份是**归档件**"（避免将来成为两份会漂移的副本）。**不必**改 `/tmp` 逻辑（已由 `IDX_DUMP` 依次查找覆盖 ✅）。

---

## §3 执行回执区（WorkBuddy 只追加）

> 本区归 WorkBuddy：**只追加、不改上文**。格式：`- [YYYY-MM-DD HH:MM] R<n> 已落/未落/存疑 · 证据：<命令> → <输出摘要> · commit <sha>`；存疑项写「存疑 + 理由」。

（暂无 —— 待本份被取件后追加）

---

## §4 我自己的登记

1. **R77 定性撤回**（§1.3）：两句结论都是假的；根因 = **没在仓内 grep 路径字符串** + **同名文件相减**。新纪律（我方）：**① 下"某物不存在"之前，先在仓内 grep 它的路径/名字字符串；② 比较两个同名文件前先确认"路径全等"**。
2. **我方流程缺口（我自曝）**：上一轮（`051b708`）我**只在该轮对话里核了、没落 REVIEW 文件** ⇒ 冷层缺一份。本份把 `051b708` 的复核与本轮终审**合并留痕**，下不为例（**核过的东西必须落冷层，否则换会话就"没发生过"**）。

---

## §5 队列

| 类别 | 内容 |
|---|---|
| **待执行侧（提交前）** | ① ✅ 本批次**准予提交**（我已在未提交状态实测全绿）② 提交时**一并做** 🔵 **R80 脚本归档**（7 文件 → `review/evidence/index_buildout_20260917/scripts/`）③ 若采纳 §2.4 裁定（生成器 `--check` 挂 SUITES），**必须**先行归一 CRLF（R79）并配三件套回灌；套件数 56 → 57 三处同步 ④ 🔵 白名单"不腐"提示（§2.3） |
| **待人工（复审方核不了）** | ① **真云三验** —— ⚠️ **前置已满足**（40/40 索引含 `idx_card_code_version` 复合唯一已在线）：多版本写入 / 两月 `calcAmortize` / `smokeTest`；② `ADMIN_SETUP_TOKEN` 的**值**；③ 上线三项（隐私政策 URL【硬阻塞】/ 审核测试账号 / 营业执照商户号）；④ **R45** iOS 过滤 + `wechatide` 授权 |
| **已划掉** | ✅ **索引补齐 40/40**（`1/40` → `40/40`，唯一 10/10）—— 人工项里这一条**可以删了** |
| **提示** | **R72/R73 专审已在 `REVIEW_2026-09-15_round32-verify.md §2` 交付**（13 条理由逐条 + I3 + 分母口径），唯一产物是 R78 ⇒ **不要再挂在"待专审"** |
| **下一步（条件式）** | **若 HEAD 在提交后为 X**，我 round34 核：R79/R80 是否落地、`--check` 若落则跑三件套回灌、并复跑 `check_handoff_paths` + `check_schema_sync` 回归；**若你要我出「真云三验执行单」**（三条测试的逐步操作 + 期望输出 + 失败判读），我可以下一轮直接出。 |
