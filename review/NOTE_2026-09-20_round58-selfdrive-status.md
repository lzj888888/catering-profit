# round58 自驱动巡检 · 2026-09-20 08:54–09:2x

> 仓库 `C:\Users\lzj\WorkBuddy\Claw\catering-profit`。本轮定位：**复核两个从未被本流水线独立验证过的 commit**，
> 并在其中发现并补上第 4 例「判据绑字面」型覆盖缺口。

## 1 · 探针（判据原文落 `review/evidence/selfdrive_20260920_r58/`）

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | SQLite `inflight_turn=0`；`sessions.message_count=1465`（五连持平）；`idle_sec=150561`（41.8h）；末条 approval=`approved_once` 无 pending | **idle，无需干预** |
| dsh | 窗口 `855322` 标题含 `餐饮闭店决策指标模型咨询`；初探 `rect=(-32000,…)` 最小化 → `ShowWindow(9)` 恢复为 `(852,105,1764,924)` | **第 10 轮不可用** |

dsh 不投递的两条**各自独立**的理由：
1. **后端没起**：稳定期截图白屏 **90.8%**（恢复后与 +18s 两采同值，排除"刚恢复未渲染"）；
   `/` 与 `/v1/models` 200（SPA 兜底，无效信号）而 **`/api/sessions`、`/api/health`、`/api/config` 全 404**；
   进程 `pid=3324` 起于 `2026-09-17 03:07:02` ⇒ uptime **≈78.0h**；
   🔑 **`web.log` mtime = 78.10h（≈ uptime，甚至早于进程启动 7 分钟）** ⇒ 该实例自启动起**零业务写入**；
   `api.deepseek.com` 返回 **401**（＝上游可达、仅缺鉴权）⇒ **排除断网与额度耗尽**。
2. **上下文不匹配**：当前会话是「餐饮闭店决策指标模型咨询」，与餐饮小程序仓库复审非同一主题（李老师按主题分区开会话）。
   ⇒ 即使后端健康，投进去也是上下文污染。

## 2 · 并发写入方：无

- 进程级两采（间隔 75s，排除 sheetagent/weixinpay/dsh 三类常驻噪音与自身 PID）⇒ 指向仓内脚本者 **两采皆零** `CONCURRENT_REPO_WRITER=False`。
- 工作树：`?? ` 仅 1033 个 `review/evidence/**` PNG（round41 起长期悬置，待李老师定，不代提交不代删）；
  **`M` = 0，非证据类未跟踪 = 0，近 30 分钟零写入**。
⇒ 本轮可正常写（仍只做路径级提交）。

## 3 · 本轮核心：独立复核两个未经复核的 commit

HEAD 由 `072dcec`(round55) 前进到 `c5519b5`，新增 4 个 commit，其中 **2 个动了代码/守卫**：

| commit | 内容 | 本轮独立结论 |
|---|---|---|
| `691f162` | round56 新增 `tools/check_terms_forbidden.js`（13 词 / 段 9，SUITES 第 68 项） | 🟢 **守卫是真判据**：A1/A2/A3/A4/A5 五组变异全部如期转红并还原回绿，含双向防腐（僵尸豁免 / 缺 by 元信息 / 抽真实豁免） |
| `d6edd7f` | round57 真机反馈：收入口径改回权责发生制 + 录入布局 + 调整 batch8b 的 A1/E1 判据 | 🟡 **E1 口径守卫绑字面**（详见 §4）；代码侧 `input.wxml` 两行布局改动读源码无异常 |

A1/E1 的"放宽"是否属于「放宽守卫迎合代码」？——读源码后判定 **不属于**：
A1 由 `category+label` 改为块内按 `category`（label 是展示文案，锁死会挡住正当改名）；
E1 由锁原文改为"存在非空 + 新增口径守卫"，且**同步补了一条更强的口径守卫**。方向正确，但新守卫本身强度不足（§4）。

## 4 · 第 4 例「判据绑字面」缺口（本轮实锤 + 已加固）

round57 的 E1 口径守卫实为：`必须含「出了餐就算」` 且 `不得含「按当月实际到账金额填写」`——**两条都绑死单一字面**。
独立变异回灌（`mutation_matrix_r58.txt`）证明它**两头都不严**：

| 变异体 | 语义 | 加固前 | 应然 |
|---|---|---|---|
| B1 `收入按当月实际到账金额填写` | 收付实现制（错） | RED ✅ | RED |
| **B2** `收入以当月实际到账金额为准填写，出了餐就算。` | **收付实现制（错）** | **GREEN ❌（漏）** | RED |
| **B3** `收入按业务发生日确认营业收入，不看钱到账没有。` | **权责发生制（对）** | **RED ❌（误报）** | GREEN |
| B4 `填营业额`（<10 字） | 空文案 | RED ✅ | RED |

与 round55 的 `c24e0a2`（守卫绑死 `inventorySwitch|amortizeSwitch` 旧 key 名）同族：**守的是拼写，不是意图**。

**加固**（`tools/selftest_batch8b.js`，只改判据、不动任何被测代码）：改为语义两条腿——
① **权责锚点**：`出了餐|出餐|业务发生日|权责发生|发生日确认` 之一必须出现（fail-closed：拿不出证据=不过）；
② **收付实现制框架**：`(按|以)` + ≤16 非标点字符内含 `实际到账|实际入账|实收金额|收到钱|钱到账` + ≤6 字 + `填|填写|录入`。
   ⚠️ 必须带「指令框架」而非裸扫「到账」二字——**正确文案里本就藏着「不管钱有没有到账」这种否定式提法**，裸扫会误报。加固后 B3 由红转绿正是这条起了作用。

复测（`mutation_matrix_r58_round2.txt`）：BASE 绿 / B1 红 / **B2 红（补住漏）** / **B3 绿（消除误报）** / B4 红 / B5（无任何权责锚点）红，且每条还原后回绿。

## 5 · 门禁（改动前后各跑）

`node verify_all.js` ⇒ **68/68 套件通过，RC=0** ×2；`check_error_codes.js` ⇒ **RC=0**；
`selftest_ad_gates.js` **24/24**；`selftest_ui_fix.js` **28/28**。工作树仅 `tools/selftest_batch8b.js` 一处 `M`。

## 6 · 队列状态（§6 五项）

①A6b ✅ ②R91 F1/F2 ✅ ③AD G1–G8 ✅（24/24）④R86 ✅（42/42，prod 建环境后须重走）⑤14 页走查 ✅。
⇒ **队列仍为空**，本轮正事按「队列清空 ≠ 无活」定式 = 复核未审 commit，已完成。

## 7 · 回执（round58）

- [2026-09-20 08:56] R58-① 已落 · 证据：`probe_agents.py` → InsCode `inflight=0`/`msg=1465`/`idle=150561s`；dsh `rect=(-32000,…)` 最小化 · commit <待填>
- [2026-09-20 09:00] R58-② 已落 · 证据：`git status --short | grep -v evidence` → `M=0`、非证据未跟踪=0；`find -mmin -30` 零命中；进程两采 `CONCURRENT_REPO_WRITER=False` ⇒ 无并发写入方 · commit <待填>
- [2026-09-20 09:07] R58-③ 已落（判据） · 证据：`_dsh_root_r58.py` → 白屏 90.8%（两采同值）、`/api/sessions|health|config` 全 404、进程 uptime 78.0h、`web.log` mtime **78.1h≈uptime**、`api.deepseek.com` 401 ⇒ dsh 第 10 轮不可用 · commit <待填>
- [2026-09-20 09:07] R58-④ **未落 + 理由** · 给 dsh 投复审请求 —— 后端未起（上条）+ 会话上下文为「餐饮闭店决策指标模型咨询」与仓库复审不匹配，投了是上下文污染 ⇒ 不投，改上报李老师四选一 · commit <待填>
- [2026-09-20 09:11] R58-⑤ 已落 · 证据：`mutation_matrix_r58.txt` → A1/A2/A3 全 RED 且还原回绿（面A 注入躺赚 / 面B wxml 硬编码 / 僵尸豁免）⇒ `691f162` 守卫是真判据 · commit <待填>
- [2026-09-20 09:14] R58-⑥ 已落 · 证据：`mutation_matrix_r58_round2.txt` → A4（缺 by 元信息）RED / A5（抽真实豁免）RED，还原回绿 ⇒ 双向防腐有效 · commit <待填>
- [2026-09-20 09:11] R58-⑦ 已落（发现） · 证据：`mutation_matrix_r58.txt` B2→**GREEN**（同义收付实现制说法漏判）/ B3→**RED**（正确权责写法误报）⇒ round57 E1 口径守卫绑字面，与 `c24e0a2` 同族 · commit <待填>
- [2026-09-20 09:14] R58-⑧ 已落（加固） · 证据：`tools/selftest_batch8b.js` 改语义级判据后复测 → BASE 绿 / B1 红 / B2 **红** / B3 **绿** / B4 红 / B5 红，还原回绿 · commit <待填>
- [2026-09-20 09:17] R58-⑨ 已落 · 证据：改 `specs/` 前后各跑 `verify_all.js` → **68/68 RC=0** ×2；`check_error_codes.js` → **RC=0** ×2；AD 24/24；UI 28/28 · commit <待填>
- [2026-09-20 09:17] R58-⑩ **存疑 + 理由** · 1033 个 `review/evidence/**` PNG 归属：round41 起由并发写入方产生，我方未复核其内容即入库风险不明 ⇒ **不代提交不代删**，待李老师裁决 · commit <待填>

## 8 · 待李老师（沿用 + 本轮新增）

1. 🔴 **dsh 四选一**（已 10 轮）：A 放开写+node / B 改只读判据 / C 退场自审 / **D 允许重启 dsh web**（本轮 `web.log` mtime≈uptime ⇒ D 的证据最强）⇒ round39 等 5 份 NOTE 复审仍悬。
2. 🟡 1033 个并发方过程 PNG 待裁决（不代提交、不代删）。
3. 🟡 `TERMS.forbidden` 6 项真实冲突待拍板（否定式「无法盈利」/ 微信官方「订阅消息」/ 免责声明「投资」）。
4. 🟢 建 prod 时提醒：重走 R86 定值与回读 + 替换 `ENV_MAP.prod` 占位符。
5. 🟢 隐私政策按 **6 处**占位符填（旧清单会漏 2 项，直接卡提审）。
