# REVIEW_2026-09-15 round22 · R41(a)/R42/R43/R44 落地复核（41/41 + 两个新守卫评估）

> 复审方：`miniprogram-code-reviewer`（3080 唯一复审窗口）。轮次：**第 22 轮**。**新开文件**（协议 §2）。
> 上游：`REVIEW_2026-09-15_round21-verify.md`；本轮对象 = `6022b6c`/`61dd359`（两份 review 归档）、`052308e`（R44+R43）、`e18c442`（R41a+R42）。

---

## §0 结论

**我 round21 裁决的四项，全部落地，而且两处做得比我要求的强。** 41/41 套件 + 门禁 exit 0；两个新守卫是**真守卫**（不是装饰）。
只有一件小事：**本轮没有追加回执**（第 4 次）—— 动作在 3 个提交里清楚可见，我不判断点，但记一笔（§4）。

| 项 | 结果 |
|---|---|
| 状态 | ✅ `e18c442` = HEAD = `origin/dev`；工作树干净；门禁 **exit 0** |
| 套件 | ✅ `SUITES = 41`；**我逐个跑 41/41** |
| **R43** | ✅ **成对删干净**：`pages/dish/` 不存在、双副本 `terms.js` 的 `dish:{…}` 块均无、`TERMS.dish` 残留 **0**；佐证：`check_requires` 扫描面由 **555 → 554** 个 .js（正好少那个页面文件） |
| **R44** | ✅ 新守卫 `tools/check_pages.js`（**双向**：声明↔文件；四件套 `.js`/`.wxml` 缺一不可）；输出「app.json 声明 12 页，pages/ 下实到 12 页…无孤儿页」 |
| **R41(a)** | ✅ `calcSandbox` 出参改 `margin_rate_ratio`（`service.js:71`，并 round 到 4 位）；selftest 断言 `=== 0.55`；页面 `sandbox/index.js:107` 仍 `×100` 并加了注释说明单位；wxml `{{result.margin_rate}}%` |
| **R41 口径** | ✅ `core/10:86-88` 立三条：`_pct` 恒百分数／`_ratio` 恒比率（**禁止把比率塞进 `_pct`**）／其余语义字段不得带这两个后缀；并**成对登记** `loss_pct`(wire) ↔ `loss_rate`(DB)"两者均为百分数，属层间命名映射" —— **与我裁决的 (a)/(b) 拆法逐条一致** |
| **R42** | ✅ 新守卫 `tools/check_compliance.js`（56 个前端文件）＋契约注 |

---

## §1 两个新守卫的评估（我看了实现，不只是看它通过）

**`tools/check_pages.js`（R44）—— 质量高**
- 头注释直接写清了**根因**（`pages/dish/` 无声明、8 条 i18n 词条只被它消费、K11 不管消费者）—— 把 R43 的教训固化进了守卫本身。
- `ORPHAN_WHITELIST` **当前为空**（只有一行注释示例）⇒ **没有用白名单掩盖孤儿**（这点我特意查了）。
- 失败信息里自带正确修法：「（若确需保留请加进 `ORPHAN_WHITELIST` 并写明理由；**否则连同其专属 i18n 词条一并删除**）」。
- 唯一小提醒：将来若把**组件**放进 `pages/`（如 `pages/_shared/…`），会被判孤儿 ⇒ 到时用白名单 + 理由（机制已备好）。

**`tools/check_compliance.js`（R42）—— 定位要说清：它是"流程守卫"，不是"语义守卫"**
- 机制：前端引用 `auto_subscribe` 必须带 `/* compliance-ok: <理由> */`，否则报错 ⇒ **能防"静默引用"** ✅。
- 但它**证不了**"注释写了理由、iOS 过滤真做了" ⇒ 所以它替代不了实现。**这正是我要的"留痕"**，不是"收紧"。

---

## §2 🔵 R45（新登记）：**iOS 过滤目前"零实现"** —— 已留痕，但上线前要收口

你的守卫头注释自己写明了：SEED_PLANS 里 `plan_auto_subscribe`（19.9/月、`enabled=true`）存在，而 **iOS 过滤零实现**（只存在于 `payCreateOrder` 注释里），当前靠 `utils/paywall.js` 硬编码 `plan_basic_month` 兜着。
- **当前是休眠风险**：前端没有套餐选择 UI ⇒ 没有地方能引用到 `auto_subscribe` ⇒ 守卫自然通过。
- **上线前若做套餐选择 UI（批次 7 的"套餐/导出"相关或后续迭代），必须同时实现 iOS 过滤**，否则 iOS 用户能看到并购买自动订阅（微信/苹果合规）。**要么实现、要么明确"v1.0 不做套餐选择 UI"** —— 二选一写进 `core/11 微信审核自查清单` 或 `§1.3`。
- 我把它登记为 **R45 🔵（已留痕的已知缺口）**，**不阻塞批次 7**。

---

## §3 回执（第 4 次缺席，轻记）

- 事实：`round21` 文件**正文 5408 → 仓库 5408 字符**，**无追加**。
- 我不判为断点：`6022b6c` / `61dd359` / `052308e` / `e18c442` 四个提交把动作写得很清楚。
- 但建议**排查一下 §7 清单为什么没触发** —— 你在协议里新加的那条"提交前自检：追加回执"应当在这个场景生效（本轮改了 6+ 个文件、跨 3 个提交，正是最需要回执的情形）。若清单是"每批投喂后核对"，那它覆盖不到"收尾清理型改动"，建议把适用范围写明。

---

## §4 队列现状

| 类别 | 内容 |
|---|---|
| **待人工** | ① **真云 unique 实测**（同 `card_code` 连存两次 → v1/v2 并存 + 撞唯一键响亮失败）② 真云两月 `calcAmortize` ③ 真云 `smokeTest`（验 R29）④ **上线前补齐 39 条索引**（现 1/39）⑤ **R45**：套餐选择 UI 上线前必须实现 iOS 过滤（或明确不做）⑥ 上传留提审前 |
| **待执行侧** | 无在案项（R41a/R41b留痕/R42/R43/R44 全部已落） |
| **下一步** | **批次 7（喂投包最后 1 批）** |

## §5 能力边界

- 云端（索引实况/数据实况/控制台）、开发者工具、远端 push、`verify_all` 端到端 —— 一如既往核不了（我用"逐个跑 41 个成员"替代）。
- 本轮我对仓库**只读**；核验方式是**源码/守卫实现 + 全量套件 + 残留 grep**，**未逐行审**这 4 个提交涉及的页面与云函数业务逻辑。
