# NOTE · round65 自驱动巡检 —— 功能页面清单口径守卫（同族病第 9 例）

- 时间：2026-09-20 19:30–19:56
- 执行：WorkBuddy（我，门禁/复核方）
- 证据：`review/evidence/page_manifest_20260920/`
- commit：`bdc3b6a`（已推 dev，两方一致）

---

## 0 · 一句话结论

InsCode `idle`（msg=1465 **十二连持平**、idle 52.4h、无待批）⇒ 不投喂；dsh **第 14 轮**不可用且会话上下文不匹配 ⇒ 不投递；
无并发写入方（四连全过）⇒ 本轮可正常写。HEAD `0b28b64` ≡ round64 ⇒ 无未复核新 commit。
队列 ①~⑤ 仍全清 ⇒ 按 §0.10 做**防回潮扫描** ⇒ 抓出**同族病第 9 例**：
**提审材料 §4「功能页面清单」≡ `app.json::pages` 零守卫** ⇒ 新增 `tools/check_page_manifest.js`（R101，17 条断言，SUITES **73 → 74**）。

---

## 1 · 探针（DB / 截图，不采信自述）

### 1.1 InsCode —— `idle`
`inflight_turn` = **0 行**；`approval_audit` 末行 `state='approved_once'`（无 pending）；
`sessions.message_count` = **1465**，与 round51~64 **连续 12 轮同值** ⇒ 真空闲（不是"刚跑完"）。
`idle_sec` = 188620.6（**52.4h**），`model` = `deepseek-v4-flash`。

**动作：不投喂**。理由两条：① 投喂 8 批（0~7）已收官、无「批次 8」；② 本轮正事由我方（门禁方）自己做，
投喂给写码 AI 属越权造活（§6 纪律）。

### 1.2 dsh —— 第 14 轮不可用
窗口标题 = **「餐饮闭店决策指标模型咨询 — DeepSeek Harness」**（不是仓库复审会话）。
`rect` = `-32000` ⇒ `ShowWindow(hwnd,9)` 恢复后截图（`dsh_window_r65.png`，912×819，**白 78.9%**）。

后端侧证（五步全只读）：

| 判据 | 实测 | 读法 |
|---|---|---|
| ① `/` 与 `/v1/models` | 均 200 / 12076B | **SPA 兜底，非健康信号** |
| ② `/api/sessions` `/api/health` `/api/config` | **全 404** | API 路由未就绪 |
| ③ `https://api.deepseek.com` | **401** | 上游可达、仅缺鉴权 ⇒ **排除断网 / 额度耗尽** |
| ④ `node ... dsh/bin.js web` 进程 CreationDate | 2026-09-17 03:07:02 ⇒ uptime **88.4h** | 长跑进程 |
| ⑤ `.dsh/web.log` mtime | 2026-09-17 02:59:56 ⇒ **88.55h ≈ uptime** | **自启动起零业务写入** |

⇒ 「静态层活、业务后端挂」，且⑤把"本轮抽风"这类侥幸解释排除。
**不投递的两条独立理由**：① 后端不可用（①~⑤）；② **会话上下文不匹配**（餐饮闭店决策模型咨询 ≠ 餐饮小程序仓库复审，
投进去属上下文污染）。

上报李老师四选一（round39 起悬置 26 轮，未定）：
A 给 dsh 放开写+node 子进程 / B 改只读判据复审 / C 退场我方自审（违反分权，不推荐）/ **D 允许重启 dsh web（推荐，证据最强）**。
⚠️ 无人值守轮次**不擅自动手重启**（会杀李老师窗口）。

### 1.3 并发写入方 —— 无（四连全过）
① `git status` 已跟踪 `M` = **0**；② 进程两次采样（间隔 70s，排除自身 PID）**HITS 0 / HITS 0**；
③ 门禁跑完（约 3 分钟）回读 mtime 静止；④ `git log` 未见新 commit（HEAD `0b28b64` ≡ round64）。

---

## 2 · 本轮核心：同族病第 9 例

### 2.1 缺口
`specs/dev-specs/上线材料_提审材料包_v1.md` §4「功能页面清单」—— **递交给微信审核的材料**，
明写出处 `app.json::pages`、逐行列了 14 个页面路径。
而 `tools/check_pages.js`（R44，套件 40）只守「`app.json` 声明 ↔ `pages/` 下文件四件套存在」，
**不看提审材料一个字**。

⇒ 加 / 删 / 改一个页面时：`app.json` 与 `pages/` 同步了（R44 绿），但**提审材料 §4 仍是旧清单**
⇒ 审核材料与实际小程序不符（审核问询面），**零守卫、无人发现**。

与 R97「套件数」/ R98「云函数清单」/ R99「隐私占位符」/ R100「红线条数」**完全同构**：
同一事实在多处人工抄写、零机器校验，更正一次只改到一处。

### 2.2 处置
1. 立口径单源：在提审材料 §4 加 `🔢 页面全集口径（唯一声明处）：本节表格共登记 **14** 个页面`。
2. 新增 `tools/check_page_manifest.js`（**R101**，17 条断言 Q1-①~Q9），挂 SUITES **第 74 项**：
   - **Q3** 单源声明 N ≡ `app.json::pages` 实算（双向）
   - **Q4** §4 表格登记路径集合 ≡ `app.json::pages` **逐条双向比对**（只看条数会漏"换了同名数"）
   - **Q5** 每行四字段非空 + 序号连续 1..N
   - **Q6** 全仓当前态全集陈述 ≡ N
   - **Q7/Q8/Q9** 排除面前提、命中数 fail-closed、单源不扩散
3. 同步重启键两处「套件数」人工面 **73 → 74**（R97 首跑当场报警抓出，与我方手改相互印证）。

### 2.3 🔴 首跑误杀 —— 坑⑭ 第五次印证（判据被收窄的实证）
守卫 v1 裸扫「数字+页」，首跑（证据 `guard_v1_firstpass_2falsepositive.txt`）**当场误杀两条合法的不同语义**：

| 位置 | 原文 | 真实语义 |
|---|---|---|
| `core/13:77` | 「1500 条 ≈ **15 页**往返」 | **导出分页的页数**，与小程序页面数无关 |
| `★知识存储点:311` | 「**7 页**内联色」 | **子集**（有内联色的 7 个页面） |

⇒ 改为**「全集语义锚点就近（±30 字符）」两条腿**：锚点（`个页面` / `app.json` / `清单` / `全量` / `真数据` /
`扩展名` / `逐页`）出现在**数字附近**才判红；否则归入**弱面，只打印 ⚠️ 明示、不判红**（宁可少判红，不可误杀）。
⇒ 关键细节：「7 页内联色」那一行**也含 `app.json`**，但离数字 45 字符远 ⇒ 判弱面。**同行即锚会误杀，必须就近。**

### 2.4 双向变异回灌（8 组，全如期）
详见 `evidence/page_manifest_20260920/mutation_matrix.md`。摘要：

- **能抓错 6 组**：M1 声明 14→13 / M3 删 §4 表格末行 / M4 `app.json` 加页 / M6 删口径声明 / M7 扫描面写错 / **M8 子集页码挪近锚点**。
  每条都**点名到具体文件:行 + 具体值**，不是笼统失败。
- **不错杀 2 组**：M2 正确实现换措辞 ⇒ 仍绿（证明不绑字面）；M5 弱面新增无关页数 ⇒ 仍绿。
- **M8 是本轮最要紧的一组**：它证明「锚点就近」这层判据**有机械力、不是恒真**（挪近即转红）。
- 还原：8 组全部 `git checkout --` 成功，末次基线 RC=0（变异前已 `git add`，符合铁律⑩）。

### 2.5 已知覆盖缺口（如实标注，不谎报）
若有人写成「本小程序共 13 页」而全句**无任何全集锚点**，Q6 抓不到，只会出现在 ⚠️ 明示列表里供人读。
**真正的硬判据是 Q3（单源声明）与 Q4（表格逐条双向）。**

---

## 3 · 门禁

| 项 | 改前 | 改后 |
|---|---|---|
| `node verify_all.js` | **73/73** RC=0 | **74/74** RC=0 |
| `node specs/dev-specs/prototype/check_error_codes.js`（A–L） | RC=0 | RC=0 |
| `node tools/selftest_ad_gates.js` | — | 24/24 |
| `node tools/selftest_ui_fix.js` | — | 31/31 |

⚠️ 中途一次 **73/74** 是 R97（套件数口径守卫）报「重启键 73 / 实算 74」——**这是我方手改前的真实漂移**，
不是误报，已按要求同步两处人工面后转绿。另有一次 **R92** 红（新套件未入库即跑）属预期，入 index 后消失。

---

## 4 · 队列（§6）逐条状态 —— 全部维持闭环，无一回潮

| # | 项 | 状态 | 依据 |
|---|---|---|---|
| ① | A6b 代码层兜底 | ✅ 闭环 | HEAD ≡ round64，无新 commit ⇒ 不重跑 |
| ② | R91 F1/F2 UI 缺陷 | ✅ 闭环 | `selftest_ui_fix.js` **31/31** |
| ③ | AD 适配 G1–G8 | ✅ 闭环 | `selftest_ad_gates.js` **24/24** |
| ④ | R86 超时值 | ✅ 闭环（人工面） | round45/46/50 三方一致 42/42 ⇒ 按结论不再回读 |
| ⑤ | 14 页真数据走查 | ✅ 闭环 | 无新 commit ⇒ 不重跑 |

🟡 **溢出观察（round64 记录，未修、未立守卫，待李老师裁决）**：AD-1~AD-26 只有 6 条有机械判据，20 条靠真机 —— 如实标注，未谎报覆盖。

---

## 5 · 回执（格式见 SOUL.md）

- [2026-09-20 19:56] R65-① 探针已落 · 证据：`probe_agents.py` → InsCode `inflight=0`/`msg=1465` 十二连持平、dsh `minimized` 恢复后截图白 78.9% · commit bdc3b6a
- [2026-09-20 19:56] R65-② InsCode 不投喂（已落）· 证据：8 批 0~7 收官、无批次 8 · commit bdc3b6a
- [2026-09-20 19:56] R65-③ dsh 第 14 轮不投递（已落）· 证据：`/api/*` 全 404 + `web.log` 88.55h ≈ uptime 88.4h + 会话「餐饮闭店决策指标模型咨询」上下文不匹配 · commit bdc3b6a
- [2026-09-20 19:56] R65-④ 并发方判定（已落）· 证据：`M`=0 + 进程两采 0/0 + 门禁后 mtime 静止 + `git log` 无新 commit · commit bdc3b6a
- [2026-09-20 19:56] R65-⑤ 同族病第 9 例守卫（已落）· 证据：新增 `tools/check_page_manifest.js` 17 条断言，SUITES 73→74 · commit bdc3b6a
- [2026-09-20 19:56] R65-⑥ 重启键套件数两处同步 73→74（已落）· 证据：R97 首跑报「文档 73 / 实算 74」→ 改后 74/74 RC=0 · commit bdc3b6a
- [2026-09-20 19:56] R65-⑦ 双向变异 8 组（已落）· 证据：`mutation_matrix.md` → 6 红 / 2 绿（含 M8 证明就近锚点非恒真）、还原后 RC=0 · commit bdc3b6a
- [2026-09-20 19:56] R65-⑧ 门禁（已落）· 证据：`node verify_all.js` → 74/74 RC=0；A–L RC=0；AD 24/24；UI 31/31 · commit bdc3b6a
- [2026-09-20 19:56] R65-⑨ 证据落盘并 `ls` 回读（已落）· 证据：`review/evidence/page_manifest_20260920/` 7 个文件 · commit bdc3b6a
- [2026-09-20 19:56] R65-⑩ 提交并推 dev（已落）· 证据：`git status` 空 + `ls-remote` ≡ `rev-parse HEAD` · commit bdc3b6a
- [2026-09-20 19:56] R65-⑪ **未落**：dsh 重启（方案 D）—— 理由：无人值守不动手，会杀李老师窗口，须李老师批准
- [2026-09-20 19:56] R65-⑫ **未落**：1033 个并发方过程 PNG —— 理由：归属待李老师裁决，不代提交不代删（round65 实测仍是 1033 个，仍未动）

---

## 6 · 待下轮

① **李老师四选一**（dsh 放开写+node / 改只读判据 / 退场 / 允许重启 dsh web）—— round39 起悬置 26 轮；
② 并发方 1000+ 过程 PNG 待裁决（**round65 实测仍是 1033 个**）；
③ 建 prod 时提醒重走 R86 定值与回读 + 替换 `ENV_MAP.prod` 占位符；
④ **队列清空 ≠ 无活**：每轮先 `git log` 比对上轮 HEAD；
⑤ 提醒李老师：隐私政策按 **6 处**填（旧清单会漏 2 项，直接卡提审）；
⑥ 新增：Q6 弱面 ⚠️ 明示列表 6 处（含 2 条真误杀已降级），若后续有人把无关页数写进强锚点旁会被抓 —— 属预期行为，非缺陷。
