# NOTE · round66 自驱动巡检 —— 商业化额度口径守卫（同族病第 10 例）

> 本件由 WorkBuddy（执行方）落，供 DeepSeek Harness（复审方）只读复核。
> 证据目录：`review/evidence/quota_limits_20260920/`（8 个文件，已 `ls` 回读）。
> 本件 commit sha：**<待回填>**

## 0. 一句话结论

队列五项无新 commit ⇒ 维持全闭环；本轮正事（队列空 ⇒ 防回潮扫描）抓到**同族病第 10 例**：
**商业化免费/硬上限额度**的收钱口径单源 `checkQuota/service.js` 与 6 份 .md 之间存在漂移，
`core/04_核对清单.md:70` 仍写「M3 成本卡免费上限 **8**」（v1.1 旧值），而同文件 :55/:64/:117 三处写 3、代码也是 3
⇒ **上线前核对清单自相矛盾**。已修陈述 + 立口径单源 + 新增守卫 `tools/check_quota_limits.js`（R102，
SUITES 74→**75**），9 组双向变异全如期。

## 1. 探针（两个外部代理）

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | SQLite `inflight=0`、`message_count=1465`（**十三连持平**）、`idle_sec≈194386`（54.0h）、`last_approval=approved_once` 无 pending | **idle，无待批** |
| dsh（:3080） | 窗口 `hwnd=855322` 最小化 ⇒ `ShowWindow(9)` 恢复为 `(852,105,1764,924)`；OCR 仅读到地址栏 `127.0.0.1:3080`，**无输入框、无正文** | **第 15 轮不可用** |

dsh 侧证五步（全只读，未动 GUI 焦点）：
1. `/` `/v1/models` → 200（**SPA 兜底，非健康信号**）；`/api/sessions` `/api/health` `/api/config` → **全 404**
2. 上游 `https://api.deepseek.com` → **401**（= 可达且仅缺鉴权 ⇒ **排除断网 / 额度耗尽**）
3. 进程 uptime = **90.06 h**（`bin.js web`，自 2026-09-17 03:07 未重启）
4. `.dsh/web.log` mtime = **90.18 h ≈ uptime** ⇒ **自启动起零业务写入**（决定性判据⑤）
5. 会话标题＝「餐饮闭店决策指标模型咨询」⇒ 与仓库复审**上下文不匹配**

⇒ **不投递**（两条独立理由：后端不可用 + 上下文污染）。仍待李老师四选一（推荐 **D 允许重启 dsh web**，无人值守未动手）。

## 2. 并发写入方判定（四连）

| 判据 | 结果 |
|---|---|
| ① 跨分钟 mtime | 仓内近 40 分钟**零新文件**，最新改动 19:54（1.3h 前） |
| ② 进程采样（.py 排除自身 PID，间隔 65s 两采） | **0 / 0** |
| ③ 门禁跑完后回读 mtime | 无变化 |
| ④ `git log` | HEAD `c0714bf` ≡ round65，**无新 commit** |

⇒ **无并发写入方，本轮可正常写**。`M` = 0；`??` 仍是 1033 个并发方过程 PNG + 2 个历史扫描文本（归属待裁决，不代提交不代删）。

## 3. 队列五项复核（无新 commit ⇒ 按 §6 不重跑，实跑抽查）

| 项 | 状态 | 抽查判据 |
|---|---|---|
| ① A6b 代码层兜底 | ✅ 闭环 | 无新 commit，维持 round65 |
| ② R91 F1/F2 UI 缺陷 | ✅ 闭环 | `node tools/selftest_ui_fix.js` → **31/31** |
| ③ AD 适配 G1–G8 | ✅ 闭环 | `node tools/selftest_ad_gates.js` → **24/24** |
| ④ R86 超时值 | 🟢 人工面 | 按 round50 三方一致结论**不再回读**，仅提醒 |
| ⑤ 14 页真数据走查 | ✅ 闭环 | 无新 commit，维持 round65 |

## 4. 本轮核心：同族病第 10 例（商业化额度口径）

### 4.1 实扫发现（不采信任何自述，含我方上一轮）

- **单源**：`cloudfunctions/checkQuota/service.js:17/19`
  `const FREE_LIMIT = { shop: 1, cost_card: 3 };` / `const HARD_LIMIT = { shop: 200, cost_card: 2000 };`
- **代码侧佐证**：`checkQuota/selftest.js:24`「成本卡 3 张（第 4 张将触发）：`hit_free_limit=true`」⇒ 真值 3。
- **漂移点**：`specs/dev-specs/core/04_核对清单.md:70`
  `- [ ] M1 账套（＝**店铺数**）免费上限 1，**月份数不限**；M3 成本卡免费上限 **8**`
  —— 8 是 **v1.1 旧值**（v1.3 用户改 8→3，`★知识存储点:745` 有演进记录）。
- **同文件自相矛盾**：`04:55`「免费版 3 张限额」、`04:64`「M3 3 张」、`04:117`「成本卡上限 **3**」——三处对一处。
- **后果（比排版重）**：`04` 是李老师**上线前逐条勾的验收清单**；照 :70 勾会按「8 张」验收，
  而代码**第 4 张就弹付费墙** ⇒ 验收判据与实现不一致。
- **零守卫**：A–L 与 `verify_all` 74 个套件中，**无一组织商业化额度计数**（已实扫确认：无 `checkQuota` 额度类判据）。

### 4.2 已修（修陈述，不动判据）

1. `core/04_核对清单.md:70`：8 → **3**
2. 立口径单源（`specs/dev-specs/商业化方案_v1.4_融合版.md`，权威终稿表下）：
   `🔢 商业化额度口径（唯一声明处）：M1 账套免费 **1** 个 / M3 成本卡免费 **3** 张 / 底层硬上限 M1 **200** · M3 **2000**`
3. 新增守卫 `tools/check_quota_limits.js`（R102，13 条断言 L1-①~L9），SUITES **74 → 75**
4. 重启键两处套件数 74 → 75（由 **R97 守卫首跑当场报警抓出**，与我方手改相互印证）

### 4.3 判据强度：首版裸扫误杀五类合法口径（坑⑭ 第六次印证）

| 误杀类 | 例子 | 收窄手段 |
|---|---|---|
| 作废历史版整文件 | `OBSOLETE_商业化方案_v1.1` 的「8 张」（v1.1 旧值，文件头已声明作废） | 文件级排除 `EXCL_FILE` + **前提守卫 L1-②**（排除数 ≥1 才绿） |
| 演进链历史值 | `★知识存储点:745`「v1.3 用户改 M3 免费 8→3 张」 | 行级 `HIST` 词放行 + **前提守卫 L7**（放行命中 ≥1） |
| 付费墙触发序号 | `喂投包:591`「3 张免费成本卡（**第 4 张**触发）」 | `ORDINAL` 剥离「第 N 张/个/套」 |
| 云资源「硬上限」 | `core/12_云开发配额与成本测算` 的「硬上限」指**云资源配额**，非商业化额度 | 硬上限模式要求 **M1/M3 类别锚点就近 ≤24/40 字符** |
| 版本号被当额度 | `core/10:176`「免费 / 硬上限配额查询（商业化 **v1.4**）」抓到 4 | 数字加 `(?<![\w.])` + 单位数排除 |

⇒ **「缺守卫」与「已违规」分开报**：修之前**确有一处真实漂移**（04:70 写 8），不是"暂无风险"。

### 4.4 双向变异回灌（9 组，能抓错 + 不错杀）

| # | 变异 | 期望 | 实测 |
|---|---|---|---|
| M1 | `04:70` 改回旧值 8 | 红 | ✅ 转红（L5 点名 `04:70 m3 写 8 ≠ 3`） |
| M2 | 单源 `cost_card: 3→5`，声明不改 | 红 | ✅ 转红（L4-② 缺 `**5**`） |
| M3 | 删唯一声明处标记 | 红 | ✅ 转红（L4-① 0 处，fail-closed） |
| M4 | **另一文件** `core/13` 写「M3 成本卡免费上限 8」 | 红 | ✅ 转红（L5 点名 `core/13:2`） |
| M5 | 单源硬上限 200→300 | 红 | ✅ 转红（L4-②） |
| M5b | 文档硬上限写 300 | 红 | ✅ 转红（**L6 本体**点名 `04:76 写 300`） |
| M6 | 正确实现换措辞「M3 成本卡免费**额度 3 张**」 | **绿** | ✅ 仍绿（不错杀） |
| M7 | 无关计数「备份免费版 7 天」 | **绿** | ✅ 仍绿（不错杀） |
| — | 各组还原后 | 绿 | ✅ 全部回绿 |

证据：`review/evidence/quota_limits_20260920/mutation_matrix.txt`
⚠️ 首轮 M4 报 `MUTATION_NOT_APPLIED` —— 锚点选了 `\r\n# ` 而该文件无第二个 `# ` 行（**不是 CRLF 坑⑨**），改锚点后命中。

## 5. 门禁（全绿）

| 判据 | 结果 |
|---|---|
| `node verify_all.js` | **75/75 套件通过，RC=0** |
| `node specs/dev-specs/prototype/check_error_codes.js`（A–L） | **RC=0** |
| `node tools/selftest_ad_gates.js` | **24/24** |
| `node tools/selftest_ui_fix.js` | **31/31** |
| `node tools/check_quota_limits.js` | **13 通过 / 0 失败，RC=0** |

## 6. 回执

- [2026-09-20 21:05] R66 已落 · 探针：`probe_agents.py` → InsCode idle（msg 1465 十三连持平 / idle 54.0h / inflight 0 / 无 pending）；dsh 最小化 · commit <待回填>
- [2026-09-20 21:09] R66 已落 · 并发方四连：进程两采 0/0 + 近 40min 零新文件 + 门禁后 mtime 静止 + `git log` HEAD `c0714bf` ≡ round65 ⇒ 无并发写入方
- [2026-09-20 21:10] R66 已落 · dsh 侧证五步：`/api/*` 全 404 + 上游 401 + uptime 90.06h + `web.log` 90.18h ≈ uptime + 会话上下文不匹配 ⇒ **不投递**（第 15 轮）
- [2026-09-20 21:12] R66 已落 · 门禁：`node verify_all.js` → 74/74 RC=0（改 specs 前基线）
- [2026-09-20 21:14] R66 已落 · 防回潮扫描①：`git ls-files` 40 个 tmp/debug/probe 命名**全在 `review/evidence/`** ⇒ 合规
- [2026-09-20 21:15] R66 已落 · 防回潮扫描②：78 个判据脚本 vs SUITES 74 引用 → 未挂者 4 在证据目录 / `verify_docx.py` 经 `check_docx_derive.js` 间接覆盖 / `verify_all.js` 是 runner ⇒ **无覆盖缺口**
- [2026-09-20 21:20] R66 已落 · **同族病第 10 例实扫**：`core/04_核对清单.md:70` 写「M3 成本卡免费上限 8」vs 单源 `FREE_LIMIT.cost_card=3` + 同文件 :55/:64/:117 写 3 ⇒ 真漂移
- [2026-09-20 21:24] R66 已落 · 修陈述：`04:70` 8→3 + 立口径单源（`商业化方案_v1.4_融合版.md`）
- [2026-09-20 21:28] R66 已落 · 新增 `tools/check_quota_limits.js`（13 断言）；首跑误杀 5 类合法口径 ⇒ 逐条收窄至 13/13 RC=0
- [2026-09-20 21:35] R66 已落 · 双向变异 9 组全如期（7 红 / 2 绿不错杀），`git checkout --` 还原全部回绿
- [2026-09-20 21:38] R66 已落 · SUITES 74→75 + 重启键两处套件数同步（**R97 首跑报警「文档 74 / 实算 75」抓出**）
- [2026-09-20 21:40] R66 已落 · 门禁 **75/75 RC=0** + A–L RC=0 + AD 24/24 + UI 31/31
- [2026-09-20 21:33] R66 已落 · 证据：`review/evidence/quota_limits_20260920/` 8 文件，`ls` 回读确认
- [2026-09-20 21:4x] R66 **未落** · 1033 个并发方过程 PNG 仍在工作区未提交 —— 归属待李老师裁决，按纪律不代提交不代删（round58 实测 1033 个，本轮同数未动）
- [2026-09-20 21:4x] R66 **未落** · dsh 四选一未定 ⇒ round39 等 5 份 NOTE 复审仍悬
- [2026-09-20 21:4x] R66 存疑 · R86 超时值：按 round50 三方一致结论不再回读 ⇒ 本轮**未实测**，仅提醒（prod 新建须重走定值与回读）

## 7. 待办（交复审方 / 李老师）

1. **李老师四选一**（dsh 放开写+node / 改只读判据 / 退场 / **允许重启 dsh web**）—— 推荐 D。
2. **1033 个并发方过程 PNG** 归属裁决。
3. 建 prod 时提醒：**重走 R86 定值与回读** + 替换 `ENV_MAP.prod` 占位符。
4. 提醒：隐私政策按 **6 处**填（旧清单写 4 处会漏 2 项，直接卡提审）。
5. 🟡 溢出观察（round64 记录，未修未立守卫）：AD-1~AD-26 只有 6 条有机械判据，20 条靠真机 —— 如实标注，未谎报覆盖。
