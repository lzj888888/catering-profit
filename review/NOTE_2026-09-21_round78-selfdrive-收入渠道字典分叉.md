# NOTE · 2026-09-21 round78 自驱动巡检 —— 收入渠道字典分叉（同族病第 16 例，R110）

## 1. 一句话结论

**队列仍空，本轮正事 = 防回潮扫描抓出第 16 例**：收入渠道字典**三处分叉且零守卫**
（前端 `terms.js` 堂食 **7 项** / 后端种子 `SEED_INCOME_ITEMS` 堂食 **5 项** / 规范文档沿用旧口径），
该分叉自 round69 起挂了 **5 轮**靠人记。本轮**不裁定**哪侧为准（属李老师决策），
只把「副本一致」做成硬判、把「堂食分歧」做成**冻结 + 防扩散**（差异 ≡ 在册，双向含僵尸防腐），
新增守卫 `tools/check_income_channel_seed.js`（R110，18 条），SUITES **82→83**，门禁 **83/83 RC=0**，
双向变异 **10 组 0 异常**。

## 2. 探针（硬判据，非印象）

- **InsCode**：`inflight=0`、`message_count=1465`（**二十连持平**）、`idle_sec=269243`（≈74.8h）、
  `model=deepseek-v4-flash`、`permission=auto` ⇒ **idle**。8 批投喂（0~7）已收官、无「批次 8」⇒ **不投喂**。
- **dsh（:3080）**：`verdict=missing`（无标题含 DeepSeek Harness 的窗口）
  ⇒ 按李老师 2026-09-21「暂停参与本仓」之令**不投递**。
- **并发方**：进程级两采（17:56:07 / 17:57:18）**0 / 0** 命中；`git status` 已跟踪面除本轮 4 件外**零改动**；
  门禁后回读 mtime 最新 `terms.js 12:54`（≥5h 前）；`git log` 未见新 commit ⇒ **停手四连通过，恢复写入**。

## 3. 第 16 例：怎么找到的、为什么值得守

沿用 round67 定式（扫「被守卫引用次数」的零覆盖面）⇒ 零覆盖文档 5 份中排除 `OBSOLETE`，
实读 `core/开发规范v1.0_ModuleM3/M1`、`core/11`、`BRANCH_STRATEGY`；
发现其中「免费版 3 张」等额度陈述**其实被 `check_quota_limits.js` 全仓扫 .md 间接覆盖**
⇒ 修正认识：**「文件名被引用次数」只是代理指标，不能直接当零覆盖结论**。

转而核一条挂了 5 轮的已知项（round69 起每轮 NOTE 都写「未决、等李老师裁定」），**回源实扫**：

| 源 | 堂食项数 | 内容 |
|---|---|---|
| 前端 `miniprogram/i18n/terms.js::ledger.income` | **7** | 现金收款、微信扫码收款、支付宝收款、银行卡/POS刷卡、储值卡消费、个人/单位挂账消费、团购/代金券核销 |
| 云函数种子 `cloudfunctions/initDb/collections.js::SEED_INCOME_ITEMS` | **5** | 现金、微信支付宝、储值消费、团购券核销、企业挂账消费 |
| 原型副本 `specs/dev-specs/prototype/init_db.js` | **5** | ≡ 云函数副本（当前一致） |

**后果**：新建账套时 `shop_income_item` 由种子写入 ⇒ 账套内预设渠道名与前端渲染的全集**对不上**
（后端「企业挂账消费」 vs 前端「个人/单位挂账消费」等），导出/对账/按名归并时同名不同 key、异名同义。
前端 G10（渠道清单只认 `terms.js`、后端只回传部分仍补齐全集）**只盖渲染层，盖不住字典本身**。
`tools/selftest_batch8b.js` 守的是前端侧（G1 堂食 = 7 / G10a ≥7），`check_schema_sync`（R74）只看集合与索引
⇒ **后端种子字典的渠道名，全仓零守卫**（`grep -l "SEED_INCOME_ITEMS" tools/` 零命中）。

## 4. 处置（分三档，沿用 round56 的 EXEMPT / DEFERRED 模式）

1. **硬判（不许漂）**
   - 原型副本 ≡ 云函数副本（同一份种子的两处副本，逐项 key+name）
   - 其他业务收入（废品变卖 / 预制菜零售）前端 ≡ 后端 —— 该类别本就一致
2. **冻结 + 防扩散（堂食）**：在 `core/13_上线前查缺补漏_决策与待办总览.md` 立**唯一声明处**，
   登记「前端独有 7 / 后端独有 5」+ 决策行 `DEFERRED by 李老师（待裁定）date 2026-09-21 reason …`；
   守卫做**差异集合 ≡ 在册集合**双向比对，并加**僵尸防腐**（在册条目必须真实存在）。
   ⇒ **任一侧单方面改名/加项/删项即红**，要改必须同步更新登记并写理由 —— 把「靠人记 5 轮」变成机械判据。
3. **明示不判红（弱面）**：外卖前端 4 平台 vs 后端 3 构成项属**不同粒度**（平台名 vs 金额构成项），
   不构成字典分叉 ⇒ 只打印 ⚠️。

> ⚠️ **「缺守卫」与「已违规」分开说**：本例的「违规」是字典分叉（真实存在、待裁定），
> 守卫补上后当前态 = 在册分歧（不判红），**新增/未登记**的分叉才会红。

## 5. 判据（R110，18 条）

| 组 | 断言 |
|---|---|
| C1 | 三处字典源解析成功（fail-closed，解析不到即红）①②③ |
| C2 | 原型副本 ≡ 云函数副本（堂食 / 其他）①② |
| C3 | 其他业务收入 前端 ≡ 后端 ① |
| C4 | 前端独有 ≡ 在册 / 后端独有 ≡ 在册 / 在册无僵尸（双向）①②③ |
| C5 | 在册格式：DEFERRED + 日期 + 理由 ①②③ |
| C6 | 唯一声明处存在（fail-closed）① |
| C7 | 前提守卫：在册非空、扫描面 ≥30 个 .md、无 CJK 八进制转义 ①②③ |
| C8 | 口径单源不扩散 ① |
| W | 弱面已打印（外卖粒度）① |

## 6. 双向变异（10 组，**0 异常**）

| 组 | 变异 | 期望 | 实际 |
|---|---|---|---|
| M1 | 前端堂食加一项（未在册） | 红 | 红 |
| M2 | 云函数种子改名（不在册 + 副本不一致） | 红 | 红 |
| M3 | 原型副本漂移 | 红 | 红 |
| M4 | 其他业务收入前端改名 | 红 | 红 |
| M5 | 删唯一声明处标记（fail-closed） | 红 | 红 |
| M6 | 在册多写一个不存在项（僵尸防腐） | 红 | 红 |
| M7 | 在册决策行换措辞（`DEFERRED（by … · date … · reason …）`） | **绿** | 绿 |
| M8 | 在册后端列表换顺序 | **绿** | 绿 |
| M9 | 外卖（弱面）加平台 | **绿** | 绿 |
| M10 | 另一份 md 出现单源标记（扩散） | 红 | 红 |

> 还原一律 `git checkout --`；变异前本轮改动已全量入 index（`git diff --stat` 为空），规避坑⑩。
> 变异后 `git status` 仅剩本轮 4 件，五份受改文件 CRLF 计数 ≡ LF 计数（无坑⑰/⑳ 行尾污染）。

## 7. 队列复核（§6，逐条实跑，不采信上轮自述）

| 项 | 状态 |
|---|---|
| ① A6b 代码层兜底 | 无新 commit，维持闭环 |
| ② R91 UI 缺陷 | 随门禁 83/83 通过 |
| ③ AD 适配 G1–G8 | 随门禁 83/83 通过 |
| ④ R86 超时值（人工面） | **存疑 · 本轮未实测** —— 按 round46/50 三方一致 42/42 不再回读；建 prod 时须重走定值与回读 |
| ⑤ 14 页真数据走查 | 无新 commit，维持闭环 |

## 8. 回执

- [2026-09-21 18:2x] R78 **已落** · 证据：`node verify_all.js` → **83/83 RC=0**；`node tools/check_income_channel_seed.js` → 18/18；`node specs/.../check_error_codes.js` → RC=0；`_mut_r78.py` 10 组 0 异常 · commit 见 §9
- [2026-09-21 18:2x] R78 **未落** · ① dsh 复审 —— 李老师 09-21 已定暂停参与本仓，且窗口缺失，不投递
- [2026-09-21 18:2x] R78 **未落** · ② InsCode 投喂 —— idle 74.8h 但 8 批已收官，不擅自造新批次（越权）
- [2026-09-21 18:2x] R78 **未落** · ③ 堂食 5 vs 7 的**拉齐方向**需李老师裁定，本轮只冻结不代修
- [2026-09-21 18:2x] R78 **未落** · ④ 坑⑱ 的 4 份前轮守卫仍只扫 index，连续多轮挂待办，本轮仍未代修（避免抢写）
- [2026-09-21 18:2x] R78 **存疑** · ⑤ 零引用的 `core/11`、`BRANCH_STRATEGY`、`开发规范v1.0_M1/M3` 未找到可机械比对的过期计数 ⇒ 未据以造例
- [2026-09-21 18:25] R78 **自伤已修复** · ⑥ 变异后门禁曾 **76/83**（坑⑳ 行尾 + 坑㉔ 取证落仓库根被 R85 自指扫到），两处均已修，复跑 **83/83 RC=0**，详见 §8.5

## 8.5 本轮自伤两次（门禁一度 76/83，已修复，如实记录）

变异后第二次跑门禁 ⇒ **76/83**，7 个套件转红。两处根因**都是我方操作**，不是代码缺陷：

1. **坑⑳ 第二次（行尾）**：变异还原用的 `git checkout --` 在 autocrlf 仓把 **LF** 的
   `miniprogram/i18n/terms.js` 还原成 **CRLF**（+751B）⇒ K11 双副本（另一副本
   `specs/dev-specs/i18n/terms.js` 仍 LF）当场红，连带 batch8-ui-fix / batch8b / batch8c /
   ad-gates / terms-forbidden / suite-assert-counts 六个套件一起红。
   **修法（可复制）**：`git show :<path> > <path>`（index blob，**无** smudge 过滤）字节级回写
   ⇒ 两副本 35812B 逐字节相等 ⇒ `git add` + `git reset -q` 复位 stat 缓存（否则留假 `M`）。
   ⇒ **新定式：变异还原后必核「双副本字节相等」，不能只看 `git status` 干净。**
2. **坑㉔（取证位置）**：我把门禁输出重定向到**仓库根** `_gate_r78.txt`，
   而 R85（`check_stale_claims.js`）扫描面 = `.md/.txt/.js/.json`、**只跳过**
   `.git / node_modules / miniprogram_npm / review` ⇒ 根 `.txt` 在面内，
   且输出里回显了 R85 自己的 S4 防回潮文本 ⇒ **自指判红**。
   ⇒ **新定式：门禁/变异/A–L 输出一律直接写进 `review/evidence/**`，绝不落仓库根。**

修复后复跑门禁 **83/83 RC=0**（证据 `review/evidence/selfdrive_20260921_r78/gate_83_r78.txt`）。

## 9. 给下轮的待办

1. **堂食字典拉齐**：前端 7 项为准还是后端 5 项为准 —— 待李老师裁定后一次改完（含 `core/开发规范v1.0_ModuleA`、`core/02` 数据集里的旧口径名）。
2. `core/13` 现在多了一节口径声明；若裁定拉齐，**必须先更新在册登记**（否则守卫红，这是设计意图，不是故障）。
3. 坑⑱ 4 份前轮守卫扫描面扩工作树（待裁决）。
4. 并发方遗留 1000+ 过程 PNG 与我方 `_mut_r*.py` 等未跟踪件 —— 只增不删，等李老师定。
