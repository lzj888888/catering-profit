# NOTE · 2026-09-22 round82 自驱动巡检（防回潮扫描 → 同族病第 20 例）

- 时间：2026-09-22 00:12–00:5x
- 仓库：`C:\Users\lzj\WorkBuddy\Claw\catering-profit`（HEAD 起 `47f08a5`）
- 角色：WorkBuddy（门禁 / 复核 / 落交付物）

## 1 探针（唯一入口，判据 = DB 与窗口原文，不采信自述）

```bash
python C:/Users/lzj/.workbuddy/skills/self-drive-loop/scripts/probe_agents.py
```

| 代理 | 判据 | 结论 |
|---|---|---|
| InsCode | `inflight=0`；`message_count=1465`（**二十四连**持平）；`idle_sec≈291991`（≈81.1h）；`model=deepseek-v4-flash`；`permission=auto` | **idle** |
| dsh | 「没有标题含 DeepSeek Harness 的窗口」 | **missing** |

⇒ 按技能 §2：InsCode 在跑则不动；本轮 idle 且**无未审产出** ⇒ **不投喂**（8 批 0~7 已收官、无批次 8）。
dsh 窗口缺失 + 李老师 09-21 已定「dsh 暂停参与本仓」 ⇒ **不投递**。

## 2 停手四连（判「有没有并发写入方」，四条全过）

1. **mtime 跨分钟两次静止**：`terms.js` 18:22:56 / `verify_all.js` 22:54:20 / 重启键 22:57:18（00:14 与 00:16 两采一致，最新 22:57 ≥1h 前）
2. **进程双采 0 命中**：`_proc_scan_r82.py`（排除自身 PID + 排除 sheetagent/weixinpay/dsh 三类常驻噪音）00:14:40 与 00:16:42 两采均 `HITS 0`
3. **门禁跑完后再回读 mtime**：无变化
4. **`git log` 无新 commit**：仍 `47f08a5`（≡ round81 收口）

补充：`git status` 的 tracked `M` 数为 **0**（工作树无他人实质改动）；`??` 全是前轮取证/变异脚本的历史遗留，**未代提交、未代删**。

## 3 队列 ①~⑤ 复验（逐条实跑，不采信自述）

- 门禁 `node verify_all.js` → 总览 **86/86 套件通过**、RC=0（改前 85/85 → 改后 86/86）
- `node specs/dev-specs/prototype/check_error_codes.js` → **RC=0**（A–L 全绿，K11 ✅）
- `node tools/selftest_ad_gates.js` → **24/24**
- `node tools/selftest_ui_fix.js` → **38/38**
- ①A6b / ②R91 F1·F2 / ③AD G1–G8 / ⑤14 页走查：无新 commit ⇒ 维持 round80/81 结论，**不重跑**
- ④**R86 超时值**：人工面（需云控制台/CLI 回读）。按 round50 三方一致结论本轮**不回读** ⇒ 记**存疑**

## 4 本轮正事：同族病第 20 例（RATE_LIMITED 触发阈值，零守卫）

### 4.1 怎么找到的——换了个方向

前 19 例都是「**文档侧**声明 vs 实然」。本轮先跑完三路常规防回潮扫描（全干净，见 §5），
零引用面已穷尽（30 份 .md 只剩 `BRANCH_STRATEGY` 与 `OBSOLETE_*`，二者 round81 已排除、**本轮回源实读复核确认排除成立**：
`BRANCH_STRATEGY` 里的 `41/41` 在代码块内是历史 commit 原文；`OBSOLETE_*` 文件头自述作废且已被 R102 文件级排除）。

⇒ 改用**反向扫描**：从代码侧扫 `const [A-Z_]{4,} = <数字>`（阈值型常量），逐条看它在 `specs/` 有无声明、在 `tools/` 有无引用
（`review/evidence/_scan_const_r82.py`）。13 个常量里 **8 个双侧零提及**，其中唯一**进了对外契约文档**的就是这一条。

### 4.2 实扫证据（回源实扫，不采信任何自述）

| 项 | 实然 |
|---|---|
| 代码单源 | `cloudfunctions/common/rateLimit.js`：`WINDOW_MS = 60 * 1000`、`MAX_WRITES = 60` |
| 扁平副本 | 42 份 `cloudfunctions/*/cx_rateLimit.js`，`MAX_WRITES`/`WINDOW_MS` 全仓 **43 处同值**（1 单源 + 42 副本） |
| 文档声明 | `core/09_统一错误码表.md:33`（错误码表 `RATE_LIMITED` 行）、`core/16_后台鉴权规范.md:48`、投喂包:46 |
| 守卫引用 | `grep -rln "MAX_WRITES\|rateLimit" tools/ specs/dev-specs/prototype/ verify_all.js` ⇒ **零命中** |

**后果**：把 `MAX_WRITES` 改成 **600**（限流形同虚设、成本与滥用敞口）或 **6**（正常用户被拒），
这份错误码表仍写 60、`tools/` 无一组织 ⇒ **门禁全绿、无人报警**。
与 round67 第 11 例（集合权限＝越权边界）、round70 第 14 例（锁阈值/token TTL＝鉴权强度边界）同族：**防护性边界口径**。

**「缺守卫」与「已违规」分开报**（round55 纪律）：实扫证明**当前零漂移**（43 处同值、4 处声明均 60）
⇒ 本守卫是**防复发**，不是修缺陷。

### 4.3 落地

- 在 `core/09` §1.1 表格下立**唯一声明处**（次数 + 窗口 + 单源 + 副本同值要求）
- 新增 `tools/check_rate_limit_params.js`（**R113**，18 断言），SUITES **85→86**
- 三处套件数同步（`verify_all.js` 头注 / 重启键 §1.1 入口行 / 「套件数会漂」行**两处**——坑㉕）

判据三条腿：**声明 ≡ 单源实算 ≡ 42 份副本**（双向）+ 全仓当前态声明锚点就近 ≡ N + 单源不扩散；
`WINDOW_MS` 用**算术表达式求值**（只接受 `[\d\s*+/()-]`），故写 `60000` 与 `60 * 1000` 等价（M11 实证）。

⚠️ **裸扫「60」必然误杀**：`60 * 1000`（窗口表达式）、60rpx（字号）、`REMIND_WINDOW_MS` 的 7 天/24h
⇒ 走语义标记 + 锚点就近 ±40（坑⑭/⑯ 第九次印证）。

## 5 防回潮扫描（三路，全干净）

1. **调试残留**：`git ls-files` 命中 `_tmp|tmp_|debug_|_debug|probe_|_probe|bak_` **0**（非证据目录）
2. **未挂 SUITES 脚本**：无新增（`tools/*.js` + `prototype/*.js` 共 55 份，均在 R97 覆盖下）
3. **零引用文档**：30 份 .md 中 2 份，均已排除（见 §4.1）

## 6 双向变异（12 组，0 异常）

`review/evidence/_mut_r82.py`，字节快照回写还原（round80 定式，不动行尾），还原后工作树 ≡ index。

| 组 | 变异 | 期望 | 实际 |
|---|---|---|---|
| M1 | 单源 `MAX_WRITES` 60→600 | 红 | 红（A3-②） |
| M2 | 单源 `WINDOW_MS` 改 30s | 红 | 红（A3-③） |
| M3 | 只改一份副本 60→6 | 红 | 红（A4 点名 adminLogin） |
| M4 | 声明次数 60→120 | 红 | 红（A3-②） |
| M5 | 声明窗口 60000→30000 | 红 | 红（A3-③） |
| M6 | 引用处 core/16 写 30 次/分钟 | 红 | 红（A5-①） |
| M7 | 删唯一声明处 | 红 | 红（A2-① fail-closed） |
| M8 | 扫描面路径写错 | 红 | 红（A1-④，扫空=零覆盖） |
| M9 | 副本文件名写错 | 红 | 红（A1-③） |
| **M10** | **正确实现换措辞**（「60 次 / 分钟」加空格） | **绿** | **绿（不错杀）** |
| **M11** | **单源窗口写等价字面量 60000** | **绿** | **绿（不错杀）** |
| **M12** | **无关口径**（core/16 增补 60rpx 字号） | **绿** | **绿（不错杀）** |

## 7 本轮新坑

- 🆕 **坑㉖：弱面前提守卫的方向正则会写反**。W-① 首版写 `/限流[^。\n]{0,80}(IP|admin_id)/`，
  而 `core/16:48` 的「IP + admin_id」在「限流」二字**之前** ⇒ 首跑即红。
  ⇒ **定式：弱面/前提断言若只是要证明「某描述存在」，用同行双词判定（`some(l => /A/.test(l) && /B/.test(l))`），
  不要写带顺序的跨度正则**；它与「变异打偏」同型，都是**用错了工具表达意图**。
- 🆕 **坑㉗：重启键「套件数会漂」定义行是跨行的**（723–728 共 6 行，`len=6178`）。
  用 `ls[i]` 单行取「链尾」只能拿到 723 行 ⇒ 追加演进链必须**先定位到含 `**85（round81` 的那一行**（实测 728），
  不能按「定义行 +N」推算。与 round61「锚点命中的是引用行（164）而非定义行（723）」是同一族，**第二次踩**。

## 8 待办 / 溢出（不代修、不代裁决）

1. 🟡 **坑⑱ 的 2 份前轮守卫仍只扫 index**（实扫复核：round68 报 4 份，现剩 `check_collection_perms.js`
   与 `check_suite_count_claims.js` 两份；其余两份已被后续轮次或并发方消化）。
   ⇒ 属**技术债、非裁决项**，下轮无并发方时可直接修；本轮未动（避免扩大改动面）。
2. 🟡 前轮溢出仍未决：四红线/红绿灯代码侧零实现（round80）、M3.7 配额指令二选一（round79）、
   `core/11:44` 措辞（round81）、R106 `CAT` 补「小时/秒」映射、dsh 方案 D、
   建 prod 须重走 R86 定值、隐私政策按 6 处填、堂食渠道 5 vs 7 拉齐。
3. 🔴 工作区内 1000+ 个历史取证 `.py`/PNG（`??`）**未代提交、未代删**，等李老师定。
4. 🟢 本轮**无新增需李老师裁决项**（第 20 例当前零漂移，纯防复发）。

---

## 5 回执

- [2026-09-22 00:2x] R82 已落 · 证据：`node verify_all.js` → 总览 **86/86 套件通过**、RC=0 · commit 见文末
- [2026-09-22 00:1x] R82 已落 · 证据：`node specs/dev-specs/prototype/check_error_codes.js` → RC=0（A–L 全绿） · commit 同上
- [2026-09-22 00:1x] R82 已落 · 证据：`node tools/selftest_ad_gates.js` → 24/24；`node tools/selftest_ui_fix.js` → 38/38 · commit 同上
- [2026-09-22 00:4x] R82 已落 · 证据：R113 首跑 17/1（W-① 方向正则写反）→ 修后 **18/18 RC=0** · commit 同上
- [2026-09-22 00:5x] R82 已落 · 证据：双向变异 **12 组 0 异常**，还原后 `git diff --stat` 空 · commit 同上
- [2026-09-22 00:1x] R82 **未落** · ④ R86 超时值（人工面，需云控制台/CLI 回读）→ 按 round50 三方一致结论本轮**不回读**，记**存疑** · commit —
- [2026-09-22 00:1x] R82 **未落** · dsh 复审请求**未投递**（窗口 missing；且李老师 09-21 已定 dsh 暂停参与本仓） · commit —
- [2026-09-22 00:1x] R82 **未落** · InsCode **未投喂**（idle 但 8 批 0~7 已收官、无批次 8 ⇒ 无下一批可投） · commit —
