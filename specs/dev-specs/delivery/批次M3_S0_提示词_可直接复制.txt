═══════════════════════════════════════════════════════════════════
店算 · M3 投喂包 · 批次 S0（地基收口 · 写码方）
主题：把「快照」补完整、把「版本可恢复」和「分类/标签筛选」补齐 —— 为 S1 套餐、S2 外卖铺路
规范依据：
  specs/dev-specs/core/开发规范v1.0_ModuleM3_菜品成本卡.md      （锁定版，M3.3 快照 / M3.4 版本 / M3.7 #7 #10 / M3.10）
  specs/dev-specs/core/开发规范v1.2_ModuleM3增量_*.md            §M3.38（分期修订：P0 之后 = 地基收口）
  review/PLAN_2026-09-26_M3整体搭建方案_按入口反推.md            §5.2 / §6（S0 定义）
═══════════════════════════════════════════════════════════════════

【你要改的仓库】
C:\Users\lzj\WorkBuddy\Claw\catering-profit

🔴 **角色边界（本批次最重要的约束）**
**可以改**：`cloudfunctions/`、`pages/`、`app.json`。
**另有一处规范侧例外（唯一）**：术语双副本 `miniprogram/i18n/terms.js` 与
`specs/dev-specs/i18n/terms.js` —— K11 守卫强制这两份**逐字一致**，而任何新可见文案都必须经它，
所以本批次允许（且要求）你改这两个文件，改完必须 `diff -q` 自证一致。
🔴 **绝对不要改**：`tools/` 下任何文件；`specs/` 下**除上述术语双副本之外**的任何文件。
（守卫与规范由门禁方维护；越界改动会被直接打回。）

⚠️ **不得回退 P0 已落地的任何东西**：原料档案页（`pages/material/*`）、手工录入
（`input_type=2`）、售价低于成本预警、目标毛利率反算入口、分类/标签字段、活动特价。
这些**都已验收通过并在真机跑过**，本批次**不得削弱或删除**。

───────────────────────────────────────────────────────────────────
一、先讲清"为什么本批次排第一"（全部已实扫，行号可核）
───────────────────────────────────────────────────────────────────
P0 把 M3 从"建不出卡"救成了"能建卡"，但**还剩三处"看着有、其实不完整"**：

1. **快照只存了 2 个字段，规范要 7 个。**
   `cloudfunctions/saveCostCard/index.js:269-281` 落库明细行时只写了：
   `material_name`（名称）+ `net_unit_cost`（净料单位成本）。
   而 v1.0 **M3.3「保存快照机制（核心红线）」** 要求把原料当时的
   `brand_spec` / `purchase_unit` / `purchase_price` / `convert_factor` / `yield_rate`
   **一并存快照**（共 7 项）。
   ⇒ 后果：改原料后**无法回溯"当时用的什么规格/单位/进价/出成率"**，
   v1.0 **M3.10「快照完整性校验」** 根本无从实施 —— 这是 M3 红线本体，不是体验优化。
   `cloudfunctions/syncCostCard/index.js:150-158` 有**同样的落库**，两处必须一致。

2. **版本历史只能看，不能恢复。**
   `pages/card/version.js:1` 自述「只读」、`version.wxml:2` 显示 `{{t.versionReadonly}}`，
   页面**没有任何按钮**。v1.0 M3.7 #7「恢复历史版本」= ❌ 无入口。

3. **筛选只有毛利率，没有分类/标签。**
   `pages/card/index.js` 有 `onKeyword`（:98 名称搜索）+ `onMarginFilter`（:99 毛利率区间），
   但**没有** v1.0 M3.7 #10 要求的「按分类、按标签筛选」（`category` / `tags`）。

⇒ 本批次**不改计算引擎、不新增机制**，只把这三处补完整 + 两处小修。

───────────────────────────────────────────────────────────────────
二、任务 1｜明细行快照补 5 字段（🔴 本批次核心）
───────────────────────────────────────────────────────────────────
### 2.1 要补的 5 个字段（连同已有的 2 个 = v1.0 M3.3 要求的完整快照）

| 字段 | 含义 | 来源 | 缺省 |
|---|---|---|---|
| `brand_spec` | 品牌/规格 | 原料档案 `brand_spec` | `''` |
| `purchase_unit` | 采购单位 | 原料档案 `purchase_unit` | `''` |
| `purchase_price` | 采购单价（**分，整数**）| 原料档案 `purchase_price` | `0` |
| `convert_factor` | 换算系数→g | 原料档案 `convert_factor` | `0` |
| `yield_rate` | 出成率 % | 原料档案 `yield_rate` | `0` |

🔴 **金额铁律**：`purchase_price` 落库必须是**整数分**（原料档案里已是分，直接取不要换算）。

### 2.2 改哪几处（**四处，缺一即半成品**）

1. `cloudfunctions/saveCostCard/service.js::buildSnapshotLines`
   —— 构造快照时把这 5 个字段一并带上。
2. `cloudfunctions/saveCostCard/index.js:269-281`
   —— 落库 `shop_cost_card_line` 的对象里加上这 5 个字段。
3. `cloudfunctions/syncCostCard/service.js::rebuildSnapshotLines`
   —— 与 ① **保持同样的字段集**（同步价 = 重取原料最新价，快照字段集必须一致）。
4. `cloudfunctions/syncCostCard/index.js:150-158`
   —— 与 ② 同样加上；**手工行**（`input_type=2`，`:81-86` 那段）**没有原料可查**，
   5 个字段按缺省值存（`''` / `0`），但 `yield_rate` 手工行**有真值就存真值**。

### 2.3 🔴 边界（这是本批次唯一允许碰 `service.js` 的地方，务必守死）

- ✅ **允许**：改 `buildSnapshotLines` / `rebuildSnapshotLines` 这两个**快照构造函数**（只加字段）。
- ❌ **禁止**：碰下面这 4 个**计算具名函数**的一行一字 ——
  `netUnitCostWan` / `lineNetCostYuan` / `calcCostCard` / `wouldCreateCycle`
  （它们分布在 5 份 `service.js` 副本里，门禁 R131 会校验等价性）。
- ❌ **禁止**：改变任何既有字段的取值逻辑（尤其是 `net_unit_cost` 的算法与精度）。

### 2.4 读侧同步（否则存了也取不出来）

`cloudfunctions/getCostCard/service.js::lineToOutput`（:29-37）补出参：
`brand_spec` / `purchase_unit` / `purchase_price` / `convert_factor` / `yield_rate`。
🔴 **老数据 fail-soft**：存量明细行**没有**这 5 个字段 ⇒ 出参给缺省值（`''` / `0`），
**不许报错、不许回填、不许改写存量行**（只 INSERT 不 UPDATE 是 M3 的地基）。

### 2.5 展示（可选，做了更好）
`pages/card/version.wxml` 的历史明细行里，把这 5 项作为**只读灰字**展示出来，
让用户能回答"这道菜当时用的什么料、什么价"。

───────────────────────────────────────────────────────────────────
三、任务 2｜版本历史「恢复」入口（v1.0 M3.7 #7）
───────────────────────────────────────────────────────────────────
`pages/card/version` 页面每个版本卡片加一个**「恢复此版本」**按钮。

🔴 **恢复的语义必须正确**（v1.0 M3.4 / M3.7 #7）：
> 恢复 = **把历史版本的数据 INSERT 成一个新版本**，旧版本**原样保留**。
> ❌ **绝不许** UPDATE 历史行、也**不许**把历史版本改成 `is_latest`
> （本仓生产实现**不用 `is_latest`**，最新 = 该 `card_code` 下 `version` 最大者 ——
> 见 `saveCostCard/index.js:11` 与 `getCostCard/index.js:5`）。

**推荐做法（复用既有链路，别再造一条）**：
拿到历史版本的 `lines` + `loss_rate` + `aux_fen` + `price_fen` + `category` + `tags`，
构造 `card` 对象调 **`saveCostCard`**（带该 `card_code`）⇒ 它会自动生成新版本。
入参契约照抄 `pages/card/edit.js:304-315`（那份已被验收）：
```
{ name, mode, lines, loss_pct, auxYuan, priceYuan, category, tags, activity_price_yuan, card_code }
```
明细行两种写法（`saveCostCard/validate.js:32-65` 已认）：
· 档案行 → `{ material_id, qty }`
· 手工行 → `{ input_type: 2, name, qty, net_unit_cost }`（**快照直传，validate 认**，见 `:47/:54-55`）

🔴 **必须二次确认弹窗**（会覆盖出新版），文案走 `terms.js`，不许硬编码中文。

───────────────────────────────────────────────────────────────────
四、任务 3｜分类 / 标签筛选（v1.0 M3.7 #10 补齐）
───────────────────────────────────────────────────────────────────
`pages/card/index` 现有 `keyword`（名称）+ `margin`（毛利率区间）两道筛，本批次补两道：

1. **分类筛选**：下拉选择 `category`（值来自列表数据里已有的 `category` 去重集合，
   **不要写死枚举** —— 菜品分类是用户自由输入的字符串）。
2. **标签筛选**：下拉选择 `tags`（同理，从数据里去重；`tags` 是字符串，按逗号分隔后取单项去重）。

🔴 **在 `applyFilter()`（`index.js:109` 那段）里统一过滤**，与现有 keyword/margin **是「与」关系**，
不要各写一份过滤逻辑（避免第二份真相源）。
🔴 **前端过滤即可**（菜品量级小，配额上限 200 张），**不要**给 `getCostCard` 新增 `keyword` 入参
（会动契约，本批次不做）。

───────────────────────────────────────────────────────────────────
五、任务 4｜两处小修（顺手带上，各 1~3 行）
───────────────────────────────────────────────────────────────────
1. **复制漏带活动特价**：`pages/card/index.js:186-196` 的 `card` 对象里
   没有活动特价 ⇒ 复制出的卡 `price_promo` 恒为 `0`。
   补一个字段即可（后端 `validate.js:92-94` 认 `activity_price_fen` 或 `activity_price_yuan`，二选一）。
   ⚠️ 注意：`src` 来自 `getCostCard` 出参，**先确认出参里有没有该字段**
   （`getCostCard/service.js::cardToOutput` 目前**没有**暴露 `price_promo`）
   ⇒ 如需，先在 `cardToOutput` 里补 `price_promo` 出参，再在 `onCopy` 里带上。
2. **导出文件类型不对**：`pages/card/index.js:275`
   `fileType: format === 'json' ? 'json' : 'csv'` —— 而 `:263` 传的是 `format: 'excel'`。
   ⇒ 要么让 `downloadContent` 正确映射 `excel`，要么把 `:263` 的 `format` 改成实际能力。
   **别改 `exportData` 云函数**（不在本批次范围）。

───────────────────────────────────────────────────────────────────
六、红线（逐条遵守，违反即打回）
───────────────────────────────────────────────────────────────────
1. **计算引擎一字不改**：`netUnitCostWan` / `lineNetCostYuan` / `calcCostCard` /
   `wouldCreateCycle` 四个具名函数（5 份 `service.js` 副本里）**一行不动**。
   本批次**唯一**允许碰的 service 代码 = 任务 1 的两个**快照构造函数**（且只加字段）。
2. **不新增集合、不新增索引**（`initDb/collections.js` 的 `COLLECTIONS` 仍 25 张、索引仍 40 条）。
   本批次全是**字段级新增**。
3. **只 INSERT 不 UPDATE**：恢复版本、同步价格都靠**生成新版本**，绝不原地改历史行。
4. **金额铁律**：入库一律**分（整数）**；前端展示除以 100 保留 2 位。
   **禁止浮点存金额、禁止字符串传金额**。
5. **R128 不预填**：任何"参考值/行业区间"**只准出现在 placeholder 或灰字**，
   **绝不预填进输入框**。
6. **术语三处同步**（漏一处 ⇒ 页面渲染成**空白**；round108 实际翻车过）：
   ① `miniprogram/i18n/terms.js` 的 `TERMS.card.*` —— 新增键统一挂 **`card`** 命名空间，
      **不新建顶层命名空间**；
   ② `specs/dev-specs/i18n/terms.js` —— **双副本，必须与 ① 逐字一致**（改完 `diff -q` 自证）；
   ③ **每个新页面自己的 `data.t` 块** —— wxml 里用到的 `t.<键>` 必须在页面 js 里显式映射，
      否则取到空串。
7. **页面零硬编码**：文案、阈值、枚举一律取自单源，不写死在 wxml。
8. **新增字号下界 28rpx**（`26rpx` 会被守卫判红）。
9. **错误码必须走 `msgOf()`**：不得把错误码原文直接弹给用户。
10. **遮罩/弹层**：若写遮罩，`inset` **必须四向展开**，用 `inset` 简写会"存不了/点不到"。
11. **写库必须用权威主键 `_id`**，不得用业务 id（真云上 `doc(<不存在的_id>).update()`
    会**静默 0 行且不报错** —— round116 已交过一次学费）。
12. **老数据 fail-soft**：新增字段在存量行上取缺省值，**不许报错、不许回填、不许改写存量行**。

───────────────────────────────────────────────────────────────────
七、自测（交付前请自己过一遍）
───────────────────────────────────────────────────────────────────
- [ ] 新建一张卡（档案行 + 手工行各一行）→ 查 `shop_cost_card_line`，
      明细行**有** `brand_spec`/`purchase_unit`/`purchase_price`/`convert_factor`/`yield_rate`
      且取值 = 保存那一刻原料档案的值。
- [ ] 改原料进价后 → **已有卡的快照不变**（快照隔离没被破坏）。
- [ ] 「同步至最新价」后生成新版本 → 新版本的快照 = **新价**，旧版本仍是**旧价**。
- [ ] 老卡（本批次之前建的，缺 5 字段）打开**不报错**，页面正常显示。
- [ ] 版本历史页有「恢复此版本」按钮 → 点后**新增一个版本**，
      **旧版本全部还在**（数量 +1，不覆盖）。
- [ ] 列表页能按分类、按标签筛选，且与名称搜索、毛利率筛选**能叠加**。
- [ ] 复制一张卡 → 新卡的**活动特价**与原卡一致（不再是 0）。
- [ ] 全仓搜索确认：**没有任何额度字面量**（1/3/5/200/2000）被写进业务代码。

───────────────────────────────────────────────────────────────────
八、本批次不做（不要越界）
───────────────────────────────────────────────────────────────────
· 不做套餐（v1.1 M3.16）· 不做外卖（M3.17）· 不做多规格（M3.15）· 不做词库/模板（M3.20）
· 不做菜品毛利复盘（v1.2 M3.33，存储落法待裁决）
· **不做精度整数化**（round125 §5.2 记的双精度累积偏差）—— 它会动 5 份引擎副本，
  门禁方要先挂 R131 才敢改，排在后面单独一批
· 不做单张导出 PDF（v1.0 #12，优先级低于本批次）
· 不改 `tools/`（守卫）；不改 `specs/`（**除术语双副本外**）
· 不部署（部署由门禁方按 `-r` 铁律逐个执行）

───────────────────────────────────────────────────────────────────
九、已知会变红的门禁项（**看到了不要修**，那是门禁方的活）
───────────────────────────────────────────────────────────────────
· **R101 `tools/check_page_manifest.js`**：本批次**不新增页面**（只在既有页里加控件），
  ⇒ 若它红了，说明你不小心动了 `app.json`，请自查。
· **R131（门禁方本批新挂）引擎 5 份等价**：若你改了 4 个计算具名函数之一，**必然红**。
  正确做法是**别碰它们**；若确实红了，在交付说明里写清楚改了哪一行、为什么。
· **R144（门禁方本批新挂）快照 5 字段完整性**：本批次的目标就是让它绿，
  交付时它应当**是绿的**（若红 ⇒ 你的落库漏了字段）。
· 其它沿用 P0 的既有约束：
  · **R44** 页面四件套齐备；**跳转目标必须在册**
  · **不要新增 `tools/` 下任何文件**（含 selftest）—— 会让"套件数/断言数一致性"类守卫判红
  · **新 `.wxss` 不得引入已下线色值**（旧橘黄 `#ff6b35` 等），色值取 `app.wxss` 单源
  · **`.wxml` 结构**：`<input>` 这类自闭合标签**不得写在属性集合中间**被 `/>` 提前收掉
  · 新增文案**不得含禁用词**（`TERMS.forbidden`），**不得把中文硬编码进 `.wxml`**
═══════════════════════════════════════════════════════════════════
