# 云函数清单与接口契约（锁定版 · 可作写码 AI 输入基线）

> 创建：2026-09-11（补「收口体检」P1-②）
> 用途：单一文档列出**全部云函数 → 入参 / 出参 / 归属批次 / 鉴权**，防止前端（批次 4/7）与后端（批次 1/2/3/5/6）各自生成导致命名/字段错位。
> 配套：与本表同时锁定的 `09_统一错误码表.md`、`prototype/init_db.js`。
> 通用约定：**所有函数经批次 0 鉴权中间件**（除 `initDb`/`payCallback` 见注）；所有写操作带 `client_request_id` 幂等；金额分、时间 Unix 毫秒 UTC、月份 YYYY-MM；返回格式见错误码表。

---

## 0. 通用入参 / 出参约定

- **鉴权注入**：`cloud.getWXContext()` 取 OPENID → 中间件解析出 `user.id` 与 `shop_id`（业务请求体可带 `shop_id`，但**权限主体以 `user_id` 判定**）。
- **标准响应**：`{ code, msg, data }`（详见 `09_统一错误码表.md`）。
- **分页**：列表类入参 `{ page, pageSize }`，出参 `{ list, total }`。
- **首次建档（批次 0 负责）**：用户首次进入、鉴权中间件解析出 OPENID 后，**必须自动建档** `user`（openid→user_id）、一个默认 `shop`、以及 `shop_entitlement`（`user_id` 唯一，`expire_at=0`，视为免费档）。后续 `payQueryEntitlement` 对该记录返回 `isActive:false`，**不要**因查不到而报 `RESOURCE_NOT_FOUND`。

---

## 1. 地基 / 公共（批次 0）

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `initDb` | **一次性**建集合+索引+种子数据（**仅 dev 环境部署**，含 env 门禁；prod 25 张集合由控制台手工建，禁止部署 prod） | 无 | `{ created[], indexes[], errors[] }` | 无（仅 dev 管理员手动运行一次） |
| `auth`（中间件） | 解析 OPENID→user→shop 归属校验 | — | 注入 `ctx.user/id`、`ctx.shopId` | — |
| `utilTime` | 服务端 UTC 时间/月份工具 | `{ op: 'now' 或 'month' }` | `{ ts, month }` | 有 |
| `dataAdapter`（层） | 统一注入软删+字段，供各函数复用 | — | — | — |

---

## 2. M1 月度经营（批次 1 POC3 / 批次 2 摊销）

> ⚠️ **函数名即契约**：下表函数名**必须与各批次喂投指令完全一致**（批次 1 用 `calcMonthlyProfit`、批次 2 用 `calcAmortize`，无 `m1`/`m2`/`m3` 前缀）。前端与后端**按此表同名对接**，inscode 生成代码时函数名以此为准。

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `getMonthList` | 历史月份列表（含归档态） | `{ shopId }` | `{ list:[{month,isArchive}] }` | user+shop |
| `getLedger` | 取某月录入与双利润结果 | `{ shopId, month }` | `{ income[], expense[], inventory, bizRefProfit, fullProfit, grossProfit, grossMargin }` | user+shop |
| `saveLedger` | 保存录入（触发双利润重算） | `{ shopId, month, incomes[], expenses[], directCostFen, inventory?, clientRequestId }` | `{ accountId, bizRefProfit, fullProfit }` | user+shop+幂等 |
| `calcMonthlyProfit` | **纯计算**（Service 层，POC3 锚点，**对齐批次 1 函数名**） | `{ incomesYuan[], expensesYuan[], directCostYuan, inventoryOn, beginInvYuan?, purchaseYuan?, endInvYuan?, amortYuan?, pendingSettleYuan? }` | 见 POC3（收入/费用/毛利/经营参考/全要素/差异） | user+shop |
| `getAmortSchedule` | 摊销台账与某月摊销 | `{ shopId, assetId?, month }` | `{ assets[], rows[] }` | user+shop |
| `saveAsset` | 新增/编辑摊销资产 | `{ shopId, asset:{name,valueFen,startMonth,totalMonths,terminateMonth?}, clientRequestId }` | `{ assetId }` | user+shop+幂等 |
| `calcAmortize` | **纯计算**（Service 层，POC1 锚点，**对齐批次 2 函数名，原契约表缺失已补**） | `{ asset, targetMonth }` | `{ totalAmountFen, details[] }`（对接 `calcMonthlyProfit` 的「当月摊销总费用」入参） | user+shop |

> 锚点：POC3（12/12）、POC1 摊销（多资产独立尾差倒挤）。

---

## 3. M3 菜品成本卡（批次 3 POC2 BOM）

> ⚠️ **函数名即契约**：下表函数名**必须与批次 3「云函数命名约定」完全一致**（无 `m3` 前缀），前端与后端按此对接。

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `getMaterial` | 原料档案（含虚拟半成品） | `{ shopId, isVirtual? }` | `{ list:[{id,name,isVirtual,unitCostFen}] }` | user+shop |
| `saveMaterial` | 新增/编辑原料（改价不影响旧卡） | `{ shopId, material, clientRequestId }` | `{ id }` | user+shop+幂等 |
| `getCostCard` | 成本卡列表/详情（最新版本） | `{ shopId, cardCode? }` | `{ list:[{cardCode,version,lines:[{materialId,netCostFen快照}]}] }` | user+shop |
| `saveCostCard` | 保存成本卡（**只 INSERT 新版本**） | `{ shopId, card:{name,lines:[{materialId,qty,grossLossPct}]}, clientRequestId }` | `{ cardCode, version }` | user+shop+幂等 |
| `syncCostCard` | 同步原料最新价→生成新版本 | `{ shopId, cardCode, clientRequestId }` | `{ newVersion, oldVersion }` | user+shop+幂等 |
| `calcBom` | **纯计算**（Service 层，POC2 锚点，**对齐批次 3 命名约定**） | `{ nodes:[{id,type,children[],qty,grossLossPct,...}] }` | 见 POC2（净料成本/单品总成本/单品毛利/毛利率/反算售价） | user+shop |
| `detectCycle` | 循环引用检测（DFS 深度≤5） | `{ edges:[{from,to}] }` | `{ hasCycle:boolean }`（命中抛 `BOM_CYCLE_DETECTED`） | user+shop |

> 锚点：POC2（宫保 9.75 / 毛利 18.25 / 毛利率 65.18% / 反算 24.38 / 红油 4.48 / 麻辣 12.35 / 同步 12.09 / 循环拦截 / 旧卡快照不变）。

---

## 4. M2 选址盈利沙盘（批次 4 页面 + 本表新增计算函数）

> ⚠️ **函数名即契约**：下表函数名**必须与批次 4 指令一致**（批次 4 明确要求前端调 `calcSandbox`，无 `m2` 前缀）。

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `getPlan` | 方案列表/详情（免费≤3） | `{ shopId, planId? }` | `{ list:[{id,name,fixedTotal,result?}] }` | user+shop |
| `savePlan` | 保存/复制方案（软删 30 天） | `{ shopId, plan, clientRequestId }` | `{ planId }` | user+shop+幂等 |
| `calcSandbox` | **纯计算**（Service 层，POC4/S4 锚点，**对齐批次 4 函数名**） | `{ rentYuan, propertyYuan, laborYuan, otherYuan, includeAmort, simAmortYuan?, varFoodPct, varMktPct, varOtherPct, targetProfitYuan }` | `{ fixedTotal, compositeVarRatePct, marginRatePct, redAlert, breakEvenMonthly, breakEvenDaily, targetMonthly, targetDaily }` | user+shop |

> 锚点：POC4/S4（固定 21,300 / 变动率 45% / 边际 55% / 保本 38,727.27 / 日均 1,290.91 / 目标 66,000 / 日均 2,200；红警≥100%→null）。**v1.0 仅正算，反推（M2.12）排 v1.1**。

---

## 5. 付费（批次 5）

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `payCreateOrder` | 创建支付订单（仅超限/导出触发） | `{ shopId, planId, channel }` | `{ orderNo, payParams }` | user |
| `payCallback` | **微信支付回调**（⚠️ 不鉴权 OPENID，先校验官方签名） | 微信回调原始报文 | **返回微信要求的成功报文**（如 HTTP 200 + 空体或特定 XML/JSON，具体按微信支付文档），**不要**返回业务 `{code:'OK'}` 包装；业务内幂等键 `transaction_id` 去重后更新 `expire_at` | **无**（验签通过后处理） |
| `payQueryEntitlement` | 查当前权限（只读 `expire_at`） | `{ shopId }` | `{ expireAt, isActive }`（**无记录时返回 `expireAt:0, isActive:false`，不报 RESOURCE_NOT_FOUND**） | user |
| `payRenew` | 续费=再次创建订单并由 `payCallback` 累加 `expire_at`；**前端无需直接调用本接口**，如保留则入参为选中的套餐 | `{ planId }` | `{ orderNo }`（引导走支付流程） | user |
| `payExpireNotify` | 到期前 7 天提醒（定时触发） | 无 | `{ notified[] }` | 无（定时任务） |

> 权限解耦铁律：前端**只读 `expire_at` 判定**，不读 `plan_id`；套餐名/价/文案后端下发。iOS 不展示自动订阅，仅一次性套餐。

---

## 6. 管理后台 H5（批次 6，独立 admin 云函数，走 `adminAuth`）

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `adminLogin` | 管理员登录（加盐哈希校验，5 错锁 30 分） | `{ username, password }` | `{ token, role, expiresAt }` | 无（独立） |
| `adminQueryUser` | 查用户/权益 | `{ keyword?, page, pageSize }` | `{ list:[{userId,openidMask,expireAt}] }` | adminAuth |
| `adminGrantEntitlement` | 手动调权（未到期累加/过期从当日） | `{ userId, days, reason, clientRequestId }` | `{ expireAt }` | adminAuth+超管 |
| `adminManualOrder` | 录私域订单（标记已付→自动发权益 `source=manual`） | `{ userId, planId, paidAt, clientRequestId }` | `{ orderNo, expireAt }` | adminAuth |
| `adminOrderList` | 订单管理 | `{ userId?, status?, page, pageSize }` | `{ list:[{orderNo,amount,status,paidAt}] }` | adminAuth |
| `adminRefundMark` | 退款标记（权益回退至退款前） | `{ orderId, reason, clientRequestId }` | `{ ok }` | adminAuth |
| `adminExport` | 数据导出（超管全量/运营订单明细） | `{ scope, format('excel' 或 'json'), clientRequestId }` | `{ fileUrl }` | adminAuth+角色 |

> 留痕：所有 admin 写操作写 `audit_log`（action 前缀 `ADMIN_`），含前后值/操作人/原因。退款 action=`ADMIN_REFUND`。

---

## 7. 中优先级体验（批次 7）

| 函数名 | 用途 | 入参 | 出参 | 鉴权 |
|---|---|---|---|---|
| `shopList` | 多店铺切换器（仅 `is_deleted=false`） | `{ }` | `{ list:[{shopId,name}] }` | user |
| `shopSwitch` | 切换/持久化当前店铺 | `{ shopId }` | `{ ok }`（写本地缓存由前端做） | user |
| `exportData` | 异步导出（不阻塞，进度提示） | `{ shopId, month?, format, clientRequestId }` | `{ taskId, fileUrl? }` | user+`expire_at`判定 |

> 导出付费触发：免费用户导出 → 返回 `FEATURE_LOCKED`，前端弹「开通真实利润」。多店仅保存第 2 家时触发，切换不触发。

---

## 8. 命名与字段一致性红线

1. **函数名即契约**：前端按本表函数名 `callFunction`，**不得自造**。本文档已统一为 `calcMonthlyProfit`/`calcAmortize`/`calcSandbox`/`calcBom`（无模块前缀，与批次 1/2/3/4 指令一致）；M3 另含 `getMaterial`/`saveMaterial`/`getCostCard`/`saveCostCard`/`syncCostCard`/`detectCycle`（对齐批次 3 命名约定）。任何批次生成的函数名若与本文档不符，以本文档为准统一改名。
2. **字段名锁死 + 命名映射**：数据库 / 云函数出入参一律 `snake_case`（`expire_at`、`is_deleted`、`client_request_id`、`shop_id`、金额后缀 `Fen`、时间 `At`），前端 JS/WXML 一律 `camelCase`（`expireAt`、`clientRequestId`、`planId`）；`snake_case ↔ camelCase` 转换**只在 DataAdapter 层或前端适配层**做，业务逻辑/Service 层禁止混用。前后端绑定的字段名必须一致。详见批次 0 §2.9。
3. **主体维度**：权限/订阅/订单主体=`user_id`；`shop_id` 仅业务关联；免费配额按 `shop.is_deleted=false` 计数。
4. **计算下沉**：所有 `*Calc*` 函数为 Service 层纯逻辑，前端**只展示**其返回值，不重算。
