# 04 · 核对清单（开发自检 + 人工验收）

> 交付包文件 4/5
> 创建：2026-09-09
> 原则：**不过不许往下走**。每阶段自检通过 + 22 项验收清单全绿，才算交付完成。

---

## A. 开发自检（按阶段，开发方填写）

### 阶段 0 · 地基
- [ ] 全部数据表已建（6 张锁定表 + 9 张草案表）
- [ ] 所有查询/写入强制带 `shop_id`，复合索引已建
- [ ] `audit_log` 已建，只 INSERT
- [ ] 软删除机制统一（`is_deleted` + `delete_at`，30 天清理）
- [ ] 金额字段全部 INT(分)，无一处用浮点存金额
- [ ] 时间字段全部 Unix 毫秒；记账年月 `YYYY-MM` 字符串
- [ ] 百分比字段 `DECIMAL(5,2)`

### 阶段 1 · POC 三复杂逻辑
- [ ] 摊销：3 条并行各自独立计算，互不干扰
- [ ] 摊销边界"含两端"：开始当月即摊 / 终止当月仍摊 / 到期当月仍摊 / 次月起停
- [ ] 摊销尾差：末月倒挤，总和严格 = 原值
- [ ] 终止残值 → 转"资产处置损失"，不继续摊
- [ ] BOM 最多 2 层，半成品不能引半成品
- [ ] 循环引用检测生效，返回 `BOM_CYCLE_DETECTED`
- [ ] 双口径锁：开库存后经营参考仍用直接填消耗

### 阶段 2 · Module A + M1
- [ ] 收入三分法（堂食 / 平台外卖 / 其他业务）字段齐全
- [ ] 费用四大类（运营 / 人工 / 营销 / 其他）分项正确
- [ ] 团购与外卖佣金**分列**（不合并）
- [ ] 毛利仅扣食材，房租/人工未进营业成本
- [ ] 库存倒轧：期初 + 采购 − 期末
- [ ] 储值双栏：充值不进营收、消费进营收
- [ ] 专业模式重算：弹确认 + 连锁重算后续月 + 留痕 + 历史快照保留
- [ ] 基础模式**不触发**连锁重算
- [ ] 成本红线四项已实现并预警
- [ ] MVP 台账极简：老板只填 1 个"平台实际到账合计"
- [ ] 待结算**未进入**利润计算

### 阶段 3 · M3 菜品成本卡
- [ ] 改卡 = 另存新版本，主表只 INSERT 不 UPDATE
- [ ] 保存即快照，改原料档案**不自动**更新历史卡
- [ ] 手动"同步至最新价格"生成新版，旧版保留
- [ ] 只能编辑 `is_latest=true` 版本，历史版本只读
- [ ] 列表按 `card_id` 去重取最新版
- [ ] 批量预制自动生成虚拟原料（`is_virtual=true`）
- [ ] 虚拟原料 `convert_factor=1`、`net_unit_cost` 存"每份成本"
- [ ] 辅料与原料一并按 (1−损耗率) 放大
- [ ] 循环引用拦截生效
- [ ] 售价反算正确
- [ ] 售价低于成本弹确认
- [ ] 15 项功能全部实现
- [ ] 免费版 5 张限额（按 `shop_id` 统计、按 `card_id` 去重）**读配置**（未硬编码）

### 阶段 4 · M2 选址盈利沙盘
- [ ] 不读取 M1 任何真实数据
- [ ] "从 M1 复制"只复制 4 项固定成本，复制后断开
- [ ] 固定人工成本 = 工资+社保+宿舍+员工餐（不含提成/奖金）
- [ ] 日均固定 ÷30
- [ ] 参数变更实时重算，无需点按钮
- [ ] 变动成本率 ≥100% 红警，不计算负数保本额
- [ ] 免费版 **M3 5 张**限额**读配置**（`feature_permissions.plan_free.limits`）；**M2 无数量限制**（不受 `subscription_plan` 约束、任何路径不弹付费窗）
- [ ] 软删除 30 天

### 阶段 5 · 配额、权限与导出（v1.1 融合版）
- [ ] 所有限额/价格读 `subscription_plan` / `feature_permissions`，**禁硬编码**
- [ ] **权限判定只读 `expire_at`**：业务代码不判断套餐类型 / 价格 / 周期
- [ ] M1 账套（＝**店铺数**）免费上限 1，**月份数不限**；M3 成本卡免费上限 5
- [ ] 达上限"新建"置灰 + 升级提示；**删除后立即释放配额**
- [ ] **M2 全免费**：不限套、无水印、**任何路径都不弹付费窗**
- [ ] 付费弹窗**仅"保存超限 / 导出"两类动作**触发；录入 / 试算 / 进页面**不弹**
- [ ] **后端二次权限校验**：绕过前端弹窗直接调接口 → 被拦截
- [ ] M3 导出（含单张 PDF）**全禁仅付费解锁**；M1 报表导出付费
- [ ] 底层硬上限生效：M1 账套 200 / M3 成本卡 2000（页面不展示）

---

## B. 22 项验收清单（权威，源自 `02_模拟测试数据集.md`）

| # | 验收点 | 场景 | 通过标准 | 实测 | 结果 |
|---|---|---|---|---|---|
| 1 | 收入三分法汇总 | S1 | 收入合计=64,000（堂食43,000/外卖20,000/其他1,000） | | ☐ |
| 2 | 费用子类自动汇总 | S1 | 门店10,300/人工15,000/营销7,240/其他300/全部32,840 | | ☐ |
| 3 | 毛利铁律（仅扣食材） | S1 | 毛利=42,000，房租人工不进成本 | | ☐ |
| 4 | 双利润口径一致 | S1 | 经营参考 == 全要素 == 9,160 | | ☐ |
| 5 | 库存倒轧公式 | S2 | 真实消耗=23,000 | | ☐ |
| 6 | 摊销并行独立 | S2 | 3,333.33 / 1,250 / 100，合计 4,683.33 | | ☐ |
| 7 | **经营参考口径锁** | S2 | 开库存后经营参考仍=9,160 | | ☐ |
| 8 | 全要素 = 经营参考−摊销−库存差 | S2 | 3,476.67 | | ☐ |
| 9 | 摊销到期自动停止 | S2 | 装修 2029-01 起 = 0 | | ☐ |
| 10 | **专业模式重算连锁** | 2-R | 2026-08 全要素→5,476.67 + 弹确认 + 留痕 | | ☐ |
| 11 | 历史快照不可覆盖 | 2-R | 2026-07 原值保留 | | ☐ |
| 12 | 2 层 BOM 成本汇总 | S3 | 红油底料 4.48 / 麻辣香锅 12.35 | | ☐ |
| 13 | 成本只系统算 | S3 | 无直接输入单份成本入口 | | ☐ |
| 14 | 倒推售价 | S3 | 目标毛利 60% → 24.38 | | ☐ |
| 15 | 循环引用拦截 | 3.5 | `BOM_CYCLE_DETECTED` | | ☐ |
| 16 | 成本卡快照不联动 | 3-R | 改原料档案后旧卡仍 9.75 | | ☐ |
| 17 | 刷新 = 另存新版 | 3-R | v2=12.08 生成，v1=9.75 保留 | | ☐ |
| 18 | 沙盘保本营业额 | S4 | 38,727.27 | | ☐ |
| 19 | 沙盘目标利润营业额 | S4 | 66,000 | | ☐ |
| 20 | 沙盘日均 + 实时重算 | S4 | 日均 ÷30；改参实时重算 | | ☐ |
| 21 | **中途终止边界** | 2b | 2026-08 仍摊 333.33、09 起=0；残值转处置损失 | | ☐ |
| 22 | **到期自动停止边界** | 2b | 2026-12 仍摊 500、2027-01 起=0 | | ☐ |

> 容差：金额误差 **≤ 0.01 元** 视为通过。

---

## B2. 付费与权限验收清单（v1.1 融合版新增）

| # | 验收点 | 通过标准 | 实测 | 结果 |
|---|---|---|---|---|
| 1 | **M2 永久免费** | 全功能开放（正算＋反推＋不限套＋可导出），**任何路径都不弹付费窗** | | ☐ |
| 2 | M1 免费限制 | 账套（**店铺数**）上限 1；**月份数不限**；仅「经营参考」口径；禁导出 | | ☐ |
| 3 | M3 免费限制 | 成本卡上限 **5**（按 `shop_id` 统计，按 `card_id` 去重）；导出（含单张 PDF）全禁仅付费解锁；批量导出禁用 | | ☐ |
| 4 | **付费解锁** | 买任一套餐 → M1 账套不限 ＋ **专业模式解锁** ＋ M1/M3 导出全开 ＋ M3 卡数不限 | | ☐ |
| 5 | **专业模式付费墙** | 免费仅「经营参考」；付费后可用全要素＋库存倒轧＋连锁重算 | | ☐ |
| 6 | 时效计算 | 按**自然日**（月 31 / 季 90 / 年 365），续费累加，到期自动停权 | | ☐ |
| 7 | 无买断 | 不存在"一次性买断永久有效"的商品 | | ☐ |
| 8 | 套餐展示 | 阶段 1 **仅展示单次月包 25.9**；季 / 年 / 自动订阅后台预埋隐藏 | | ☐ |
| 9 | 弹窗触发 | 仅「保存超限」「导出」触发；录入 / 试算 / 进页面不弹；关闭后不执行动作 | | ☐ |
| 10 | 后端鉴权 | 绕过前端弹窗直接调接口 → **被后端拦截** | | ☐ |
| 11 | 硬上限 | M1 账套 200 / M3 成本卡 2000 生效，**页面不展示** | | ☐ |
| 12 | 支付预留 | `enable_real_payment=false` 时走私域 ＋ 后台 `source=manual` 发放，权益正常生效 | | ☐ |
| 13 | 解耦 | 业务代码**只读 `expire_at`**；改套餐 / 调价**不改业务代码** | | ☐ |

> 完整商业规则见 `商业化方案_v1.4_融合版.md`（v1.1 历史版已改名 `OBSOLETE_商业化方案_v1.1_融合版.md`，仅作演进追溯，**喂投/写码禁止引用**）；架构见 `01_架构总览.md` 1.8 / 1.8.3 / 1.8.4 / 1.8.5。

---

## C. 红线复核表（一票否决，任一不符即打回）

> 🔢 **红线全集口径（唯一声明处）**：本表共 **12** 条（一票否决，末行为「操作必留痕」）。
> 该数字与本节表格的**逐行一致性**由 `tools/check_redline_inventory.js`（R100）机器校验；
> 其它文档引用本口径时须与此数一致 —— 改表后**只改这一处**，改错或漏跟即转红。

| 红线 | 检查方法 | 不符后果 |
|---|---|---|
| 金额整数分，禁浮点 | 全局搜索 `float` / `double` / `Number(` 参与金额运算 | 全面返工 |
| 计算下沉云函数 | 检查前端代码是否出现利润/成本计算 | 全面返工 |
| 毛利仅扣食材 | S1 毛利 = 42,000 | 核心数据错误 |
| 双口径锁 | S2 经营参考 = 9,160（开库存后不变） | 核心数据错误 |
| 辅料同损耗口径 | S3 总成本 = **9.75**（非 9.73） | 核心数据错误 |
| 成本卡只 INSERT 不 UPDATE | 改卡后查旧版本是否仍可读 | 数据治理失效 |
| 循环引用拦截 | 构造 A→B→A 是否报错 | 数据可能死循环 |
| 待结算不进利润 | 改待结算数，利润是否变化 | 利润虚增/虚减 |
| 限额读配置禁硬编码 | 改 `subscription_plan` 数值，行为是否随之变 | 违反架构铁律 |
| M2/M3 不读 M1 | 清空 M1 数据，M2/M3 是否仍正常 | 隔离失效 |
| 归档不可改 | 归档月尝试修改是否被拦截 | 账目可信度失效 |
| 操作必留痕 | 关键操作后查 `audit_log` | 无法追溯 |

---

## D. 人工验收脚本（你可以照着一步步点）

### 第 1 步 · M1（最重要）
1. 新建门店 → 进入月度核算 → 选 **2026-07**
2. 按 S1 录入收入（堂食 43,000 / 外卖 20,000 / 其他 1,000）
3. 录入成本 22,000、费用四项合计 32,840
4. **看两个数**：经营参考利润 和 全要素真实利润 是否都 = **9,160**
5. 切到 2026-08，**在「月度录入」页底部「这两笔钱怎么算」里把 ① 食材消耗 选成「按库存盘点倒算」**（= 开库存开关；⚠️ 2026-09-20 起开关入口已从「店铺设置」迁到此处），录期初 5,000 / 采购 25,000 / 期末 7,000
6. **关键**：经营参考利润是否**仍是 9,160**（不是 8,160）；全要素是否 = **3,476.67**
7. 录 3 条摊销资产 → 当月摊销合计是否 = **4,683.33**

### 第 2 步 · M3
1. 录入 11 种原料（按 S3.1 单价与出成率）
2. 建「宫保鸡丁」单份卡 → 总成本是否 = **9.75**、毛利率 **65.18%**
3. 建「红油底料」批量卡（产出 10 份）→ 单份 **4.48**，原料档案自动生成虚拟原料
4. 建「麻辣香锅」引用红油底料 → 总成本 **12.35**
5. 改鸡胸肉单价 15→20 → 重开宫保鸡丁，**应仍是 9.75**
6. 点「同步至最新价格」→ 应生成 **12.08** 新版，旧版 9.75 仍在

### 第 3 步 · M2
1. 新建方案 → 录房租 8,000 / 物业 500 / 固定人工 12,000 / 杂费 800
2. 录变动率 35% + 8% + 2%，目标利润 15,000
3. **看**：保本月营收 **38,727.27**、日均 **1,290.91**、目标月营收 **66,000**、日均 **2,200**
4. 随便改一个数字，结果应**立刻变**（不用点计算）

---

## E. 常见错误对照（发现这些数，说明哪里写错了）

| 现象 | 实际值 | 应为 | 错在哪 |
|---|---|---|---|
| S3 宫保鸡丁成本 | 9.73 | **9.75** | 辅料没按 (1−损耗率) 放大 |
| S2 经营参考利润 | 8,160 | **9,160** | 违反双口径锁，用了倒轧消耗 |
| S4 保本日均 | 1,250.00 | **1,290.91** | 按当月实际天数除，不是 ÷30 |
| S2 全要素利润 | 8,160 | **3,476.67** | 摊销没进全要素 |
| 改原料后成本卡变了 | 12.08 | **9.75** | 快照没做 / 做了自动联动 |
| 改卡后旧版本没了 | — | 旧版在 | 用了 UPDATE 而非 INSERT 新版 |
| 待结算大额时利润飙升 | — | 不变 | 待结算错误计入了利润 |

---

## F. 多端适配核对表（安卓 / iOS）· **AD-1~AD-26**（2026-09-10 并入，2026-09-12 校订）

> 结构：F.1~F.5 = **AD-1~AD-21**（原 21 条）；F.6 = **AD-22~AD-26**（二轮复核补充 5 条）。**合计 26 条**。

> 来源：《需求总清单·110条检查清单》第 31/32/33/35/36/37/41/42/43/44/64/65/67/68/69 条 + 虚拟支付费率条款
> **为什么必须有**：原 21 条（F.1~F.5）此前只散在源材料、**未进 01~06 锁定规范**，写码 AI 不会逐条强制过；AD-22~AD-26（F.6）是二轮复核补的 5 个高频坑。现统一固化为写码必过项（覆盖度审计 #8 唯一真缺口）。
> **已在别处覆盖、本表不重复**：金额整数分（01 铁律 1）、时间戳毫秒（01 铁律 3）、软删（01 铁律 10）、配置读表禁硬编码（01 铁律 7）、幂等 `client_request_id`（06 §6.1）。

### F.1 显示适配（双端）

| 编号 | 检查项 | 规范要求 | 验收标准 | 过 |
|---|---|---|---|---|
| AD-1 | 深色模式 | 用 `prefers-color-scheme` 媒体查询，禁止写死白底黑字 | 系统切深色，文字/背景不糊成同色 | ☐ |
| AD-2 | 大字体适配 | 容器用 `min-height`，禁止固定高度截断文字 | 系统最大字体下文字不被切掉 | ☐ |
| AD-3 | 字号/按钮下限 | 字体 ≥ 28rpx，按钮高 ≥ 88rpx，关键按钮醒目色 | 真机实测可读可点（老板 40+ 眼神） | ☐ |
| AD-4 | 日期跳变 | 用 `new Date(year, month, 15, 12, 0, 0)` 取月中固定时刻，禁止直接 `new Date()` 处理年月 | 记账年月不漂移（呼应 01 铁律 3：`YYYY-MM`） | ☐ |

### F.2 输入框 / 键盘（iOS 独有坑）

| 编号 | 检查项 | 规范要求 | 验收标准 | 过 |
|---|---|---|---|---|
| AD-5 | iOS 小数键盘收起 | 金额输入加「确认」按钮或监听 `blur` 自动收起（iOS 小数键盘无「完成」） | iPhone 输入金额能收起键盘、不卡住 | ☐ |
| AD-6 | 输入框防抖 | 搜索/实时计算必须 300ms 防抖；**禁止 input 事件实时调云函数** | 连续输入不暴增云函数调用、不卡页 | ☐ |
| AD-7 | 计算触发方式 | 金额/成本计算改「手动点击触发」，不在每次 input 实时调云函数；**M2 实时试算例外**：走前端本地共用 calculator（calcM2）即时反馈、不每次 input 调云函数，保存时由云函数 Service 权威重算（见 ModuleM2 M2.7） | 双端一致，见 AD-6 | ☐ |

### F.3 弱网 / 断网 / 换机（数据不丢）

| 编号 | 检查项 | 规范要求 | 验收标准 | 过 |
|---|---|---|---|---|
| AD-8 | 弱网本地缓存 | 关键数据本地缓存，断网读缓存 + 提示「离线模式，未同步」 | 弱网打开不全白；恢复网络能同步 | ☐ |
| AD-9 | 换手机同步 | 数据全走云数据库，**非本地 Storage** | 新手机登录同微信，历史数据全在 | ☐ |
| AD-10 | 草稿自动存 | 表单页 `onHide` 自动存 `wx.setStorageSync` 草稿，回来回填 | 填一半退出再进不丢 | ☐ |
| AD-11 | 同步中心页 | 可视化展示本地待同步 / 冲突待解决 | **v1.5+ 规划，v1.0 不做**（体验增强，MVP 不需要，避免提前投入非核心功能） | ☐ |
| AD-12 | 更新机制 | `wx.getUpdateManager()` 检测新版本，弹窗提示重启后 `applyUpdate()` | 发版后用户能感知并应用更新 | ☐ |
| AD-13 | 页面清理 | `onUnload`/`onHide` 清理定时器、监听器、事件订阅 | 不内存泄漏、不串数据 | ☐ |

### F.4 隐私 / 客服 / 反馈（双端合规）

| 编号 | 检查项 | 规范要求 | 验收标准 | 过 |
|---|---|---|---|---|
| AD-14 | 敏感信息单独同意 | 手机号、位置必须弹窗单独同意，不可默认勾选 | 不默认采集 | ☐ |
| AD-15 | 头像昵称填写 | 用 `button open-type="chooseAvatar"` + `input type="nickname"`，**废弃** `wx.getUserProfile`/`getUserInfo` | 保存数据授权走新能力 | ☐ |
| AD-16 | 客服入口 | 加「意见反馈」按钮（`wx.openFeedback()` 或跳客服微信）+ 关键词自动回复 | 用户遇问题有出口 | ☐ |
| AD-17 | 数据可携带 | 提供「导出全部个人数据」JSON + Excel 压缩包 | 满足合规退出 | ☐ |
| AD-18 | 分享场景 | `onLoad(options)` 判断 `options.scene`，分享场景特殊处理 | 分享卡片进得来、不崩 | ☐ |

### F.5 虚拟支付双端费率（收费相关）

| 编号 | 检查项 | 规范要求 | 验收标准 | 过 |
|---|---|---|---|---|
| AD-19 | 接入时机 | **执照办理完成 + 微信支付商户号开通后**才接入；当前阶段 `enable_real_payment=false`，**走私域收款 + 后台 `source=manual` 发权益**过渡（原"2026-04-01 起强制"是旧版规划，与当前"执照下来才接支付"的节奏不符） | 执照+商户号就绪后改配置即接，前端一行不改 | ☐ |
| AD-20 | 费率差 | **iOS 走苹果 IAP 约 12%**（2026 年调整后）；**安卓/鸿蒙微信支付约 0.6%~1%，以实际商户号签约费率为准**（非固定 1%，原表述模糊） | 定价按双端实收测算（iOS 侵蚀大 → 主推年付 / 分端定价） | ☐ |
| AD-21 | iOS 订阅门槛 | ≥ 90 天 + 近 30 天日均 DAU ≥ 1 万 + 认证备案 | 阶段 1/2 不碰 iOS 内购，走私域 + 后台 `manual` 发放 | ☐ |

### F.6 二轮复核补充项（2026-09-10 新增 AD-22~AD-26）

> 原表只覆盖了"iOS 小数键盘收起"和"IAP 费率"，漏了 5 个双端高频坑，二轮复核补齐。

| 编号 | 检查项 | 规范要求 | 验收标准 | 优先级 | 过 |
|---|---|---|---|---|---|
| AD-22 | **键盘遮挡适配** | 输入框聚焦时用 `adjust-position` 或手动滚动保证输入框可见（原表只提了键盘"收起"，漏了"挡住输入框"）；安卓默认适配但须验证 | 键盘弹出时输入框不被遮挡 | 高 | ☐ |
| AD-23 | **iOS 侧滑返回防丢数据** | 表单编辑页监听 `onUnload` 提示「是否保存草稿」，防 iOS 侧滑返回直接丢弃未保存内容 | 侧滑返回有草稿提示，不静默丢数据 | 高 | ☐ |
| AD-24 | **iOS 支付通道降级** | iOS 端一次性套餐**真机实测微信支付可用性**；不可用则引导至安卓端 / 鸿蒙端（**禁止 H5 购买页兜底**，对应商业化 v1.4 §10.7） | iOS 用户付不了钱时有可用兜底路径 | 高 | ☐ |
| AD-25 | **下拉刷新 / 上拉加载一致性** | 统一用微信原生 `pull-down-refresh`，**禁止自定义下拉动画**（双端样式/触发距离差异大，iOS 易卡顿） | 双端下拉表现一致、不卡 | 中 | ☐ |
| AD-26 | **触控反馈一致性** | 按钮/列表点击统一 `wx.vibrateShort` 轻反馈；iOS 关闭系统原生触觉反馈，保证双端一致 | 双端点击手感一致 | 低 | ☐ |

> ⚠️ **AD-1/2/4/5/6/8/9/14/15/19/20/21 + AD-22/23/24** 是 01~06 **明确未写**的硬项，inscode 生成后必须逐条实测，不得只看代码。
> ℹ️ **二轮修正留档**：AD-11 降级为 **v1.5+**（v1.0 不做）；AD-19 改为"执照+商户号开通后接入、当前走私域"；AD-20 费率改为"iOS IAP 约 12% / 安卓鸿蒙 0.6%~1% 以签约为准"。

---

*核对清单 v1.2 · **22 项验收全绿 + 12 条红线全过（§C 表共 12 行，末行为"操作必留痕"） + AD-1~AD-26 多端适配全过** = 允许交付。*
*⚠️ 2026-09-12 校订：原写「11 条红线 / AD-1~AD-21」，漏计 1 条红线（操作必留痕，对应 01 铁律 9）与 F.6 的 AD-22~AD-26（其中 AD-22/23/24 为高危，且属 01~06 未覆盖、只能靠真机实测的项）。*
