# 店算 · 上线前查缺补漏 · 决策与待办总览

> **用途**：把"全链路查缺补漏"清单的五层（上线硬阻塞 → 安全合规 → 工程健壮 → 运营兜底 → 体验隐性）逐项拍板为决策 + 待办，避免只停留在讨论。
> **生成日期**：2026-09-11（基于 8 批喂投包已收官后的二次体检 + 微信审核/云开发配额调研）。
> **配套文档**：`11_微信审核自查清单.md`、`12_云开发配额与成本测算.md`、`10_云函数清单与接口契约.md`、`09_统一错误码表.md`。

---

## 🔴 高优先级：上线硬阻塞（已调研 + 已决策）

### 1. 微信审核规则 — 类目/隐私/注销/虚拟支付
- **类目**：选 `工具 > 记账`（无资质即生效）；企业主体可加 `商业服务 > 一般财务服务`。
- **隐私合规（缺失→必补）**：8 批原未覆盖。已写入批次 7 §2.9 硬门槛：隐私指引配置 + app.json 声明 + 首次弹窗（`onNeedPrivacyAuthorization`/`requirePrivacyAuthorize`）+ 撤回入口。
- **账号注销（原排 v1.1→改为 v1.0 必做）**：《个人信息保护法》要求。已写入批次 7 §2.9：轻量注销入口（≤3 步，后端匿名化 openid/手机号 + 软删店铺/权益）。
- **虚拟支付**：v1.0 保持 `enable_real_payment=false`（私域手动发权益），**彻底绕开 iOS 虚拟支付雷区**。详见 `11_微信审核自查清单.md §4`。
- 落点文档：`11_微信审核自查清单.md`。

### 2. 云开发配额与成本 — 已测算
- 基础版 19.9 元/月支撑 ~4000 活跃用户/月；开发用免费环境、生产转基础版；配 70%/90% 配额告警。
- 落点文档：`12_云开发配额与成本测算.md`。

### 3. 归档触发机制与跨月补录 — 已锁死决策 ✅
- **触发（双轨）**：① 主：**用户手动「结账归档」**（确认某月数据无误后归档）；② 辅：**次月 1 日起软提示**——上月未归档在月份列表标"待归档"徽标（**不强制锁**，仍可编辑）。
- **补录（7 天宽限）**：已归档月默认只读；允许在**归档后 7 天内**补录（需二次确认，保存后重算）；超过 7 天**硬锁**，仅可查看/导出（有权限时），修改须走客服后台。
- 写入：批次 4 §2.6（归档逻辑补全）+ 批次 0（表结构 `is_archive` 字段已存在）。

### 4. 免费配额维度 — 已锁死决策 ✅
- **维度 = `user_id`（全店通用，非按店铺）**：
  - M1：每用户 **1 个免费账套**（全用户仅 1 家免费店，第 2 家触发付费弹窗）；
  - M3：每用户 **3 张免费成本卡**（跨该用户所有店铺共享 3 张）；
  - M2：**永久全免费、方案不限套、永不变现**（与批次 4 一致；原"3 个免费方案"已随 v1.4 作废）。
- 计数仅含 `is_deleted=false`（软删不占额，已由 DataAdapter 统一过滤）。
- 写入：批次 5（付费触发逻辑）+ 批次 7（多店切换：第 2 家仅保存时触发）+ 批次 0（权限主体铁律）。

### 5. 云函数 `timeout` 没有单源钉住 — ✅ **已闭环**（R86，2026-09-18 实测 / 2026-09-19 改完并双轮独立回读）

#### ✅ 闭环判据（2026-09-19 round46 · WorkBuddy 独立回读，非采信对方自述）

- **终值回读**：`cli.bat cloud functions info --project <repo> -e cloud1-d4gphpoxy337f2a25 --names <42 函数>`（只读，44.4s，rc=0）
  ⇒ **42/42 与定值表一致，平台默认 `3` 秒残留 = 0**。
- **定值分布**：`adminExport`=60 / `exportData`=60 / `initDb`=30 / `smokeTest`=15 / 其余 38 个 = 20。
- **证据**：`review/evidence/r86_final_20260919/`（`cli_info_42_raw.txt` 原始表 + `final_readback.json` 逐函数比对 + `README.md`）。
- **交叉验证**：并发会话 round45（16:00，其 `ground_truth_42.json`）与本轮 round46（16:57 我方独立回读）
  **两次独立 CLI 回读结论一致** ⇒ R86 关闭，`initDb`=3s 卡住上线第一步的风险解除。
- ⚠️ **单源缺口仍在**（不阻塞上线，属工程债）：平台不采纳 `config.json::timeout`，值只存在于云端控制台
  ⇒ **新环境（prod）必须重走一遍本 §5 的定值与回读**，别以为"dev 改过就永久生效"。

- **实测事实**（`review/evidence/realcloud_20260917/47_timeout_all.txt`，纯读操作）：
  - 线上 dev **实际只部署了 2 个函数**：`initDb` / `smokeTest`（`cli cloud functions list` 原文）。
  - **两个都是 `timeout=3`**（`cli cloud functions info` 原文，`status=Active` / `Nodejs16.13`）。
- **单源缺口**：全仓只有 1 个 `config.json`（`cloudfunctions/initDb/config.json`），内容只有 `envVariables.DEV_ENV_ID`，**不含 `timeout`**
  ⇒ **3 秒是平台默认值，没有任何单源钉住它**，它不是为这 44 个函数做过的决策。
- **与本仓铁律冲突**：`core/06:410` 写「导出走异步，禁止同步大文件（**云函数 60s 超时铁律**）」；
  本仓是按 **60s** 设计的，线上实际是 **3s**。
- 🔴 **风险（R93 已收窄口径，2026-09-18）**：**不是**「25 集合 + 40 索引 + 种子」——
  ① 建集合是「**已存在即拒绝、不阻断**」⇒ 可重入，重跑不会报错也不会重建；
  ② **40 条索引本来就建不上**（A7 定案：SDK 无 `createIndex`）⇒ 那 40 次调用在云端是**必失败且已被吞**的，不计入耗时主体。
  ⇒ **`initDb` 的真实工作量 = 25 次 `createCollection` + 种子写入**，比原表述小得多，**但不能因此判定 3s 够用**。
  **真实风险表述**：`initDb` 可能**建到一半被杀，而界面仍可能显示成功** —— 与「发现 A（冷启动必撞 3s 但重跑一次能过）」同族：
  **界面/返回值的"成功"只代表调用成功，不代表函数跑完**（本条与 `:55` 那条是同一条纪律的两次命中）。
  ⇒ **验收必须回读**：跑完要**回读 25 张集合清单 + 种子数据是否齐**，**不看返回值、不看界面「测试结果：成功」**。
  ⚠️ 该风险**尚未实测**：`initDb` 已初始化过，"已存在即拒绝"的前置会把重跑直接拦下（这正是那条 fail-closed 设计），
  所以**无法用"重跑一次"测时长**；在没有别的证据前不得写成"已经会失败"。
  🔴 **prod 首次初始化必撞此处**（dev 已初始化过、躲过了；prod 是全新环境，25 次 createCollection 一次都跑不掉）。
- **波及面（口径已更正 2026-09-18）**：可部署云函数 = **42** 个（`cloudfunctions/` 下 44 个目录减去 `common/`、`_adminCore/` 两个
  **非函数目录**；旧文案写的「44 个业务函数」是**目录数**，错）。**42 个已于 2026-09-18 逐个重部署到 dev 全部成功**
  （两轮 42/42，证据 `review/evidence/cloud_probe_20260918/deploy_pass1_42ok.txt`），
  但其 timeout **仍是部署时吃到的 3s 平台默认**（`config.json` 加 `timeout` 实测不被采纳，见下）
  ⇒ `adminExport`/`exportData`/`calcAmortize`/`adminQueryUser` 这类重活都会撞（导出类本应按 60s 铁律走）。
- **处置（三步，先证后改，不要只把 `smokeTest` 调到 10s 就收工）**：
  1. **定值**（2026-09-18 复审 round38 给出，**一次点完，别只改 `smokeTest`**）：

     | 函数 | 建议值 | 理由 |
     |---|---|---|
     | `smokeTest` | ≥10s（建议 **15s**） | 约 10 次串行往返 + 冷启动 567ms |
     | `initDb` | ≥20s（建议 **30s**） | 25 次 createCollection + 种子；**prod 首次初始化必撞** |
     | `adminExport` / `exportData` | **60s** | 对齐 `core/06:410` 铁律；1500 条 ≈ 15 页往返 |
     | `calcAmortize` / `adminQueryUser` | **20s** | 资产多 / 上限 500 条 |
     | 其余 | 默认，或统一 **20s** 写下来即可 | 关键是「**有取值且留痕**」，不是精确到毫秒 |

     ⚠️ **两条纪律**：① **先定值再全量部署**（否则 42 个函数各吃 3s，等于把默认值固化 42 份）；
     ② 点完**逐个 `cli cloud functions info` 回读留证** —— 「设置了 ≠ 生效了」，
     与 `config.json`「随包上传 ≠ 生效」是**同族病**（本仓第五次栽在「存在类判据」上）。
  2. **试单点**：只在一个函数上给 `config.json` 加 `"timeout": <N>`，重发后 `cli cloud functions info` **回读**是否生效。
     ⚠️ 该字段是否被平台接受**尚未证实**，这一步就是去证它。
  3. **二选一**：生效 ⇒ **推广成单源**（合"配置从单源"铁律）；不生效 ⇒ 退在控制台手改，并把最终值补回本表。
- ⚠️ 判"超时值"**只能看 `info` 的 `timeout` 列**；界面「测试结果：成功」只表示**调用**成功，与函数是否跑完无关。

#### ✅ 第二步已实测（2026-09-18 05:2x）：`config.json` 的 `timeout` **不被采纳** ⇒ 只能退控制台

- **实验设计（单点、可回退、dev-only）**：给 `cloudfunctions/smokeTest/config.json` 写 `{"timeout": 20}` → `cli cloud functions deploy -r` 重发 → 回读。
- **执行证据（四个观察点）**：

| 观察点 | 结果 | 出处 |
|---|---|---|
| 部署是否成功 | `success=true` / `13 files` / `packSize=14.6 KB`（上次 14.5 KB） | `cli` 部署输出 |
| 文件是否随包上传 | ✅ 探针 `fsDiag.root` 出现 `config.json` | `review/evidence/realcloud_20260917/48_result_raw_afterdrop.txt` |
| 部署是否生效 | 「最后更新时间」`2026-09-18 03:34:55 → 05:24:52` | 控制台函数列表（点刷新后） |
| **超时值** | ❌ **仍是 3**：`smokeTest \| Active \| timeout 3` | `cli cloud functions info` |
| **超时值（权威面）** | ❌ **仍是 3**：高级配置 → 执行超时 = `3` 秒 | 控制台 → 云函数 → 版本管理 → 「配置」 |

- 🔴 **结论（否定式，附替代路径）**：`cloudfunctions/<fn>/config.json` 里的 `timeout` 字段**不被 IDE CLI 部署通道采纳**
  —— **"该文件随包上传" ≠ "该字段生效力"**。实验文件已**删除还原**（库里不留"看着像生效"的死配置），
  完整留痕见 `review/evidence/realcloud_20260917/47_timeout_all.txt §F`。
- ✅ **唯一可行路径（第三步 · 退控制台）**：
  **控制台 → 云函数 → 选函数 → 版本管理 → 「配置」→ 高级配置 → 执行超时（±秒）→ 确定**。
  取值范围 **1–300 秒**（官方「上传云函数配置」接口 `type=3` 的 `timeout`，单位秒，默认 3 秒）。
  ⚠️ 属**云侧配置变更，必须人工手点**（自动化不代改控制台里的配置值）。
- **待定值（需拍板）**：`smokeTest` ≥10s；**`initDb` ≥20s（建议）**；导出类（`adminExport`/`exportData`）对齐 `core/06:410` 的 **60s 铁律**；其余按实测耗时定。
  **改完必须回读 `info` 的 `timeout` 列复核**（控制台绿字"成功"不算证据）。
- ⚠️ **平台默认运行环境 = `Nodejs16.13`**（`info` 与「配置」对话框两处一致）：该版本已 EOL，是否随上线一并评估**另立待办**，本项不掺进来。

### 6.（✅ 已闭环 2026-09-19 · round39）写码侧 · 快马：`saveCostCard` 的 `mode` 无白名单 —— R81，**曾挂 4 轮**

- **原始定义**：`review/REVIEW_2026-09-15_round34-realcloud-sheet.md §4.5`（三处证据逐字：`saveCostCard/validate.js:31`、`saveCostCard/service.js:55`、`saveCostCard/index.js:167`）。
- 🔴 **风险**：当前**唯一"已知会静默算错成本"的开口**。调用方传 `"b"`（小写）/`2`（照库里的口径）/`"C"`/`""`/缺失
  ⇒ **静默当成 A**：不生成虚拟半成品、忽略 `batch_output`、落库 `calc_mode:1` ⇒ **成本语义悄悄变错，且无任何报错**。
- **为什么必须修**：同文件对越界 `loss_pct`、缺失 `batch_output` 都是**响亮拒**；本仓 R27/R30/R32/R35 一路纪律是
  「涉金额的模糊入参一律响亮失败」—— 这里是**唯一例外**；且是**同一语义两份实现**（`validate` 与 `service` 各兜一次），
  正是 R62/R72/R73 反复清过的那类病。
- **修法（三处）**：① `validate.js:31` 改白名单：非 `"A"`/`"B"` ⇒ `err('card.mode 必须是 "A" 或 "B"')`；
  ② `service.js:55` 改**断言式**（非法 `mode` 抛 `INVALID_PARAM`，不再兜底 A）；③ 补 **4 条断言**：`mode:'b'` / `mode:2` / `mode:'C'` / `mode:''` ⇒ **`INVALID_PARAM`**（当前会静默通过）。
- **验收（可证伪）**：4 条断言转绿 **+ 变异回灌**（把白名单退回原来的三元兜底 ⇒ 这 4 条必须转红）。
- **状态**：round34 提出 → round36/37/38 三轮均记「未落（排队）」⇒ **2026-09-19 round39 已修复（挂 4 轮后闭环）**。
- ✅ **落地明细（2026-09-19，共 8 处）**：
  ① `saveCostCard/validate.js:31` → 白名单（非 `'A'`/`'B'` ⇒ `INVALID_PARAM`，含**缺失**）；
  ② `calcBom/validate.js:41` → 同口径白名单（防「预览按 A 算、保存按 B 存」的口径分裂）；
  ③–⑤ 三份成本引擎副本 `calcBom/service.js`·`saveCostCard/service.js`·`syncCostCard/service.js`
  的 `mode` 行 → **断言式**（非法值 `throw {code:'INVALID_PARAM'}`，不再兜底 A）；
  ⑥–⑧ 三处调用点（`calcBom/index.js`·`saveCostCard/index.js`·`syncCostCard/index.js`）包 `try/catch`
  把引擎抛错**透传为 `INVALID_PARAM`**，避免退化成 `SYSTEM_ERROR`。
- ✅ **断言 19 条**（`saveCostCard/selftest.js` ⑥段 14 条 + `calcBom/selftest.js` R81 段 11 条… 合计去重后
  **入口 4 越界 + 缺失 + 4 反向 = 10 条；引擎 4 越界 + 缺失 + 3 反向 = 8 条**，两文件各含一条 `mode 缺失`）：
  越界样本 `'b'` / `2` / `'C'` / `''` / 缺失 ⇒ `INVALID_PARAM`；反向证据 `'A'`/`'B'` 必须放行（防"过度收紧"）。
- ✅ **变异回灌已做**：把四处白名单条件改回 `false`（等价旧的三元兜底）⇒ **19 条断言转红、两个 selftest RC=1**；
  还原后 `saveCostCard` 29/29、`calcBom` 40/40、`syncCostCard` 4/4 全绿；套件 64/64、门禁 A–L RC=0。
- ⚠️ **前端不受影响（已核）**：`pages/card/edit.js:71` 的 `calcMode` 取自 `getCostCard` 出参，
  而 `getCostCard/service.js:13` 已把 DB 的 `calc_mode(1/2)` 映射成 wire 的 `'A'/'B'` ⇒ 前端**不会**传 `2`。
  （若将来有人改成直读 DB 字段，会立刻被白名单拦下——这正是本修复的价值。）

### 7.（✅ 已闭环 2026-09-18）真机/模拟器走查 —— 2 条缺陷 + 3 条排版优化（R91）

> **状态（2026-09-19 回写）**：下面 F1/F2a/F2b/L1/L2/L3 六项**已全部修复并经独立核验 6/6 通过**，
> 证据 `review/evidence/ui_fix_20260918/`（`verify_after.json` + 5 张修复后截图 + `verify_ui_fix.js` 可复现脚本），
> 静态自测 `tools/selftest_ui_fix.js`（38 条，已挂 `verify_all.js` 的 SUITES `batch8-ui-fix` 一档）。
> ⚠️ 此处**不写套件序号**：序号随 SUITES 增删整体平移（本处曾写「套件 59」，如今已不对应任何一条）⇒ 用**展示名**引用，防再次漂移。**本节保留原始走查记录供追溯，不再视为待办。**

- **动作**：走开发者工具自动化（`cli auto --auto-port 9420` + `miniprogram-automator`），对 `app.json` 里 **14 个页面**逐页 `reLaunch`，
  每页**截图 + 机器抽取 `<button>` 文案/`class`/`disabled`**；对可疑页做 4s / 8s 长等待复测。
- **证据**：`review/evidence/ui_review_20260918/`（17 张截图 + `report_run1/2/3.json` 三份机器抽取结果）。

#### 🔴 F1【中】`pages/month/result` 在"无账本 / 加载失败"时**永久显示「加载中…」**
- **判据（源码）**：`pages/month/result.wxml:1` = `wx:if="{{!loading && r}}"`；`:67` = `wx:else` 渲染 `{{t.loading}}`（"加载中…"）。
  ⇒ **`&& r` 让 else 分支同时承接"加载中"与"r 为空"两种状态**：只要 `r` 为假（账本不存在，或 `getLedger` 报错被 catch），页面就停在"加载中…"。
- **实测**：`r3`（等 8s）/ `r4`（等 4s）两次复跑，`page.data().loading === false`、`r === null`，界面仍是"加载中…"
  （`06_month_result.png` / `r3_month_result.png` / `r4_month_result.png` 三张同形态）。
- **反例已排除（防误判为系统性问题）**：`grep` 全 `pages/**/*.wxml` —— 其余 **12 页**均为 `wx:if="{{!loading}}"`，
  else 分支**只在 loading 时触发**，属正确写法；`card/index`、`mine/index` 无此分支 ⇒ **本条只针对 `month/result` 一页**。
- **修法（属快马）**：拆三段 —— `wx:if="{{loading}}"` 显示加载 → `wx:elif="{{!r}}"` 显示**空态/错误态**
  （需新增文案，如"本月还没有账本，先去录入" + 跳 `/pages/month/input` 的按钮）→ `wx:else` 显示结果。
- **验收（可证伪）**：无账本 / 断网时**不得再出现"加载中…"**；有账本时结果页渲染与现状逐字一致。

#### 🟡 F2【低】两处"数值占位缺失"（首屏 / 失败态渲染出半截 UI）
- **(a) `pages/month/amortize`**：`amortize.js:38` 的 `data` 只初始化 `totalFen: 0`，**未初始化 `totalYuan`**；
  而 `amortize.wxml:5` 绑定 `{{t.cur}}{{totalYuan}}` ⇒ 首屏（或加载失败）渲染成**孤零零一个「¥」**
  （实拍 `05_month_amortize.png`）。修法：`data` 补 `totalYuan: '0.00'`（或 wxml 写 `{{totalYuan || '0.00'}}`）。
- **(b) `pages/month/index`**：无任何账本时 `curMonth === ""` 且 `months === []`（`report_run3.json` 实抽，
  非推断），而 `index.wxml:16` 绑定 `{{curMonth}}` ⇒ **「月份」行右侧空白**（实拍 `r2_month_index.png`）。
  修法：`curMonth` 兜底为当前月（`ui.nowMonth()`），使"还没录入过"时选择器也有可选值。
- ⚠️ 两条**同属一类**：`data` 缺兜底初值 ⇒ 空数据/错误态渲染出"半截 UI"。建议顺手扫一遍全页 `data` 初值（本轮只实测了这两处）。

#### 🔵 L1~L3【排版优化，非缺陷】

| # | 位置 | 现象 | 建议 |
|---|---|---|---|
| L1 | `pages/month/input.wxml:24` + `:26` | **「食材消耗合计（元）」连续出现两次**：`:24` 是段落小标题（`class="sec"`）、`:26` 是字段标签，**两处绑同一个 i18n key `t.directConsume`** | 小标题改用短标题（如"食材消耗"）或删掉标题行（`:30` 的 hint 已说明用途） |
| L2 | `pages/month/index` | 免费用户只有 1 个 tab（`t.freeTab`「经营参考估算」；`t.paidTab` 需 `isPaid` 才渲染，`index.wxml:28`），但 `.tab.active` 是**满宽橙色** ⇒ 视觉上像一条橙色横幅，不像 tab | 单 tab 时隐藏 tab 栏；或加"未选中"对照 / 左右留白 |
| L3 | `pages/mine/index.wxml:26` | 「注销账号」用**内联样式** `background:#e74c3c;color:#fff`（红色危险色，语义正确） | 抽成 `.btn.danger` 类，便于统一改主题（`app.wxss:26-31` 现只有 `.primary`/`.ghost`/`.disabled`） |

#### ✅ 本轮通过项（按钮合规，勿再当风险重复提）
- **文案**：14 页 `<button>` 全量机器抽取（`report_run1.json`）—— 设置 / 我的 / 保存 / 保存库存 / 新增资产 / 保存设置 / 开始测算 /
  `+ 新增成本卡` / 导出 · 打印 / `+ 添加明细行` / 反算售价 / 保存成本卡 / `+ 新增店铺` / 注销账号；
  **无「会员 / 订阅 / 权益」等禁词，付费按钮没有裸"开通"**（合 `miniprogram/i18n/terms.js` 的 `forbidden` 表）。
- **付费边界**：`pages/card/index.js:84 onExport()` —— 先 `fetchEntitlement()`，非 active 即 `openPaywall('export')`，后端 `exportData` 兜底。
  免费用户看到的「导出 / 打印」**不是"可用但白点"，是付费墙入口** ⇒ **不是漏洞**。
- **主次样式**：`app.wxss:26-31` 的 `.btn` 基类**无背景色**，`.primary`=橙实心 / `.ghost`=白底橙描边 / `.disabled`=灰 ⇒ 未观察到误染。
- **只读门禁**：`pages/month/input.wxml:11/19/27/34` 输入框与保存按钮均 `disabled="{{readOnly}}"`（归档月只读的生效路径存在）。

#### ⚠️ 本轮未证（不计为缺陷，勿据此下结论）
- 自动化**首次** `reLaunch` 偶发 `timeout waiting for automator response`（属握手，重跑即正常：`r1_index` 首轮 ERR、复跑 OK）。
- 首轮两个疑似项**已被复测推翻 / 改判**，留痕防回潮：
  ① "`/pages/month/index` 会重定向回首页" → **证伪**（`r2` 的 `actualPath = pages/month/index`，首轮是 reLaunch 与读页的**时序错位**）；
  ② "`month/result` 卡在加载" → **改判为 F1**（不是卡住，是 `wx:else` 兜底分支语义错）。

---

## 🟡 中优先级：上线后 1 月内补（工程短板）

| 项 | 决策 / 待办 | 归属 |
|---|---|---|
| 监控告警 | 先做基础：云函数错误率/超时、DB 配额、调用量突增告警；再补监控看板 | 运维（v1.0 末期） |
| 数据迁移与版本兼容 | 表结构变更必须写迁移脚本 + 版本号；旧前端访问新后端不报错；预留灰度回滚 | 工程（v1.1 前） |
| 核心异常兜底 | 网络异常提示+重试、云函数超时友好提示、后端强校验（金额负数/日期非法/数值超限） | 批次 7（已含日志脱敏/双层校验，补网络重试） |
| 基础数据埋点 | v1.0 末期补 5 事件：新增用户/启动/建店/建卡/付费触发；核心转化：弹窗曝光→点击→开通 | 运营（v1.0 末期） |

---

## 🟢 低优先级：后续迭代

| 项 | 备注 |
|---|---|
| 体验隐性 | 空/错状态统一设计、首次引导、表单草稿自动保存、M2 方案保存（v1.0 已含 M2 保存） |
| 运营客服 | 用户反馈入口、后台帮改数据、运营看板 |
| 最坏预案 | 云开发故障降级、备份恢复演练（周备份保留 30 天已在批次 7）、支付异常补偿 |

---

## 一句话总结
**硬卡点已全部决策落地**：审核（隐私弹窗+注销补进批次 7、虚拟支付保持关闭）、配额（基础版 19.9 元/月够用）、归档（手动为主+次月软提示+7 天宽限补录）、配额维度（按 user_id：M1=1 账套、M3=3 张、M2 不限套永久免费）。中低优项已进待办，不阻塞写码与上线。

> ⚠️ **2026-09-18 更正（R86）**：上面那句"已全部决策落地"**只对 §1~§4 成立**。
> 新查出的 **§5 云函数 `timeout`（线上实测 3s，无单源钉住）** 是**未决策、未处置**的硬卡点，
> 且它卡在**上线第一步（`initDb`）**上 ⇒ **不要把本页读成"零阻塞"**。
>
> ⚠️ **2026-09-18 追加（R91）**：**§6**（`saveCostCard` 的 `mode` 白名单，唯一"静默算错成本"开口）
> 与 **§7**（真机走查：`month/result` 永久"加载中…" + 两处数值占位缺失）**均为待修**，
> 写码归快马 `inscode`，未落之前本页同样**不是"零待办"**。
>
> → **2026-09-21 补记（round76 收口）· 上面两段警告的时态已过期，逐条更正**：
>   ① **§5 已闭环**（2026-09-19 round46：42/42 定值 + 双轮独立回读，判据见 §5）：上述 R86 那条
>      "**未决策、未处置**"与"**只对 §1~§4 成立**"**均已过期** —— 本页现对 **§1~§5** 成立；
>   ② **§6 与 §7 均已闭环**（§6 = 2026-09-19 round39 落地，见 §6 明细；§7 = 2026-09-19，见 §7 状态块）：
>      上述 R91 那条"**均为待修**""**不是"零待办"**"**均已过期**。
>   ⚠️ **但仍不要读成"零阻塞"**：本页只覆盖 §1~§7；**剩余上线阻塞不在本页**，以 `★知识存储点 §1.1` 为准。
>   🔴 **本页曾长期自相矛盾（标题/页脚说待修 ↔ 正文说已闭环）而 A–L 无一组可测** —— 2026-09-21
>      新增 `tools/check_stale_status.js` 把该形态做成机械判据（扫活文档面 `specs/dev-specs/`）。

---

## 🔢 收入渠道字典口径（唯一声明处）

- **前端独有**：现金收款、微信扫码收款、支付宝收款、银行卡/POS刷卡、储值卡消费、个人/单位挂账消费、团购/代金券核销
- **后端独有**：现金、微信支付宝、储值消费、团购券核销、企业挂账消费
- 决策：DEFERRED by 李老师（待裁定）date 2026-09-21 reason 前端 7 项是 2026-09-21「挂账/团购不见了」后拍板的渠道全集（前端 G10 只认 `terms.js`），后端种子 `SEED_INCOME_ITEMS` 仍是旧 5 项；已建账套数据不受影响（保存只提交填了金额的行）。**由哪一侧为准需李老师裁定，本轮不代修。**

> 由 `tools/check_income_channel_seed.js`（R110，第 83 套件）守：前端 `terms.js::ledger.income` 堂食清单
> 与后端种子堂食清单的**差异集合 ≡ 本处在册集合**，双向比对（含僵尸条目防腐）⇒ 任一单侧改名/加项/删项即红。
> 另有两条硬判：原型副本 ≡ 云函数副本（同一份种子的两处副本）、其他业务收入 前端 ≡ 后端。
> 背景：该分叉自 round69 起挂了 5 轮靠人记，`tools/` 对「渠道字典」零命中（`selftest_batch8b` 只守前端侧）。
