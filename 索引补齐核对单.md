# 索引补齐核对单（三种建法通用 · 防抄错清单）

> ⚠️ **本文件由 `tools/gen_index_checklist.js` 从单源 `cloudfunctions/initDb/collections.js` 生成，请勿手工编辑。**
> 索引改了 → 重跑 `node tools/gen_index_checklist.js` 重新生成本单即可（这样它永远不会与单源漂移）。
> 生成时间：2026-09-18 22:45（**本机本地时间**）

> **线上状态：已建满**（**40 / 40** 条 · 唯一 10 / 10，实测于 2026-09-17）
> 证据目录 `review/evidence/index_buildout_20260917/`（本行由证据派生，非人工填写）。

## 0 · 为什么需要这份单子（以及索引的三种建法）

真 `wx-server-sdk` 里 **没有** `createIndex`（`typeof === "undefined"`，A7 定案 · 2026-09-14 云端实测），
所以 `initDb` 里那段建索引的代码**永远建不上** —— **这一半是真的**。

但「**除了控制台就没有别的建法**」**这一半已被证伪**（2026-09-17）：**SDK 没有方法 ≠ 平台没有接口**。

| 建法 | 前置条件 | 说明 |
| --- | --- | --- |
| **① 脚本化（首选）** `tools/apply_indexes.js` | 有 **AppSecret** | 走官方 HTTP API `POST /tcb/updateindex`；**幂等、可回读**；默认 dry-run，`--apply` 才真写 |
| **② GUI 键鼠自动驾驶** | 无密钥，需开着微信开发者工具 | 2026-09-17 实证 **40/40**；脚本见 `review/evidence/index_buildout_20260917/scripts/` |
| **③ 控制台逐条手填** | 无密钥、无脚本 | **本单就是给这条路用的** —— 照 §3 逐条勾，防抄错 |

> ⚠️ 旧版核对单 §0 **曾断言「索引只能靠人一条条填」** —— 那是 A7 当时的口径，且曾**直接指导人工动作**
> （李老师按它手建完 40 条）。结论更新后**活文档没跟**（R82）⇒ 现已改正。**能走 ① 就别走 ③**。

## 1 · 【优先】先建这一条（唯一有性能后果的）

**`audit_log` → `idx_audit_idem`（字段 `idempotency_key` ↑ 升序，非唯一）**

- 幂等预检（`common/idempotency.js::checkIdempotent`）按 `idempotency_key` 查重；
- 而 `audit_log` 是**只增不删**的审计表 ⇒ **缺这条索引，每次幂等检查都是全表扫描**，并随数据量**线性恶化**；
- 其余索引缺了只是慢一点，这条缺了会**越用越慢**。⇒ 先建它，再建其余。

## 2 · 操作要点（控制台）

1. 云开发控制台 → **数据库** → 选中**集合** → **索引管理** → **新建索引**；
2. **复合索引**要把字段**逐行添加**，顺序按本单「字段」列**从左到右**（顺序不同 = 另一个索引）；
3. 方向照抄：本单写 `↑ 升序` 就是升序，写 `↓ 降序` 就是降序（源数据里的 `1` / `-1`）；
4. 「唯一」开关：**只在本单「唯一」列写"是"时打开**（共 **10** 条，清单见 §4）；
5. 索引名**照抄**（对不上不影响功能，但会让本单与线上无法逐条核对）；
6. 一条建完在左列勾一下 □→✓，**不要凭记忆**。

## 3 · 逐条勾选（共 **40** 条 · 集合 **25** 张）

| ✓ | # | 集合 | 索引名 | 字段（按此顺序添加） | 唯一 |
|---|---|---|---|---|---|
| □ | 1 | `shop_material` | `idx_mat_shop` | shop_id ↑ 升序 | 否 |
| □ | 2 | `shop_material` | `idx_mat_shop_virtual` | shop_id ↑ 升序 , is_virtual ↑ 升序 | 否 |
| □ | 3 | `shop_cost_card` | `idx_card_shop` | shop_id ↑ 升序 | 否 |
| □ | 4 | `shop_cost_card` | `idx_card_code_version` | shop_id ↑ 升序 , card_code ↑ 升序 , version ↑ 升序 | **是** |
| □ | 5 | `shop_cost_card_line` | `idx_line_card` | card_id ↑ 升序 | 否 |
| □ | 6 | `shop_sandbox` | `idx_sb_shop` | shop_id ↑ 升序 | 否 |
| □ | 7 | `shop_sandbox` | `idx_sb_shop_del` | shop_id ↑ 升序 , is_deleted ↑ 升序 | 否 |
| □ | 8 | `shop_amortize` | `idx_amort_shop` | shop_id ↑ 升序 | 否 |
| □ | 9 | `audit_log` | `idx_audit_shop` | shop_id ↑ 升序 | 否 |
| □ | 10 | `audit_log` | `idx_audit_action` | action ↑ 升序 | 否 |
| □ | 11 | `audit_log` | `idx_audit_created` | created_at ↓ 降序 | 否 |
| □ | 12 | `audit_log` | `idx_audit_idem` | idempotency_key ↑ 升序 | 否 |
| □ | 13 | `shop_monthly_account` | `idx_acc_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | **是** |
| □ | 14 | `shop_monthly_account` | `idx_acc_shop_del` | shop_id ↑ 升序 , is_deleted ↑ 升序 | 否 |
| □ | 15 | `shop_monthly_income` | `idx_inc_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | 否 |
| □ | 16 | `shop_monthly_expense` | `idx_exp_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | 否 |
| □ | 17 | `shop_inventory` | `idx_inv_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | **是** |
| □ | 18 | `shop_stored_value` | `idx_sv_shop` | shop_id ↑ 升序 | 否 |
| □ | 19 | `shop_stored_value` | `idx_sv_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | 否 |
| □ | 20 | `shop_credit_ledger` | `idx_cl_shop` | shop_id ↑ 升序 | 否 |
| □ | 21 | `shop_credit_ledger` | `idx_cl_shop_created` | shop_id ↑ 升序 , created_at ↓ 降序 | 否 |
| □ | 22 | `subscription_plan` | `idx_plan_enabled` | enabled ↑ 升序 , sort ↑ 升序 | 否 |
| □ | 23 | `feature_permissions` | `idx_fp_plan` | plan_id ↑ 升序 | 否 |
| □ | 24 | `shop_switch` | `idx_switch_shop_key` | shop_id ↑ 升序 , switch_key ↑ 升序 | **是** |
| □ | 25 | `shop_income_item` | `idx_ii_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | 否 |
| □ | 26 | `shop_expense_item` | `idx_ei_shop_month` | shop_id ↑ 升序 , month ↑ 升序 | 否 |
| □ | 27 | `shop_subscription` | `idx_sub_user` | user_id ↑ 升序 | 否 |
| □ | 28 | `shop_payment_flow` | `idx_pay_user` | user_id ↑ 升序 | 否 |
| □ | 29 | `shop_payment_flow` | `idx_pay_order` | order_no ↑ 升序 | **是** |
| □ | 30 | `shop_payment_flow` | `idx_pay_shop` | shop_id ↑ 升序 | 否 |
| □ | 31 | `user` | `idx_openid` | openid ↑ 升序 | **是** |
| □ | 32 | `user` | `idx_user_id` | user_id ↑ 升序 | **是** |
| □ | 33 | `shop` | `idx_shop_user` | user_id ↑ 升序 | 否 |
| □ | 34 | `shop` | `idx_shop_user_del` | user_id ↑ 升序 , is_deleted ↑ 升序 | 否 |
| □ | 35 | `shop_entitlement` | `idx_ent_user` | user_id ↑ 升序 | **是** |
| □ | 36 | `shop_entitlement` | `idx_ent_expire` | expire_at ↑ 升序 | 否 |
| □ | 37 | `order_refund` | `idx_refund_order` | order_id ↑ 升序 | **是** |
| □ | 38 | `order_refund` | `idx_refund_user` | user_id ↑ 升序 | 否 |
| □ | 39 | `admin_user` | `idx_admin_username` | username ↑ 升序 | **是** |
| □ | 40 | `admin_login_log` | `idx_login_admin` | admin_id ↑ 升序 | 否 |

## 4 · 唯一索引清单（**10** 条，只有这些要打开"唯一"）

1. `shop_cost_card.idx_card_code_version`
2. `shop_monthly_account.idx_acc_shop_month`
3. `shop_inventory.idx_inv_shop_month`
4. `shop_switch.idx_switch_shop_key`
5. `shop_payment_flow.idx_pay_order`
6. `user.idx_openid`
7. `user.idx_user_id`
8. `shop_entitlement.idx_ent_user`
9. `order_refund.idx_refund_order`
10. `admin_user.idx_admin_username`

## 5 · 无任何索引的集合

（无 —— 每张集合都至少一条索引）

## 6 · 建完收尾（**实测数**，由证据派生 —— 不是"应该"）

- 已建条数：**40** / 40　✅（本次新建 13 · 此前已存在 27）
- `audit_log` 的索引条数：**4** / 4（**含 `idx_audit_idem` 才算数**）　✅
- 唯一索引条数：**10** / 10　✅
- 核对人：WorkBuddy（键鼠 GUI 自动驾驶 + **独立**复核脚本）　日期：2026-09-17
- 证据目录：`review/evidence/index_buildout_20260917/`（建库日志 / 名称核对 / 组成核对 / 逐条 JSON / 关键截图）

> 数字来源（**本生成器内不写死任何计数**）：逐条结果 `review/evidence/index_buildout_20260917/04_逐条结果.json`
> （`OK`＝本次脚本新建、`EXISTS`＝建前已存在；唯一数按**单源 unique 清单**交叉计数）。

---

### 附：与仓库其他文件的关系（防"同一事实写多遍"）

- **唯一真相源**：`cloudfunctions/initDb/collections.js`（本单是它的派生渲染）；
- 仓内既有副本链（已由守卫 `tools/check_schema_sync.js` 机械守住）：镜像 `specs/dev-specs/prototype/init_db.js`、
  手册 §5.2b、工序清单 §5.5 正文 + **逐条对照表**、重启键 `★知识存储点_2026-09-10.md`；
- ⇒ **本单不替代上述任何一份**，它只是"手工操作时的勾选视图"（唯一增量：☐ 勾选位 + 操作要点 + 风险置顶）。
