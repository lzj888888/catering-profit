round74 · 渠道别名归并 + 预设行一律不显 ×

触发：李老师真机反馈「美团和现金 后面有红色X这个，和上面重复了，去掉吧」。

取证（真云，非猜测）：页面上下文调 getLedger 实测 2026-09 堂食
sub_items = [{美团:5000000},{现金:200000}] ⇒ 这两行不是预设渠道，是「+ 添加渠道」
填的口语名；装配口只认完全同名 ⇒ 被当自定义行另立一行，且只有这两行带 ×。

修法：
- terms.ledger.channelAliases（17 条口语 → 正式渠道，单源，双副本同步）
- normalizeDineRows 归并：别名行并进预设行、金额相加，且只在真归并时格式化
- markFixedRows + decorateRows(g, rows) 唯一分派口 ⇒ 所有大类的预设行都不给删除键
- WXML 预设细项名改纯文本渲染（+ .sub-item-fixed 样式）

自愈：归并只发生在渲染层，保存按正式渠道名写回 ⇒ 下次保存后端口语渠道自动收敛。

守卫：batch8b 新增 G11（13 条，直接 require 真模块跑现场数据回放 + 2 条反向证据）。
回灌：_mut_r73.py 6/6 抓到，还原后 0 失败；过程中撞红 G10b/G10e（无条件两位小数 =
口径漂移）⇒ 收窄为「仅归并时格式化」。
门禁：verify_all 81/81 RC=0；batch8b 121/121（同轮把断言数唯一声明处 104 更新为 121）。
