---
name: external-plan-review
description: 评审外部 AI（豆包/元宝/DeepSeek/其他）产出的方案文档，判定「吸收 / 舍弃」，用于本仓（店算·catering-profit）或任何已有既定规范与代码的项目。当用户说"看看豆包生成的这个文件有没有借鉴的""它这个方案吸收哪些舍弃哪些""你先看看输出想法，然后我们汇总"时使用。核心是**先读码对齐仓内现状再判**，避免重复造轮子、重复讨论已裁决事项。
agent_created: true
---

# 外部方案吸收 / 舍弃评审

## 为什么需要它

外部 AI 的方案文档有一个共同特征：**写得比我们现有的更"完整"，但大比例内容是我们已经落地的能力**。
不先读码对齐就直接评价，会得出两个错误结论：**"它有好多我们没想到的"**（其实是重复造轮子）、
**"这个建议不错"**（其实早就被我们裁决驳回过了）。

> 实证（round117）：豆包《M3 菜品成本卡模块设计方案 v1.0》14 章 22 条主张，
> 对仓内现状逐条核完 ⇒ **10 条已具备 / 1 条语义已覆盖 / 4 条半新增 / 7 条真新增**。
> 它的"免费额度 8 张"**是重复提议** —— 仓内规范尾部《与豆包 M3 版差异修正记录》里早就驳回过。

---

## 铁律（每条都是实证踩出来的）

1. **先读码，后评价。** 判定表里每一条都必须带**证据位置**（`文件:行号`），不许凭印象写"我们有"。
2. **必先读仓内既有的《与 XX 差异修正记录 / 分歧追踪表》。** 外部 AI 常重复提议已被驳回的事
   （它不知道我们的裁决）。不读这个，就会跟着它把已裁的事重新讨论一遍。
   🔴 **round126 实证（认知升级）**：外部方案可能是**另一个 AI 复用豆包原版**生成的
   ⇒ 已驳回项会**成套复现**。InsCode/快马那份 M3 PRD 的 4 条硬冲突
   （免费 8 张 / 佣金基数可切换 / 7 业态枚举 / BOM 多层）**逐条命中 v1.0《与豆包 M3 版差异修正记录》里的旧主张**；
   其中"8 张"在 `商业化方案_v1.4_融合版.md` 里明确记着"**v1.3 曾由 8 张改回 PRD 原值 3 张**"。
   ⇒ 实操：**先把"差异记录 + 定案表"里的已裁决项列成一张清单，再逐条去外部方案里找复现** ——
   找得越快，越说明它只是旧版换皮。
3. **外部方案的"阈值/区间"不得当判定阈值。** 例：豆包给"套餐折扣率 75-82% 才够吸引力"（单一来源）
   ⇒ 最多做**软提示文案**。本仓刚在 round113 栽过"把行业常见区间当警戒线"的坑。
4. **来源不可验证的数字不进规范。** 它给的"月活率第 2 个月 25-35%"之类，属推演 ⇒ 舍数字、取定性判断。
5. **不改锁定规范与代码。** 评审产出是**评估稿**，放 `review/`；规范改动要另起"增量规范"并单独送审。
6. **必须自我纠错。** 把自己上一轮的方案也放进来比（本轮就是这样发现"套餐=第3层BOM"与
   「BOM 最多 2 层」红线冲突的）。
7. **必须补"双方都没想到的"。** 评审的价值不在转述，在**找出那个两边都漏的点**。
8. **对外部文档做数字自洽性抽查。** 实证：豆包把两组不同场景的数字写进同一句
   （`24.38` 对应目标毛利率 60%；`28−9.75=18.25` 对应售价 28、毛利率 65.18%）⇒ 表述瑕疵，要如实标注。

---

## 流程

### 步 0 先判输入形态（round126 实证新增）
- **文件投喂**（用户给了路径 / 附件）⇒ 走 **步 1 + 步 2**（md5 同源 + 提取正文）。
- **对话内长文本投喂**（用户把 PRD 直接粘进消息，如 InsCode/快马那种"复制全部内容"的文档）
  ⇒ **跳过步 1/2**：文本已在上下文里，**无副本可验**，**直接进步 3**（读码对齐）。
  ⚠️ 但必须在本稿"未做的事"里**如实标注**"未做同源校验，因输入为对话内文本"（round126 已如此处理）。
  ⚠️ 这种情况下**别去找文件** —— 找了也是白找，浪费时间。

### 步 1 找到并验同源
用户常重复下载同一份文件（`(1)` `(2)` 副本）。**先算 md5 确认同源**，读一份即全量。

```bash
node -e "const fs=require('fs'),c=require('crypto');for(const f of process.argv.slice(1)){const b=fs.readFileSync(f);console.log(c.createHash('md5').update(b).digest('hex')+'  '+b.length+'  '+f)}" <文件1> <文件2>
```

搜索落点（本机实测最全）：`~/Desktop` `~/Downloads` `~/Documents` `~/WorkBuddy` `~/WPS Cloud Files`。
本机 `bash` 的 coreutils 已坏（`find`/`ls`/`grep` 均不可用）⇒ 用 Node `readdirSync` 递归 + 深度限制 5。

### 步 2 提取文本
docx 用 `scripts/extract_docx.py`（按阅读顺序出段落 + 表格，表格转 `| a | b |`）：

```bash
python scripts/extract_docx.py "<in.docx>" "<out.md>"
```

落盘到 `review/evidence/<来源>_<主题>_extract.md` —— 它是评审依据，**要入库**（可读可 diff）；
原始 docx **不入库**（二进制，且在 Downloads 有原件）。

### 步 3 ★ 读码对齐仓内现状（本流程的核心，占一半工作量）
**不要按章节读全文**，改用清单式核对。对外部方案的每条主张，去仓里找"有没有、在哪"：

- 集合与索引 → `cloudfunctions/initDb/collections.js`（集合名 + `INDEXES`，看索引名就能猜出机制，如 `idx_mat_shop_virtual` ⇒ 虚拟原料已存在）
- 引擎与公式 → 对应云函数的 `service.js`
- 已锁定条目 → `specs/dev-specs/core/开发规范v1.0_Module*.md`（**按标题扫**，再定点读）
- 前端呈现 → `pages/`
- 别忘了查**副本**：引擎常有 5 份副本（`calcBom`/`saveCostCard`/`syncCostCard`/`getCostCard`/`getCardVersions`），
  判定"要不要改"时副本数是主要成本。

批量核对脚本见 `scripts/find_lines.js`（一次列出多个文件的关键行号，避免引错位置）。

> ⚠️ **转义坑（实测）**：本机 shell 会吃掉一层反斜杠 ⇒ `node -e "..." 'x.js:BIZ_KEYS\\s*='` 传到脚本里
> 变成 `\\s`，结果**静默"未命中"**（不报错，最容易被当成"仓里没有"）。
> ⇒ 正则参数**从文件里传**（写个 3 行测试脚本调它），或用不含 `\` 的模式（`BIZ_KEYS` 而非 `BIZ_KEYS\s*=`）。
> 记住：**"未命中"要先怀疑传参，再怀疑事实。**

### 步 4 逐条判定，四分类

| 分类 | 含义 | 处理 |
|---|---|---|
| ✅ 已具备 | 仓内已实现 | **不重复做**，只需防止被改坏 |
| 🟡 半新增 | 有地基、缺一环 | 补那一环，且**优先用现有机制表达** |
| 🆕 真新增 | 仓内确实没有 | 逐条给落法 + 红线 + 代价 |
| ❌ 舍弃 | 与锁定规范冲突 / 依赖不可控 / 违反纪律 | **必须写明理由**，不许只说不采纳 |

### 步 5 列"硬冲突"
单独一节列出，每条写成 `外部主张 ↔ 仓内既定（证据）`。四类高发冲突：
① 与已裁决项冲突（配额/枚举/命名）② **枚举打架**（两套业态/分类枚举互认）
③ 与用户**当轮**要求冲突（外部 AI 不知道用户这轮要什么）④ **破红线**（层级上限、锚点、口径锁）。

### 步 6 自我纠错 + 补缺口
- 把自己上一轮的方案列成表，逐条自查（**上轮哪里错了要写出来**）。
- 补"双方都漏的"：找那一条**只有本项目能做到、因为只有我们同时拥有两个模块**的洞察。

### 步 7 落盘 + 上总线
产出 `review/REVIEW_<日期>_<来源>方案_吸收舍弃评估.md`，结构：
```
〇 一句话结论（含"它有多少是我们已有的"这个量化）
一 主张 vs 仓内现状（带 文件:行号 的表）
二 建议吸收（按性价比排序，每条：它原话 / 我方落法 / 为何值得 / 代价与红线）
三 建议舍弃（表：主张 / 理由）
四 硬冲突（须裁决）
五 自我纠错
六 双方都漏的缺口
七 待拍板清单（编号，回编号即可）
八 本文件未做的事（如实标注）
```
跑门禁 → 带 pathspec 提交 → 推 dev（`dangerouslyDisableSandbox: true`）→ 核 `ls-remote` 一致。

---

## 输出给用户的形态

1. 一句话结论 + 量化（"22 条里 10 条我们已有"）
2. 一张对照图（三分：已具备 / 半新增 / 真新增 + 冲突带）
3. 硬冲突与"必先讲清的口径边界"
4. **待拍板清单编号**（让用户回编号，不代选）
5. 如实标注未做项

## 反面清单（别做）

- ❌ 别把外部方案当"需求原文"照做 —— 它是**来源之一**，冲突裁决权在本项目锁定规范。
- ❌ 别在评审稿里直接改代码/规范。
- ❌ 别只输出文字不上总线 —— 复审方与下次会话读仓库，不读聊天。
- ❌ 别把外部方案里"更好的模型/更全的表格"当成优点 —— 先问一句"这个我们是不是已经有了"。
