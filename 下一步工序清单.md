# 下一步工序清单 · 李老师专用

> 生成日期：2026-09-14 ｜ 制作：WorkBuddy
> 这份清单**只管你现在要做的这一段**：把库建起来、跑通探针、把结果发给我。
> 每一步都写清「点哪里 → 看到什么 → 填什么 → 对了是什么样」。**卡住就截图发我，别自己乱试。**

---

## 0. 先抄下这几条（后面一直要用）

| 项目 | 值 |
|---|---|
| 项目文件夹 | `C:\Users\lzj\WorkBuddy\Claw\catering-profit` |
| 小程序 AppID | `wx33c110dc57a9c8dc` |
| dev（测试）环境名 | `cloud1` |
| dev（测试）环境 ID | `cloud1-4dgphoxy337f2a25` |
| prod（正式）环境 | **还没建**（现在用不到，等上线前再说） |

**你已经完成的**：① 真实 AppID 已拿到并填进项目 ② 微信开发者工具已装好、项目已导入 ③ 云开发已开通、dev 环境 `cloud1` 已建好 ④ 代码里的 dev 环境 ID 已由 WorkBuddy 配好。

---

## 工序 1 · 确认当前环境是 cloud1

1. 打开**微信开发者工具**，顶部右侧点「**云开发**」按钮。
2. 看弹出来的**云开发控制台**左上角，写的是不是 `cloud1`。
   - **不是** → 点它，在下拉里选 `cloud1`（就是标着「免费开发环境」的那个）。
3. 确认左侧菜单里能看到「**数据库**」「**存储**」「**云函数**」三项。能看到就说明环境正常。

---

## 工序 2 · 部署 initDb（建库工具）

1. 回到**开发者工具主窗口**，左侧是「文件树」。
2. 找到 `cloudfunctions` 文件夹 → 展开 → 找到 `initDb`。
3. 在 **`initDb` 文件夹上点右键** → 选「**上传并部署：云端安装依赖**」。
   - 弹确认就点「确定」。
   - 右下角会有进度条，等它提示「**部署成功**」（通常 20~60 秒）。
4. ❌ 如果报「**找不到模块 ./common**」→ **停下，截图发我**（我要跑一次同步脚本，你别自己弄）。

---

## 工序 3 · 核对环境变量 DEV_ENV_ID（关键！）

> 为什么有这一步：咱们的 dev 环境叫 `cloud1`，名字里**没有 dev 两个字**，所以要靠一个「白名单」让它放行。WorkBuddy 已经在代码里放好了配置，但你要确认它真的生效了。

1. 云开发控制台 → 左侧点「**云函数**」。
2. 在列表里找到 `initDb`，**点它的名字**进详情页。
3. 点「**版本与配置**」标签 → 再点「**配置**」。
4. 往下找到「**环境变量**」那一块，看有没有这一行：

   ```
   DEV_ENV_ID  =  cloud1-4dgphoxy337f2a25
   ```

5. **有** → 很好，直接去工序 4。
6. **没有** → 点「编辑」/「新增环境变量」：
   - 名称（key）填：`DEV_ENV_ID`
   - 值（value）填：`cloud1-4dgphoxy337f2a25`
   - 点保存 → 会提示「需要重新部署」→ **选重新部署**。

---

## 工序 4 · 运行 initDb，建 25 张表

1. 云开发控制台 →「云函数」→ `initDb`。
2. 右上角点「**测试**」（有的版本叫「云端测试」）。
3. 弹出框里有个**输入区**（测试参数）。把里面内容**全删掉**，只留一对花括号：

   ```
   {}
   ```

4. 点「**运行**」/「确定」。
5. 下面会出现一段返回结果（JSON 格式），你找两样东西：

   - 找到 `created` 这一段 —— 它后面是一串集合名，**数一数是不是 25 个**；或者看到 `"length": 25`。
   - **确认没有** `blocked: true` 这一行。

6. ✅ **看到 25 个、且没有 blocked** → 建库成功！去工序 5。
7. ❌ **出现 `blocked: true`** → 环境变量没配好，回到工序 3 重做。
8. ❌ **报错 / 数字不对** → 截图发我。

---

## 工序 5 · 核对 25 张表，并把权限改掉

1. 云开发控制台 → 左侧点「**数据库**」。
2. 数一数左边列出的**集合（表）是不是 25 张**。
3. **逐一点开每一张**，右上角找「**权限设置**」，把权限改成：

   > **仅管理端可读写**

   ⚠️ 默认通常是「仅创建者可读写」，**那个不对，必须改**。

   ⚠️ 25 张都要改 —— 有点枯燥，但必须做（不改后面会读不到数据）。

---

## 工序 6 · 部署 smokeTest（探针）

操作和工序 2 完全一样：

1. 文件树 → `cloudfunctions` → `smokeTest`。
2. 在 `smokeTest` 上**右键** →「**上传并部署：云端安装依赖**」。
3. 等「部署成功」。

---

## 工序 7 · 跑探针，把结果整段发给我

1. 云开发控制台 →「云函数」→ `smokeTest` →「**测试**」。
2. 输入区同样只留 `{}` → 点「运行」。
3. 会返回**一大段 JSON**（可能几十行）。
4. **全选复制**（点结果区 → `Ctrl + A` → `Ctrl + C`），然后**完整粘贴回 WorkBuddy 的聊天窗口**发给我。
   - 我要看的字段：`env`、`requireCommon.ok`、`docGet.hasDataField`、`createIndex`、`uniqueEnforce`、`assertShopOwner`、`createCollection`、`dataAdapterGet`。
5. 我判读完会告诉你：**四个前提是不是都成立**，以及能不能开始投喂「批次 1」。

---

## 做完之后 · 接下来发生什么

| 谁 | 做什么 |
|---|---|
| **你** | 把工序 7 的 JSON 发给我 |
| **我（WorkBuddy）** | 判读探针 → 点名 A6/A7/`require('./common')`/`doc().get()` 四前提 |
| **你** | 四前提通过后，投喂「批次 1」给快马 InsCode（网址 https://www.inscode.net） |
| **我** | 快马产出的代码 → 我跑门禁（8 套件）+ 复审 → 通过才合入 |

---

## 常见报错速查

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 报「找不到模块 ./common」 | 云函数里的 common 副本没同步 | **停下**，截图发我（我跑 `node tools/sync_common.js`） |
| 返回里有 `blocked: true` | `DEV_ENV_ID` 环境变量没配/没生效 | 回工序 3 重做，然后重新部署 |
| 集合不是 25 张 | 建库失败 | 截图发我 |
| 「云开发」按钮点不动 | AppID 是 touristappid 或没实名 | 截图发我 |
| 部署一直转圈不动 | 云端装依赖慢 | 等 1~2 分钟；还不行截图发我 |
| 索引建不上 | 云端 createIndex 可能不可用 | 截图发我（这个关系到 A7 判定，很重要） |

---

## 三条切记

1. ⚠️ **只在 `cloud1`（dev）里操作**，别碰别的环境，更不要把 initDb 部署到正式环境。
2. ⚠️ **千万别删掉 `cloud1`** —— 官方规则是「环境注销后要超过 1 个月才能再免费创建」，删了可能 1 个月内建不了新环境。
3. ⚠️ **卡住就截图发回 WorkBuddy 窗口**，不要反复乱点。

---

> 清单结束。做到哪一步卡住，随时截图发我。—— WorkBuddy
