# 下一步工序清单 · 李老师专用

> ⚠️ 本文档为**派生件**：同名 `.txt` / `.docx` / `.pdf` 均由本 `.md` 生成，**冲突一律以 `.md` 为准**（勿手改派生件）。
> 生成日期：2026-09-14 ｜ 制作：WorkBuddy
> 这份清单**只管你现在要做的这一段**：把库建起来、跑通探针、把结果发给我。
> 每一步都写清「点哪里 → 看到什么 → 填什么 → 对了是什么样」。**卡住就截图发我，别自己乱试。**

---

## 0. 先抄下这几条（后面一直要用）

| 项目 | 值 |
|---|---|
| 项目文件夹 | `C:\Users\lzj\WorkBuddy\Claw\catering-profit` |
| 小程序 AppID | `wx33c110dc57a9c8dc` |
| dev（测试）环境名 | `cloud1` |
| dev（测试）环境 ID | `cloud1-d4gphpoxy337f2a25` |
| prod（正式）环境 | **还没建**（现在用不到，等上线前再说） |

**你已经完成的**：① 真实 AppID 已拿到并填进项目 ② 微信开发者工具已装好、项目已导入 ③ 云开发已开通、dev 环境 `cloud1` 已建好 ④ 代码里的 dev 环境 ID 已由 WorkBuddy 配好。

> ✅ **2026-09-14 晚实测：工序 1~4 已全部走通** —— 25 张集合建成、门禁放行。
> ✅ **2026-09-15 实测：工序 6 已走通** —— 服务端口开启后改走命令行部署，`smokeTest` 已上云（success: true）。
> 过程中踩到三个坑（环境变量的值被敲错字符、SDK 不支持用代码建索引、**环境 ID 被抄错且门禁查不出来**），**已回写进下面的工序 3 / 工序 4 / 工序 5.5 / 工序 6 / 报错速查**。照新写法做就能一次过。

---

## 工序 1 · 确认当前环境是 cloud1

1. 打开**微信开发者工具**，顶部右侧点「**云开发**」按钮。
2. 看弹出来的**云开发控制台**左上角，写的是不是 `cloud1`。
   - **不是** → 点它，在下拉里选 `cloud1`（就是标着「免费开发环境」的那个）。
3. 确认左侧菜单里能看到「**数据库**」「**存储**」「**云函数**」三项。能看到就说明环境正常。

---

## 工序 2 · 部署 initDb（建库工具）

1. 回到**开发者工具主窗口**，左侧是「文件树」。
2. 找到 `cloudfunctions` 文件夹 → 展开 → 找到 `initDb`。
3. 在 **`initDb` 文件夹上点右键** → 选「**上传并部署：云端安装依赖**」。
   - 弹确认就点「确定」。
   - 右下角会有进度条，等它提示「**部署成功**」（通常 20~60 秒）。
4. ⚠️ **如果右键菜单里根本没有「上传并部署」** → 说明工具没把这个目录识别成云函数目录（缺 `cloudfunctionRoot` 配置，**已于 2026-09-14 由 WorkBuddy 补上**）。解决：
   - 菜单栏「**项目**」→「**重新打开当前项目**」（或直接关掉工具、再打开项目）。
   - 重开后云函数目录左边会出现「☁ 云」小图标，右键就有「上传并部署」了。
   - 重开后仍没有 → **截图发我**，别自己乱点。
5. ❌ 如果报「**找不到模块 ./common**」→ **停下，截图发我**（我要跑一次同步脚本，你别自己弄）。
6. ✅ 部署成功的标志：出现「上传成功」提示，或云开发控制台「云函数」列表里能看到 `initDb`。

---

## 工序 3 · 核对环境变量 DEV_ENV_ID（关键！）

> 为什么有这一步：咱们的 dev 环境叫 `cloud1`，名字里**没有 dev 两个字**，所以要靠一个「白名单」让它放行。WorkBuddy 已经在代码里放好了配置，但你要确认它真的生效了。

1. 云开发控制台 → 左侧点「**云函数**」。
2. 在列表里找到 `initDb`，**点它的名字**进详情页。
3. 点「**版本与配置**」标签 → 再点「**配置**」。
4. 往下找到「**环境变量**」那一块，确认里面有**两行**（一行都不能少）：

   ```
   DEV_ENV_ID  =  cloud1-d4gphpoxy337f2a25
   TCB_ENV     =  cloud1-d4gphpoxy337f2a25
   ```

   > **为什么还要 `TCB_ENV`**：这个控制台的「测试」按钮**不走小程序网关**，云函数拿不到环境上下文，只能靠 `TCB_ENV` 这个兜底变量告诉它"我现在在哪个环境"。缺它 → `env` 读到空 → 门禁必然拦截。

5. 🔴 **值必须和上面这一行一字不差**（2026-09-14 真事：手工录入时敲错了一个字符，就卡在这儿两轮）。
   - **为什么难查**：这个输入框很窄，**值的末尾会被截断看不见**；而云函数做的是"一字不差地比较"，错一个字符就 `blocked`，界面上完全看不出来。
   - 👍 **推荐用复制粘贴** —— 直接从这里复制 `cloud1-d4gphpoxy337f2a25` 粘进去，比手打稳。
   - 实在要手打，**打完必须逐字符核对**：`cloud1` - 横杠 - `d4gphpoxy` - `337` - `f` - `2a` - `25`（共 24 个字符）。
   - 💡 **唯一靠谱的验证**：跑一次测试，看日志里 `env="..."` 打出来的到底是什么 —— **值对不对，日志说了算，别用眼睛比对**。

> 🔴🔴 **2026-09-15 重大更正：上面这个 ID 我们曾整整抄错了一天！**
>
> 之前文档里一直写的是 `cloud1-4dgphp oxy337f2a25`（错），真实 ID 是 **`cloud1-d4gphpoxy337f2a25`**（对）—— 差一个 `d`，而且 `oxy` 前面那段拼错了。
>
> **为什么没被发现**：门禁是拿 `env` 和 `DEV_ENV_ID` 做**相等比较**，两个都填成同一个错值 → 照样相等 → **门禁照常放行**，界面上看起来"一切正常"。
>
> **真正的查法**（以后就用这个，别再靠眼睛抄控制台）：
> ```bat
> "C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat" cloud env list --project "C:\Users\lzj\WorkBuddy\Claw\catering-profit"
> ```
> 这命令打出来的就是账号下**唯一的权威环境 ID**。注意：需要先在「设置 → 安全设置」里把**服务端口**打开（见工序 6）。

6. 缺行就点「编辑」/「新增环境变量」补齐：名称填 `DEV_ENV_ID` 或 `TCB_ENV`，值都**复制粘贴** `cloud1-d4gphpoxy337f2a25`（**别手打** —— 输入框窄，错一个字符肉眼看不出来）→ 点保存。
   - 保存后**即时生效**，不必重新部署也能直接去跑测试。

---

## 工序 4 · 运行 initDb，建 25 张表

1. 云开发控制台 →「云函数」→ `initDb`。
2. 右上角点「**测试**」（有的版本叫「云端测试」）。
3. 弹出框里有个**输入区**（测试参数）。把里面内容**全删掉**，只留一对花括号：

   ```
   {}
   ```

4. 点「**运行**」/「确定」。
5. 先看**日志区**（不是返回结果），确认里面**没有** `[INITDB_BLOCKED]` 这一行 —— 有它说明门禁没放行，回工序 3。
6. 再数集合：云开发控制台 → 左侧「**数据库**」→ 数是不是 **25 张**（列表会折叠，慢慢数别漏）。
7. ✅ **日志无 INITDB_BLOCKED + 数据库 25 张** → 建库成功！去工序 5。
8. ⚠️ **看到一整片红字 `createIndex is not a function` 不要慌** —— 这是**预期内的**：微信的 SDK 压根不支持用代码建索引，代码里早就留了话（"建不了就记一笔、不阻断"），集合和种子数据照样落库。索引怎么办见下面「工序 5.5」。
9. ❌ **仍有 `blocked: true`** → 回工序 3，**把两行的值重新填一遍（推荐复制粘贴）**。

---

## 工序 5 · 核对 25 张表，并把权限改掉

1. 云开发控制台 → 左侧点「**数据库**」。
2. 数一数左边列出的**集合（表）是不是 25 张**。
3. **逐一点开每一张**，右上角找「**权限设置**」，把权限改成：

   > **仅管理端可读写**

   ⚠️ 默认通常是「仅创建者可读写」，**那个不对，必须改**。

   ⚠️ 25 张都要改 —— 有点枯燥，但必须做（不改后面会读不到数据）。

   > 💤 **今晚可以先不做**：`smokeTest` 是在**云函数里**跑的（属"管理端"），不受这张表的权限限制 → 工序 6、7 照常能跑通。
   > 🔴 **上线前必须全部改掉**，否则小程序端读不到数据。

---

## 工序 5.5 · 索引 39 条（可延后，上线前必须补）

> dev 阶段**可以跳过**：数据量小、不影响跑通。但**上线前在 prod 必须补齐**，否则唯一约束（比如同一个微信只能有一个 `user`、管理账号名不能重复）形同虚设。

25 张集合共 **39 条索引**，全部定义在 `cloudfunctions/initDb/collections.js` 的 `INDEXES` 里。

**微信云开发的 SDK 不支持用代码建索引**（实测就是 `createIndex is not a function`），只能手工：

1. 云开发控制台 →「数据库」→ 点开某个集合 →「**索引管理**」→「**添加索引**」。
2. 按 `collections.js` 里对应条目填：**索引名、字段（+升序/降序）、是否唯一**。
3. 逐条重复 39 次。

⚠️ 枯燥但绕不开。下面是**逐条对照表**（共 39 条；`【唯一】`= 建索引时勾选「唯一」；控制台一次只建一张集合的索引）：

| # | 集合 | 索引名 | 字段（升/降） | 唯一 |
|---|---|---|---|---|
| 1 | user | idx_openid | openid ↑ | 【唯一】 |
| 2 | user | idx_user_id | user_id ↑ | 【唯一】 |
| 3 | shop | idx_shop_user | user_id ↑ | |
| 4 | shop | idx_shop_user_del | user_id ↑, is_deleted ↑ | |
| 5 | shop_entitlement | idx_ent_user | user_id ↑ | 【唯一】 |
| 6 | shop_entitlement | idx_ent_expire | expire_at ↑ | |
| 7 | shop_subscription | idx_sub_user | user_id ↑ | |
| 8 | shop_payment_flow | idx_pay_user | user_id ↑ | |
| 9 | shop_payment_flow | idx_pay_order | order_no ↑ | 【唯一】 |
| 10 | shop_payment_flow | idx_pay_shop | shop_id ↑ | |
| 11 | order_refund | idx_refund_order | order_id ↑ | 【唯一】 |
| 12 | order_refund | idx_refund_user | user_id ↑ | |
| 13 | shop_monthly_account | idx_acc_shop_month | shop_id ↑, month ↑ | 【唯一】 |
| 14 | shop_monthly_account | idx_acc_shop_del | shop_id ↑, is_deleted ↑ | |
| 15 | shop_monthly_income | idx_inc_shop_month | shop_id ↑, month ↑ | |
| 16 | shop_monthly_expense | idx_exp_shop_month | shop_id ↑, month ↑ | |
| 17 | shop_inventory | idx_inv_shop_month | shop_id ↑, month ↑ | 【唯一】 |
| 18 | shop_cost_card | idx_card_shop | shop_id ↑ | |
| 19 | shop_cost_card | idx_card_code_version | shop_id ↑, card_code ↑, version ↑ | 【唯一】 |
| 20 | shop_cost_card_line | idx_line_card | card_id ↑ | |
| 21 | shop_material | idx_mat_shop | shop_id ↑ | |
| 22 | shop_material | idx_mat_shop_virtual | shop_id ↑, is_virtual ↑ | |
| 23 | shop_sandbox | idx_sb_shop | shop_id ↑ | |
| 24 | shop_sandbox | idx_sb_shop_del | shop_id ↑, is_deleted ↑ | |
| 25 | shop_amortize | idx_amort_shop | shop_id ↑ | |
| 26 | shop_switch | idx_switch_shop_key | shop_id ↑, switch_key ↑ | 【唯一】 |
| 27 | audit_log | idx_audit_shop | shop_id ↑ | |
| 28 | audit_log | idx_audit_action | action ↑ | |
| 29 | audit_log | idx_audit_created | created_at ↓ | |
| 30 | admin_user | idx_admin_username | username ↑ | 【唯一】 |
| 31 | admin_login_log | idx_login_admin | admin_id ↑ | |
| 32 | subscription_plan | idx_plan_enabled | enabled ↑, sort ↑ | |
| 33 | feature_permissions | idx_fp_plan | plan_id ↑ | |
| 34 | shop_stored_value | idx_sv_shop | shop_id ↑ | |
| 35 | shop_stored_value | idx_sv_shop_month | shop_id ↑, month ↑ | |
| 36 | shop_credit_ledger | idx_cl_shop | shop_id ↑ | |
| 37 | shop_credit_ledger | idx_cl_shop_created | shop_id ↑, created_at ↓ | |
| 38 | shop_income_item | idx_ii_shop_month | shop_id ↑, month ↑ | |
| 39 | shop_expense_item | idx_ei_shop_month | shop_id ↑, month ↑ | |

---

## 工序 6 · 部署 smokeTest（探针）

### ✅ 2026-09-15 实测：已改用命令行部署（推荐，不用右键点）

**前置条件（一次性）**：微信开发者工具主窗口 →「设置」→「安全设置」→「**服务端口**」选「**开启**」。
> 这是微信官方给命令行留的正规口子，只对本机程序开放，用完可随时关回去。

开启后，部署这件事**我（WorkBuddy）可以代劳**，命令长这样：

```bat
cd "C:\Users\lzj\WorkBuddy\Claw\catering-profit"
"C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat" cloud functions deploy ^
  --env cloud1-d4gphpoxy337f2a25 --names smokeTest ^
  --project "C:\Users\lzj\WorkBuddy\Claw\catering-profit" --remote-npm-install
```

看到 `success: true` 就是成了。

| 常用命令 | 作用 |
|---|---|
| `cli cloud env list --project "<项目路径>"` | **查真实环境 ID**（唯一权威来源，别靠肉眼抄控制台） |
| `cli cloud functions list --env <环境ID> --project "<项目路径>"` | 看云端已有哪些云函数 |
| `cli cloud functions deploy --env <环境ID> --names <函数名> --project "<路径>" -r` | 部署（`-r` = 云端装依赖） |

⚠️ **首次部署会撞一次 `Creating`/`Updating` 状态冲突**（报错：`当前函数处于Creating状态，无法进行此操作`）——不是故障，**等 1 分钟再跑一次同一条命令**即可成功。

### 老办法（命令行不通时兜底）

1. 文件树 → `cloudfunctions` → `smokeTest`。
2. 在 `smokeTest` 上**右键** →「**上传并部署：云端安装依赖**」。
3. 等「部署成功」。

---

## 工序 7 · 跑探针，把结果整段发给我

1. 云开发控制台 →「云函数」→ `smokeTest` →「**测试**」。
2. 输入区同样只留 `{}` → 点「运行」。
3. 会返回**一大段 JSON**（可能几十行）。
4. **全选复制**（点结果区 → `Ctrl + A` → `Ctrl + C`），然后**完整粘贴回 WorkBuddy 的聊天窗口**发给我。
   - 我要看的字段：`env`、`requireCommon.ok`、`docGet.hasDataField`、`createIndex`、`uniqueEnforce`、`assertShopOwner`、`createCollection`、`dataAdapterGet`。
5. 我判读完会告诉你：**四个前提是不是都成立**，以及能不能开始投喂「批次 1」。

---

## 做完之后 · 接下来发生什么

| 谁 | 做什么 |
|---|---|
| **你** | 把工序 7 的 JSON 发给我 |
| **我（WorkBuddy）** | 判读探针 → 点名 A6/A7/`require('./common')`/`doc().get()` 四前提 |
| **你** | 四前提通过后，投喂「批次 1」给快马 InsCode（网址 https://www.inscode.net） |
| **我** | 快马产出的代码 → 我跑门禁（9 套件）+ 复审 → 通过才合入 |

---

## 常见报错速查

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 报「找不到模块 ./common」 | 云函数里的 common 副本没同步 | **停下**，截图发我（我跑 `node tools/sync_common.js`） |
| 返回 `blocked: true`，且日志里 `env=""` | 缺 `TCB_ENV`（控制台测试不走网关，拿不到环境 ID） | 回工序 3 补 `TCB_ENV` |
| 返回 `blocked: true`，但日志里 `env` **有值**仍被拦 | 环境变量的值和 `env` **不是一字不差**（多为手工录入时敲错字符，界面上看不出来） | 回工序 3 重填（**复制粘贴**更稳），然后重跑测试看日志 |
| 一整片红字 `createIndex is not a function` | 微信 SDK 不支持用代码建索引（**预期内**） | **不用管**，不影响建表；上线前走「工序 5.5」手工补 |
| 门禁**明明放行了**、日志里 `env` 也有值，但环境就是"不对劲" | 🔴 **ID 抄错了**：`DEV_ENV_ID` 和 `TCB_ENV` 填的是同一个错值 → 相等比较照样通过，门禁成了摆设（2026-09-15 真事，错了一整天） | 跑 `cli cloud env list --project "<路径>"` 取**权威 ID** 重填两个变量 |
| 命令行报 `IDE service port disabled` | 服务端口没开 | 工具主窗口 →「设置」→「安全设置」→「服务端口」选「开启」 |
| 命令行报 `env xxx not exists (code 10)` | 环境 ID 写错 | 同上，用 `cloud env list` 查真实 ID |
| 命令行报 `当前函数处于Creating状态` / `Updating` | 云端正在创建/更新，撞了并发 | **不是故障**：等 1 分钟，原命令再跑一次 |
| 集合不是 25 张 | 建库失败 | 截图发我 |
| 右键 `initDb` **没有**「上传并部署」 | 工具未识别云函数目录（缺 `cloudfunctionRoot` 配置） | 已补该配置 →「项目 → 重新打开当前项目」即可；仍无则截图发我 |
| 「云开发」按钮点不动 | AppID 没填对或没实名 | 截图发我 |
| 部署一直转圈不动 | 云端装依赖慢 | 等 1~2 分钟；还不行截图发我 |

---

## 三条切记

1. ⚠️ **只在 `cloud1`（dev）里操作**，别碰别的环境，更不要把 initDb 部署到正式环境。
2. ⚠️ **千万别删掉 `cloud1`** —— 官方规则是「环境注销后要超过 1 个月才能再免费创建」，删了可能 1 个月内建不了新环境。
3. ⚠️ **卡住就截图发回 WorkBuddy 窗口**，不要反复乱点。

---

> 清单结束。做到哪一步卡住，随时截图发我。—— WorkBuddy
